# Agent Handoff State

This file is the current continuation state for Codex threads. Update it after non-trivial work, before delegating to another chat, or before stopping in the middle of a task.

## Latest Snapshot

Date: 2026-06-12

Fresh 2026-06-12 status:

- Current Phase 12 transparent marker scaffold slice is telemetry/test-only and behavior-preserving. `client/rust_ext/src/lib.rs` reserves `RUMPELMC_GPU_TERRAIN_TRANSPARENT` plus `transparent_requested`, `transparent_active`, and `transparent_fallback` marker fields. `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` keeps the requested flag inactive, so current runtime behavior remains opaque-only even if the env flag is set. `scripts/gpu_terrain_report.sh` now aggregates the transparent marker fields and metric origins when fresh captures include them. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No transparent face buffer, alpha blending, sort policy, shader alpha path, Godot transparent material, block ID, atlas asset, protocol, storage, worldgen, shadow quality, or render behavior changed.
- Fresh transparent fallback artifact: `logs/gpu_transparent_fallback_capture` from `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_transparent_fallback_capture`. It passed with `rust_ext_profile=release`, `shadow_path=godot_proxy`, `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, `smoke_err=0`, `terrain_samples=384`, `ground_hits=9`, `current_chunk_loaded=1`, `current_chunk_submeshes=2`, `current_chunk_collision=2`, `terrain_queue_max_ms=4.140`, `gpu_compositor_submit_max_ms=0.129`, and `process_wall_p95_ms=0.055`. The aggregate report over `logs` resolves max `transparent_requested=1.000`, max `transparent_active=0.000`, and max `transparent_fallback=1.000`, with origins pointing at the fresh marker.
- Checks passed for this transparent marker scaffold slice: source inspection of native-shadow marker patterns in `client/rust_ext/src/lib.rs` and `scripts/gpu_terrain_report.sh`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_transparent_gate_stays_disabled_until_implemented`, `sh -n scripts/gpu_terrain_report.sh`, `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1` release movement smoke above, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-fallback.txt`, `git diff --check`, `./scripts/check.sh fast` (Rust `110/110`), and `./scripts/diff_guard.sh`.
- Next useful GPU step after checkpointing this slice: add a tiny report/harness assertion for transparent requested/active/fallback marker captures, or start a transparent fixture plan only after a real transparent block/material contract exists. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, or render behavior from this marker scaffold alone.

- Current Phase 12 transparent terrain path slice is docs-only and behavior-preserving. `docs/GPU_TRANSPARENT_PATH.md` records the rollout for a future transparent terrain path: keep the current opaque pass as default rollback, split render opacity from collision solidity, preserve opaque face visibility next to transparent blocks, render future transparent terrain after opaque terrain with an explicit sort policy, and require requested/active/fallback telemetry plus visual/depth/collision parity and external profiler evidence before default enablement. `docs/GPU_ROADMAP.md`, `docs/GPU_PROFILING.md`, and `docs/GPU_TRENDS.md` link/record the checkpoint. No Rust runtime, shader, Godot scene, atlas, protocol, block ID, storage, worldgen, shadow quality, or render behavior changed.
- Checks passed for this transparent path design slice: `sh scripts/handoff.sh`, OntoIndex `gn_explore` for transparent/opaque terrain entry points, source inspection of `client/rust_ext/src/blocks.rs`, `client/shaders/mesher.glsl`, `client/shaders/gpu_terrain_render.glsl`, `client/rust_ext/src/lib.rs`, and `client/rust_ext/src/gpu_terrain.rs`, `LC_ALL=C rg` non-ASCII check, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add transparent requested/active/fallback marker scaffolding behind an implementation gate, while keeping current opaque rendering inactive for transparency until a fixture, parity gates, and profiler evidence exist.

- Current handoff GPU report freshness slice is behavior-preserving. `scripts/handoff.sh` no longer prints `logs/gpu-terrain-report.txt` as if it were current when the report's recorded commit differs from HEAD; by default it prints a stale-report notice with the report commit and current commit. Setting `RUMPELMC_HANDOFF_REFRESH_GPU_REPORT=1` opts into generating a fresh temporary report during handoff. This avoids misleading continuation snapshots when ignored log artifacts are stale, while keeping ordinary handoff runs lightweight. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this handoff freshness slice: `sh -n scripts/handoff.sh`, standalone fresh report generation to `/tmp/rumpel-gpu-report-handoff-freshness.txt` showing `Git commit: 5b47072`, no profiler results summary, and the pending real-log capture pack with `results_file_status=missing`, default `sh scripts/handoff.sh >/tmp/rumpel-handoff-freshness-default.txt` showing stale report detection for `logs/gpu-terrain-report.txt` (`d270c2a` vs current `5b47072`), `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step remains external Metal/Xcode profiler capture for `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-capture-pack.txt`; locally, do not cite pending capture-pack rows as profiler evidence.

- Current Phase 12 real-log shadow profiler capture-pack slice is artifact-only and behavior-preserving. `scripts/gpu_terrain_shadow_profiler_capture_pack.sh` was run over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` and wrote the ignored local artifacts `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-plan.txt`, `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-results-template.txt`, and `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-capture-pack.txt`. The pack reports `rows=4`, `results_file_status=missing`, and `capture_pack_status=pending_external_profiler`; it is a checklist only, not profiler evidence. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this real-log capture-pack slice: real-log capture pack generation above, `scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-profiler-real-pack.txt` showing no `Selected Shadow Profiler Results Summary` but showing `Selected Shadow Profiler Capture Pack` with `results_file_status=missing` and `capture_pack_status=pending_external_profiler`, negative-path validation confirming the generated `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-results-template.txt` is rejected by `scripts/gpu_terrain_shadow_profiler_results_check.sh` with `results have no external_profile_status rows`, `git status --short --ignored=matching` confirming the generated log artifacts remain ignored under `logs/`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: capture real Metal/Xcode profiler data for the pack rows, write only real captured rows to `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-results.txt`, then run `scripts/gpu_terrain_shadow_profiler_results_check.sh` before citing any profiler numbers.

- Current Phase 12 shadow profiler capture-pack report surfacing slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now includes a `Selected Shadow Profiler Capture Pack` section sourced from the latest `shadow-radius-profiler-capture-pack.txt` under the selected log directory. This is intentionally separate from `Selected Shadow Profiler Results Summary`: a capture pack is pending handoff/checklist state, not external profiler evidence. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this capture-pack report surfacing slice: `sh -n scripts/gpu_terrain_report.sh`, temp report fixture with `shadow-radius-profiler-capture-pack.txt` showing the new selected capture-pack section and no results summary, normal report over `logs` to `/tmp/rumpel-gpu-report-shadow-profiler-current.txt` showing no matching capture-pack artifact yet, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: run the capture pack into the real log directory before external profiling, then fill and validate `shadow-radius-profiler-results.txt` only from real Metal/Xcode capture data.

- Current Phase 12 shadow profiler capture pack slice is behavior-preserving and external-profiler-handoff-only. `scripts/gpu_terrain_shadow_profiler_capture_pack.sh` reads `shadow-radius-profiler-manifest.txt`, regenerates the pending `shadow-radius-profiler-plan.txt`, regenerates the commented TODO `shadow-radius-profiler-results-template.txt`, and writes a `shadow-radius-profiler-capture-pack.txt` checklist with the exact validate/report commands for future real profiler rows. The pack records `capture_pack_status=pending_external_profiler` and is not profiler evidence. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this capture pack slice: `sh -n scripts/gpu_terrain_shadow_profiler_capture_pack.sh`, `sh -n` over the existing profiler plan/template/results scripts, pack generation over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` to `/tmp/rumpel-shadow-profiler-capture-pack.txt` producing `rows=4 capture_pack_status=pending_external_profiler`, negative-path validation confirming the generated `/tmp/shadow-radius-profiler-results-template.txt` is rejected by `scripts/gpu_terrain_shadow_profiler_results_check.sh` with `results have no external_profile_status rows`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: run the capture pack into the real log directory before external profiling, fill `shadow-radius-profiler-results.txt` only from a real Metal/Xcode capture, validate it, and then surface the validated summary through `scripts/gpu_terrain_report.sh`.

- Current Phase 12 shadow profiler results template slice is behavior-preserving and external-profiler-handoff-only. `scripts/gpu_terrain_shadow_profiler_results_template.sh` reads a pending `shadow-radius-profiler-plan.txt` and writes a fillable `shadow-radius-profiler-results-template.txt` whose candidate `external_profile_status=captured` rows are commented and contain `TODO` placeholders. The existing results validator ignores those commented rows and rejects the template until real `profiler_tool`, `profiler_artifact`, and positive `gpu_shadow_pass_ms` values are recorded. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this results template slice: `sh -n scripts/gpu_terrain_shadow_profiler_results_template.sh`, `sh -n` over the existing profiler plan/results scripts, plan generation over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` to `/tmp/rumpel-shadow-profiler-plan.txt` producing `rows=4 usable=3 rejected=1`, template generation to `/tmp/rumpel-shadow-profiler-results-template.txt` producing `summary rows=4 template_status=todo`, negative-path validation confirming `scripts/gpu_terrain_shadow_profiler_results_check.sh` rejects the commented template with `results have no external_profile_status rows`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: fill `shadow-radius-profiler-results.txt` only from a real Metal/Xcode capture, then run `scripts/gpu_terrain_shadow_profiler_results_check.sh` and let `scripts/gpu_terrain_report.sh` surface the validated summary. Do not cite the template rows as evidence.

- Current Phase 12 shadow profiler report surfacing slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now includes a `Selected Shadow Profiler Results Summary` section sourced from the latest `shadow-radius-profiler-results-summary.txt` under the selected log directory. This only surfaces already-validated external profiler summaries; it does not create profiler evidence, capture Metal/Xcode data, or change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, or production shadow decisions.
- Checks passed for this report surfacing slice: `sh -n scripts/gpu_terrain_report.sh`, normal report over `logs` to `/tmp/rumpel-gpu-report-shadow-profiler-current.txt` showing no matching profiler summary yet, temp report fixture with `shadow-radius-profiler-results-summary.txt` showing the new selected summary section, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), and `./scripts/diff_guard.sh`.
- Next useful GPU step: use this report section only with real validated `shadow-radius-profiler-results-summary.txt` artifacts, not synthetic `/tmp` rows.

- Current Phase 12 shadow profiler results validator slice is behavior-preserving and external-profiler-recording-only. `scripts/gpu_terrain_shadow_profiler_results_check.sh` validates a future `shadow-radius-profiler-results.txt` against `shadow-radius-profiler-plan.txt`: result rows must start with `external_profile_status=captured`, match a planned `priority`/`radius`/`artifact`, include non-placeholder `profiler_tool` and `profiler_artifact`, and include positive `gpu_shadow_pass_ms`. By default all planned rows must be captured; `RUMPELMC_SHADOW_PROFILER_RESULTS_ALLOW_PARTIAL=1` allows partial handoff validation while listing missing rows. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed so far for this results validator slice: `sh -n scripts/gpu_terrain_shadow_profiler_results_check.sh`, synthetic captured results validation against `/tmp/rumpel-shadow-profiler-plan.txt` producing `planned_rows=4 captured_rows=4 missing_rows=0`, negative-path validation rejecting `external_profile_status=pending`, strict partial validation rejecting missing rows, and explicit partial handoff validation with `RUMPELMC_SHADOW_PROFILER_RESULTS_ALLOW_PARTIAL=1` listing `missing_rows=3`.
- Next useful GPU step: run the standard checks, then use this results validator only after a real Metal/Xcode capture has produced actual `profiler_artifact` and `gpu_shadow_pass_ms` values. Do not cite the synthetic `/tmp` result rows as evidence.

- Current Phase 12 shadow profiler plan guard slice is behavior-preserving and external-profiler-handoff-only. `scripts/gpu_terrain_shadow_profiler_plan.sh` reads `shadow-radius-profiler-manifest.txt`, validates required row fields plus referenced artifact paths, requires `profiler_required=external_metal`, sorts rows by priority, and writes a line-oriented `shadow-radius-profiler-plan.txt` with `external_profile_status=pending`. It maps `shadow_normal_total_savings_evidence=usable` to `normal_total_decision=may_cite_after_external_profiler`, `rejected` to `do_not_cite`, and `unknown` to `needs_review`. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this profiler plan guard slice: `sh -n scripts/gpu_terrain_shadow_profiler_plan.sh`, plan generation over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` to `/tmp/rumpel-shadow-profiler-plan.txt` producing `rows=4 usable=3 rejected=1`, negative-path manifest validation rejecting invalid `shadow_normal_total_savings_evidence=bad`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: use `/tmp/rumpel-shadow-profiler-plan.txt` or a regenerated `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-plan.txt` as the exact external Metal/Xcode capture checklist. Do not treat any `external_profile_status=pending` row as profiler evidence.

- Current Phase 12 shadow normal-total evidence guard slice is behavior-preserving. `scripts/gpu_terrain_shadow_radius_matrix.sh` now derives `shadow_normal_total_savings_evidence` from `shadow_normal_total_status`: `reduced` becomes `usable`, `increased` becomes `rejected`, and anything else becomes `unknown`. Both `shadow-radius-matrix-summary.txt` and `shadow-radius-profiler-manifest.txt` include this field and a note that `rejected` rows must not be cited as normal-total savings evidence. The wrapper also avoids reusing the function-scoped `summary_path` name for the top-level matrix output, so POSIX `sh` global variable semantics cannot move the matrix summary into the last radius case's child summary path. Report-mode regenerated `logs/gpu_shadow_radius_matrix_wide` without new screenshots; the wide artifact now marks `scene`, radius `2`, and radius `4` as `usable`, while radius `1` is `rejected` because its normal total increased. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Checks passed for this evidence guard slice: `sh -n scripts/gpu_terrain_shadow_radius_matrix.sh`, matrix radius `0` guard check, report-mode regeneration over `logs/gpu_shadow_radius_matrix_wide` producing top-level `shadow-radius-matrix-summary.txt` and `shadow-radius-profiler-manifest.txt`, grep checks for `radius=1` `shadow_normal_total_savings_evidence=rejected` and `scene`/`2`/`4` `usable`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-evidence.txt` with no error patterns, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: use `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` for external Metal profiling; locally, do not cite `radius=1` as normal-total savings evidence unless a future artifact changes its evidence field to `usable`.

- Current Phase 12 shadow-radius profiler manifest slice is behavior-preserving. `scripts/gpu_terrain_shadow_radius_matrix.sh` now writes `shadow-radius-profiler-manifest.txt` next to `shadow-radius-matrix-summary.txt`. The manifest is line-oriented and lists each radius row's priority, profiler role, artifact directory, full/compact screenshot paths, compact marker path, compact proxy counters, visual luma/sample fields, mesh max timings, and `profiler_required=external_metal`. Report-mode regenerated `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` without new screenshots. Priorities are `scene` = `1`/`default_high_proxy`, radius `2` = `2`/`mid_proxy_control`, radius `1` = `3`/`low_proxy_control`, and remaining positive radii = `4`/`extra_proxy_control`. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Checks passed for this manifest slice: `sh -n scripts/gpu_terrain_shadow_radius_matrix.sh`, matrix radius `0` guard check, report-mode regeneration over `logs/gpu_shadow_radius_matrix_wide` producing both `shadow-radius-matrix-summary.txt` and `shadow-radius-profiler-manifest.txt`, `test -s logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-manifest.txt` with no error patterns, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: use the manifest as the capture checklist for external Metal profiling. If continuing locally only, the next safe code slice is to add a small parser/check that refuses to treat `shadow_normal_total_status=increased` rows as savings evidence.

- Current Phase 12 wide shadow-radius matrix capture is behavior-preserving and uses the committed matrix wrapper without code changes. Fresh artifact: `logs/gpu_shadow_radius_matrix_wide/shadow-radius-matrix-summary.txt` from `RUMPELMC_SHADOW_RADIUS_MATRIX_CAPTURE=1 RUMPELMC_SHADOW_RADIUS_MATRIX_RADII="scene 1 2 4" RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_shadow_radius_matrix.sh logs/gpu_shadow_radius_matrix_wide`. It reused the pre-existing server on `25565` and left it running. All rows passed with full/compact `avg_luma=0.2805` and `terrain_samples=472`. Compact row signals: `scene` `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3932`; radius `1` `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`; radius `2` `compact_shadow_proxy=58`, `compact_shadow_normals_saved=1492`; radius `4` `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3932`.
- The wide matrix validates `scene`, `1`, `2`, and `4` as profiler controls, not production shadow-distance choices. Radius `1` again reported `shadow_normal_total_status=increased`, so cross-row/cross-run `normals_total` deltas remain unreliable; use per-marker `compact_shadow_normals_saved`, `compact_shadow_proxy`, visual parity fields, and external Metal profiler data for decisions.
- Checks passed for this wide matrix capture: fresh release `scene 1 2 4` matrix capture above, report-mode regeneration over `logs/gpu_shadow_radius_matrix_wide`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-matrix-wide.txt` with no error patterns, port check confirming the reused server PID `96670` still listens, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: take external Metal profiler captures for the wide matrix rows, with priority on `scene`, `radius-2`, and `radius-1` as the high/default, middle, and low proxy-load controls. Do not change production shadow behavior from the local proxy counters alone.

- Current Phase 12 shadow-radius matrix wrapper slice is behavior-preserving. `scripts/gpu_terrain_shadow_radius_matrix.sh` now runs or re-reports multiple compact proxy shadow-radius controls into one `shadow-radius-matrix-summary.txt`. It delegates all capture and marker validation to `scripts/gpu_terrain_compact_proxy_benchmark.sh`, accepts `RUMPELMC_SHADOW_RADIUS_MATRIX_RADII` entries of `scene` or positive integers, rejects radius `0` before launch because that remains the benchmark's shadow-disabled control, and supports `RUMPELMC_SHADOW_RADIUS_MATRIX_CAPTURE=0` report-mode validation over existing case directories. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Fresh release matrix artifact: `logs/gpu_shadow_radius_matrix/shadow-radius-matrix-summary.txt` from `RUMPELMC_SHADOW_RADIUS_MATRIX_CAPTURE=1 RUMPELMC_SHADOW_RADIUS_MATRIX_RADII="scene 1" RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_shadow_radius_matrix.sh logs/gpu_shadow_radius_matrix`. It reused the pre-existing server on `25565` and left it running. The `scene` row passed with full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3956`, and `shadow_normal_total_delta=5538` with status `reduced`. The radius `1` row passed with full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`, and `shadow_normal_total_delta=732` with status `reduced`.
- Checks passed for this matrix wrapper slice: `sh -n scripts/gpu_terrain_shadow_radius_matrix.sh`, matrix radius `0` guard check, fresh release `scene 1` matrix capture above, report-mode regeneration over `logs/gpu_shadow_radius_matrix`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-matrix.txt`, port check confirming the reused server PID `96670` still listens, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: take an external Metal profiler capture against `logs/gpu_shadow_radius_matrix/scene` and `logs/gpu_shadow_radius_matrix/radius-1`, or use the matrix wrapper to add one more positive radius control if the profiler needs a wider sweep. Do not change production shadow distance or start a native-shadow default path from these local CPU/proxy counters alone.

- Current Phase 12 explicit shadow-radius benchmark slice is behavior-preserving. `scripts/gpu_terrain_compact_proxy_benchmark.sh` now accepts `RUMPELMC_COMPACT_PROXY_BENCH_SHADOW_RADIUS` for the full/compact shadow-casting cases, rejects radius `0` because that is the existing shadow-disabled control case, records `shadow_radius_override` in `compact-proxy-benchmark-summary.txt`, and supports `RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1` for intentional captures against an already-running local server without killing it afterward. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Fresh release radius-1 shadow proxy artifact: `logs/gpu_shadow_proxy_radius1_capture/compact-proxy-benchmark-summary.txt` from `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1 RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1 RUMPELMC_COMPACT_PROXY_BENCH_SHADOW_RADIUS=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_compact_proxy_benchmark.sh logs/gpu_shadow_proxy_radius1_capture`. It reused the pre-existing server on `25565` and left it running. The capture passed with `shadow_radius_override=1`, `gpu_upload_fail=0`, full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`, `mesh_max_ms=3.23`, and `coll_avg_ms=0.51`. The summary also reports `shadow_normal_total_status=increased`, so cross-run `normals_total` deltas are not reliable savings evidence in this low-radius/reuse-server control; use per-marker `compact_shadow_normals_saved` plus external profiler data instead.
- Checks passed for this explicit-radius benchmark slice: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, report-mode validation over `logs/gpu_shadow_proxy_focused_capture`, radius `0` guard check, fresh radius-1 release capture above, report-mode regeneration over `logs/gpu_shadow_proxy_radius1_capture`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius1.txt`, port check confirming the reused server PID `96670` still listens, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: either capture the current compact/full proxy workload with an external Metal profiler using the explicit-radius artifact controls, or start the first tiny native-shadow renderer proof only with the existing `godot_proxy` rollback and parity gates intact.

- Current Phase 12 native-shadow fallback parity gate slice is behavior-preserving. `scripts/gpu_terrain_parity_smoke.sh` now normalizes relative output directories to the repo root, prepares the requested Godot Rust extension profile before full captures, requires the matching `rust_ext_profile` in fresh marker files, and adds `gpu-terrain-native-shadow-fallback-parity` as a 14th case. The new case runs with `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1` while `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, so it must keep `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, and `native_shadow_fallback=1`. Validate-only mode remains compatible with older 13-case parity artifact directories.
- Fresh release parity artifact: `logs/gpu_native_shadow_fallback_parity/parity-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=4000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_parity_smoke.sh logs/gpu_native_shadow_fallback_parity`. It passed with `case_count=14`; the fallback marker reports `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `gpu_upload_fail=0`, `gpu_frames=239`, `gpu_subchunks=88`, `gpu_faces=314`, `compact_shadow_proxy=191`, and `compact_collision_proxy=0`. The `pair=native_shadow_fallback` visual comparison against the ordinary GPU marker reports `avg_luma delta=0.0000`, `terrain_luma_range delta=0.0000`, `terrain_samples delta=0`, `terrain_color_buckets delta=0`, and `terrain_chroma_samples delta=0`.
- Checks passed for this fallback parity gate slice: `sh -n scripts/gpu_terrain_parity_smoke.sh`, validate-only over old 13-case `logs/gpu_shader_branchless_corners_parity`, validate-only over fresh 14-case `logs/gpu_native_shadow_fallback_parity`, full release parity run above, marker/summary grep for the fallback triplet, `sips` dimension check for the fallback screenshot (`1280x720`), `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: stop adding native-shadow marker scaffolding unless a renderer proof needs a new invariant; either take an external Metal profiler capture for the current compact proxy path, or start the first tiny native-shadow renderer proof behind the still-false implementation gate with `godot_proxy` rollback and this parity gate intact.

- Current Phase 12 native-shadow proxy-radius contract slice is behavior-preserving. `client/rust_ext/src/lib.rs` now routes Godot shadow-proxy refresh decisions through `terrain_godot_shadow_proxy_chunk_distance`: while `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, the current `godot_proxy` path keeps the same proxy radius; if a future implementation makes `native_shadow_active=true`, `terrain_shadow_path_decision` can report `gpu_native_shadow` while the Godot shadow-proxy refresh radius drops to `0`, so future renderer proof cannot keep building Godot shadow proxies under a native marker. `docs/GPU_SHADOW_PATH.md` and `docs/GPU_TRENDS.md` record the contract. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this proxy-radius contract slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow_active_separates_renderer_path_from_godot_proxy_radius`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_shadow_path_keeps_production_and_diagnostic_paths_explicit`, `cargo test --manifest-path client/rust_ext/Cargo.toml cpu_proxy_refresh_tracks_collision_and_shadow_radius_edges`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh` (expected Rust extension notice only), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: either take the external Metal profiler capture for the current compact proxy path, or start the first real native renderer experiment behind the still-false implementation gate only after wiring parity/fallback checks to this proxy-radius contract.

- Current Phase 12 native-shadow fallback marker slice is behavior-preserving. `client/rust_ext/src/lib.rs` now emits `native_shadow_fallback=1` only when `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` is requested but the implementation is not active; the current implementation gate remains `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, so even with the env flag set runtime shadow path stays `godot_proxy`. `scripts/gpu_terrain_report.sh` surfaces `native_shadow_fallback` and its metric origin. Fresh env-on release movement stress wrote `logs/gpu_native_shadow_fallback_capture/gpu-terrain-movement-stress.png.txt` and reported `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `gpu_upload_fail=0`, `terrain_queue_work_ms=0.325/0.534/1.340`, `gpu_compositor_submit_ms=0.013/0.018/0.206`, and `process_wall_p95_ms=0.036`. Aggregate report `/tmp/rumpel-gpu-report-native-shadow-fallback.txt` now reports max `native_shadow_requested=1.000`, max `native_shadow_active=0.000`, and max `native_shadow_fallback=1.000` with origins pointing at the fresh marker. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this fallback marker slice so far: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `sh -n scripts/gpu_terrain_report.sh`, `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_fallback_capture`, marker grep for `shadow_path=godot_proxy native_shadow_requested=1 native_shadow_active=0 native_shadow_fallback=1`, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-fallback.txt`.
- Next useful GPU step after checkpointing this slice: stop adding native-shadow marker scaffolding unless a renderer proof needs a new invariant; either take an external Metal profiler capture for the current compact proxy path, or start the first tiny renderer proof behind the still-false implementation gate with `godot_proxy` rollback and parity gates intact.

- Current Phase 12 requested/active native-shadow capture is complete and behavior-preserving. Fresh release movement stress reused the already-running local server on `127.0.0.1:25565` and wrote `logs/gpu_native_shadow_requested_active_capture/gpu-terrain-movement-stress.png.txt`; the marker reports `shadow_path=godot_proxy`, `native_shadow_requested=0`, `native_shadow_active=0`, `shadow_mode=conservative`, `shadow_mesh=compact`, `gpu_upload_fail=0`, `terrain_queue_work_ms=0.421/0.545/1.824`, `gpu_compositor_submit_ms=0.017/0.018/0.368`, and `process_wall_p95_ms=0.036`. Aggregate report `/tmp/rumpel-gpu-report-native-shadow-requested-active-fresh.txt` now resolves max `native_shadow_requested=0.000` and max `native_shadow_active=0.000` with metric origins pointing at that fresh marker instead of `n/a`. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this capture/docs slice: `env -u RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_requested_active_capture`, marker grep for `shadow_path=godot_proxy native_shadow_requested=0 native_shadow_active=0`, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-requested-active-fresh.txt`.
- Next useful GPU step after checkpointing this capture: either take an external Metal profiler capture for the current compact proxy path, or start a very small renderer proof with `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` still behind `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false` until parity and rollback evidence exist.

- Current Phase 12 GPU-native shadow requested/active telemetry slice is behavior-preserving. `client/rust_ext/src/lib.rs` now splits `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` into `native_shadow_requested` and `native_shadow_active` marker fields; requested reflects the env flag, while active still requires `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=true`, so current runtime behavior stays on `godot_proxy`. `scripts/gpu_terrain_report.sh` surfaces both metrics and metric origins; existing historical logs report them as `n/a` until fresh captures include the new marker fields. `docs/GPU_SHADOW_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this requested/active telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_shadow_path_keeps_production_and_diagnostic_paths_explicit`, `sh -n scripts/gpu_terrain_report.sh`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-requested-active.txt` (new fields present as `n/a` on old logs), `git diff --check`, `./scripts/check.sh fast` (Rust `108/108`), `./scripts/diff_guard.sh` (expected sensitive-path notice only), staged MCP `gn_review_diff` (no high-risk findings; dirty overlay only), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS` in file-scope mode; symbol-name mode is heuristic without graph ids for the new dirty helper), and post-commit OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` plus `bash ./scripts/ontoindex.sh status` (HEAD up-to-date; native graph writer unavailable/non-blocking).
- Follow-up capture for this slice is recorded above; next useful GPU step is either external profiling of the current compact proxy path or a tiny renderer proof with `godot_proxy` rollback intact.

- Current Phase 12 GPU-native shadow marker scaffold is behavior-preserving. `client/rust_ext/src/lib.rs` now reserves `GpuTerrainShadowPath::GpuNativeShadow` and the marker token `shadow_path=gpu_native_shadow`, adds `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW`, and keeps that env flag inert until a future implementation flips `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=true`; current runtime markers therefore stay on `godot_proxy` by default and even with the flag set. `terrain_shadow_path_decision` tests now cover production, disabled, diagnostic, and future native paths plus the implementation gate. `scripts/gpu_terrain_compact_proxy_benchmark.sh` now validates shadow-casting paths through a helper that accepts only `godot_proxy` and future `gpu_native_shadow`, keeping them separate from `scene_shadows_disabled` and `diagnostic_no_shadow_proxy`. `docs/GPU_SHADOW_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this marker scaffold slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_shadow_path_keeps_production_and_diagnostic_paths_explicit`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, report-mode `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=0 sh scripts/gpu_terrain_compact_proxy_benchmark.sh logs/gpu_shadow_proxy_focused_capture`, `git diff --check`, `./scripts/check.sh fast` (Rust `108/108`), `./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`c43171e`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS` in file-scope mode; symbol-name mode is heuristic without graph ids for the new dirty helper).
- Next useful GPU step after checkpointing this slice: add explicit requested-vs-active native-shadow telemetry/report fields, or start a tiny renderer proof only after preserving `godot_proxy` rollback and the existing shadow parity gates.

- Current Phase 12 GPU-native shadow path slice is docs-only and behavior-preserving. `docs/GPU_SHADOW_PATH.md` now records the rollout for a future native terrain shadow path: keep `godot_proxy` as the production default, keep `scene_shadows_disabled` and `diagnostic_no_shadow_proxy` as diagnostics only, require an explicit off-by-default rollback flag for any prototype, reuse the current `SunLight.SHADOW_MAX_DISTANCE` radius and chunk-role refresh rules, keep collision proxies independent, and require visual parity plus an external profiler comparison before default enablement. `docs/GPU_ROADMAP.md`, `docs/GPU_PROFILING.md`, and `docs/GPU_TRENDS.md` link/record the checkpoint. No Rust runtime, shader, Godot scene, shadow quality, capture harness, or artifact-generation behavior changed.
- Checks passed for this design slice: `git diff --check`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`705f6b9`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS` after explicit untracked-doc file override), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add telemetry/test scaffolding for an off-by-default `gpu_native_shadow` marker path, or take an external Metal profiler capture for the current compact proxy artifact before writing renderer code.

- Current Phase 12 focused shadow proxy capture slice is harness/report-only and behavior-preserving. `scripts/gpu_terrain_compact_proxy_benchmark.sh` now normalizes relative output directories to the repo root, exact-parses metric tokens so fields like `cpu_proxy` are not confused with `max_mesh_cpu_proxy`, writes `compact-proxy-benchmark-summary.txt`, and requires/cleans a free local `25565` server port in capture mode. No renderer, Rust runtime, shadow quality, or production shadow-path behavior changed.
- Fresh focused shadow proxy artifact: `logs/gpu_shadow_proxy_focused_capture/compact-proxy-benchmark-summary.txt` from `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_compact_proxy_benchmark.sh logs/gpu_shadow_proxy_focused_capture`. It captured full, compact, shadow-disabled, and collision-only proxy rows. The compact shadow row preserved the same `avg_luma=0.2812` as full, reported `compact_shadow_proxy=189`, `shadow_normals_saved=3158`, and reduced `normals_total` from `49834` to `45472` (`shadow_normal_total_reduction=8.8%`). The shadow-disabled and collision-only rows both used `compact_collision_proxy=13`, with collision normal payload reductions of `1.5%`; all four markers passed benchmark validation and the cleanup check left port `25565` free.
- Checks passed for this focused capture slice: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, report-mode validation over `logs/gpu_terrain_visual_smoke/parity`, fresh capture command above, report-mode regeneration over `logs/gpu_shadow_proxy_focused_capture`, manual `lsof -nP -iTCP:25565 -sTCP:LISTEN` cleanup verification, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-proxy-focused-capture.txt`, `git diff --check`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`447e2d5`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: do not change shadow behavior from these local counters alone; either take an external Metal profiler capture for the focused artifact or design the GPU-native terrain shadow path behind a rollback flag.

- Current Phase 12 shadow proxy report slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now surfaces existing compact shadow/collision proxy savings counters and metric origins: `compact_shadow_normals_saved`, `compact_collision_proxy`, `compact_collision_normals_saved`, and `fast_proxy`, alongside existing `mesh_shadow_only`, `proxy_shadow`, `proxy_shadow_only`, `compact_shadow_proxy`, and `proxy_refresh_reuse`. No renderer, runtime, shadow-quality, capture, or artifact-generation behavior changed.
- Fresh shadow proxy report artifact: `/tmp/rumpel-gpu-report-shadow-proxy-cost.txt` from `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-proxy-cost.txt`. It reported max `mesh_shadow_only=262`, `proxy_shadow=262`, `proxy_shadow_only=252`, `compact_shadow_proxy=949`, `compact_shadow_normals_saved=1579008`, `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `fast_proxy=949`, and `proxy_refresh_reuse=267`, with no summary error patterns. These are historical cumulative marker maxima, not fresh per-frame shadow-cost measurements.
- Checks passed for this report slice: `sh -n scripts/gpu_terrain_report.sh`, `git diff --check`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-proxy-cost.txt`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`3ee1b22`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` in file-scope mode (`PASS`; symbol-name mode is heuristic without graph ids for shell functions).
- Next useful GPU step after checkpointing this slice: take a fresh focused capture or external profiler measurement before changing shadow behavior; the current slice only makes already-recorded proxy/savings counters visible in the aggregate report.

- Current Phase 11 lighting/depth/shadow contract slice is test-only and behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_lighting_contract_keeps_vertex_lighting_and_fragment_handoff`, which locks the render shader assumption that lighting is computed in the vertex stage from `face_normal(face_idx)` plus sanitized push constants, passed through `lighting_out`/`lighting_in`, and only applied to the sampled atlas texel in the fragment stage with forced opaque alpha. This complements the existing reverse-Z `GREATER_OR_EQUAL` depth-state guard and the existing shadow proxy mode/path tests; no GLSL runtime path, renderer setting, or artifact producer changed.
- Checks passed for this contract slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_lighting_contract_keeps_vertex_lighting_and_fragment_handoff`, `git diff --check`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`541cf55`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: stop adding shader hot-path guards unless a new assumption is about to change; either take an external Metal profiler capture for the current branchless shader or move to the next roadmap area with fresh measured evidence.

- Current Phase 11 parity reporting slice is report-only. `scripts/gpu_terrain_parity_smoke.sh` now writes `parity-summary.txt` after successful full or validate-only parity validation. The summary records the output directory, thresholds, all 13 parity cases, per-case smoke/terrain/shadow metadata, and visual metric deltas for `default`, `atlas_depth`, `lighting_shadow`, `lighting_shadow_compact`, and `texture_stand`.
- Fresh parity summary artifact: `logs/gpu_shader_branchless_corners_parity/parity-summary.txt` from `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh scripts/gpu_terrain_parity_smoke.sh /Users/daniil/Documents/RUMPELMC/logs/gpu_shader_branchless_corners_parity`. It passed and reported tight deltas for the existing branchless-corner parity artifacts: `default avg_luma delta=0.0076`, `atlas_depth avg_luma delta=0.0141`, `lighting_shadow avg_luma delta=0.0137`, `lighting_shadow_compact avg_luma delta=0.0000`, and `texture_stand avg_luma delta=0.0075`; all are under the current `max_avg_luma_delta=0.16` threshold.
- Checks passed for this parity-summary slice: `sh -n scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh scripts/gpu_terrain_parity_smoke.sh /Users/daniil/Documents/RUMPELMC/logs/gpu_shader_branchless_corners_parity`, `git diff --check`, `./scripts/check.sh fast` (Rust `106/106`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`770d613`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: use `parity-summary.txt` in future Phase 11 lighting/depth/shadow decisions, then either add shader contract guards for lighting/depth assumptions or take an external Metal profiler capture before more shader rewrites.

- Current Phase 11 shader face-corner geometry slice is behavior-preserving. `client/shaders/gpu_terrain_render.glsl` replaces the `face_corner` per-face `if` chain and local `vec3 corners[4]` arrays with 32-entry `FACE_CORNER_BASES`, `FACE_CORNER_EXTENT_X_FACTORS`, and `FACE_CORNER_EXTENT_Y_FACTORS` lookup tables. The lookup still builds each vertex as `base + extent.x * factor + extent.y * factor`, and rows `6/7` preserve the previous front-face fallback for masked nonstandard face indices. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_uses_branchless_face_corner_tables` to guard the table contract and absence of the old local arrays.
- `scripts/gpu_terrain_parity_smoke.sh` now parses marker metrics by exact `key=value` token instead of suffix matching. Full parity exposed this pre-existing harness ambiguity because `collision` could be read from a later token such as `max_array_mesh_collision_bodies=0`; the parser fix is test infrastructure only and does not change runtime behavior.
- Fresh shader corner artifact: `logs/gpu_shader_branchless_corners` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_shader_branchless_corners`. It passed with `smoke_err=0`, `terrain_samples=384`, `terrain_color_buckets=6`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.007`, `gpu_compositor_submit_max_ms=0.212`, `process_wall_p95_ms=0.035`, and `gpu_scene_target_replace=0`. The smoke-started server PID `96079` was stopped and port `25565` was cleared.
- Fresh geometry parity artifact: `logs/gpu_shader_branchless_corners_parity` from `RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=4000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_parity_smoke.sh /Users/daniil/Documents/RUMPELMC/logs/gpu_shader_branchless_corners_parity`. It passed all 13 CPU/GPU terrain parity cases and produced 26 files (13 PNGs plus 13 markers). A first relative-output attempt wrote under `client/logs/...` and failed path validation; that generated directory was removed before the absolute-output pass. The parity-started server PID `20822` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-corners.txt` selected `logs/gpu_shader_branchless_corners/movement-stress-summary.txt` as the current movement summary and reported no error patterns.
- Checks passed so far for this shader corner slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_uses_branchless_face_corner_tables`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (106/106), `sh -n scripts/gpu_terrain_parity_smoke.sh`, release movement stress, full terrain parity smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-corners.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`dc1324b`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` in file-scope mode (`PASS`; symbol-name mode remains heuristic without graph ids), and MCP `gn_review_diff` staged review (no high-risk symbols; degraded only by dirty overlay/no embeddings metadata).
- Next useful GPU step after checkpointing this slice: with `face_normal`, `face_uv`, and `face_corner` now table-driven, stop broad geometry rewrites unless a profiler points at shader ALU/control flow. Continue Phase 11 with lighting/depth/shadow parity assumptions, shader contract guards, or an external profiler capture of the current branchless shader.

- Current Phase 11 shader UV hot-path slice is behavior-preserving. `client/shaders/gpu_terrain_render.glsl` replaces the `face_uv` branch/local-array chain with a 32-entry `FACE_UV_FACTORS` lookup table indexed by `(face_idx & 7u) * 4u + corner_idx`, then multiplies by the existing face extent. The table preserves the previous UV order for left/right, bottom/top, back, front, and the fallback rows for masked indices `6/7`. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_uses_branchless_face_uv_table` to guard the table and absence of old `vec2 uvs[4]` branches.
- Fresh shader UV artifact: `logs/gpu_shader_branchless_uv` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_shader_branchless_uv`. It passed with `smoke_err=0`, `terrain_samples=384`, `terrain_color_buckets=6`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.265`, `gpu_compositor_submit_max_ms=0.225`, `process_wall_p95_ms=0.050`, and `gpu_scene_target_replace=0`. The smoke-started server PID `2631` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-uv.txt` selected `logs/gpu_shader_branchless_uv/movement-stress-summary.txt` as the current movement summary and reported no error patterns.
- Checks passed so far for this shader UV slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_uses_branchless_face_uv_table`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (105/105), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-uv.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`f332190`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), and MCP `gn_review_diff` staged review (no high-risk symbols; degraded only by staged dirty-overlay/embeddings metadata).
- Next useful GPU step after checkpointing this slice: leave `face_corner` for a separate geometry-focused pass with stronger parity evidence. The UV table removed shader branching without changing atlas layout or fragment sampling, but `face_corner` changes would affect vertex positions directly.

- Current Phase 11 shader hot-path slice is behavior-preserving. `client/shaders/gpu_terrain_render.glsl` replaces the `face_normal` `if` chain with an 8-entry `FACE_NORMALS` lookup table; indices `5`, `6`, and `7` still resolve to `+Z`, preserving the old fallback behavior for masked or unexpected face indices. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_uses_branchless_face_normal_table` to guard the branchless lookup and fallback entries.
- Fresh shader artifact: `logs/gpu_shader_branchless_normals` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_shader_branchless_normals`. It passed with `smoke_err=0`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.417`, `gpu_compositor_submit_max_ms=0.195`, `process_wall_p95_ms=0.050`, `gpu_scene_target_replace=0`, `gpu_uniform_set_create=1`, `gpu_push_constant_avg_bytes=112.0`, and `terrain_samples=384`. The smoke-started server PID `29998` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-normals.txt` selected `logs/gpu_shader_branchless_normals/movement-stress-summary.txt` as the current movement summary and reported no error patterns.
- Checks passed so far for this shader slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_uses_branchless_face_normal_table`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (104/104), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-normals.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`a3761de`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), and MCP `gn_review_diff` staged review (no high-risk symbols; degraded only by staged dirty-overlay/embeddings metadata).
- Next useful GPU step after checkpointing this slice: continue Phase 11 with shader branch audit for `face_corner` / `face_uv`, but keep any larger geometry/UV table rewrite behind focused parity evidence. The current slice only removed the cheap normal branch chain and did not change lighting inputs or atlas UV math.

- Current Phase 10 binding invariant guard is test-only and behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_binding_layout_matches_uniform_set_contract`, which locks the render shader contract that set 0 binding 0 is `FaceBuffer`, set 0 binding 1 is the atlas `sampler2D` in both vertex/fragment stages, vertex reads `face_buffer.faces[face_instance]`, and fragment samples `atlas_texture`. No renderer runtime path, quality setting, artifact producer, or telemetry field changed.
- Checks passed so far for this guard slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_binding_layout_matches_uniform_set_contract`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (103/103), `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`ab54ae6`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this guard: keep the atlas layout in push constants unless a focused benchmark/profiler capture shows the constants phase matters. The current fresh frame-data evidence still shows constants work as tiny (`0.004ms` max constants component), so moving atlas data into immutable uniform data should be justified before changing behavior.

- Current Phase 10 frame-data telemetry slice is behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now counts actual `draw_list_set_push_constant` updates, total pushed bytes, average bytes per update, and exposes the fixed camera/lighting/atlas byte split. `client/rust_ext/src/lib.rs` publishes `gpu_push_constant_updates`, `gpu_push_constant_total_bytes`, `gpu_push_constant_avg_bytes`, `gpu_push_constant_camera_bytes`, `gpu_push_constant_lighting_bytes`, and `gpu_push_constant_atlas_bytes`; movement/workload summaries and the aggregate GPU report surface the same fields.
- Fresh frame-data artifact: `logs/gpu_frame_data_push_constants` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_frame_data_push_constants`. It passed with `gpu_frames=851`, `gpu_push_constant_updates=852`, `gpu_push_constant_total_bytes=95424`, `gpu_push_constant_avg_bytes=112.0`, byte split `gpu_push_constant_camera_bytes=64`, `gpu_push_constant_lighting_bytes=32`, `gpu_push_constant_atlas_bytes=16`, `gpu_scene_target_create=1`, `gpu_scene_target_reuse=850`, `gpu_scene_target_replace=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.463`, `gpu_compositor_submit_max_ms=0.151`, `process_wall_p95_ms=0.045`, `current_chunk_collision=2`, and `ground_misses=0`. The extra push constant update over `gpu_frames` is the one-time offscreen debug draw. The smoke-started server PID `31149` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-frame-data-push-constants.txt` now reports max `gpu_push_constant_updates=852`, max `gpu_push_constant_total_bytes=95424`, max `gpu_push_constant_avg_bytes=112.000`, and byte split `64/32/16`, with Metric Origins pointing to `logs/gpu_frame_data_push_constants/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for this Phase 10 frame-data telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (102/102), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-frame-data-push-constants.txt`, `git diff --check`, `./scripts/check.sh fast`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`c56a746`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: decide whether to move the 16-byte atlas layout out of per-frame push constants into immutable uniform data or leave it until profiler evidence shows constants cost matters. Current measured constants phase stayed tiny (`gpu_compositor_submit_max_parts_ms` constants component `0.004` in the fresh run), so a behavior change should be justified by a focused follow-up, not assumed.

- Current Phase 10 binding/frame-data telemetry slice is behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now tracks scene target create/reuse/replace counts, immutable uniform-set/atlas texture/sampler creation counts, and last push-constant byte size. `client/rust_ext/src/lib.rs` exposes these as `gpu_scene_target_create`, `gpu_scene_target_reuse`, `gpu_scene_target_replace`, `gpu_uniform_set_create`, `gpu_atlas_texture_create`, `gpu_atlas_sampler_create`, and `gpu_push_constant_bytes`; movement/workload summaries and the aggregate GPU report now surface the same fields.
- Fresh binding-churn artifact: `logs/gpu_binding_churn_movement` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_binding_churn_movement`. It passed with `gpu_scene_target_create=1`, `gpu_scene_target_reuse=852`, `gpu_scene_target_replace=0`, `gpu_uniform_set_create=1`, `gpu_atlas_texture_create=1`, `gpu_atlas_sampler_create=1`, `gpu_push_constant_bytes=112`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.859`, `gpu_compositor_submit_max_ms=0.087`, `process_wall_p95_ms=0.036`, `current_chunk_collision=2`, and `ground_misses=0`. The smoke-started server PID `47199` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-binding-churn.txt` now reports max `gpu_scene_target_create=1`, max `gpu_scene_target_reuse=852`, max `gpu_scene_target_replace=0`, max immutable binding creation counts `1`, and max `gpu_push_constant_bytes=112`, with Metric Origins pointing to `logs/gpu_binding_churn_movement/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the Phase 10 telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (102/102), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-binding-churn.txt`, `git diff --check`, `./scripts/check.sh fast`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`161c950`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS` in file-diff mode).
- Next useful GPU step after checkpointing this slice: stay in Phase 10 and inspect per-frame camera/light/constant updates now that scene-target and immutable binding churn are measured as stable. Do not change binding caching unless a fresh artifact shows `gpu_scene_target_replace` or immutable creation counts increasing unexpectedly.

- Current Phase 9 report-headroom slice is report-only. `scripts/gpu_terrain_report.sh` now derives `gpu_draw_cmd_occupancy_pct` and `gpu_draw_cmd_headroom_bytes` from existing `gpu_draw_cmd_bytes` / `gpu_draw_cmd_capacity_bytes` markers, and records the max occupancy source in Metric Origins. No runtime, renderer, workload, or artifact format producer changed.
- Fresh report artifact: `/tmp/rumpel-gpu-report-draw-command-occupancy.txt` from `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-occupancy.txt`. It reports max `gpu_draw_cmd_occupancy_pct=30.298`, min `gpu_draw_cmd_headroom_bytes=91360.000`, and points the occupancy source to `logs/gpu_draw_command_buffer_workload_heavy_radius16/extended/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the report-headroom slice: `sh -n scripts/gpu_terrain_report.sh`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-occupancy.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh`, OntoIndex reindex/status via `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings` and `bash ./scripts/ontoindex.sh status` (`8bd11dc`, up-to-date, 1,922 nodes / 5,068 edges / 68 clusters / 168 flows), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this report-only slice: move to Phase 10 binding/frame-data metrics, starting with uniform-set/scene-target recreation frequency, because current draw-command occupancy and compositor-submit data do not justify grouped draw submission yet.

- Current Phase 9 follow-up locks the indirect draw command ABI in code. `client/rust_ext/src/gpu_terrain.rs` now documents why the four `DrawIndirectCommand` fields stay present even when `first_vertex` is always `0`, and adds a shader contract test proving the render shader consumes `gl_InstanceIndex` for the face-buffer range and `gl_VertexIndex` only for generated quad corners. This is behavior-preserving and completes the "remove or justify unused draw command fields" audit without changing render output.
- Fresh heavy draw-command telemetry artifact: `logs/gpu_draw_command_buffer_workload_heavy_radius16` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=480 RUMPELMC_WORKLOAD_MATRIX_CASE_SET=heavy RUMPELMC_WORKLOAD_MATRIX_SERVER_VIEW_DISTANCE=16 RUMPELMC_WORKLOAD_MATRIX_CLIENT_KEEP_CHUNK_DISTANCE=16 RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0 sh scripts/gpu_terrain_workload_matrix.sh logs/gpu_draw_command_buffer_workload_heavy_radius16`. It passed all six radius-16 cases with `gpu_upload_fail=0` and `terrain_samples=384`. Matrix maxima were `gpu_draws=2482`, `gpu_draw_cmd_bytes=39712`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `gpu_faces=3256`, `terrain_queue_max_ms=3.781`, `gpu_compositor_submit_max_ms=0.161`, and `process_wall_p95_ms=0.055`. Peak command-buffer occupancy is about 30.3%, so current evidence does not justify grouped draw submission on capacity pressure alone. The smoke-started server PID `23125` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-heavy-radius16.txt` now points `gpu_draw_cmd_bytes=39712` to `logs/gpu_draw_command_buffer_workload_heavy_radius16/extended/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the Phase 9 follow-up: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (102/102), release heavy radius-16 workload matrix, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-heavy-radius16.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex reindex/status via `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings` and `bash ./scripts/ontoindex.sh status` (`99fae04`, up-to-date, 1,922 nodes / 5,067 edges / 68 clusters / 168 flows), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this follow-up: avoid grouped draw submission until profiler or heavier evidence shows submit pressure. A smaller next Phase 9 slice could add a draw-command occupancy/headroom guard to reports, or move to the next roadmap phase if no draw-submit bottleneck appears.

- Current draw-submission telemetry slice is behavior-preserving. It adds explicit indirect draw command buffer byte telemetry (`gpu_draw_cmd_bytes`, `gpu_draw_cmd_capacity_bytes`, `gpu_draw_cmd_stride`) plus an ABI/layout unit test for the 16-byte `IndirectDrawCommand`; render behavior, draw count, culling/front-face state, quality settings, and rollback flags are unchanged.
- Changed by this slice so far: `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_movement_stress.sh`, `scripts/gpu_terrain_workload_matrix.sh`, `scripts/gpu_terrain_report.sh`, `docs/AGENT_HANDOFF.md`, and `docs/GPU_TRENDS.md`.
- Fresh draw command telemetry movement artifact: `logs/gpu_draw_command_buffer_telemetry` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_draw_command_buffer_telemetry`. It passed with `gpu_draws=311`, `gpu_draw_cmd_bytes=4976`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `terrain_queue_max_ms=2.093`, `gpu_compositor_submit_max_ms=0.120`, `process_wall_p95_ms=0.044`, `current_chunk_collision=2`, `ground_misses=0`, and `gpu_upload_fail=0`. The smoke-started server was stopped and port `25565` was cleared.
- Fresh draw command telemetry standard workload artifact: `logs/gpu_draw_command_buffer_workload_standard` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=360 RUMPELMC_WORKLOAD_MATRIX_CASE_SET=standard RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_workload_matrix.sh logs/gpu_draw_command_buffer_workload_standard`. It passed `short`, `long`, `long-filled`, and `max-resident` with `gpu_upload_fail=0` and `terrain_samples=384` in all cases. Matrix maxima were `gpu_draw_cmd_bytes=15936`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `gpu_draws=996`, `gpu_faces=1522`, `terrain_queue_max_ms=2.324`, `gpu_compositor_submit_max_ms=0.177`, and `process_wall_p95_ms=0.048`. The smoke-started server PID `32480` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-buffer-workload.txt` now includes aggregate maxima for `gpu_draw_cmd_bytes`, `gpu_draw_cmd_capacity_bytes`, and `gpu_draw_cmd_stride`, and points `gpu_draw_cmd_bytes=15936` to `logs/gpu_draw_command_buffer_workload_standard/long-filled/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the draw command telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (101/101), `sh -n` for changed GPU shell scripts, release movement telemetry smoke, release standard workload matrix, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-buffer-workload.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh` (expected sensitive-path notices only), OntoIndex reindex/status via `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings` and `bash ./scripts/ontoindex.sh status` (`dad073e`, up-to-date, 1,921 nodes / 5,062 edges / 68 clusters / 168 flows), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: use the explicit draw command buffer byte telemetry to decide whether Phase 9 should target grouped/merged draw submission or only unused command-field cleanup behind a flag. Do not optimize draw submission solely from this telemetry until a heavier resident workload or profiler evidence shows the submit path moving toward budget.

- Current local refactor slice is behavior-preserving Rust/GPU terrain hotspot cleanup. Changed by this slice: `client/rust_ext/src/lib.rs`, `client/rust_ext/src/gpu_terrain.rs`, plus project instruction cleanup in `AGENTS.md` and `docs/PARALLEL_DEVELOPMENT_MONTH_PLAN.md` removing the old `5 files / 300 lines` stop rule at the user's request.
- Rust cleanup details: `GameClient.render_subchunk_mesh` now delegates proxy-refresh reuse, GPU upload, mesh data construction, ArrayMesh surface construction, and mesh-node/collision application to local helpers; `GameClient.get_perf_text` now delegates GPU-terrain perf text construction; `gpu_terrain::build_packed_faces` now uses a face-neighbor offset table instead of six repeated `push_visible_face` blocks. Intended behavior, env flags, GPU/CPU fallback paths, collision, shadows, and perf marker field names/order are unchanged.
- Checks passed for the refactor slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (97/97), `./scripts/check.sh fast`, and `git diff --check`.
- Focused post-refactor GPU smoke passed in `logs/gpu_refactor_smoke` using `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_refactor_smoke`. It reported `smoke_err=0`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_misses=0`, `terrain_queue_max_ms=2.036`, `gpu_compositor_submit_max_ms=0.136`, and `gpu_upload_fail=0`.
- OntoIndex was refreshed at current commit `55b9a07` through `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings`; `bash ./scripts/ontoindex.sh status` reports up-to-date with 1,904 nodes, 5,029 edges, 68 clusters, 167 flows, and semantic query loads the CPU embedding model successfully. Native graph writer remains unavailable, which is non-blocking for this repo.
- Current dirty edge-edit smoke slice extends `scripts/gpu_terrain_block_edit_stress.sh` with optional expected edge/bounds/partial/neighbor-refresh/collision/shadow assertions and adds `scripts/gpu_terrain_edge_block_edit_stress.sh`. The wrapper defaults to a toggle edit at global `127,64,95`, local chunk bounds `31,64,31:31,64,31`, expected edges `pos_x,pos_z`, opt-in `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1`, expected loaded-neighbor refresh of two edge chunks / four subchunks for the last update, and positive collision/shadow/proxy evidence.
- Fresh edge collision/shadow artifact: `logs/gpu_edge_collision_shadow_refresh_stress` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_edge_block_edit_stress.sh logs/gpu_edge_collision_shadow_refresh_stress`. It passed with `block_edit_dirty_observed=1`, `chunk_replace=2`, `dirty_chunks=2`, `dirty_blocks=2`, `dirty_edge_chunks=2`, `dirty_last_edges=pos_x,pos_z`, `dirty_last_bounds=31,64,31:31,64,31`, `dirty_edge_neighbor_chunks=4`, `dirty_edge_neighbor_subchunks=8`, `dirty_last_edge_neighbor_chunks=2`, `dirty_last_edge_neighbor_subchunks=4`, `dirty_partial_chunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_rebuilt=36`, `collision_refresh_last_rebuilt=1`, `mesh_shadow_only=200`, `proxy_shadow=200`, `proxy_shadow_only=190`, `compact_shadow_proxy=602`, `proxy_refresh_reuse=23`, `terrain_queue_max_ms=2.195`, `gpu_compositor_submit_max_ms=0.405`, `gpu_upload_fail=0`, and `ground_misses=0`. The smoke-started local server PID `29771` was stopped manually and port `25565` was cleared.
- Fresh full-vs-partial edge compare artifact: `logs/gpu_edge_dirty_compare` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_edge_dirty_compare.sh logs/gpu_edge_dirty_compare`. It runs the same edge edit once with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` and once with `=1`, requiring a clean port before start and stopping the smoke-started server between runs. The compare passed with matching dirty surface (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, bounds `31,64,31:31,64,31`, edges `pos_x,pos_z`), full control `dirty_partial_subchunks=0`, `dirty_edge_neighbor_subchunks=0`, partial candidate `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `dirty_edge_neighbor_subchunks=8`, both `current_chunk_collision=2`, both `collision_refresh_last_rebuilt=1`, both `proxy_shadow=162`, both `proxy_refresh_reuse=21`, full `terrain_queue_max_ms=2.304`, partial `terrain_queue_max_ms=5.039`, and both `gpu_upload_fail=0`.
- Fresh partial edge repeat artifact: `logs/gpu_edge_dirty_repeat` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_edge_dirty_repeat.sh logs/gpu_edge_dirty_repeat`. It runs the same opt-in partial edge edit three times in fresh server sessions, requires a clean port before start, and stops the smoke-started server after each run. The repeat sample passed with stable dirty surface and neighbor refresh evidence: each run had `dirty_edge_neighbor_subchunks=8`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=21`, and `gpu_upload_fail=0`. Aggregate maxima stayed under the 6.667ms 150 FPS CPU-side budget: `terrain_queue_max_ms_max=4.905`, `gpu_compositor_submit_max_ms_max=0.140`, and `process_wall_p95_ms_max=0.052`.
- Fresh single-edge partial artifact: `logs/gpu_single_edge_block_edit_stress` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_block_edit_stress.sh logs/gpu_single_edge_block_edit_stress`. It covers a non-corner `pos_x` edit at global `127,64,80` with exact edge validation enabled. The smoke passed with `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`, `dirty_edge_neighbor_chunks=2`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_chunks=1`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `compact_shadow_proxy=659`, `proxy_refresh_reuse=21`, `terrain_queue_max_ms=3.494`, `gpu_compositor_submit_max_ms=0.148`, `process_wall_p95_ms=0.052`, `ground_misses=0`, and `gpu_upload_fail=0`. The smoke-started local server PID `92444` was stopped manually and port `25565` was cleared.
- Fresh single-edge full-vs-partial compare artifact: `logs/gpu_single_edge_dirty_compare` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_dirty_compare.sh logs/gpu_single_edge_dirty_compare`. It reuses `scripts/gpu_terrain_edge_dirty_compare.sh` with global edit `127,64,80`, exact expected `dirty_last_edges=pos_x`, and bounds `31,64,16:31,64,16`. The compare passed with matching dirty surface (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`), full control `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, partial candidate `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, both `current_chunk_collision=2`, both `collision_refresh_last_rebuilt=1`, both `proxy_shadow=162`, full `terrain_queue_max_ms=2.222`, partial `terrain_queue_max_ms=3.541`, queue delta `1.319ms`, and both `gpu_upload_fail=0`.
- Fresh single-edge partial repeat artifact: `logs/gpu_single_edge_dirty_repeat` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_dirty_repeat.sh logs/gpu_single_edge_dirty_repeat`. It reuses the now-parameterized `scripts/gpu_terrain_edge_dirty_repeat.sh` with global edit `127,64,80`, exact expected `dirty_last_edges=pos_x`, bounds `31,64,16:31,64,16`, and last-neighbor threshold `1` edge chunk / `2` subchunks. The repeat sample passed three fresh server sessions with stable dirty evidence: every run had `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `proxy_refresh_reuse=23`, and `gpu_upload_fail=0`. Aggregate maxima stayed under the 6.667ms budget: `terrain_queue_max_ms_max=3.059`, `gpu_compositor_submit_max_ms_max=0.140`, and `process_wall_p95_ms_max=0.035`.
- Current default-on partial dirty slice changes `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` from opt-in to default-on. Absence of the env var now enables partial dirty upload; explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` remains the rollback/full-rebuild control. `scripts/gpu_terrain_movement_stress.sh` now forwards default `1`, matching the Rust default.
- Fresh default-on sanity artifact: `logs/gpu_default_partial_dirty_block_edit` from `env -u RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 ... sh scripts/gpu_terrain_block_edit_stress.sh logs/gpu_default_partial_dirty_block_edit` with the single-edge edit `127,64,80`. It passed without the partial env var present, proving the new default path: `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `compact_shadow_proxy=607`, `proxy_refresh_reuse=23`, `terrain_queue_max_ms=1.635`, `gpu_compositor_submit_max_ms=0.284`, `process_wall_p95_ms=0.040`, `ground_misses=0`, and `gpu_upload_fail=0`. The smoke-started local server PID `46591` was stopped manually and port `25565` was cleared.
- Fresh default-on rollback-control artifact: `logs/gpu_single_edge_dirty_compare_default_on` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_dirty_compare.sh logs/gpu_single_edge_dirty_compare_default_on`. It passed after the default flip, proving explicit rollback still works: full control with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` had `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `terrain_queue_max_ms=2.217`, and `gpu_upload_fail=0`; partial candidate with `=1` had `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `terrain_queue_max_ms=1.980`, and `gpu_upload_fail=0`. Dirty surface matched in both modes (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`).
- Fresh default-on movement sanity artifact: `logs/gpu_default_partial_movement_stress` from `env -u RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_default_partial_movement_stress`. It passed the normal movement path with wrapper-default partial dirty enabled but no block edit: `block_edit_dirty_observed=0`, `dirty_chunks=0`, `dirty_blocks=0`, `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_misses=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.202`, `gpu_compositor_submit_max_ms=0.147`, `process_wall_p95_ms=0.052`, `gpu_draws=336`, `gpu_faces=637`, `gpu_fragmentation_pct=0.0`, and `gpu_upload_fail=0`. The smoke-started local server PID `30702` was stopped manually and port `25565` was cleared.
- Fresh default-on standard workload artifact: `logs/gpu_default_partial_workload_standard` from `env -u RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=360 RUMPELMC_WORKLOAD_MATRIX_CASE_SET=standard RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_workload_matrix.sh logs/gpu_default_partial_workload_standard`. It passed `short`, `long`, `long-filled`, and `max-resident` under the new wrapper default. All four cases had `gpu_upload_fail=0` and `terrain_samples=384`; maxima across the matrix were `terrain_queue_max_ms=2.196`, `gpu_compositor_submit_max_ms=0.143`, `process_wall_p95_ms=0.055`, `gpu_draws=996`, and `gpu_faces=1522`. The smoke-started local server PID `74314` was stopped manually and port `25565` was cleared.
- Checks passed for the edge collision/shadow slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (98/98), `sh -n` for `scripts/gpu_terrain_block_edit_stress.sh`, `scripts/gpu_terrain_edge_block_edit_stress.sh`, and `scripts/gpu_terrain_report.sh`, release edge collision/shadow smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-edge-collision-shadow.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh`, OntoIndex reindex/status, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), and MCP `gn_verify_diff` (`PASS`) with explicit `changedFiles` including the untracked edge wrapper.
- Additional checks passed for the compare slice: `sh -n scripts/gpu_terrain_edge_dirty_compare.sh`, release full-vs-partial compare smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-edge-dirty-compare.txt`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (98/98), `./scripts/check.sh fast`, and `git diff --check`.
- Additional checks passed for the repeat slice: `sh -n` for `scripts/gpu_terrain_block_edit_stress.sh`, `scripts/gpu_terrain_edge_block_edit_stress.sh`, `scripts/gpu_terrain_edge_dirty_compare.sh`, `scripts/gpu_terrain_edge_dirty_repeat.sh`, and `scripts/gpu_terrain_report.sh`; release partial edge repeat smoke; `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-edge-dirty-repeat.txt`; `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`; `cargo test --manifest-path client/rust_ext/Cargo.toml` (98/98); `./scripts/check.sh fast`; and `git diff --check`.
- Additional checks passed for the single-edge slice: `sh -n` for `scripts/gpu_terrain_block_edit_stress.sh`, `scripts/gpu_terrain_single_edge_block_edit_stress.sh`, `scripts/gpu_terrain_edge_block_edit_stress.sh`, `scripts/gpu_terrain_edge_dirty_compare.sh`, `scripts/gpu_terrain_edge_dirty_repeat.sh`, and `scripts/gpu_terrain_report.sh`; release single-edge partial smoke; and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-single-edge.txt`.
- Additional checks passed for the single-edge compare slice: `sh -n scripts/gpu_terrain_edge_dirty_compare.sh`, `sh -n scripts/gpu_terrain_single_edge_dirty_compare.sh`, release single-edge full-vs-partial compare, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-single-edge-compare.txt`.
- Additional checks passed for the single-edge repeat slice: `sh -n scripts/gpu_terrain_edge_dirty_repeat.sh`, `sh -n scripts/gpu_terrain_single_edge_dirty_repeat.sh`, release single-edge partial repeat, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-single-edge-repeat.txt`.
- Additional checks passed so far for the default-on slice: `sh -n` for all changed GPU scripts, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, default-on release single-edge sanity smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-default-partial-dirty.txt`, `./scripts/check.sh fast` with Rust `99/99`, `git diff --check`, `sh ./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`) with explicit `changedFiles` including untracked GPU wrappers.
- Additional checks passed for the default-on rollback-control slice: release single-edge full-vs-partial compare after default flip, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-default-on-rollback-compare.txt`.
- Additional checks passed for the default-on movement sanity slice: release normal movement stress with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset; release standard workload matrix with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset; `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-default-on-movement.txt`; `cargo test --manifest-path client/rust_ext/Cargo.toml` (99/99); `git diff --check`; `sh ./scripts/diff_guard.sh`; `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings`; `bash ./scripts/ontoindex.sh status` (up-to-date, 1,911 nodes / 5,039 edges / 67 clusters / 167 flows); MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS` in file-scope mode; symbol-name mode is heuristic and lacks graph ids for supplied names).
- Next useful GPU step: checkpoint this dirty-update/default-on slice before starting the next roadmap area, or run a heavier radius workload only if more confidence is needed before checkpointing.

- User retired the multi-chat worker plan for now. Continue GPU work locally/sequentially from this HQ chat; do not poll, resume, or coordinate the old worker chats unless the user explicitly re-enables them.
- First parallel worker round was reviewed in HQ and integrated as adapted main commits, not direct worktree merges, because all worker worktrees were detached/dirty and behind current `main`.
- Integrated commits from the worker round: `58543c6` strengthens `scripts/diff_guard.sh` artifact warnings; `f0bbb79` adds world/chunk invariant tests; `5a96759` adds protocol docs plus packet/framing compatibility tests; `ad449e4` documents/tests current RocksDB key invariants; `724dd66` adds Rust client packet length guards and graceful stream-clone failure.
- Do not continue worker edits from the old detached worktrees without an explicit user request and a fresh sync plan. Storage/World/Protocol/Client Rust/Review QA worktrees may still contain their original dirty worker patches, some of which are now obsolete or adapted differently in `main`.
- Client Rust worker was based on an old pre-GPU-resident tree. Its meshing patch was not applied because current `main` already has a different shader/Rust path with capacity guards; only the current-main-safe packet guard and clone handling were integrated.

- Current chunk-streaming stability slice addresses player-visible chunk holes after fast fly-out/fly-back travel. Root cause is client-side immediate chunk unload outside `RUMPELMC_CLIENT_KEEP_CHUNK_DISTANCE` while the server/client reload path is still batch/queue limited; with fly noclip toggled off over a not-yet-rebuilt chunk, the player can fall through missing collision. This is not a worldgen/RocksDB cut by default.
- The client now keeps recently seen chunks for `RUMPELMC_CLIENT_CHUNK_UNLOAD_GRACE_SEC` seconds before unloading them. Default is `20.0`, max clamp is `120.0`, and `0` restores immediate unload behavior for rollback/control runs. Identical resent chunks refresh the residency timestamp without re-enqueueing full geometry rebuilds. This increases transient resident chunk memory during fast travel but does not reduce draw distance, lighting, shadows, texture quality, or visible quality.
- Checks passed for the chunk-unload grace slice: `cargo test --manifest-path client/rust_ext/Cargo.toml` (97/97) and `./scripts/check.sh fast`.
- Current flyback stress slice uses visual-smoke motion `chunk_fly_snap_back` by default. It moves diagonally into negative Z faster than the client's `0.5s` position-send cadence, then snaps back to origin and waits only `0.65s` for one normal position/collision tick. The wrapper is correctness-first: terrain/process/compositor budgets are report-only by default, while final chunk visibility, chunk collision, and a 3x3 downward ground raycast grid around the return point are enforced. Redundant single-ray ground assertions were removed; `ground_hit`/`ground_distance` remain logged as diagnostics.
- Fresh flyback artifact: `logs/gpu_flyback_snap_back_default` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_flyback_stress.sh logs/gpu_flyback_snap_back_default`. It passed with `motion_steps=9`, `motion_chunks=8`, `current_chunk="0,0"`, `current_chunk_loaded=1`, `current_chunk_submeshes=2`, `current_chunk_collision=2`, `ground_samples=9`, `ground_hits=9`, `ground_misses=0`, `ground_max_distance=12.000`, `terrain_samples=378`, `terrain_queue_max_ms=2.257`, `gpu_compositor_submit_max_ms=0.124`, and `gpu_upload_fail=0`. The local server was stopped and port `25565` was cleared.
- Control artifact: `logs/gpu_flyback_snap_back_grace0` from `RUMPELMC_CLIENT_CHUNK_UNLOAD_GRACE_SEC=0 ... sh scripts/gpu_terrain_flyback_stress.sh logs/gpu_flyback_snap_back_grace0` also passed the 3x3 ground grid with `ground_misses=0`, but logs show heavy unload/reload churn and lower residency (`chunks="53 loaded, 42 submeshes, 6 collision"`, `queue_max=192`, `queue_stale=14`, `gpu_draws=102`, `mem=53.0MB`) compared with the default grace run (`chunks="102 loaded, 94 submeshes, 10 collision"`, `queue_max=182`, `queue_stale=2`, `gpu_draws=200`, `mem=102.0MB`).
- Next useful stability step: add a manual/gameplay validation path for the fly-mode safety guard, then continue reducing residency churn without lowering draw distance, lighting, shadows, or texture quality.

- Latest GPU upload-frame telemetry slice adds Rust perf fields `terrain_queue_gpu_uploads=last/avg/max` and `terrain_queue_gpu_upload_kb=last/avg/max` for successful GPU uploads completed during terrain mesh-queue frames. `scripts/gpu_terrain_movement_stress.sh` surfaces those fields in movement summaries, and `scripts/gpu_terrain_report.sh` aggregates them from markers/reports. This is telemetry-only; upload behavior, draw distance, lighting, shadows, and texture quality are unchanged.
- Fresh release movement artifact: `logs/gpu_terrain_queue_upload_frame/movement-stress-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 ./scripts/gpu_terrain_movement_stress.sh logs/gpu_terrain_queue_upload_frame`. It passed with `terrain_queue_gpu_uploads=1/0.84/1`, `terrain_queue_gpu_upload_kb=0.0/0.0/1.5`, `gpu_upload_ms=0.014/0.005/0.136`, `gpu_upload_fail=0`, `gpu_fragmented_free_faces=18`, `gpu_fragmentation_pct=0.0`, `smoke_err=0`, `terrain_queue_max_ms=3.660`, `process_wall_p95_ms=0.037`, and `gpu_compositor_submit_max_ms=0.169`. Visual FPS was display-paced at 60 Hz on macOS/Metal. The local server left on port `25565` after the stress run was stopped.
- Checks passed for the slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (90/90), `sh -n` for the changed GPU scripts, `./scripts/check.sh fast` (required an unsandboxed rerun because Go/sccache cache access was blocked in sandbox), `./scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-upload-frame-full.txt`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Fresh heavy radius-16 upload artifact: `logs/gpu_terrain_upload_heavy_radius16` from `RUMPELMC_WORKLOAD_MATRIX_CASE_SET=heavy`, `RUMPELMC_WORKLOAD_MATRIX_SERVER_VIEW_DISTANCE=16`, `RUMPELMC_WORKLOAD_MATRIX_CLIENT_KEEP_CHUNK_DISTANCE=16`, `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0`, `GODOT_QUIT_AFTER_FRAMES=36000`, `GODOT_TIMEOUT_SEC=480`, run through `sh scripts/gpu_terrain_workload_matrix.sh ...`. Use `sh scripts/...` for these GPU scripts in this environment if direct `./scripts/...` invocation stalls before script body.
- Heavy radius-16 result: 5/6 cases passed. Successful cases stayed stable at `terrain_queue_gpu_uploads_max=1`, `terrain_queue_gpu_upload_kb_max=1.5`, `gpu_upload_fail=0`, `gpu_upload_ms_max=0.062`, `gpu_effective_draws=1584`, `gpu_faces=2246`, `terrain_queue_max_ms=1.928`, `gpu_compositor_submit_max_ms=0.165`, and `process_wall_p95_ms=0.051`. `extended-filled` reached `current_chunk="11,8"` but failed/hung during screenshot capture; a single-case retry also reached motion completion and then hung post-capture, was killed, and port `25565` was cleared.
- `scripts/gpu_terrain_report.sh` now aggregates `movement_terrain_queue ... max_ms=...` as `terrain_queue_max_ms`, so movement-only heavy artifacts no longer report `n/a` for terrain queue max. Verified report output at `/tmp/rumpel-gpu-report-heavy-radius16-upload-after.txt`.
- Current GPU upload breakdown slice adds marker/report fields for `gpu_upload_encode_ms`, `gpu_upload_stage_ms`, and `gpu_upload_update_ms` as `last/avg/max` triplets. This is telemetry-only and does not change upload behavior.
- Fresh upload-breakdown artifact: `logs/gpu_upload_breakdown_frame` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_upload_breakdown_frame`. It passed with `gpu_upload_ms=0.046/0.006/0.052`, `gpu_upload_encode_ms=0.002/0.000/0.004`, `gpu_upload_stage_ms=0.009/0.001/0.014`, `gpu_upload_update_ms=0.034/0.004/0.043`, `terrain_queue_max_ms=2.042`, `gpu_compositor_submit_max_ms=0.154`, and `gpu_upload_fail=0`. The local server left by the smoke run was stopped and port `25565` was cleared.
- Current dirty-update telemetry slice adds chunk diff counters to the Rust perf marker and unified report: `chunk_initial`, `chunk_replace`, `dirty_chunks`, `dirty_blocks`, changed/rebuild subchunk totals, last dirty masks, bounds, and edge labels. This is telemetry-only; chunk updates still use the existing full-subchunk geometry rebuild path.
- Fresh dirty telemetry artifact: `logs/gpu_dirty_update_telemetry_frame` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_dirty_update_telemetry_frame`. It passed with `chunk_initial=168`, `chunk_replace=0`, `dirty_chunks=0`, `dirty_blocks=0`, `dirty_last_rebuild_subchunks=0`, `terrain_queue_max_ms=2.032`, `gpu_compositor_submit_max_ms=0.170`, and `gpu_upload_fail=0`; no block edits were exercised in this movement-only run. The local server was stopped and port `25565` was cleared.
- Current block-edit stress slice adds an env-driven visual-smoke block edit path plus `scripts/gpu_terrain_block_edit_stress.sh`. The default action is `toggle` at `112,64,80` with block id `1`, so repeated runs return the block to air while still producing a dirty chunk update.
- Fresh block-edit artifact: `logs/gpu_block_edit_stress` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_block_edit_stress.sh logs/gpu_block_edit_stress`. It passed with `block_edit_dirty_observed=1`, `chunk_replace=2`, `dirty_chunks=2`, `dirty_blocks=2`, `dirty_last_blocks=1`, `dirty_last_changed_subchunks=1`, `dirty_last_rebuild_subchunks=2`, `dirty_last_bounds=16,64,16:16,64,16`, `dirty_last_edges=none`, `terrain_queue_max_ms=2.231`, `gpu_compositor_submit_max_ms=0.229`, and `gpu_upload_fail=0`. The local server was stopped and port `25565` was cleared.
- Current partial dirty-upload slice adds opt-in `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1`. When enabled for a dirty chunk replacement, the client queues only the dirty `rebuild_subchunk_mask` plus touched edge-neighbor subchunks; the existing full chunk replacement enqueue path remains the default rollback/control path. Rust perf text and `scripts/gpu_terrain_report.sh` now surface `dirty_partial_chunks`, `dirty_partial_subchunks`, and `dirty_partial_saved_subchunks`.
- Fresh partial dirty artifact: `logs/gpu_partial_dirty_block_edit_retry` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1 GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_block_edit_stress.sh logs/gpu_partial_dirty_block_edit_retry`. It passed with `dirty_partial_chunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `terrain_queue_max_ms=2.685`, `process_wall_p95_ms=0.044`, `gpu_compositor_submit_max_ms=0.194`, and `gpu_upload_fail=0`. The local server was stopped and port `25565` was cleared.
- Caveat: `logs/gpu_partial_dirty_block_edit_clean` was a failed clean retry with the same partial flag; visual and dirty counters passed, but `terrain_queue_max_ms=7.514` exceeded the 6.667 ms budget. Treat it as a transient spike/regression sample to watch before making partial dirty upload default.
- Next useful GPU step: add edge-edit smoke coverage for dirty updates near chunk borders, then verify neighbor face refresh before widening partial dirty upload beyond opt-in.

Fresh 2026-06-11 status:

- Long-running GPU backlog is now local/sequential, not delegated to worker chats. `docs/GPU_ROADMAP.md` defines 120 small GPU iterations across stabilization, stress gates, workload scaling, memory/residency, upload, dirty updates, draw submission, bindings, shader hot path, and larger GPU directions. `docs/GPU_PROFILING.md` defines trusted local metrics and macOS/Windows profiler workflows.
- GPU worker chats created during the interrupted delegation attempt were archived and should not be treated as active sources of changes; continue this backlog locally/sequentially from the main HQ thread unless the user explicitly re-enables delegation.
- Completed checkpoints: `2635006` enables GPU terrain back-face culling; `c5dad7f` adds the GPU roadmap/profiling docs. Next sequential step is to add the unified report script from the roadmap measurement phase, then continue with visual/stress gates.
- `scripts/gpu_terrain_report.sh` now aggregates existing GPU terrain summaries and markers into `logs/gpu-terrain-report.txt` by default. It is report-only, scans historical artifacts under the selected log dir, and marks local visual FPS/GPU timestamp caveats explicitly.
- Current marker-visibility slice adds `gpu_cull` and `gpu_front_face` to GPU terrain perf markers, movement-stress summaries, and the unified report's rasterization-state section. Fresh artifact: `logs/gpu_terrain_cull_summary/movement-stress-summary.txt` reports `gpu_cull=back gpu_front_face=clockwise`, `smoke_err=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=4.705`, `gpu_compositor_submit_max_ms=0.866`, and `fps_p05=144.0`. A first short validation run missed screenshot capture with `GODOT_QUIT_AFTER_FRAMES=12000`; rerunning with `36000` passed, though that run reported a very large `post_draw_wait_ms`, so capture-window stability remains worth watching.
- `scripts/gpu_terrain_fill_stress.sh` supports `RUMPELMC_FILL_STRESS_REPORT_ONLY_REPEATS`, intended for heavy cases such as repeat `16`. Normal repeats still fail the script on missing markers or movement-stress errors; report-only repeats write `status=failed` and continue. During validation, an attempted empty-repeat no-op exposed that `${VAR:-default}` ignored an explicitly empty `RUMPELMC_FILL_STRESS_REPEATS`; the script now uses `${VAR-default}` so empty repeats are respected for wrapper tests. The accidental test run was stopped and port `25565` was cleared.
- Fill-stress pass lines now include `status=pass`, `smoke_err`, `terrain_samples`, and `terrain_color_buckets`, so correctness and workload pressure are visible from `fill-stress-summary.txt` without opening the full marker. A normal repeat=1 validation in `logs/gpu_terrain_fill_summary_fields` reached chunk `11,8` but missed screenshot capture; the server was stopped afterward. Report-only failure behavior was validated with `GODOT_TIMEOUT_SEC=1`, producing `repeat=1 status=failed reason=movement_stress_failed` and exit 0 under `/tmp/rumpel_fill_report_only_fail`.
- Fill-stress report-only failure lines now include `run_status` and `marker_present`, making capture/window failures diagnosable directly from `fill-stress-summary.txt`.
- GPU residency telemetry now derives `gpu_fragmented_free_faces` and `gpu_fragmentation_pct` from allocator free ranges. This is report-only; allocator behavior is unchanged. `scripts/gpu_terrain_report.sh` includes both fields in aggregate signals when artifacts contain them.
- `scripts/gpu_terrain_report.sh` now includes a Metric Origins section for max workload/cost fields, so future agents can jump from an aggregate spike to the exact source summary or marker file.
- GPU upload telemetry now reports `gpu_upload_ms=last/avg/max` for successful `upload_subchunk` calls. Upload queue depth/latency was not added because the current code does not have a separate upload queue; adding one would be a broader behavior change.
- `docs/GPU_TRENDS.md` now records durable GPU optimization checkpoints and caveats. Update it after meaningful successful stress/profiling runs instead of burying all trend history in this handoff file.
- `scripts/handoff.sh` now includes GPU roadmap/profiling/trends in Read First and prints existing GPU trends/report snippets when available.

- Current weak-spot slice enables GPU terrain back-face culling by default in `client/rust_ext/src/gpu_terrain.rs`. The rasterization state now uses `PolygonCullMode::BACK` with `PolygonFrontFace::CLOCKWISE`; `RUMPELMC_GPU_TERRAIN_CULL_MODE=disabled|none|off` is a rollback control and `front` is available for diagnostics. This reduces hidden/back-face GPU raster work without reducing draw distance, lighting, shadows, texture quality, or visible quality.
- Fresh correctness artifact: `logs/gpu_terrain_cull_back_smoke/movement-stress-summary.txt` passed radius-16 extended movement with `gpu_upload_fail=0`, `gpu_draws=1582`, `gpu_effective_draws=1582`, `gpu_faces=2244`, `terrain_queue_max_ms=3.152`, `gpu_compositor_submit_max_ms=0.210`, and `smoke_err=0`.
- Fresh fill-stress artifacts: `logs/gpu_terrain_fill_stress_cull_back/fill-stress-summary.txt` and control `logs/gpu_terrain_fill_stress_cull_disabled_control/fill-stress-summary.txt`. In the current macOS session both cull-back and cull-disabled repeat=4 runs report `fps_p05=60.0`, so this run is display/driver-limited and is not a clean fps comparison against the earlier repeat=4 `fps_p05=144.0` result. Error scans were clean and `gpu_upload_fail=0`.
- Next useful weak-spot step: keep culling as a safe default if checks pass, then add a platform-specific GPU profiling path or a stronger synthetic stress/report gate that avoids the current 60 Hz visual limiter. Do not reduce visible quality to chase the 150 FPS target.

- Latest synthetic fill-stress slice adds `RUMPELMC_GPU_TERRAIN_COMPOSITOR_DRAW_REPEAT` (default `1`, clamp `64`) to repeat the same GPU terrain indirect draw inside the compositor pass. This is opt-in and keeps visible terrain quality unchanged while increasing GPU draw/fill pressure. Perf text now reports `gpu_effective_draws` and `gpu_draw_repeat`; movement stress summaries include the same fields. `scripts/gpu_terrain_fill_stress.sh` runs radius-16 extended movement with configurable repeat values.
- Fresh fill-stress artifacts: `logs/gpu_terrain_fill_stress_probe/fill-stress-summary.txt` and `logs/gpu_terrain_fill_stress_repeat8/fill-stress-summary.txt`. At radius-16/extended, `1x` produced `1582` effective draws and `fps_p05=144.0`; `4x` produced `6328` effective draws and still `fps_p05=144.0`; `8x` produced `12656` effective draws and dropped back to `fps_p05=60.0`. CPU-side submit stayed low (`gpu_compositor_submit_max_ms <= 0.148`) and `gpu_upload_fail=0`.
- A manual `16x` run in `logs/gpu_terrain_fill_stress_high/repeat-16.run.log` reached `Visual smoke motion complete` but did not save a screenshot before Godot exited, so treat `16x` as above the current automated smoke window rather than a clean pass. The fill-stress script was fixed so future movement-stress failures are not hidden by `tee`.
- Current conclusion: synthetic compositor overdraw gives a useful GPU/fill pressure knob. The visible automated marker can distinguish `4x` from `8x`, but CPU submit/terrain queue metrics still do not expose GPU cost; next useful step is a report-mode fill-stress gate around `4x/8x` or a platform GPU profiler path for macOS/Windows.

- Latest stress-profiling run pushed the opt-in heavy workload beyond the initial radius-12 slice without code changes. Fresh artifacts: `logs/gpu_terrain_workload_heavy_radius14/workload-matrix-summary.txt` and `logs/gpu_terrain_workload_heavy_radius16/workload-matrix-summary.txt`.
- Radius-14 heavy passed cleanly with peak resident load `1222` GPU draws / `1816` faces, `gpu_upload_fail=0`, worst `terrain_queue_max_ms=2.414`, worst `gpu_compositor_submit_max_ms=0.157`, and worst `process_wall_p95_ms=0.062`.
- Radius-16 heavy passed cleanly with peak resident load `1588` GPU draws / `2246` faces, `gpu_upload_fail=0`, worst `terrain_queue_max_ms=2.421`, worst `gpu_compositor_submit_max_ms=0.221`, and worst `process_wall_p95_ms=0.056`. Error scans were clean and the local server was stopped after each run.
- Current conclusion: within the existing opt-in stress clamp, terrain queue work and GPU compositor submit are not the limiting factor for the 150 FPS target. Local visual FPS is still display/driver-paced around 60 Hz on macOS, and Godot GPU timestamp values still report `0.0us`, so the next useful step is either a platform GPU profiler path or a heavier synthetic draw/fill-rate stress that does not reduce visible quality.

- Latest heavy-radius workload slice adds opt-in stress radius controls without changing default gameplay radius: server `RUMPELMC_SERVER_VIEW_DISTANCE` defaults to `10` and clamps to `16`; client `RUMPELMC_CLIENT_KEEP_CHUNK_DISTANCE` defaults to `10` and clamps to `16`. `scripts/gpu_terrain_workload_matrix.sh` keeps `RUMPELMC_WORKLOAD_MATRIX_CASE_SET=standard` by default, while `case_set=heavy` defaults both stress radii to `12`.
- The same slice adds visual-smoke motion `chunk_walk_extended` ending at chunk `11,8`, plus heavy matrix cases `extended` and `extended-filled`. This raises test pressure without reducing draw distance, lighting, shadows, texture quality, or visible quality; it only asks the server/client to hold more chunks during opt-in stress runs.
- Fresh radius-12 artifact: `logs/gpu_terrain_workload_heavy_radius12/workload-matrix-summary.txt` from `RUMPELMC_WORKLOAD_MATRIX_CASE_SET=heavy`, `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0`, `GODOT_QUIT_AFTER_FRAMES=36000`, `GODOT_TIMEOUT_SEC=420`. It raised resident load from the previous `~628` draws / `~1090` faces to `876` draws / `1402` faces. Worst CPU-side metrics still pass the 150 FPS budget (`6.667ms`): `extended terrain_queue_max_ms=2.405`, `extended gpu_compositor_submit_max_ms=0.213`, `short process_wall_p95_ms=0.067`, and all cases had `gpu_upload_fail=0`. Error scan was clean and the local server was stopped afterward.
- Review note: the local review pass caught an accidental replacement of existing `configuredChunksPerUpdate` tests in `server/pkg/network/server_test.go`; those tests were restored and new view-distance tests were added beside them.
- Next likely optimization step: push the opt-in radius to `14` or `16` only if needed, then optimize the first metric that approaches budget. Current radius-12 results do not justify changing visible quality or rewriting renderer code.

- Latest repeat workload stability slice adds phase logs around visual-smoke capture in `client/main.gd`: capture start, motion completion, post-draw completion, and image-save timing. This diagnosed a repeat-run failure where `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0` made Godot reach `--quit-after 8000` immediately after motion completion, before `RenderingServer.frame_post_draw`; use a larger `GODOT_QUIT_AFTER_FRAMES` for delayed/heavy captures.
- `scripts/gpu_terrain_workload_matrix.sh` now passes `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC` through to movement stress as `SMOKE_DELAY_SEC`. Default remains `5.0`; the fresh heavier repeat used `20.0` plus `GODOT_QUIT_AFTER_FRAMES=20000`.
- Fresh repeat artifact: `logs/gpu_terrain_workload_repeat_2/workload-repeat-summary.txt` from `RUMPELMC_WORKLOAD_MATRIX_REPEAT_COUNT=2`, `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0`, `GODOT_QUIT_AFTER_FRAMES=20000`, `GODOT_TIMEOUT_SEC=240`, batch `64`. Repeat summary passed with worst reported values: `long terrain_queue_max_ms=3.791`, `max-resident gpu_compositor_submit_max_ms=0.167`, `long process_wall_p95_ms=0.077`, and `gpu_upload_fail=0` in both per-run matrix summaries. Error scans over repeat logs were clean. The local server process was stopped afterward.
- Next likely optimization step: increase resident/load pressure beyond the current `~628` GPU draws / `~1090` faces before optimizing more code; current repeated batch=64 metrics are still comfortably under the 150 FPS CPU-side budget (`6.667ms`) and the local visual FPS remains display-paced at 60 Hz.

- Latest compositor-submit breakdown slice adds Rust perf fields `gpu_compositor_submit_parts=setup/target/constants/draw` for the last compositor frame and `gpu_compositor_submit_max_parts=setup/target/constants/draw` for the same frame that produced `gpu_compositor_submit_ms` max. Movement stress and workload matrix summaries now print `gpu_compositor_submit_max_parts_ms=setup/target/constants/draw`.
- `scripts/gpu_terrain_workload_matrix.sh` now also supports optional repeat runs via `RUMPELMC_WORKLOAD_MATRIX_REPEAT_COUNT=N`; it writes `workload-repeat-summary.txt` with min/avg/max for `gpu_compositor_submit_max_ms`, `terrain_queue_max_ms`, and `process_wall_p95_ms` per case. Default remains a single run.
- Fresh breakdown artifacts: `logs/gpu_compositor_submit_breakdown/movement-stress-summary.txt` and `logs/gpu_compositor_submit_breakdown_matrix/workload-matrix-summary.txt`. Movement stress passed with `gpu_compositor_submit_max_ms=0.120` and max parts `0.026/0.000/0.028/0.065`; matrix passed with representative max parts like `short 0.023/0.000/0.031/0.089` and `long-filled 0.029/0.000/0.019/0.102`. Error scans were clean.
- Checks for this breakdown slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (84/84), `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_movement_stress.sh`, release movement stress capture, release workload matrix capture with shortened max-resident settle, log error scans, `./scripts/check.sh fast`, and `git diff --check` all passed. The local server process started for captures was stopped afterward.
- Next likely step is to rerun a heavier batch=64 workload with `RUMPELMC_WORKLOAD_MATRIX_REPEAT_COUNT=2` or `3` after starting a fresh server, then inspect whether any repeated submit spike is dominated by setup, target, constants, or draw.

- Latest server streaming test-mode slice adds `RUMPELMC_SERVER_CHUNKS_PER_UPDATE` to the Go server. Default behavior stays unchanged at `6` chunks per update; invalid, zero, or negative env values fall back to the default. `scripts/gpu_terrain_workload_matrix.sh` now passes `RUMPELMC_WORKLOAD_MATRIX_SERVER_CHUNKS_PER_UPDATE` (default `64`) through to Godot/server-started runs and prints `server_reused=0/1` per case so reused already-running servers are obvious.
- Fresh batch-aware workload artifact after rebuilding the local server binary and starting a fresh server: `logs/gpu_terrain_workload_matrix_batch_fresh/workload-matrix-summary.txt`. The short case reports `server_reused=0` and server logs confirm `batch=64`; later cases reuse that fresh server. Batch streaming raises short-route load from the prior `~260` draws to `gpu_draws=536`, while max-resident reaches `gpu_draws=628`, `gpu_faces=1090`, `gpu_upload_fail=0`. Gates still pass: max-resident `terrain_queue_max_ms=1.822`, `process_wall_p95_ms=0.042`, `gpu_compositor_submit_max_ms=0.124`. The long case had one compositor submit spike at `5.277ms`, still under the 150 FPS budget (`6.667ms`) and worth watching in future heavier runs.
- Checks for this server streaming slice: `go test ./pkg/network ./pkg/world`, `sh -n scripts/gpu_terrain_workload_matrix.sh`, release workload matrix capture, matrix log error scan, `./scripts/check.sh fast`, and `git diff --check` all passed.
- The local batch=64 server process was stopped after the capture so a hidden test-mode server is not left running. Next likely step: push the resident set higher than ~630 draws/faces ~1090, or repeat batch=64 with a larger view/load path if upload or compositor submit spikes start approaching budget.

- Latest max-resident workload slice extends `scripts/gpu_terrain_workload_matrix.sh` with a `max-resident` case. It reuses `chunk_walk_long` and a configurable `RUMPELMC_WORKLOAD_MATRIX_MAX_RESIDENT_SETTLE_SEC` defaulting to `30.0`, so the current player radius has more time to fill without reducing draw distance, lighting, shadows, texture quality, or visible quality.
- Fresh max-resident artifact: `logs/gpu_terrain_workload_matrix_max/workload-matrix-summary.txt`. The new case raises simultaneous GPU terrain load to `gpu_draws=634`, `gpu_subchunks=634`, `gpu_faces=1064` with `gpu_upload_fail=0`. It still passes refresh-independent gates: `terrain_queue_max_ms=1.961`, `process_wall_p95_ms=0.045`, `gpu_compositor_submit_max_ms=0.131`; local visual `frame_p95_ms` remains `16.667` due 60 Hz pacing and GPU timestamp max remains `0.0us` on macOS/Metal.
- Checks for this max-resident slice: `sh -n scripts/gpu_terrain_workload_matrix.sh`, `git diff --check`, release workload matrix capture, matrix log error scan, and `./scripts/check.sh fast` all passed.
- Next likely step is to push the resident set higher or make the server/client streaming fill radius faster in test mode, then optimize only if `gpu_compositor_submit_max_ms`, `terrain_queue_max_ms`, or upload failures start moving toward budget.

- Latest workload-scaling slice adds `RUMPELMC_VISUAL_SMOKE_MOTION=chunk_walk_long`, parameterizes `scripts/gpu_terrain_movement_stress.sh` with `RUMPELMC_MOVEMENT_STRESS_MOTION` and `RUMPELMC_MOVEMENT_STRESS_EXPECTED_CHUNK`, and adds `scripts/gpu_terrain_workload_matrix.sh` to compare short, long, and long-filled movement captures without reducing draw distance, lighting, shadows, texture quality, or visible quality.
- Fresh workload matrix artifact: `logs/gpu_terrain_workload_matrix/workload-matrix-summary.txt`. The `long` path alone does not increase simultaneous GPU load because far chunks are unloaded around the current player chunk, but `long-filled` gives the server more settle time at the final chunk and raises GPU terrain load from `short gpu_draws=261 gpu_faces=537` to `long-filled gpu_draws=440 gpu_faces=769`. Under that higher load, real terrain queue/process/submit metrics still pass: `terrain_queue_max_ms=1.737`, `process_wall_p95_ms=0.061`, `gpu_compositor_submit_max_ms=0.144`, `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` due local 60 Hz display/driver pacing, and GPU timestamp max remains `0.0us` on the current macOS/Metal backend.
- Checks for this workload slice: `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `git diff --check`, release workload matrix capture, log error scan for matrix artifacts, and `./scripts/check.sh fast` all passed.
- Next likely optimization target is no longer basic compositor submit or terrain queue under 440 draws; use the workload matrix to push to a heavier simultaneous loaded set or add an explicit no-quality-loss stress mode for max resident terrain, then optimize the first metric that grows past budget.

- Latest GPU compositor timestamp probe slice adds report-first Godot `RenderingDevice` timestamp capture around the GPU terrain compositor draw list. Rust perf text now includes `gpu_compositor_gpu_samples`, `gpu_compositor_gpu_ms=last/avg/max`, and `gpu_compositor_gpu_us=last/avg/max`; baseline and movement stress summaries print the same data.
- Timestamp budget enforcement is intentionally opt-in for now: `RUMPELMC_PERF_BASELINE_GPU_TIMESTAMP_BUDGET_MODE=report` and `RUMPELMC_MOVEMENT_STRESS_GPU_TIMESTAMP_BUDGET_MODE=report` are the defaults. On the current macOS/Metal Godot run, timestamp samples are captured but GPU delta is reported as `0.0us`, so this probe must not be treated as a reliable GPU-time gate locally.
- Fresh timestamp artifacts: `logs/gpu_compositor_timestamp_baseline/perf-baseline-summary.txt` and `logs/gpu_compositor_timestamp_us/movement-stress-summary.txt`. Static GPU baseline passes the real terrain queue/process/submit gates with `terrain_queue_max_ms=1.957`, `process_wall_p95_ms=0.043`, `gpu_compositor_submit_max_ms=0.120`, `gpu_compositor_gpu_samples=356`, `gpu_compositor_gpu_max_us=0.0`, and `gpu_upload_fail=0`. Movement stress passes with `terrain_queue_max_ms=1.875`, `process_wall_p95_ms=0.076`, `gpu_compositor_submit_max_ms=0.140`, `gpu_compositor_gpu_samples=673`, `gpu_compositor_gpu_max_us=0.0`, and `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` due local 60 Hz display/driver pacing.
- Checks for this timestamp slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `sh -n scripts/gpu_terrain_perf_baseline.sh && sh -n scripts/gpu_terrain_movement_stress.sh`, `git diff --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (84/84), release movement stress capture, release static baseline capture, and `./scripts/check.sh fast` all passed. Error scans for the fresh timestamp movement and baseline logs had no `ObjectDB`, leaked-instance, panic, script-error, failed, or exceeds matches.
- Next optimization should not trust the local timestamp gate as proof of GPU time. Prefer a workload-scaling stress matrix that increases visible GPU terrain load/draws/faces without reducing quality, or a platform GPU profiler path for Windows/macOS, then optimize whichever metric actually moves under load.

- Latest GPU compositor submit gate slice adds `gpu_compositor_submit=count` and `gpu_compositor_submit_ms=last/avg/max` to Rust perf text, perf baseline summaries, and movement stress summaries. The gate measures CPU callback submit/setup time inside the Godot compositor path; it is not a GPU timestamp query.
- Fresh compositor submit artifacts: `logs/gpu_compositor_submit_baseline/perf-baseline-summary.txt` and `logs/gpu_compositor_submit_gate/movement-stress-summary.txt`. Static GPU baseline passes: `gpu_compositor_submit_avg_ms=0.029`, `gpu_compositor_submit_max_ms=0.121`, `terrain_queue_max_ms=2.017`, `process_wall_p95_ms=0.045`, `gpu_upload_fail=0`. Movement stress passes: `gpu_compositor_submit_avg_ms=0.033`, `gpu_compositor_submit_max_ms=0.128`, `terrain_queue_max_ms=2.051`, `process_wall_p95_ms=0.053`, `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` due local 60 Hz display/driver pacing.

- Latest process-wall gate slice adds refresh-independent `_process` wall-time gates to terrain perf scripts. `scripts/gpu_terrain_perf_baseline.sh` now defaults `RUMPELMC_PERF_BASELINE_PROCESS_WALL_BUDGET_MODE=enforce`; `scripts/gpu_terrain_movement_stress.sh` now defaults `RUMPELMC_MOVEMENT_STRESS_PROCESS_WALL_BUDGET_MODE=enforce` and prints `process_wall_p95_ms` in `movement-stress-summary.txt`.
- Fresh process-wall movement artifact: `logs/terrain_queue_max_parts/movement-stress-summary.txt`. Release GPU movement stress passes both real queue and process-wall gates: `terrain_queue_avg_ms=0.874`, `terrain_queue_max_ms=1.977`, `max_mesh_ms=1.977`, `max_coll_ms=0.000`, `process_wall_p95_ms=0.042`, `budget_status=pass`, `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` because the current macOS/Metal visual smoke is display/driver paced at 60 Hz.

- Latest real queue breakdown slice adds `terrain_queue_work_max_parts=mesh/collision` to Rust perf text and both terrain perf summaries. This records the mesh and collision portions from the same frame that produced `terrain_queue_work_ms` max, so future optimization targets the real peak instead of the independent `mesh_max + coll_max` pessimistic sum.
- Fresh queue breakdown artifact: `logs/terrain_queue_max_parts/movement-stress-summary.txt`. GPU movement stress passes the 150 FPS real queue budget: `terrain_queue_avg_ms=0.981`, `terrain_queue_max_ms=2.111`, `max_mesh_ms=2.111`, `max_coll_ms=0.000`, `budget_status=pass`, while independent maxima were `mesh_max_ms=4.490` and `coll_max_ms=3.540` in different frames. The fresh marker has `terrain_queue_work_max_parts=2.111/0.000`, `gpu_upload_fail=0`, and no run-log error matches.

- Latest terrain queue budget gate slice makes the real `terrain_queue_work_ms` max enforceable by default in both static and movement terrain perf scripts. `scripts/gpu_terrain_perf_baseline.sh` still keeps visual `frame_p95_ms` in report mode by default because the local automated visual FPS metric is display/driver paced at 60 Hz, but `RUMPELMC_PERF_BASELINE_TERRAIN_QUEUE_BUDGET_MODE=enforce` is now the default for CPU and GPU terrain queue max against the 150 FPS budget. `scripts/gpu_terrain_movement_stress.sh` also defaults to `RUMPELMC_MOVEMENT_STRESS_BUDGET_MODE=enforce`.
- Fresh terrain queue gate artifact: `logs/terrain_queue_budget_gate/movement-stress-summary.txt`. GPU terrain movement stress passes the real queue budget: `terrain_queue_avg_ms=0.945`, `terrain_queue_max_ms=1.921`, `budget_status=pass`, `mesh_max_ms=4.960`, `coll_max_ms=3.890`, `frame_p95_ms=16.667`, `fps_p05=60.0`, `gpu_upload_fail=0`, `current_chunk="3,2"`. Static validate-only baseline against `logs/indexed_cpu_array_mesh` also passes the new queue gate: CPU `terrain_queue_max_ms=2.698`, GPU `terrain_queue_max_ms=1.985`, budget `6.667ms`.

- Latest movement-stress gate slice updates `scripts/gpu_terrain_movement_stress.sh` to normalize relative output dirs to repo-root absolute paths and write `movement-stress-summary.txt` with `terrain_queue_avg_ms`, `terrain_queue_max_ms`, `mesh_*`, `coll_*`, and 150 FPS budget status.
- Fresh movement artifact: `logs/movement_queue_work/movement-stress-summary.txt`. GPU terrain movement stress over `chunk_walk` passes the real queue budget: `terrain_queue_avg_ms=0.934`, `terrain_queue_max_ms=1.936`, `budget_status=pass`, `mesh_max_ms=4.820`, `coll_max_ms=3.840`, `gpu_upload_fail=0`, `current_chunk="3,2"`.
- Checks for this slice: `sh -n scripts/gpu_terrain_movement_stress.sh`, release movement stress capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/movement_queue_work/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest CPU ArrayMesh spike slice changes packed-face CPU fallback meshes to use 4 quad vertices plus 6 indices per unit quad instead of 6 duplicated vertices. UV tiling and triangle order are preserved, and visible quality/draw distance/lighting/shadows are unchanged.
- Fresh release artifact: `logs/indexed_cpu_array_mesh/perf-baseline-summary.txt`. CPU real queue work now passes the 150 FPS budget even at max: `terrain_queue_avg_ms=1.085`, `terrain_queue_max_ms=2.698`, `terrain_queue_budget_status=pass`. The previous CPU ArrayMesh spike was `terrain_queue_max_ms=16.271` with `max_mesh_phase array_mesh_ms=15.89`; after indexing, CPU `max_mesh_phase array_mesh_ms=1.13`, `mesh_max_ms=4.520`, `coll_max_ms=3.520`. GPU remains within real queue budget: `terrain_queue_avg_ms=0.872`, `terrain_queue_max_ms=1.985`.
- Checks for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (82/82), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/indexed_cpu_array_mesh/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest collision-refresh weak-spot slice makes collision refresh build direct packed collision faces from chunk data when cached faces are missing, instead of falling back to Godot `MeshInstance3D.create_trimesh_collision()`. It also adds `collision_refresh_phase_last=faces/clear/create/count/node_counts` and `collision_refresh_phase_max=...` to Rust perf text, and makes the `mesh` perf triplet reflect full `render_subchunk_mesh` job time including ArrayMesh/collision/node work.
- Fresh release artifact: `logs/collision_refresh_direct_faces/perf-baseline-summary.txt`. CPU collision max dropped from the previous `coll_max_ms=23.460` to `coll_max_ms=3.240`; CPU terrain average remains within 150 FPS budget (`avg_ms=1.840`, `budget_status=pass`). The remaining CPU max is now clearly an ArrayMesh surface creation spike: `terrain_queue_max_ms=16.271`, `mesh_max_ms=16.270`, `max_mesh_phase array_mesh_ms=15.89`. GPU remains within real queue budget: `terrain_queue_avg_ms=0.876`, `terrain_queue_max_ms=2.095`.
- Checks for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (80/80), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/collision_refresh_direct_faces/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest CPU fallback weak-spot slice adds `terrain_queue_work_ms=last/avg/max` and `terrain_queue_work_frames` to Rust perf text, and `scripts/gpu_terrain_perf_baseline.sh` now reports `terrain_queue_avg_ms`, `terrain_queue_max_ms`, and `terrain_queue_budget_status`. This gives a real per-frame mesh+collision queue max instead of relying only on independent mesh/collision maxima.
- CPU ArrayMesh fallback now uses the existing packed-face ArrayMesh path by default, with `RUMPELMC_CPU_ARRAY_MESH_PACKED_FACES=0` as a control/rollback switch for old compute/readback meshing. No draw distance, lighting, shadows, texture quality, or visible quality settings were reduced.
- Fresh release artifact: `logs/cpu_packed_faces_baseline/perf-baseline-summary.txt`. CPU terrain work improved from the previous queue-work run `avg_ms=9.340`, `mesh_max_ms=19.660`, `terrain_queue_avg_ms=7.594` to `avg_ms=1.290`, `mesh_max_ms=1.570`, `terrain_queue_avg_ms=1.575`. CPU average now passes the 150 FPS budget, but CPU `terrain_queue_max_ms=23.482` still fails due to collision/ArrayMesh spike (`coll_max_ms=23.460`, `max_array_mesh_phase array_mesh_ms=15.91` in the marker). GPU remains comfortably within real queue budget: `terrain_queue_avg_ms=0.899`, `terrain_queue_max_ms=2.388`.
- Checks for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (79/79), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/cpu_packed_faces_baseline/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest terrain spike-smoothing slice runs collision refresh before mesh queue work and skips mesh queue processing in frames where collision actually rebuilt. This keeps near-player collision updates responsive while avoiding stacked mesh+collision terrain work in the same frame.
- `scripts/gpu_terrain_perf_baseline.sh` now normalizes relative output dirs to repo-root absolute paths before launching Godot, matching the pacing matrix behavior and preventing markers from being written under `client/logs/...`.
- The perf baseline summary now reports `max_pessimistic_ms` plus per-component max fields (`mesh_max_ms`, `coll_max_ms`, `draw_rebuild_max_ms`, `draw_patch_max_ms`) instead of a plain ambiguous `max_ms`; the pessimistic max is still a sum of independent component maxima, not a same-frame timing sample.
- Fresh release spike-smoothing artifact: `logs/gpu_terrain_spike_smoothing/perf-baseline-summary.txt`. GPU terrain work: `avg_ms=1.005`, `max_pessimistic_ms=5.561`, `capacity_avg_fps=995.0`, `budget_status=pass`, `max_pessimistic_budget_status=pass` for the 150 FPS budget (`6.667ms`), `mesh_max_ms=1.660`, `coll_max_ms=3.870`. CPU fallback remains over budget: `avg_ms=8.650`, `max_pessimistic_ms=42.400`.
- Checks for the spike-smoothing slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (77/77), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/gpu_terrain_spike_smoothing/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest leak-cleanup slice adds explicit visual-smoke shutdown for `GameClient`, detaches the GPU terrain compositor before freeing its RIDs, and makes `ComputeMesher` free its local `RenderingDevice` through `Gd::free()` after releasing compute pipeline/shader RIDs.
- Fresh verbose smoke artifact `logs/gpu_terrain_leak_fix/run.log` no longer reports `ObjectDB instances leaked at exit`; the previous leak was `RenderingDevice` plus two internal `Object` instances from the local compute mesher device lifetime. Release wrapper smoke also passed in `logs/gpu_terrain_leak_fix_release/run.log`.
- Latest weak-spot cleanup adds `scripts/fix_godot_userdata_case.sh`, a safe local maintenance helper for the repeated macOS Godot userdata warning where `project.godot` uses `config/name="rumpelgame"` but the local app userdata directory existed as `RUMPELGAME`.
- The helper renames through a temporary path when both names resolve to the same directory on a case-insensitive filesystem. If both variants are real different directories, it refuses to merge or delete user data automatically.
- `scripts/gpu_terrain_pacing_matrix.sh` now normalizes relative output directories to repo-root absolute paths before launching Godot, so screenshot markers are not accidentally written under `client/`.
- Current pacing investigation slice adds `RUMPELMC_VISUAL_SMOKE_MAX_FPS` and `scripts/gpu_terrain_pacing_matrix.sh`.
- Fresh pacing matrix artifacts: `logs/gpu_terrain_pacing_matrix/gpu-maxfps-30.png.txt`, `gpu-maxfps-0.png.txt`, `gpu-maxfps-150.png.txt`, `gpu-maxfps-240.png.txt`, and `pacing-matrix-summary.txt`.
- Fresh pacing matrix result: `engine_max_fps=30` produces `fps_avg=30.0`, proving the Godot FPS cap works downward. `engine_max_fps=0`, `150`, and `240` all produce `fps_avg=60.0`, `vsync_mode=0`, and `screen_refresh_hz=60.000`, proving the current automated visual FPS metric is capped by display/driver pacing above the 60 Hz screen refresh.
- Godot 4.6.2 command-line help says `--disable-vsync` does not override driver-level V-Sync enforcement. This matches the matrix result and explains why `Engine.max_fps` above refresh is not enough on the current macOS/Metal run.

- Stabilization commit created on `main`: `58ebe46` (`Stabilize GPU terrain perf baseline`). It captured the previous broad GPU terrain/perf slice before starting the next profiler slice.
- Current post-commit dirty slice is intentionally small: `client/main.gd`, `scripts/gpu_terrain_perf_baseline.sh`, and `docs/AGENT_HANDOFF.md`.
- The new slice adds visual-smoke end-to-end profiler marker fields: `process_wall_samples`, `process_wall_avg_ms`, `process_wall_p95_ms`, `process_wall_max_ms`, `post_draw_wait_ms`, `image_read_ms`, `image_save_ms`, and `image_metrics_ms`. The perf baseline summary now prints these fields.
- Fresh release e2e profile artifacts: `logs/gpu_terrain_e2e_profile/cpu-arraymesh-baseline.png.txt` and `logs/gpu_terrain_e2e_profile/gpu-terrain-baseline.png.txt`. Current GPU marker: `fps_avg=60.0`, `process_wall_avg_ms=0.030`, `process_wall_p95_ms=0.044`, `post_draw_wait_ms=31.802`, `screen_refresh_hz=60.000`, `gpu_terrain_work avg_ms=0.976`, `capacity_avg_fps=1024.6`. This points at frame/display pacing rather than GDScript `_process` or terrain update work.

- The committed `58ebe46` slice covered `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `client/shaders/gpu_terrain_render.glsl`, `client/main.gd`, `scripts/gpu_terrain_perf_baseline.sh`, and `scripts/gpu_terrain_movement_stress.sh`.
- The committed GPU terrain slice adds greedy packed-face merging, face extents in the GPU terrain shader, direct/cached collision-face accounting, and collision timing perf records.
- A review pass found and fixed a CPU ArrayMesh fallback cap bug: merged faces can expand into many unit quads for UV parity, so CPU fallback must enforce `MAX_CPU_ARRAY_MESH_VERTICES` against expanded vertices, not packed face count. Guard test added.
- Fresh checks passed: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh`, `./scripts/gpu_terrain_compact_proxy_benchmark.sh`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, and `cargo test --manifest-path client/rust_ext/Cargo.toml` with 76/76 tests.
- Fresh release perf baseline passed after building the release Rust GDExtension with `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1`. Artifacts: `logs/gpu_terrain_perf_baseline/cpu-arraymesh-baseline.png.txt` and `logs/gpu_terrain_perf_baseline/gpu-terrain-baseline.png.txt`.
- Fresh release movement stress passed. Artifact: `logs/gpu_terrain_visual_smoke/movement_stress/gpu-terrain-movement-stress.png.txt`.
- Fresh perf numbers: CPU fallback `frame_p95_ms=12.500`, `fps_p05=80.0`; GPU terrain `frame_p95_ms=8.333`, `fps_p05=120.0`; movement stress GPU `frame_p95_ms=8.333`, `fps_p05=120.0`, `current_chunk="3,2"`, `gpu_upload_fail=0`.
- The 150 FPS target is still not proven. The latest GPU release captures are exactly 120 FPS, which may be a renderer/display cap rather than pure workload limit; the next step should verify or bypass that cap before chasing smaller CPU/GPU optimizations.
- FPS cap investigation added visual-smoke runtime fields to marker logs: `engine_max_fps`, `vsync_mode`, and `screen_refresh_hz`. `RUMPELMC_VISUAL_SMOKE_FORCE_UNCAPPED=1` now forces `Engine.max_fps=0` and `DisplayServer.VSYNC_DISABLED` from `client/main.gd`; the perf baseline and movement stress scripts pass that flag through.
- Fresh forced-uncapped release baseline artifacts: `logs/gpu_terrain_fps_cap/cpu-arraymesh-baseline.png.txt` and `logs/gpu_terrain_fps_cap/gpu-terrain-baseline.png.txt`. Both reported `engine_max_fps=0`, `vsync_mode=0`, `screen_refresh_hz=60.000`, and exactly ~60 FPS (`frame_p95_ms=16.667`). This confirms the current visual smoke FPS metric is display/frame-pacing limited on this run, not a reliable proof of terrain renderer capacity above monitor refresh.
- In the forced-uncapped GPU marker the terrain work itself is light: `mesh_avg_ms=0.82`, `mesh_max_ms=1.26`, `coll_avg_ms=0.16`, `gpu_upload_fail=0`, `gpu_subchunks=132`, `gpu_draws=132`, `gpu_faces=338`.
- `scripts/gpu_terrain_perf_baseline.sh` now writes `logs/gpu_terrain_fps_cap/perf-baseline-summary.txt` with refresh-independent `*_terrain_work` lines. Current derived results: CPU terrain work `avg_ms=9.930`, `capacity_avg_fps=100.7`, `budget_status=fail`; GPU terrain work `avg_ms=0.986`, `capacity_avg_fps=1014.2`, `budget_status=pass` for a 150 FPS budget (`6.667ms`). Treat this as a terrain update/work proxy, not a full end-to-end FPS claim.
- Current post-commit profiler slice passes `./scripts/diff_guard.sh`; the previous 1005-line warning was resolved by committing the broad GPU terrain slice as `58ebe46`.

Goal:

- Continue performance optimization toward stable high FPS without reducing draw distance, lighting, shadows, or visible quality.
- Current technical direction is GPU-resident terrain on Godot RenderingDevice, compatible with macOS and Windows.

Done:

- Added GPU terrain upload/render prototype behind `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Added Godot compositor integration that attaches to the player camera and draws GPU terrain into the scene color target.
- Added GPU terrain texture atlas sampling from `res://assets/textures/blocks/block_texture_atlas.png`.
- Kept the current GPU terrain solid-block pass opaque; atlas alpha is not used for block opacity until transparent block metadata and a dedicated transparency path exist.
- Moved GPU terrain atlas layout from a hard-coded shader column count into Rust-validated atlas metadata pushed to the render shader; the current atlas is validated as 64px tiles, 10 columns, 1 row.
- Made the GPU terrain atlas texture prefer `R8G8B8A8_SRGB` sampling for closer albedo parity with Godot materials, with a guarded fallback to `R8G8B8A8_UNORM` if the backend rejects sRGB sampling.
- Made the opt-in GPU terrain compositor path depth-compatible by requesting resolved depth, binding a color+depth framebuffer, and enabling depth test/write for scene rendering.
- Fixed the GPU compositor depth test for Godot 4.6 reverse-Z by using `GREATER_OR_EQUAL`; `LESS_OR_EQUAL` could produce sky-only screenshots even while GPU draw counters advanced.
- Added a lighting-aware GPU terrain slice: the Rust client reads the scene `SunLight` direction/color/energy, passes it through GPU terrain push constants, and the GPU terrain shader shades block faces from per-face normals with directional diffuse plus ambient.
- Added a conservative shadow-compatibility slice: terrain `MeshInstance3D` shadow casting is explicitly configured as double-sided, so the retained ArrayMesh terrain can serve as the Godot shadow-map proxy while the opt-in GPU RD compositor path handles visible terrain rendering.
- Added an opt-in GPU render bridge where CPU ArrayMesh subchunk nodes switch to Godot `SHADOWS_ONLY` after the GPU compositor is attached and the GPU render pipeline is ready, preserving fallback visibility if GPU render setup fails.
- Added the first safe CPU ArrayMesh reduction step for the opt-in GPU path: after GPU upload, distant subchunks that do not need local collision or a Godot shadow proxy drop their CPU `MeshInstance3D` while keeping the GPU slot intact.
- Added a delayed visible-path transition: CPU fallback/proxy nodes are not hidden or removed until the GPU compositor has produced at least one frame, and a subchunk keeps CPU fallback if its GPU upload did not produce a slot.
- Added a GPU-visible refresh so existing CPU subchunk nodes are re-evaluated once the GPU compositor becomes the confirmed visible terrain path, instead of waiting for later player movement or chunk updates.
- Added a faster CPU shadow/collision proxy path for opt-in GPU terrain: after successful GPU upload and confirmed GPU visible rendering, retained CPU proxy `ArrayMesh` geometry is built directly from `PackedFaceBatch` vertices/normals instead of rerunning the full compute mesher/readback/UV path.
- Added CPU proxy reason counters to the Rust client perf text: `proxy_coll`, `proxy_shadow`, `proxy_both`, and `proxy_shadow_only`. These counters explain why retained CPU `SubchunkMesh_*` nodes still exist in the opt-in GPU terrain path.
- Added opt-in CPU shadow proxy strategy selection through `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MODE`. The default remains `conservative`; `collision_only` drops shadow-only CPU proxies while preserving nearby collision proxies. Rust perf text now includes `shadow_mode=...`.
- Made compact shadow-only CPU proxy meshes the default for GPU-visible shadow-only proxies. `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=full` remains an explicit opt-out/control mode; collision proxies plus CPU fallback meshes stay on the full ArrayMesh path. Rust perf text includes `shadow_mesh=...` and `compact_shadow_proxy=...`.
- Added compact proxy payload measurement to Rust perf text: `compact_shadow_normals_saved=...`, plus `normals last=... total=...`. The parity gate now requires zero saved normals outside compact mode and positive saved normals in the compact shadow-proxy case.
- Extended the parity gate with a compact `lighting_shadow` pose case. It compares compact shadow-only proxy rendering against the full GPU `lighting_shadow` marker using the same visual metric envelopes before compact can be considered for default use.
- Added `scripts/gpu_terrain_compact_proxy_benchmark.sh` as a report-first benchmark helper for compact shadow-only and collision-only proxy payload/runtime metrics. By default it reads the existing parity artifacts; direct Godot captures are opt-in with `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1` because shell-launched Godot wrappers can still stall in the current Codex PTY environment.
- Kept visible CPU fallback behavior unchanged: CPU-only rendering and failed GPU uploads still use the existing compute mesher with UVs/materials.
- Added visual smoke capture hooks in `client/main.gd` through `RUMPELMC_VISUAL_SMOKE_PATH`, `RUMPELMC_VISUAL_SMOKE_DELAY_SEC`, and `RUMPELMC_VISUAL_SMOKE_HIDE_HUD`.
- Strengthened visual smoke validation with `terrain_samples` and `smoke_err`; sky-only frames now fail even if the image is bright and GPU counters are nonzero.
- Extended visual smoke markers with region, color-bucket, chroma, and terrain-luma-range metrics so parity can catch atlas/color/lighting/depth distribution regressions beyond a basic non-sky frame.
- Added multi-pose visual smoke support through `RUMPELMC_VISUAL_SMOKE_POSE=default|atlas_depth|lighting_shadow`; capture waits for `RenderingServer.frame_post_draw` after applying the pose so the screenshot reflects the fixed camera.
- Added `scripts/gpu_terrain_visual_smoke.sh` as a CPU/GPU smoke wrapper; in the current Codex PTY environment direct Godot commands are the reliable gate because the wrapper can stall before project logs appear.
- Added `scripts/gpu_terrain_parity_smoke.sh` as a CPU/GPU/radius=1 parity gate. It validates marker files for `smoke_err=0`, terrain samples, region coverage, atlas/color diversity, terrain luma range, CPU fallback counters, CPU proxy reason counters, GPU counters, fast proxy usage, radius=1 proxy/collision parity, and conservative CPU/GPU visual envelopes across default, atlas/depth, and lighting/shadow poses.
- Extended the parity gate with a GPU `shadow_radius=0` conservative case that validates `shadow_path=scene_shadows_disabled` and keeps retained CPU proxy work collision-only when scene shadows are disabled.
- Extended the parity gate with a `collision_only` GPU case that verifies shadow proxy counters stay at zero while CPU proxy count still matches collision body count.
- Added `RUMPELMC_VISUAL_SMOKE_DISABLE_PLAYER_INPUT=1` for automated visual smokes so player input does not accidentally break/place blocks and mutate the world during capture.
- Added GPU terrain log/perf visibility, including `gpu_frames`.
- Kept ArrayMesh terrain fallback active.
- Added this handoff system: `docs/HANDOFF.md`, `docs/AGENT_HANDOFF.md`, and `scripts/handoff.sh`.

Relevant files:

- `client/rust_ext/src/gpu_terrain.rs`
- `client/rust_ext/src/lib.rs`
- `client/rust_ext/src/player.rs`
- `client/shaders/gpu_terrain_render.glsl`
- `scripts/gpu_terrain_visual_smoke.sh`
- `scripts/gpu_terrain_parity_smoke.sh`
- `scripts/gpu_terrain_compact_proxy_benchmark.sh`
- `logs/gpu_terrain_visual_smoke/direct-cpu.png`
- `logs/gpu_terrain_visual_smoke/direct-cpu.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-cpu-smoke-gate.png`
- `logs/gpu_terrain_visual_smoke/direct-cpu-smoke-gate.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z-proxy-radius1.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z-proxy-radius1.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-final.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-final.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-radius1-final.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-radius1-final.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-cpu-fast-proxy-final.png`
- `logs/gpu_terrain_visual_smoke/direct-cpu-fast-proxy-final.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/cpu-arraymesh-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/cpu-arraymesh-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-radius1-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-radius1-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-collision-only-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-collision-only-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-shadow-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-shadow-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-lighting-shadow-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-lighting-shadow-parity.png.txt`
- `logs/godot-gpu-terrain-smoke.log`
- `logs/godot-gpu-terrain-atlas-smoke.log`
- `logs/godot-gpu-terrain-depth-smoke.log`
- `logs/godot-gpu-terrain-lighting-smoke.log`
- `logs/godot-gpu-terrain-shadow-proxy-smoke.log`
- `logs/godot-gpu-terrain-opaque-smoke.log`
- `logs/godot-gpu-terrain-atlas-layout-smoke.log`
- `logs/godot-gpu-terrain-atlas-layout-shadow-proxy-smoke.log`
- `logs/godot-gpu-terrain-srgb-atlas-shadow-proxy-smoke.log`
- `docs/HANDOFF.md`
- `scripts/handoff.sh`

Checks:

- `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the shadow-proxy slice.
- `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the lighting-aware GPU terrain slice.
- `./scripts/check.sh full` passed after the GPU compositor changes.
- `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after atlas/depth work.
- `./scripts/check.sh full` with sccache failed locally with `sccache: error: Operation not permitted (os error 1)`; rerun without sccache passed.
- `cargo fmt -- --check`, `cargo build`, `cargo check`, and `cargo test` passed in `client/rust_ext`; Rust tests: 7/7.
- `cargo build --manifest-path client/rust_ext/Cargo.toml` passed.
- Godot smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot atlas smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot depth smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot lighting smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot shadow-proxy smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot opaque solid-block smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot atlas-layout smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot atlas-layout plus shadow-proxy bridge smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot sRGB atlas plus shadow-proxy bridge smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`; no UNORM fallback warning was observed on the local Metal backend.
- Direct CPU visual smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=0`, screenshot and marker saved under `logs/gpu_terrain_visual_smoke/`.
- Direct GPU visual smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`, screenshot and marker saved under `logs/gpu_terrain_visual_smoke/`; marker included nonzero `gpu_frames`, `gpu_subchunks`, `gpu_draws`, and `gpu_faces`.
- Direct GPU visual smoke passed after the reverse-Z depth fix: `direct-gpu-reverse-z.png`, `terrain_samples=576`, `smoke_err=0`, `gpu_frames=160`, `gpu_subchunks=88`, `gpu_faces=143452`.
- Direct GPU proxy-reduction smoke passed with `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE=1`: `direct-gpu-reverse-z-proxy-radius1.png`, `cpu_proxy=10`, `collision=10`, `gpu_subchunks=92`, `terrain_samples=288`, `smoke_err=0`.
- Direct CPU visual smoke passed after the strengthened visual gate: `direct-cpu-smoke-gate.png`, `terrain_samples=288`, `smoke_err=0`, `current_chunk="0,0"`.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the reverse-Z/fallback-transition/smoke-gate fixes; `golangci-lint` is not installed locally and was skipped by the script.
- Latest Rust fast-proxy checks passed in `client/rust_ext`: `cargo fmt -- --check`, `cargo check`, `cargo test` (10/10), and `cargo build`.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the fast CPU proxy slice; `golangci-lint` is not installed locally and was skipped by the script.
- Direct GPU fast-proxy smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`: `direct-gpu-fast-proxy-final.png`, `terrain_samples=288`, `smoke_err=0`, `gpu_frames=190`, `gpu_subchunks=90`, `gpu_faces=145506`, `fast_proxy=163`.
- Direct GPU fast-proxy radius smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1` and `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE=1`: `direct-gpu-fast-proxy-radius1-final.png`, `cpu_proxy=10`, `collision=10`, `fast_proxy=20`, `terrain_samples=288`, `smoke_err=0`, `gpu_frames=195`.
- Direct CPU fallback smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=0`: `direct-cpu-fast-proxy-final.png`, `terrain_samples=288`, `smoke_err=0`, `fast_proxy=0`, `current_chunk="0,0"`.
- Latest parity smoke artifacts passed on the final Rust build with player input disabled:
  - CPU fallback: `parity/cpu-arraymesh-parity.png`, `terrain_samples=256`, `avg_luma=0.3517`, `smoke_err=0`, `fast_proxy=0`, `collision=10`.
  - GPU terrain: `parity/gpu-terrain-parity.png`, `terrain_samples=256`, `avg_luma=0.3442`, `smoke_err=0`, `gpu_frames=68`, `gpu_subchunks=48`, `gpu_faces=80994`, `fast_proxy=62`, `collision=10`.
  - GPU radius=1: `parity/gpu-terrain-radius1-parity.png`, `terrain_samples=256`, `avg_luma=0.3442`, `smoke_err=0`, `gpu_frames=74`, `cpu_proxy=10`, `collision=10`, `fast_proxy=19`.
- `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the latest parity artifacts.
- `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
- `sh -n scripts/gpu_terrain_visual_smoke.sh` passed after adding wrapper checks for `smoke_err=0` and nonzero `terrain_samples`.
- `scripts/gpu_terrain_visual_smoke.sh` passed `sh -n`, but can time out in this Codex PTY environment before Godot project logs appear. Prefer the direct `/opt/homebrew/bin/timeout ... /usr/bin/env ... /opt/homebrew/bin/godot --path client ...` commands for the current gate.
- `sh -n scripts/handoff.sh` passed.
- `./scripts/handoff.sh` ran successfully and printed the continuation snapshot.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the parity smoke slice; `golangci-lint` is not installed locally and was skipped by the script.
- `./scripts/diff_guard.sh` passed after the dirty-tree cleanup baseline was committed.
- Latest direct parity captures passed after the extended visual metrics slice:
  - CPU fallback: `parity/cpu-arraymesh-parity.png`, `terrain_samples=256`, `terrain_mid_samples=64`, `terrain_bottom_samples=192`, `terrain_left_samples=128`, `terrain_right_samples=128`, `terrain_color_buckets=8`, `terrain_chroma_samples=256`, `terrain_luma_range=0.3655`, `smoke_err=0`, `fast_proxy=0`, `collision=10`.
  - GPU terrain: `parity/gpu-terrain-parity.png`, `terrain_samples=256`, `terrain_color_buckets=8`, `terrain_chroma_samples=256`, `terrain_luma_range=0.3532`, `smoke_err=0`, `gpu_frames=143`, `gpu_subchunks=81`, `gpu_faces=133218`, `fast_proxy=127`, `collision=10`.
  - GPU radius=1: `parity/gpu-terrain-radius1-parity.png`, `terrain_samples=256`, `terrain_color_buckets=8`, `terrain_chroma_samples=256`, `terrain_luma_range=0.3532`, `smoke_err=0`, `gpu_frames=149`, `gpu_subchunks=82`, `gpu_faces=135266`, `cpu_proxy=10`, `collision=10`, `fast_proxy=20`.
- Latest `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the extended parity artifacts.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the extended visual metrics slice; `golangci-lint` is not installed locally and was skipped by the script.
- Latest `./scripts/diff_guard.sh` passed on the small 3-file slice before commit.
- Latest proxy-reason counter slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (11/11), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Parity captures were refreshed with the new perf counters and `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed.
  - CPU fallback marker: `cpu_proxy=80`, `proxy_coll=10`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `collision=10`.
  - GPU default marker: `cpu_proxy=82`, `proxy_coll=10`, `proxy_shadow=82`, `proxy_both=10`, `proxy_shadow_only=72`, `collision=10`.
  - GPU radius=1 marker: `cpu_proxy=10`, `proxy_coll=10`, `proxy_shadow=10`, `proxy_both=10`, `proxy_shadow_only=0`, `collision=10`.
- Latest multi-pose parity slice passed:
  - Default CPU/GPU/radius captures were refreshed with `pose="default"`.
  - Atlas/depth pose captures passed: CPU `terrain_samples=510`, `terrain_color_buckets=10`, `terrain_luma_range=0.3378`; GPU `terrain_samples=510`, `terrain_color_buckets=9`, `terrain_luma_range=0.3294`.
  - Lighting/shadow pose captures passed: CPU `terrain_samples=457`, `terrain_color_buckets=8`, `terrain_luma_range=0.3654`; GPU `terrain_samples=453`, `terrain_color_buckets=10`, `terrain_luma_range=0.3570`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed across all seven marker files.
  - `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest shadow-proxy mode slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (12/12), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Fresh direct parity captures were produced with `RUMPELMC_VISUAL_SMOKE_DELAY_SEC=1.5` because the shell wrapper can time out in the current Codex PTY before Godot writes markers.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed across eight marker files.
  - Collision-only marker: `cpu_proxy=10`, `proxy_coll=10`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `shadow_mode=collision_only`, `collision=10`.
  - Conservative radius=1 marker: `cpu_proxy=9`, `proxy_coll=9`, `proxy_shadow=9`, `proxy_both=9`, `proxy_shadow_only=0`, `shadow_mode=conservative`, `collision=9`.
- Latest compact shadow-proxy mesh slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (15/15), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
  - Fresh direct parity captures were produced across nine marker files, including the compact shadow-only proxy case.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the latest parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Compact marker: `cpu_proxy=19`, `proxy_coll=10`, `proxy_shadow=19`, `proxy_both=10`, `proxy_shadow_only=9`, `shadow_mode=conservative`, `shadow_mesh=compact`, `compact_shadow_proxy=9`, `collision=10`, `gpu_frames=32`.
  - Conservative radius=1 marker after rerun with `RUMPELMC_VISUAL_SMOKE_DELAY_SEC=3.0`: `cpu_proxy=10`, `proxy_coll=10`, `proxy_shadow=10`, `proxy_both=10`, `proxy_shadow_only=0`, `shadow_mesh=full`, `compact_shadow_proxy=0`, `gpu_frames=131`.
- Latest compact proxy measurement slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (16/16), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
  - Fresh direct parity captures were produced across nine marker files with the new `compact_shadow_normals_saved` and `normals last/total` perf fields.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the latest parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Compact marker: `cpu_proxy=70`, `proxy_coll=10`, `proxy_shadow=70`, `proxy_both=10`, `proxy_shadow_only=60`, `shadow_mesh=compact`, `compact_shadow_proxy=98`, `compact_shadow_normals_saved=1327104`, `collision=10`, `gpu_frames=145`.
  - Full/default GPU marker: `shadow_mesh=full`, `compact_shadow_proxy=0`, `compact_shadow_normals_saved=0`, `normals last=18432 total=1575360`.
- Latest compact shadow visual parity slice passed:
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
  - Direct compact lighting/shadow capture passed: `gpu-terrain-compact-lighting-shadow-parity.png`, `pose="lighting_shadow"`, `terrain_samples=453`, `terrain_color_buckets=11`, `terrain_luma_range=0.3570`, `shadow_mesh=compact`, `compact_shadow_proxy=97`, `compact_shadow_normals_saved=1320960`, `collision=10`, `gpu_frames=141`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the full parity artifact set, including the compact-vs-full `lighting_shadow` visual metric pair.
  - `./scripts/check.sh fast` passed; Rust tests remained 16/16.
- Latest compact proxy benchmark helper slice passed:
  - `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh` passed.
  - `./scripts/gpu_terrain_compact_proxy_benchmark.sh` passed in default report mode against current parity artifacts.
  - `./scripts/check.sh fast` passed; Rust tests remained 16/16.
  - Benchmark summary from current artifacts: full `normals_total=1593792`, `mesh_avg_ms=10.18`, `mesh_max_ms=38.49`, `coll_avg_ms=0.73`, `gpu_frames=144`; compact `normals_total=260544`, `compact_shadow_proxy=97`, `compact_shadow_normals_saved=1320960`, `mesh_avg_ms=9.32`, `mesh_max_ms=34.15`, `coll_avg_ms=0.79`, `gpu_frames=141`.
  - Reported `normal_total_delta=1333248` and `normal_total_reduction=83.7%` for the compact `lighting_shadow` marker pair.
- Latest compact-default slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (16/16), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` and `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh` passed.
  - Direct baseline GPU capture with empty `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=` passed: `shadow_mesh=compact`, `compact_shadow_proxy=95`, `compact_shadow_normals_saved=1284096`, `collision=10`, `gpu_frames=138`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the full parity artifact set; explicit `full` markers remain as control/opt-out cases.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest collision-only render-state slice passed:
  - Collision-only CPU proxies now use `visible=false` and `ShadowCastingSetting::OFF` when a subchunk has a GPU slot but does not need a shadow proxy. CPU fallback meshes stay visible/double-sided if a subchunk is not actually backed by a GPU slot.
  - Rust perf text now reports terrain mesh render-state counters: `mesh_visible`, `mesh_shadow_off`, `mesh_shadow_double`, and `mesh_shadow_only`. The parity gate uses them to catch `collision_only` regressions where retained collision proxies become visible or cast shadows again.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (17/17), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Direct collision-only capture passed after adding render-state counters: `mesh_visible=0`, `mesh_shadow_off=10`, `mesh_shadow_double=0`, `mesh_shadow_only=0`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `collision=10`, `gpu_frames=104`, `terrain_samples=256`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the full parity artifact set.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain render flag safety slice passed:
  - `RUMPELMC_GPU_TERRAIN_RENDER` remains opt-in by default; the Rust decision helper now has a unit test that default/no env stays disabled, explicit true enables, and explicit false disables.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (19/19), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest opaque solid GPU terrain shader guard slice passed:
  - Added a Rust unit test that reads the included GPU terrain render shader and verifies the solid pass forces fragment alpha to `1.0` and does not use `texel.a`. This protects the prior grass/solid block transparency regression.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (20/20), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain shader push-constant layout guard slice passed:
  - Added a Rust unit test that reads the included GPU terrain render shader and verifies the GLSL push-constant order matches the Rust byte layout: clip matrix, lighting block, then atlas layout.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (21/21), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain reverse-Z depth-state guard slice passed:
  - Added a Rust unit test for the pure depth-state config used by the RD render pipeline: scene depth keeps test/write paired with `enable_depth`, and the compare operator stays `GREATER_OR_EQUAL` for Godot 4.6 reverse-Z.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (22/22), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain shadow-proxy radius slice passed:
  - `GameClient::terrain_shadow_proxy_chunk_distance()` now delegates to a pure helper covered by unit tests. Explicit radius overrides are clamped, finite Godot shadow distance is converted to chunk radius, disabled scene shadows return radius `0`, unavailable scene data uses the conservative default distance, and non-finite distances fall back to `DEFAULT_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE`.
  - This keeps the default conservative shadow-proxy behavior when the `SunLight` cannot be inspected, while avoiding unnecessary shadow-only CPU proxies when Godot shadows are explicitly disabled.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (25/25), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest CPU proxy decision guard slice passed:
  - `GameClient::subchunk_needs_collision()`, `subchunk_needs_shadow_proxy()`, and `subchunk_needs_cpu_proxy()` now delegate to pure helpers covered by unit tests. The tests lock the fallback rule, nearby collision radius, conservative-vs-collision-only shadow mode, shadow radius behavior, and the rule that GPU-visible subchunks without collision or shadow reasons may drop their CPU proxy.
  - The shadow helper now treats `radius <= 0` as no shadow proxy even before `current_player_chunk` is known, matching disabled Godot shadows and avoiding an origin-only shadow proxy in that state.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (28/28), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest CPU proxy refresh guard slice passed:
  - `GameClient::enqueue_cpu_proxy_refresh()` now delegates its per-chunk refresh condition to pure helpers covered by a unit test. The test locks start-up refresh with no previous player chunk, collision radius enter/leave edges, shadow-only refresh outside collision radius, and disabled shadow radius `0`.
  - Behavior is unchanged: the refresh still queues loaded chunks that were or are inside the collision radius or the active conservative shadow-proxy radius.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (29/29), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU attach refresh guard slice passed:
  - `GameClient::refresh_cpu_proxies_after_gpu_attach()` now delegates the list of chunks to refresh to a pure helper covered by a unit test. The test locks the intentional behavior: no refresh before the GPU visible path is active, and all loaded chunks are refreshed after GPU visibility is confirmed.
  - This protects the distant ArrayMesh removal path from a future optimization that only refreshes nearby chunks and leaves old CPU terrain nodes alive after the first GPU frame.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (30/30), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU terrain mesh-build plan guard slice passed:
  - `GameClient::render_subchunk_mesh()` now delegates the CPU mesh path choice to a pure `TerrainMeshBuildPlan` helper covered by a unit test. The test locks the three production-sensitive outcomes: remove the CPU node when uploaded GPU terrain has no CPU proxy reason, build a packed-face CPU proxy only after the GPU visible path is active and packed faces are available, and fall back to the full ArrayMesh path otherwise.
  - This preserves the ArrayMesh fallback for failed GPU uploads, pre-confirmation GPU state, and missing packed-face data while making future distant CPU mesh removal changes easier to review.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (31/31), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU terrain upload flag guard slice passed:
  - `gpu_terrain_upload_enabled()` now delegates its decision to a pure helper covered by a unit test. The test locks that upload stays off by default, explicit upload enables it, and opt-in GPU render also enables upload so visible GPU terrain cannot run without GPU face uploads.
  - This preserves the current invariant that `RUMPELMC_GPU_TERRAIN_RENDER` itself remains opt-in while keeping the render/upload dependency explicit and reviewable.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (32/32), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU terrain visible activation guard slice passed:
  - `GameClient::gpu_terrain_visible_render_active()` now delegates its decision to a pure helper covered by a unit test. The test locks the delayed transition invariant: CPU fallback/proxy nodes are only allowed to switch after the render flag is enabled, the compositor is attached, and the GPU terrain buffer pool has confirmed at least one visible render frame.
  - This protects the visible-path transition from hiding or removing ArrayMesh fallback too early when GPU setup is only partially complete.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (33/33), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest terrain mesh render-state counter guard slice passed:
  - `NodePerfCounts::record_mesh_render_state()` now delegates the visible/shadow bucket updates to a pure helper covered by a unit test. The test locks the `mesh_visible`, `mesh_shadow_off`, `mesh_shadow_double`, and `mesh_shadow_only` buckets that the parity gate uses for collision-only and proxy render-state regressions.
  - Behavior is unchanged for real `MeshInstance3D` nodes; the helper only makes the counter mapping reviewable without constructing Godot scene nodes in unit tests.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (34/34), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest terrain proxy-reason counter guard slice passed:
  - `NodePerfCounts` now records CPU proxy reason buckets through a pure `record_cpu_proxy_reasons()` helper covered by a unit test. The test locks `proxy_coll`, `proxy_shadow`, `proxy_both`, and `proxy_shadow_only` mapping for collision-only, shadow-only, both, and no-reason cases.
  - Behavior is unchanged for real terrain nodes; `GameClient::current_node_perf_counts()` still derives the same collision and shadow proxy reasons before recording the counters.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (35/35), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest terrain shadow-path visibility slice passed:
  - Rust perf text now includes `shadow_path=...`, with a pure `terrain_shadow_path_decision()` helper covered by a unit test. The values make the current shadow strategy explicit: `arraymesh` for CPU/fallback terrain, `godot_proxy` for the conservative GPU-visible production path, `scene_shadows_disabled` when scene shadows resolve to radius `0`, and `diagnostic_no_shadow_proxy` for `collision_only`.
  - `scripts/gpu_terrain_parity_smoke.sh` now requires the expected `shadow_path` in CPU, GPU conservative, compact proxy, radius, multi-pose, and collision-only markers. This keeps `collision_only` visibly diagnostic and protects the current Godot shadow-proxy production path from accidental removal.
  - Behavior is unchanged: GPU terrain render remains opt-in, conservative Godot shadow proxies remain active, and ArrayMesh fallback remains intact.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (36/36), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Fresh direct parity captures passed across all ten marker files; examples include `shadow_path=arraymesh` for CPU fallback, `shadow_path=godot_proxy` for conservative GPU cases, and `shadow_path=diagnostic_no_shadow_proxy` for the collision-only diagnostic case.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh` passed against the fresh artifacts. Direct executable wrapper launch can still stall in this Codex PTY environment, matching the known wrapper issue; use explicit `sh` or direct Godot commands for the reliable local gate.
- Latest collision-only compact proxy slice passed:
  - Collision-only CPU proxy meshes now use the same vertex-only compact payload as compact shadow-only proxies. This applies only when a GPU-visible proxy needs collision and does not need a Godot shadow proxy; conservative collision+shadow proxies and ArrayMesh fallback stay on the full mesh path.
  - Rust perf text now includes `compact_collision_proxy=...` and `compact_collision_normals_saved=...`. `fast_proxy` remains the total packed-face CPU proxy counter, while the new compact collision counters make the collision-only diagnostic payload reduction explicit.
  - `scripts/gpu_terrain_parity_smoke.sh` now requires zero compact-collision payload in CPU fallback and conservative shadow-proxy cases, and requires `compact_collision_proxy == fast_proxy` in the `collision_only` diagnostic marker.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (37/37), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Fresh direct parity captures passed across all ten marker files. The collision-only marker included `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `fast_proxy=20`, `mesh_visible=0`, `mesh_shadow_off=10`, and `shadow_path=diagnostic_no_shadow_proxy`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh` passed against the fresh artifacts.
- Latest compact proxy benchmark report slice passed:
  - `scripts/gpu_terrain_compact_proxy_benchmark.sh` now validates and reports the new collision-only compact proxy counters alongside compact shadow proxy counters. Conservative full/compact rows are required to keep `compact_collision_proxy=0`, while the collision-only diagnostic row requires `compact_collision_proxy == fast_proxy`.
  - The benchmark summary now prints `compact_collision_proxy`, `collision_normals_saved`, `shadow_normal_total_reduction`, and `collision_normal_payload_reduction`.
  - Checks passed: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/check.sh fast` (Rust tests 37/37), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, and `git diff --check`.
  - Current artifact report: full `normals_total=1243584`, compact `normals_total=260544`, collision-only `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `shadow_normal_total_reduction=79.0%`, and `collision_normal_payload_reduction=71.5%`.
- Latest shadow-disabled parity guard slice passed:
  - `scripts/gpu_terrain_parity_smoke.sh` now captures and validates `gpu-terrain-shadow-disabled-parity` with `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE=0`, conservative mode, and `shadow_mesh=full`.
  - The new marker is required to report `shadow_path=scene_shadows_disabled`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `mesh_visible=0`, and `mesh_shadow_off == cpu_proxy`, while keeping `compact_collision_proxy == fast_proxy`.
  - Direct Godot capture passed for the new marker: `cpu_proxy=10`, `collision=10`, `compact_collision_proxy=20`, `compact_collision_normals_saved=192336`, `fast_proxy=20`, `mesh_shadow_off=10`, `gpu_frames=172`, and `terrain_samples=256`.
  - Checks passed: `sh -n scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `./scripts/check.sh fast` (Rust tests 37/37), and `git diff --check`.
- Latest compact proxy benchmark shadow-disabled report slice passed:
  - `scripts/gpu_terrain_compact_proxy_benchmark.sh` now validates and reports the production-safe `shadow_path=scene_shadows_disabled` marker alongside the diagnostic `collision_only` marker.
  - Report mode now prints a `shadow_disabled` row and separate collision normal payload reductions for `shadow_disabled` and `collision_only`, so disabled-scene-shadow savings are visible without treating `collision_only` as production behavior.
  - Checks passed: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/check.sh fast` (Rust tests 37/37), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, and `git diff --check`.
  - Current artifact report: `shadow_disabled compact_collision_proxy=20`, `shadow_disabled compact_collision_normals_saved=192336`, `shadow_disabled_collision_normal_payload_reduction=72.1%`; `collision_only_collision_normal_payload_reduction=71.5%`.
- Latest GPU-visible CPU-node removal guard slice passed:
  - `terrain_mesh_build_plan()` now requires `gpu_visible_render_active` before returning `RemoveCpuNode`, even if a future caller incorrectly reports no CPU proxy reason before the first confirmed GPU terrain frame.
  - The existing mesh-build-plan unit test now locks that uploaded GPU terrain still falls back to `FullArrayMesh` before the GPU visible path is active.
  - Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (37/37), `./scripts/check.sh fast`, `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Latest GPU-backed CPU proxy activation guard slice passed:
  - Added pure `terrain_cpu_proxy_mesh_active()` and routed both mesh-build planning and render-mode refresh through it. CPU nodes are treated as GPU-backed proxy meshes only when the GPU visible path is confirmed and the specific subchunk has a GPU slot.
  - Added a unit test that locks all four active/inactive combinations, protecting fallback meshes from being switched to proxy render modes without an actual GPU subchunk.
  - Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (38/38), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Latest GPU upload-failure fallback guard slice passed:
  - Extended the mesh-build-plan unit test so an active GPU-visible path still returns `FullArrayMesh` when the specific subchunk has no GPU slot, even if there is no CPU proxy reason. This protects fallback rendering for per-subchunk upload failures.
  - Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (38/38), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `git diff --check`, and `./scripts/diff_guard.sh`.

Useful log lines:

- `GPU terrain compositor initialized`
- `GPU terrain compositor attached to player camera`
- `GPU terrain compositor draw: size=1280x720 views=1 draws=2 faces=7242`
- `GPU terrain compositor draw: size=1280x720 views=1 depth=true draws=2 faces=7242`
- `Visual smoke screenshot saved ... current_chunk="0,0"`
- `perf="... gpu_subchunks=66 gpu_draws=66 gpu_faces=116824 gpu_frames=116 ..."`
- `GPU terrain visible path confirmed`
- `Visual smoke screenshot saved ... terrain_samples=576 ... smoke_err=0 ... gpu_frames=160 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... cpu_proxy=10 collision=10 ... gpu_subchunks=92 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... current_chunk="0,0"` for CPU fallback.
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... fast_proxy=163 ... gpu_frames=190 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... cpu_proxy=10 collision=10 fast_proxy=20 ... gpu_frames=195 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... fast_proxy=0 ... current_chunk="0,0"` for CPU fallback.
- `Visual smoke screenshot saved ... parity/cpu-arraymesh-parity.png ... avg_luma=0.3517 ... terrain_samples=256 ... smoke_err=0 ... fast_proxy=0 collision=10 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-parity.png ... avg_luma=0.3442 ... terrain_samples=256 ... smoke_err=0 ... fast_proxy=62 ... gpu_frames=68 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-radius1-parity.png ... terrain_samples=256 ... smoke_err=0 ... cpu_proxy=10 fast_proxy=19 collision=10 ... gpu_frames=74 ...`
- `Visual smoke screenshot saved ... parity/cpu-arraymesh-parity.png ... terrain_color_buckets=8 terrain_chroma_samples=256 terrain_luma_range=0.3655 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-parity.png ... terrain_color_buckets=8 terrain_chroma_samples=256 terrain_luma_range=0.3532 ... gpu_frames=143 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-radius1-parity.png ... terrain_color_buckets=8 terrain_chroma_samples=256 terrain_luma_range=0.3532 ... cpu_proxy=10 fast_proxy=20 ... gpu_frames=149 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-parity.png ... cpu_proxy=82 proxy_coll=10 proxy_shadow=82 proxy_both=10 proxy_shadow_only=72 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-radius1-parity.png ... cpu_proxy=10 proxy_coll=10 proxy_shadow=10 proxy_both=10 proxy_shadow_only=0 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-atlas-depth-parity.png pose="atlas_depth" ... terrain_samples=510 terrain_color_buckets=9 terrain_luma_range=0.3294 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-lighting-shadow-parity.png pose="lighting_shadow" ... terrain_samples=453 terrain_color_buckets=10 terrain_luma_range=0.3570 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-compact-lighting-shadow-parity.png pose="lighting_shadow" ... shadow_mesh=compact compact_shadow_proxy=97 compact_shadow_normals_saved=1320960 ...`

Known limitations:

- GPU terrain render is still opt-in via `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- GPU terrain now samples the real block texture atlas and applies directional lighting, but visual parity still needs shadow integration and visual tuning.
- GPU terrain solid blocks force opaque alpha in the shader; do not infer grass/solid block transparency from the RGBA atlas alpha.
- GPU terrain shader atlas UVs now use runtime atlas layout push constants instead of a hard-coded column count.
- GPU terrain atlas texture now prefers sRGB sampling; color parity should still be visually compared against the ArrayMesh fallback before deleting more CPU rendering.
- Depth-compatible scene rendering is wired and smoke-tested with Godot 4.6 reverse-Z depth compare.
- GPU terrain is now directional-light aware, and retained ArrayMesh terrain is explicitly configured as a double-sided Godot shadow caster proxy.
- With `RUMPELMC_GPU_TERRAIN_RENDER=1`, CPU ArrayMesh nodes become `SHADOWS_ONLY` only after the GPU visible render path has rendered at least one compositor frame; this avoids double visible terrain while preserving fallback if GPU setup fails or a subchunk fails GPU upload.
- Distant CPU terrain node removal is also gated by the confirmed GPU-visible path inside `terrain_mesh_build_plan()`, not only by the current caller's proxy decision.
- CPU terrain nodes are considered GPU-backed proxies only when the confirmed GPU-visible path and the per-subchunk GPU slot are both present.
- With `RUMPELMC_GPU_TERRAIN_RENDER=1`, CPU ArrayMesh nodes are still kept where needed for nearby collision and the conservative Godot shadow-map proxy. Distant CPU `MeshInstance3D` removal has started, but the retained proxy radius is intentionally conservative.
- With `RUMPELMC_GPU_TERRAIN_RENDER=1`, retained CPU proxy meshes can now be built from packed GPU terrain faces after the GPU visible path is confirmed. These proxy meshes intentionally omit UVs because they are used for collision and `SHADOWS_ONLY`, not visible terrain.
- `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MODE=collision_only` is diagnostic only for now. It confirms how much CPU work is shadow-proxy-only. In this mode retained collision CPU proxies are invisible and do not cast shadows, but it must not become the default until GPU terrain has a dedicated shadow-compatible path or native Godot shadow-map participation.
- `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=compact` is now the default for GPU-visible shadow-only CPU proxy meshes. Use `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=full` for a control run or opt-out. This is still a Godot shadow proxy and does not replace native RD shadow-map participation.
- `scripts/gpu_terrain_parity_smoke.sh` can validate existing artifacts with `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1`. It now expects markers produced by the extended visual smoke metrics and multi-pose support in `client/main.gd`, plus proxy reason counters, terrain mesh render-state counters, `shadow_mode=...`, `shadow_mesh=...`, and `compact_shadow_normals_saved=...` from `client/rust_ext/src/lib.rs`. It validates CPU/GPU pose parity, compact-vs-full `lighting_shadow` parity, disabled-shadow-radius behavior, and `collision_only` proxy render-state invariants. In this Codex PTY environment, launching Godot through shell wrappers can still stall before project logs appear, so direct Godot commands plus validate-only are the reliable local gate.
- `scripts/gpu_terrain_compact_proxy_benchmark.sh` defaults to report mode over existing parity artifacts and now includes both the disabled-shadow-radius marker and the collision-only diagnostic marker. Use `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1` only when the local shell/Godot wrapper is known to be reliable, or prefer direct Godot captures followed by benchmark report mode.
- The custom RD compositor draw still does not natively participate in Godot shadow maps as a real shadow caster/receiver. Full shadow parity without ArrayMesh proxy needs a separate render integration plan.
- Final ArrayMesh replacement for distant terrain is not finished.
- Keep the working tree small and commit completed slices; `diff_guard` is expected to stay green after the dirty-tree cleanup baseline.

Next steps:

1. After any GPU terrain edit, run the parity gate: direct CPU/GPU/radius=1 visual smoke captures with `RUMPELMC_VISUAL_SMOKE_DISABLE_PLAYER_INPUT=1`, then `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh`.
2. Keep compact shadow-only proxy meshes as the default unless fresh parity or visual shadow checks show a regression; use `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=full` for comparison runs.
3. Decide the production shadow path next: either build a dedicated lower-cost shadow proxy beyond ArrayMesh, or make the RD terrain participate in Godot shadow maps. The new `collision_only` mode is useful for measurement, not a production default.
4. Do not make `collision_only` default or remove conservative shadow proxies until multi-pose parity and visible shadow behavior are explicitly verified.
5. Continue reducing CPU ArrayMesh generation for distant chunks while preserving nearby collision meshes, shadow proxy requirements, and the fallback path.
6. Run `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, parity visual smoke validation, and `./scripts/diff_guard.sh` before handing off again.
