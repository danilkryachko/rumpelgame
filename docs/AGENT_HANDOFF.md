# Agent Handoff State

This file is the current continuation state for Codex threads. Update it after non-trivial work, before delegating to another chat, or before stopping in the middle of a task.

## Latest Snapshot

Date: 2026-06-16

Fresh 2026-06-16 status:

- Current `GPU` branch slice verifies GPU upload failure recovery for Phase 6 without changing runtime behavior. `client/rust_ext/src/lib.rs` now has explicit unit coverage that a failed/missing per-subchunk GPU slot keeps `FullArrayMesh` mesh-build fallback, and proxy-refresh reuse stays on `BuildMesh` until `existing_gpu_slot=true`; CPU node removal remains allowed only after the subchunk has a confirmed GPU slot and no CPU proxy role is needed. New docs: `docs/GPU_UPLOAD_FAILURE_RECOVERY.md`; updated docs: `docs/GPU_PROFILING.md`, `docs/GPU_ROADMAP.md`, `docs/GPU_TRENDS.md`, `docs/AGENT_MEMORY.md`, and this handoff. Targeted checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_upload_failure_recovery_keeps_cpu_fallback_until_slot_exists`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_mesh_build_plan_preserves_gpu_proxy_and_fallback_paths`, and `cargo fmt --manifest-path client/rust_ext/Cargo.toml --check`. No failure-injection runtime, retry loop, runtime backoff, upload capacity, allocator policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed.

- Current `GPU` branch slice adds explicit GPU upload retry/backoff telemetry for Phase 5 without changing runtime upload policy. `client/rust_ext/src/gpu_terrain.rs` now carries marker-only retry/backoff stats with policy `none`, and `client/rust_ext/src/lib.rs` exposes `gpu_upload_retry_policy`, `gpu_upload_retry_attempts`, `gpu_upload_retry_success`, `gpu_upload_retry_giveups`, `gpu_upload_backoff_active`, `gpu_upload_backoff_frames`, and `gpu_upload_backoff_max_frames` in the GPU terrain perf marker. `scripts/gpu_terrain_movement_stress.sh`, `scripts/gpu_terrain_in_place_upload_gate.sh`, `scripts/gpu_terrain_upload_budget.sh`, and `scripts/gpu_terrain_report.sh` now require/summarize/report those fields. New docs: `docs/GPU_UPLOAD_RETRY_BACKOFF_TELEMETRY.md`; updated docs: `docs/GPU_PROFILING.md`, `docs/GPU_TERRAIN_UPLOAD_BUDGETING.md`, `docs/GPU_ROADMAP.md`, `docs/GPU_TRENDS.md`, `docs/AGENT_MEMORY.md`, and this handoff. Fresh release movement `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_upload_retry_backoff_movement_current` passed with `gpu_upload_fail=0`, `gpu_upload_retry_policy=none`, all retry/backoff counters `0`, `terrain_queue_max_ms=1.950`, `process_wall_p95_ms=0.029`, `gpu_compositor_submit_max_ms=0.086`, upload max `1`, and new/replacement slot maxima `1/1`; the local server PID `67453` left by that run was stopped and port `25565` cleared. Fresh in-place gate `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 sh scripts/gpu_terrain_in_place_upload_gate.sh logs/gpu_upload_retry_backoff_in_place_current` passed with `gpu_in_place_uploads=1`, `gpu_in_place_upload_misses=63`, `gpu_uploads=852`, `gpu_upload_fail=0`, retry/backoff `none/0`, `terrain_queue_max_ms=1.953`, `process_wall_p95_ms=0.023`, and `gpu_compositor_submit_max_ms=0.084`. Fresh upload-budget gate over those artifacts passed with `reason=within_budget`, movement/in-place retry policies `none`, all retry/backoff counters `0`, and zero upload failures. Aggregate report smoke `/tmp/rumpel-gpu-report-upload-retry-backoff.txt` reports latest `gpu_upload_retry_policy=none`, retry/backoff sums/maxima all `0`, and a clean error scan. Targeted checks passed: `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_in_place_upload_gate.sh scripts/gpu_terrain_upload_budget.sh scripts/gpu_terrain_report.sh`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml upload_retry_backoff_telemetry_defaults_to_disabled_policy`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, and `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_upload_breakdown_separates_new_and_replacement_slots`. No protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, upload capacity, allocator policy, runtime retry loop, runtime backoff, or visible quality changed.

- Current `GPU` branch slice adds a mass chunk-load summary gate for Phase 5. New `scripts/gpu_terrain_mass_chunk_load_gate.sh` composes high resident-set load-scaling evidence with the current per-frame upload-budget evidence and fails unless thousands of GPU subchunks/draws/faces are resident, draw-command occupancy is nontrivial, queue/process/submit budgets stay under the 150 FPS target budget, and load plus upload-budget failure counters remain zero. New docs: `docs/GPU_TERRAIN_MASS_CHUNK_LOAD.md`; updated docs: `docs/GPU_PROFILING.md`, `docs/GPU_TERRAIN_LOAD_SCALING.md`, `docs/GPU_ROADMAP.md`, `docs/GPU_TRENDS.md`, `docs/AGENT_MEMORY.md`, and this handoff. Fresh heavy evidence: `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=600 GODOT_QUIT_AFTER_FRAMES=36000 sh scripts/gpu_terrain_load_scaling.sh logs/gpu_terrain_load_scaling_radius16_summary_check` passed with `max_gpu_subchunks=2482`, `max_gpu_draws=2482`, `max_gpu_faces=3165`, draw-command occupancy `30.298%`, headroom `91360`, `max_terrain_queue_ms=2.214`, `max_process_wall_p95_ms=0.059`, `max_gpu_compositor_submit_ms=5.677`, and zero upload failures. Fresh mass-load gate `sh scripts/gpu_terrain_mass_chunk_load_gate.sh logs/gpu_terrain_mass_chunk_load_current` passed with `reason=mass_load_within_budget`, movement upload max `1/1`, movement payload `0.2/2.0 KiB`, movement/in-place new-slot maxima `1/1`, and movement/in-place replacement-slot maxima `1/1`. Negative validation `RUMPELMC_GPU_MASS_LOAD_MIN_GPU_SUBCHUNKS=999999 sh scripts/gpu_terrain_mass_chunk_load_gate.sh logs/gpu_terrain_mass_chunk_load_negative` failed as expected with `reason=mass_subchunk_pressure`; the generated negative log directory was removed after validation. The local test server left by the heavy run had already exited before cleanup and port `25565` is clear. No protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, upload capacity, allocator policy, runtime throttling, or visible quality changed.

- Current `GPU` branch slice adds a summary-only GPU terrain upload budget gate. New `scripts/gpu_terrain_upload_budget.sh` consumes movement lane-split evidence plus in-place dirty-update evidence and gates per-frame total upload count/KiB, new-slot count/KiB, replacement-slot count/KiB, and upload failure counters without changing runtime upload behavior. `scripts/gpu_terrain_report.sh` now also surfaces aggregate maxima for `terrain_queue_gpu_upload_new_slots`, `terrain_queue_gpu_upload_replace_slots`, `terrain_queue_gpu_upload_new_slot_kb`, and `terrain_queue_gpu_upload_replace_slot_kb`. New docs: `docs/GPU_TERRAIN_UPLOAD_BUDGETING.md`; updated docs: `docs/GPU_PROFILING.md`, `docs/GPU_ROADMAP.md`, `docs/GPU_TRENDS.md`, `docs/AGENT_MEMORY.md`, and this handoff. Fresh evidence: `sh scripts/gpu_terrain_upload_budget.sh logs/gpu_terrain_upload_budget_current` passed with `movement_uploads_max=1/1`, movement payload `0.2/2.0 KiB`, movement new/replace upload maxima `1/1`, in-place new/replace upload maxima `1/1`, and upload failures `0`. Negative validation `RUMPELMC_GPU_UPLOAD_BUDGET_MAX_UPLOAD_KB_PER_FRAME=0.05 sh scripts/gpu_terrain_upload_budget.sh logs/gpu_terrain_upload_budget_negative` failed as expected with `reason=movement_upload_kb_budget`; the generated failed log directory was removed after validation to avoid polluting future summary scans. Aggregate report smoke `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-upload-budget-lanes.txt` reported the new lane maxima (`1/1`, `0.2/0.1 KiB`). Checks passed: `sh -n scripts/gpu_terrain_upload_budget.sh scripts/gpu_terrain_report.sh`, positive upload budget gate, negative tight-budget gate, aggregate report smoke, `./scripts/check.sh fast` (171 Rust tests), `git diff --check`, and `./scripts/diff_guard.sh`. No protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, upload capacity, allocator policy, runtime throttling, or visible quality changed.

- Current `GPU` branch slice splits terrain queue GPU upload telemetry into new-slot and replacement-slot lanes. `client/rust_ext/src/lib.rs` now carries `GpuUploadBreakdown` from `upload_gpu_subchunk` through mesh jobs into `PerfStats`, preserving the existing total `terrain_queue_gpu_uploads` / `terrain_queue_gpu_upload_kb` markers while adding `terrain_queue_gpu_upload_new_slots`, `terrain_queue_gpu_upload_replace_slots`, `terrain_queue_gpu_upload_new_slot_kb`, and `terrain_queue_gpu_upload_replace_slot_kb`. `scripts/gpu_terrain_movement_stress.sh` now requires and summarizes those markers; `scripts/gpu_terrain_in_place_upload_gate.sh` requires nonzero new-slot and replacement-slot maxima for block-edit dirty evidence. Fresh evidence: release movement `./scripts/gpu_terrain_movement_stress.sh logs/gpu_upload_lane_split_movement_current` passed with `gpu_upload_fail=0`, `terrain_queue_max_ms=2.053`, `process_wall_p95_ms=0.056`, `gpu_compositor_submit_max_ms=0.148`, `terrain_queue_gpu_upload_new_slots=0/0.46/1`, `terrain_queue_gpu_upload_replace_slots=0/0.04/1`, new/replace max payload `0.2/0.1 KiB`, `gpu_draws=788`, and current render/collision readiness `1/1`; release in-place gate `./scripts/gpu_terrain_in_place_upload_gate.sh logs/gpu_upload_lane_split_in_place_current` passed with `gpu_in_place_uploads=1`, `gpu_in_place_upload_misses=63`, `gpu_uploads=852`, `gpu_upload_fail=0`, `terrain_queue_new_slot_uploads_max=1`, `terrain_queue_replace_slot_uploads_max=1`, `terrain_queue_max_ms=2.475`, `process_wall_p95_ms=0.054`, and `gpu_compositor_submit_max_ms=0.200`. Docs updated: `docs/GPU_PROFILING.md`, `docs/GPU_TRENDS.md`, `docs/GPU_ROADMAP.md`, `docs/AGENT_MEMORY.md`, and this handoff. Checks passed: `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml perf_records_terrain_queue_frame_work`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_upload_breakdown_separates_new_and_replacement_slots`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `sh -n scripts/gpu_terrain_in_place_upload_gate.sh`, both runtime gates above, `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `./scripts/check.sh fast` (171 Rust tests), `git diff --check`, and `./scripts/diff_guard.sh`. No protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, upload capacity, allocator policy, or visible quality changed.

- Current `GPU` branch slice adds default-off same-face-count GPU subchunk in-place upload evidence. Runtime code now reuses the packed-face upload scratch buffer, writes indirect draw command patches from fixed stack bytes, exposes `gpu_in_place_upload_enabled`, `gpu_in_place_uploads`, and `gpu_in_place_upload_misses`, and gates the actual in-place update behind explicit `RUMPELMC_GPU_TERRAIN_IN_PLACE_SUBCHUNK_UPLOAD=1`; unset/`0` keeps the existing remove/allocate/insert path. New `scripts/gpu_terrain_in_place_upload_gate.sh` runs release block-edit stress against an isolated RocksDB path so persisted server state cannot turn validation into a no-op. Fresh evidence: `./scripts/gpu_terrain_in_place_upload_gate.sh logs/gpu_terrain_in_place_upload_gate_current` passed with `gpu_in_place_upload_enabled=1`, `gpu_in_place_uploads=1`, `gpu_in_place_upload_misses=3`, `gpu_uploads=699`, `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_upload_ms_max=0.038`, `gpu_upload_stage_ms_max=0.013`, `gpu_upload_update_ms_max=0.034`, `gpu_draw_patch_ms_max=0.013`, `terrain_queue_max_ms=1.461`, `process_wall_p95_ms=0.043`, `gpu_compositor_submit_max_ms=0.157`, `dirty_partial_saved_subchunks=1`, and `dirty_last_rebuild_subchunks=2`. Docs updated: `docs/GPU_TRENDS.md`, `docs/GPU_PROFILING.md`, `docs/GPU_ROADMAP.md`, `docs/AGENT_MEMORY.md`, and this handoff. Checks passed: `sh -n scripts/gpu_terrain_in_place_upload_gate.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (170 tests), `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh`. No protocol, storage, worldgen, chunk serialization, default draw distance, lighting, shadows, texture quality, or visible quality changed. Caveat: this is still local macOS/Metal opt-in evidence; Windows/Vulkan/Direct3D profiler captures and broader upload-pressure/load-scaling gates remain separate work.

Fresh 2026-06-15 status:

- Current workspace slice adds bounded live slow-reader evidence and records fresh resident-set pressure outcome. New `server/cmd/slow_reader_smoke` and `scripts/server_slow_reader_smoke.sh` run an isolated RAW/full-bootstrap TCP smoke on port `25567`: one slow client sends an initial position and does not read, while a fast client still receives chunk `0,0`; the wrapper requires server-log `i/o timeout` evidence for the slow client. `scripts/networking_robustness_gate.sh` can run/consume that smoke and now reports `slow_client_status=live_guarded` when `slow_reader_smoke_status=pass` and `slow_reader_timeout_observed=1`; its explicit smoke run writes to top-level `logs/server_slow_reader_smoke_current` so observability current-summary naming remains clean. `scripts/observability_logs_cleanup_gate.sh` now ignores its own previous summary status when validating inputs so a failed cleanup run can recover after the underlying bad artifact is fixed. Fresh evidence: `sh scripts/server_slow_reader_smoke.sh logs/server_slow_reader_smoke_current` passed with `fast_bootstrap_ms=93.721`, `slow_timeout_observed=1`, `write_timeout_ms=150`; `RUMPELMC_NETWORKING_ROBUSTNESS_RUN_SLOW_READER_SMOKE=1 sh scripts/networking_robustness_gate.sh logs/networking_robustness_current` passed with `slow_client_status=live_guarded`; `sh scripts/observability_logs_cleanup_gate.sh logs/observability_logs_cleanup_current` passed with `summary_count=37`, `bad_status_count=0`, `bad_name_count=0`, and clean error scan. A clean explicit heavy resident-set matrix was rerun after stopping a stale local server listener: `RUMPELMC_RESIDENT_SET_MATRIX_RUN=1 RUMPELMC_RESIDENT_SET_MATRIX_RADII="16 18" ... sh scripts/world_streaming_resident_set_matrix.sh logs/world_streaming_resident_set_matrix_radius16_18_clean` failed only the pressure gate (`reason=draw_pressure_too_low`) with source rows passing, best radius `16`, `max_gpu_draws=1859`, occupancy `22.693%`, headroom `101328`, and upload failures `0`; this is negative pressure evidence for active GPU repack/draw-capacity changes. Docs updated: `docs/NETWORKING_ROBUSTNESS_PROGRAM.md`, `docs/SERVER_SCALABILITY_PASS.md`, `docs/WORLD_STREAMING.md`, `docs/GPU_TRENDS.md`, `docs/AGENT_MEMORY.md`, `docs/OBSERVABILITY_LOGS_CLEANUP.md`, and this handoff. Checks passed: `sh -n scripts/server_slow_reader_smoke.sh scripts/networking_robustness_gate.sh scripts/observability_logs_cleanup_gate.sh`, `go test ./cmd/slow_reader_smoke ./cmd/server ./pkg/network`, `go test ./...`, `go test -race ./pkg/network`, `./scripts/check.sh fast`, `./scripts/check.sh full` (Go lint skipped because `golangci-lint` is unavailable; Rust 168 tests passed), `git diff --check`, and `./scripts/diff_guard.sh`. Protocol schema/generated files were not changed. Unrelated untracked `scripts/codex_mcp_janitor.sh` is still present and not touched. Next concrete step: commit the slow-reader/docs slice.

- Commit `b9037b6` adds live server multi-client fanout evidence and the next resident-set trend wrapper. New `server/cmd/multi_client_smoke` opens two real TCP/protobuf clients, verifies both receive chunk `0,0`, sends a wood `BlockAction_PLACE`, and verifies both clients receive the updated chunk. `scripts/server_multi_client_smoke.sh` runs it on isolated port `25566` with a temporary RocksDB path; `server/cmd/server/main.go` now accepts `RUMPELMC_SERVER_ADDRESS` and `RUMPELMC_SERVER_ROCKSDB_PATH` without changing defaults. `scripts/server_scalability_pass_gate.sh` can consume or run the live smoke, and `scripts/networking_robustness_gate.sh` surfaces `multi_client_live_status`. New `scripts/world_streaming_resident_set_matrix.sh` is summary-first over resident-set growth artifacts and records max resident draw/subchunk/face pressure, draw-command occupancy/headroom, queue/process/submit maxima, and upload failure totals; default current evidence is radius `16` with `max_gpu_draws=2482`, `max_gpu_subchunks=2482`, occupancy `30.298%`, and zero upload failures. Docs updated: `docs/SERVER_SCALABILITY_PASS.md`, `docs/NETWORKING_ROBUSTNESS_PROGRAM.md`, `docs/WORLD_STREAMING.md`, `docs/GPU_PROFILING.md`, `docs/GPU_TRENDS.md`, and `docs/AGENT_MEMORY.md`. Checks passed before commit: `sh scripts/server_multi_client_smoke.sh logs/server_multi_client_smoke_current`, `sh scripts/server_scalability_pass_gate.sh logs/server_scalability_pass_current`, `sh scripts/networking_robustness_gate.sh logs/networking_robustness_current`, `sh scripts/world_streaming_resident_set_matrix.sh logs/world_streaming_resident_set_matrix_current`, `sh scripts/observability_logs_cleanup_gate.sh logs/observability_logs_cleanup_current`, `go test ./cmd/server ./cmd/multi_client_smoke ./pkg/network`, `go test ./...`, `go test -race ./pkg/network`, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh`. Protocol schema/generated files were not changed.

- Current local work was split into reviewable commits: `610b177` adds `scripts/agent_search.sh`, `docs/AGENT_CODE_SEARCH.md`, and the AGENTS search workflow; `0c24d26` adds registered server client sessions, interested-client block-edit fanout, failed non-origin broadcast cleanup, and `RUMPELMC_SERVER_CLIENT_WRITE_TIMEOUT_MS` bounded session writes without protocol/schema changes; `8517b34` expands the HUD dev diagnostics menu with tabs/actions/log snapshot helpers. Checks passed before splitting: `bash -n scripts/agent_search.sh`, `git diff --check`, `go test ./pkg/network`, `go test ./...` from `server/`, `go test -race -count=1 ./pkg/network`, `./scripts/check.sh fast`, `./scripts/check.sh full` (Go lint skipped because `golangci-lint` is unavailable; Rust 168 tests passed), `./scripts/diff_guard.sh`, `sh scripts/server_scalability_pass_gate.sh logs/server_scalability_pass_current`, `sh scripts/networking_robustness_gate.sh logs/networking_robustness_current`, `godot --headless --path client --check-only --script res://hud.gd`, and `godot --headless --path client --quit-after 2 --log-file ../logs/godot_hud_headless_check.log`. OntoIndex was not refreshed against the new commit stack yet; run `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` after this handoff commit if graph lookup needs the new symbols. Next recommended step: commit this handoff sync, refresh OntoIndex, then move to the resident-set/world-streaming experiment.

- Fresh world load/upload measurement is captured after commit `5e9fdfe` with release Rust GDExtension. Artifacts are `logs/world_load_upload_measurement_20260615/movement`, `logs/world_load_upload_measurement_20260615/workload_matrix`, and `logs/world_load_upload_measurement_20260615/gpu-terrain-report.txt`. Movement stress passed with `terrain_queue_max_ms=0.868`, `process_wall_p95_ms=0.046`, `gpu_compositor_submit_max_ms=0.212`, `gpu_upload_fail=0`, and startup `chunk_loaded_ms=107.048` / `player_spawn_ms=107.048`. The workload matrix also passed with max `gpu_effective_draws=996`, max `gpu_faces=1562`, max draw-command occupancy `12.158%`, max `terrain_queue_max_ms=2.151`, max `process_wall_p95_ms=0.053`, max `gpu_compositor_submit_max_ms=0.174`, max per-frame upload payload `terrain_queue_gpu_upload_kb=1.5`, and zero `gpu_upload_fail_capacity` / `gpu_upload_fail_fragmented`. The current data does not justify active GPU buffer repack as the next performance move; the better next slice is a world streaming/resident-set growth experiment that pushes chunk delivery and simultaneous resident terrain higher while keeping draw distance, lighting, shadows, texture quality, protocol, storage, worldgen, and chunk serialization unchanged.

Fresh 2026-06-14 status:

- Full project check gate is clean again. The old Rust clippy debt in `client/rust_ext/src/lib.rs` was removed by grouping long mesh/render helper argument lists into local request structs and replacing the dirty subchunk modulo check with `usize::is_multiple_of`; no mesh behavior, protocol, storage, worldgen, chunk serialization, GPU repack state, draw distance, lighting, shadows, or texture quality changed. Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `git diff --check`, `./scripts/check.sh full` (includes Go tests, Rust check/clippy/test, and 150 Rust unit tests), and `./scripts/diff_guard.sh`. Next recommended work remains evidence-led world load/upload measurement before returning to active GPU repack swap work.

- GPU repack disabled final-swap guard is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, the repack preview now records an explicit final swap boundary after a valid apply preview: `gpu_repack_final_swap_ready=0`, `gpu_repack_final_swap_blocked=1`, and `gpu_repack_final_swap_slots=<apply slots>`. This keeps apply readiness separate from any active RenderingDevice RID, slot-map, indirect-buffer, or allocator mutation. There is still no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, resident slot mutation, allocator mutation, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (150 tests), `./scripts/check.sh fast`, synthetic report aggregation for final-swap guard counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. MCP `gn_verify_diff` and `gn_test_gap` passed; MCP `gn_pre_commit_audit` reported LOW risk with graph hunk-attribution scope warnings only. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: disabled final-swap abort bookkeeping for the guarded path, still without replacing active RenderingDevice RIDs, slot-map state, indirect buffers, or allocator state.

- GPU repack disabled apply scaffold is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, the repack preview now consumes the commit proof and validates future operation order, staged slot mirror, face/draw byte lengths, and allocator free-range mirror before reporting apply readiness. Telemetry records `gpu_repack_apply_ready`, `gpu_repack_apply_steps`, and `gpu_repack_apply_slots`; apply proof failures map to `source_size_mismatch` for face-byte mismatch and `draw_rebuild_error` for operation-order, slot, draw-byte, or allocator-free-range mismatch. There is still no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, resident slot mutation, allocator mutation, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (148 tests), `./scripts/check.sh fast`, synthetic report aggregation for apply-scaffold counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. MCP `gn_verify_diff` and `gn_test_gap` passed; MCP `gn_pre_commit_audit` reported LOW risk with expected telemetry/pool scope warnings only. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: final-swap disabled runtime guard that keeps apply readiness separated from any active RenderingDevice RID, slot-map, indirect-buffer, or allocator mutation.

- GPU repack disabled commit-point proof is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, the repack preview now validates the future commit ordering over the staged swap object: face-buffer RID swap, render binding swap, indirect draw buffer swap, slot-map replacement, and allocator rebuild. The proof also validates contiguous sorted replacement slots, draw-byte count, and a one-tail allocator free range when free space remains. Telemetry records `gpu_repack_commit_ready`, `gpu_repack_commit_steps`, and `gpu_repack_commit_tail_free`; commit proof failures map to `source_size_mismatch` for face-byte mismatch and `draw_rebuild_error` for draw, slot-map, or tail-range mismatch. There is still no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, resident slot mutation, allocator mutation, capacity change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (143 tests), `./scripts/check.sh fast`, synthetic report aggregation for commit-proof counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. MCP `gn_verify_diff` passed; MCP `gn_pre_commit_audit` reported LOW risk with dirty-worktree/scope warnings only. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: disabled apply scaffold that consumes the commit proof and exercises success/abort bookkeeping without replacing active RenderingDevice RIDs, slot state, or allocator state.

- GPU repack staged swap guard is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, the repack preview now builds one all-or-nothing staged swap object containing compact replacement face bytes, replacement indirect draw bytes, deterministic replacement slot map, and the tail free range. Telemetry records `gpu_repack_stage_ready`, `gpu_repack_stage_slots`, and `gpu_repack_stage_bytes`; staged face-size failures report `source_size_mismatch`, and staged draw-size failures report `draw_rebuild_error`. The explicit upload preview still uploads only temporary staged face bytes, validates a temporary uniform set, then frees temporary GPU resources. There is still no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, resident slot mutation, allocator mutation, capacity change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (139 tests), `./scripts/check.sh fast`, synthetic report aggregation for staged counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. A read-only review subagent found no issues. MCP `gn_verify_diff` passed; MCP `gn_pre_commit_audit` reported LOW risk with dirty-worktree/scope warnings only. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: disabled commit-point proof for exact face-buffer, uniform-set, indirect-buffer, slot-map, and allocator replacement ordering before any final RenderingDevice swap is enabled.

- GPU repack draw-command remap preview is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, the repack preview now builds replacement indirect draw command bytes in the current draw order, but with each command's `first_instance` remapped to the compacted slot offset from the deterministic repack plan. Telemetry records `gpu_repack_draw_ready` and `gpu_repack_draw_bytes`; remap failures and dirty active draw snapshots report `gpu_repack_failure_reason=draw_rebuild_error`. There is still no active indirect-buffer upload/swap, render binding swap, resident slot mutation, allocator policy change, capacity change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (136 tests), `./scripts/check.sh fast`, synthetic report aggregation for draw-remap counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. A read-only review subagent found one stale-`draw_keys` risk when `draw_dirty` is set, and the preview now refuses readiness in that state. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: all-or-nothing staged swap guard that proves the face buffer, uniform set, draw-command bytes, and slot map are all available before any active RID or slot state is replaced.

- GPU repack binding preview is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, resident-set changes still rebuild deterministic compact payload preview. With the additional explicit `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK_UPLOAD_PREVIEW=1` profiling flag, the one-shot sample now creates a temporary replacement storage buffer, uploads the current payload, creates and validates a temporary uniform set that binds the replacement face buffer to the existing shader/atlas resources, records `gpu_repack_bind_ready` and `gpu_repack_bind_ms`, and immediately frees the temporary uniform set and buffer. Temporary binding failure reports `gpu_repack_failure_reason=draw_rebuild_error`; invalid temporary uniform-set RIDs are also freed on the failure path. There is still no active render binding swap, resident slot mutation, allocator policy change, capacity change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (134 tests), `./scripts/check.sh fast`, synthetic report aggregation for binding-preview counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. A read-only review subagent found one invalid-RID cleanup issue and it was fixed before commit. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: disabled draw-command remap preview for compacted slot offsets.

- GPU repack replacement upload preview is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, resident-set changes rebuild the deterministic compact repack payload. With the additional explicit `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK_UPLOAD_PREVIEW=1` profiling flag, the runtime takes one sample by creating a temporary replacement storage buffer, uploading the current payload, recording `gpu_repack_upload_ready`, `gpu_repack_upload_bytes`, and `gpu_repack_upload_ms`, and immediately freeing the temporary buffer. Invalid temporary buffer creation reports `gpu_repack_failure_reason=upload_error`. There is still no render binding swap, resident slot mutation, allocator policy change, capacity change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (134 tests), `./scripts/check.sh fast`, synthetic report aggregation for upload-preview counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: disabled render-binding rebuild path for the replacement buffer without activating the final swap.

- GPU repack replacement payload preview is implemented and behavior-preserving. Behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`, the repack planner now assembles a compact replacement byte payload from resident CPU source bytes in deterministic plan order, records `gpu_repack_payload_ready` and `gpu_repack_payload_bytes`, and classifies source-size errors as `gpu_repack_failure_reason=source_size_mismatch`. The payload is dropped immediately; there is still no RenderingDevice buffer upload, buffer swap, slot mutation, allocator policy change, capacity change, or visible quality change. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (134 tests), `./scripts/check.sh fast`, synthetic report aggregation for payload counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: disabled replacement-buffer upload path without swapping render bindings.

- GPU repack resident source mirror is implemented and behavior-preserving. When `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1` is enabled, successful GPU terrain uploads retain CPU-owned encoded packed-face bytes for resident subchunks; removing a subchunk removes its source bytes. Unset/`0` does not clone or retain source bytes. Repack telemetry now reports `gpu_repack_source_subchunks`, `gpu_repack_source_bytes`, and `gpu_repack_source_missing`; missing source data reports `gpu_repack_failure_reason=missing_source` and still keeps `gpu_repack_active=0`. `scripts/gpu_terrain_report.sh` aggregates the new source readiness counters. Checks passed: `cargo test --manifest-path client/rust_ext/Cargo.toml --lib` (131 tests), `./scripts/check.sh fast`, synthetic report aggregation for source counters, `git diff --check`, `sh -n scripts/gpu_terrain_report.sh`, and `./scripts/diff_guard.sh`. `./scripts/check.sh full` still fails on existing clippy debt in unrelated `src/lib.rs` functions (`too_many_arguments`, `manual_is_multiple_of`). Next repack step: assemble a replacement buffer payload from resident sources while keeping final RenderingDevice buffer swap disabled until tested.

- GPU buffer repack prototype foundation is implemented and behavior-preserving. `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK` is an explicit opt-in flag: unset/`0` keeps `gpu_repack_requested=0`, and env-on records marker-only telemetry with `gpu_repack_requested=1`, `gpu_repack_active=0`, `gpu_repack_attempts=0`, `gpu_repack_success=0`, `gpu_repack_abort=1`, and `gpu_repack_failure_reason=marker_only`. The prototype also computes a read-only deterministic compaction preview from current GPU terrain slots and reports potential moved subchunks/faces/bytes, but it does not replace the GPU buffer or mutate slot offsets. `scripts/gpu_terrain_report.sh` now aggregates numeric `gpu_repack_*` fields and latest `gpu_repack_failure_reason`. The resident source mirror now exists; next step is replacement payload assembly from those sources.

- GPU terrain buffer repack design is complete and behavior-preserving. `docs/GPU_BUFFER_REPACK.md` records the default-off `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1` rollout, deterministic repack planning, no GPU readback in the first prototype, all-or-nothing buffer swap preference, required telemetry, trigger policy, rollback behavior, and allocator/profile gates. No runtime renderer, allocator policy, buffer capacity, protocol, storage, world generation, chunk serialization, visible quality, draw distance, lighting, shadows, or texture quality changed. The marker-only prototype foundation now exists; next repack step is CPU-owned resident packed-face source data.

- GPU allocator stress gate is added and behavior-preserving. New `scripts/gpu_terrain_allocator_stress_gate.sh` refreshes a scoped `gpu-terrain-report.txt` and fails on report error patterns, nonzero `gpu_upload_fail`, nonzero `gpu_upload_fail_capacity`, nonzero `gpu_upload_fail_fragmented`, missing/too-low `gpu_free_faces` or `gpu_largest_free`, and `gpu_fragmentation_pct` over `RUMPELMC_ALLOCATOR_STRESS_MAX_FRAGMENTATION_PCT` (default `1.0`). Fresh gate artifact `logs/week2_gpu_allocator_telemetry_20260614/gpu-allocator-stress-gate-summary.txt` passed with `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_free_ranges=2.000`, `gpu_free_faces=4193006.000`, `gpu_largest_free=4193004.000`, `gpu_fragmented_free_faces=2.000`, `gpu_fragmentation_pct=0.000`, and `terrain_queue_max_ms=2.036`; a synthetic nonzero upload-failure log was rejected. This is gate/reporting only; no allocator policy, buffer capacity, runtime renderer behavior, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, draw distance, or texture quality changed. Repack design now lives in `docs/GPU_BUFFER_REPACK.md`; next roadmap item is the flag-gated prototype.

- GPU allocator telemetry refresh is complete and behavior-preserving. `scripts/gpu_terrain_movement_stress.sh`, `scripts/gpu_terrain_workload_matrix.sh`, `scripts/gpu_terrain_fill_stress.sh`, and `scripts/gpu_terrain_report.sh` now propagate upload failure causes (`gpu_upload_fail_capacity`, `gpu_upload_fail_fragmented`) and allocator free-range state (`gpu_free_ranges`, `gpu_free_faces`, `gpu_largest_free`, `gpu_fragmented_free_faces`, `gpu_fragmentation_pct`) into summary/report rows. Fresh release movement artifact `logs/week2_gpu_allocator_telemetry_20260614/movement` passed with `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_free_ranges=2`, `gpu_free_faces=4193006`, `gpu_largest_free=4193004`, `gpu_fragmented_free_faces=2`, `gpu_fragmentation_pct=0.0`, `terrain_queue_max_ms=2.036`, `gpu_compositor_submit_max_ms=0.213`, and `process_wall_p95_ms=0.049`; scoped report `logs/week2_gpu_allocator_telemetry_20260614/gpu-terrain-report.txt` surfaces these origins. Full aggregate report `logs/gpu-terrain-report.txt` was refreshed and still reports `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, historical max `gpu_free_ranges=134`, and historical max `gpu_fragmented_free_faces=926`. This is telemetry/reporting only; no allocator policy, buffer capacity, renderer behavior, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, draw distance, or texture quality changed.

- Dirty-update default/rollback checkpoint is complete and behavior-preserving. Current intended state: `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset enables partial dirty upload by default, and explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` remains the full-rebuild rollback/control path. Fresh default-on single-edge evidence remains `logs/week2_gpu_dirty_default_on_20260614` with `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, movement `terrain_queue_max_ms=1.885`, and `gpu_upload_fail=0`; historical rollback compare evidence remains `logs/gpu_single_edge_dirty_compare_default_on`, where explicit `=0` kept full-control dirty counters at zero and explicit `=1` produced partial dirty counters. This checkpoint only updates docs/status; no runtime, renderer, dirty-update behavior, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, or native-shadow behavior changed.

- Dirty-update report summary diagnostics are fixed and behavior-preserving. `scripts/gpu_terrain_report.sh` now matches indexed `movement_terrain_queue` rows when computing aggregate `terrain_queue_max_ms` and its origin, so `/tmp/rumpel-dirty-report-parser-fix.txt` reports `terrain_queue_max_ms=1.885` from `logs/week2_gpu_dirty_default_on_20260614/single_edge/movement-stress-summary.txt`; `/tmp/rumpel-full-report-parser-fix.txt` still preserves the full-log historical max `28.314` from `logs/gpu_terrain_queue_work/perf-baseline-summary.txt`. This is a report/parser slice only; no runtime, renderer, dirty-update behavior, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, or native-shadow behavior changed.

- Dirty-update default-on single-edge evidence is refreshed and behavior-preserving under `logs/week2_gpu_dirty_default_on_20260614`. Command used release Rust GDExtension with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset and `scripts/gpu_terrain_block_edit_stress.sh` at global `127,64,80`, expecting `dirty_last_edges=pos_x` and `dirty_last_bounds=31,64,16:31,64,16`; it passed with `chunk_replace=2`, `dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=20`, movement `terrain_queue_max_ms=1.885`, `gpu_compositor_submit_max_ms=0.134`, `process_wall_p95_ms=0.049`, `terrain_samples=384`, and `gpu_upload_fail=0`. Scoped report `logs/week2_gpu_dirty_default_on_20260614/gpu-terrain-report.txt` has a clean error scan and dirty counter origins. No runtime, renderer, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, or native-shadow behavior changed.

- Fresh movement/workload/fill GPU baselines are captured and behavior-preserving under `logs/week2_gpu_baseline_20260614`. Commands used release Rust GDExtension builds: movement stress, standard workload matrix, fill stress repeats `1/4/8`, and scoped report `sh scripts/gpu_terrain_report.sh logs/week2_gpu_baseline_20260614 logs/week2_gpu_baseline_20260614/gpu-terrain-report.txt`. Key results: `gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=12752`, max draw command occupancy `19.458%`, max `terrain_queue_max_ms=6.367`, max `gpu_compositor_submit_max_ms=0.180`, max `process_wall_p95_ms=0.052`, movement baseline `terrain_queue_max_ms=1.992`, workload max `terrain_queue_max_ms=2.173`, fill repeat `8` passed with `smoke_err=0` and `terrain_samples=384`; report error scan found no patterns. This is measurement/docs only; no runtime, renderer, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, or native-shadow behavior changed. Local FPS and GPU timestamp samples remain report-only on macOS/Metal.

- Current aggregate GPU report refresh is complete and behavior-preserving. Docs status is committed as `b2522a0`, handoff status is committed as `0843bc9`, and `logs/gpu-terrain-report.txt` was regenerated with `sh scripts/gpu_terrain_report.sh logs logs/gpu-terrain-report.txt` again on `b2522a0` so the ignored local report header points at current evidence. OntoIndex reindex passed via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`. The report surfaces the latest native-shadow command-buffer submit readiness metrics, keeps `sum gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=13344`, max draw command occupancy `30.298%`, max `terrain_queue_gpu_uploads=1`, max `terrain_queue_gpu_upload_kb=1.600`, max `native_shadow_active=0.000`, max `native_shadow_command_buffer_submit_ready=0.000`, max `native_shadow_command_buffer_submit_error_count=0.000`, and selects `logs/gpu_native_shadow_command_buffer_submit_contract_20260614/movement-stress-summary.txt` as the fresh movement-stress source. This is report/docs only; no runtime, renderer, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, or native-shadow gate behavior changed.

- Current Phase 12 native-shadow command buffer submit readiness contract proof slice is committed as `c98893d` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future command buffer submit readiness fields (`native_shadow_command_buffer_submit_ready`, `native_shadow_command_buffer_submit_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness/pass-validation/pass-lifecycle/record-readiness fields; the current active descriptor submit-readiness state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled command buffer submit readiness markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not submit or record command buffers, begin/end RenderDevice passes, validate pass descriptors at runtime, bind framebuffers to RenderDevice passes, validate framebuffer descriptors at runtime, claim framebuffer attachment ownership, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, submit RenderingDevice draws, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow command buffer submit readiness artifact: `logs/gpu_native_shadow_command_buffer_submit_contract_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_command_buffer_submit_contract_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled command_buffer_status=none command_buffer_record_ready=0 command_buffer_record_error_count=0 command_buffer_submit_ready=0 command_buffer_submit_error_count=0 command_buffer_submit_count=0 command_buffer_error_count=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.162`, `gpu_compositor_submit_max_ms=0.458`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-command-buffer-submit-contract.txt` selected this summary and reported max `native_shadow_command_buffer_submit_ready=0.000` and `native_shadow_command_buffer_submit_error_count=0.000` with the fresh marker as origin.
- Checks passed for this command buffer submit readiness contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `c98893d`, docs sync commit `bb233fc`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow command buffer record readiness contract proof slice is committed as `b7ddd62` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future command buffer record readiness fields (`native_shadow_command_buffer_record_ready`, `native_shadow_command_buffer_record_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness/pass-validation/pass-lifecycle fields; the current active descriptor record-readiness state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled command buffer record markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not record or submit command buffers, begin/end RenderDevice passes, validate pass descriptors at runtime, bind framebuffers to RenderDevice passes, validate framebuffer descriptors at runtime, claim framebuffer attachment ownership, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, submit RenderingDevice draws, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow command buffer record readiness artifact: `logs/gpu_native_shadow_command_buffer_record_contract_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_command_buffer_record_contract_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled command_buffer_status=none command_buffer_record_ready=0 command_buffer_record_error_count=0 command_buffer_submit_count=0 command_buffer_error_count=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=3.248`, `gpu_compositor_submit_max_ms=0.224`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-command-buffer-record-contract.txt` selected this summary and reported max `native_shadow_command_buffer_record_ready=0.000` and `native_shadow_command_buffer_record_error_count=0.000` with the fresh marker as origin.
- Checks passed for this command buffer record readiness contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `b7ddd62`, docs sync commit `8f74d88`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow pass lifecycle readiness contract proof slice is committed as `7ae1ec7` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future pass lifecycle readiness fields (`native_shadow_pass_lifecycle_ready`, `native_shadow_pass_lifecycle_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness/pass-validation fields; the current active descriptor pass lifecycle readiness state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled lifecycle markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not begin/end RenderDevice passes, validate pass descriptors at runtime, bind framebuffers to RenderDevice passes, validate framebuffer descriptors at runtime, claim framebuffer attachment ownership, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow pass lifecycle readiness artifact: `logs/gpu_native_shadow_pass_lifecycle_contract_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_pass_lifecycle_contract_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled pass_submit_status=none pass_lifecycle_ready=0 pass_lifecycle_error_count=0 pass_begin_count=0 pass_end_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.085`, `gpu_compositor_submit_max_ms=0.187`, and `process_wall_p95_ms=0.007`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-pass-lifecycle-contract.txt` selected this summary and reported max `native_shadow_pass_lifecycle_ready=0.000` and `native_shadow_pass_lifecycle_error_count=0.000` with the fresh marker as origin.
- Checks passed for this pass lifecycle readiness contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `7ae1ec7`, docs sync commit `e28cbe4`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow pass descriptor validation contract proof slice is committed as `2f49973` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future pass descriptor validation fields (`native_shadow_pass_descriptor_valid`, `native_shadow_pass_descriptor_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness fields; the current active descriptor pass validation state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled pass descriptor validation markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not validate RenderDevice pass descriptors, bind framebuffers to RenderDevice passes, validate framebuffer descriptors at runtime, claim framebuffer attachment ownership, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow pass descriptor validation artifact: `logs/gpu_native_shadow_pass_descriptor_contract_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_pass_descriptor_contract_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled framebuffer_bind_ready=0 framebuffer_bind_error_count=0 pass_descriptor_valid=0 pass_descriptor_error_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.106`, `gpu_compositor_submit_max_ms=0.096`, and `process_wall_p95_ms=0.008`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-pass-descriptor-contract.txt` selected this summary and reported max `native_shadow_pass_descriptor_valid=0.000` and `native_shadow_pass_descriptor_error_count=0.000` with the fresh marker as origin.
- Checks passed for this pass descriptor validation contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `2f49973`, docs sync commit `55f5ff3`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow framebuffer bind readiness contract proof slice is committed as `65ac059` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future framebuffer bind readiness fields (`native_shadow_framebuffer_bind_ready`, `native_shadow_framebuffer_bind_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation fields; the current active descriptor bind readiness state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled bind readiness markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not bind framebuffers to RenderDevice passes, validate framebuffer descriptors at runtime, claim framebuffer attachment ownership, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow framebuffer bind readiness artifact: `logs/gpu_native_shadow_framebuffer_bind_contract_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_framebuffer_bind_contract_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0 framebuffer_status=none framebuffer_rid_allocated=0 framebuffer_attachment_count=0 framebuffer_pass_compat_status=none framebuffer_pass_compat_error_count=0 framebuffer_depth_only_enabled=0 framebuffer_color_attachment_count=0 framebuffer_attachment_owned=0 framebuffer_attachment_reuse_count=0 framebuffer_descriptor_valid=0 framebuffer_descriptor_error_count=0 framebuffer_bind_ready=0 framebuffer_bind_error_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.026`, `gpu_compositor_submit_max_ms=0.204`, and `process_wall_p95_ms=0.011`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-framebuffer-bind-contract.txt` selected this summary and reported max `native_shadow_framebuffer_bind_ready=0.000` and `native_shadow_framebuffer_bind_error_count=0.000` with the fresh marker as origin.
- Checks passed for this framebuffer bind readiness contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `65ac059`, docs sync commit `78a77c1`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow framebuffer descriptor validation contract proof slice is committed as `d5eedc1` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future framebuffer descriptor validation fields (`native_shadow_framebuffer_descriptor_valid`, `native_shadow_framebuffer_descriptor_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime fields; the current active descriptor validation state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled descriptor validation markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not validate framebuffer descriptors at runtime, claim framebuffer attachment ownership, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow framebuffer descriptor validation artifact: `logs/gpu_native_shadow_framebuffer_descriptor_validation_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_framebuffer_descriptor_validation_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0 framebuffer_status=none framebuffer_rid_allocated=0 framebuffer_attachment_count=0 framebuffer_pass_compat_status=none framebuffer_pass_compat_error_count=0 framebuffer_depth_only_enabled=0 framebuffer_color_attachment_count=0 framebuffer_attachment_owned=0 framebuffer_attachment_reuse_count=0 framebuffer_descriptor_valid=0 framebuffer_descriptor_error_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.029`, `gpu_compositor_submit_max_ms=0.250`, and `process_wall_p95_ms=0.007`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-framebuffer-descriptor-validation.txt` selected this summary and reported max `native_shadow_framebuffer_descriptor_valid=0.000` and `native_shadow_framebuffer_descriptor_error_count=0.000` with the fresh marker as origin.
- Checks passed for this framebuffer descriptor validation contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_test_gap` (`PASS` heuristic), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `d5eedc1`, docs sync commit `68c9353`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow framebuffer attachment lifetime contract proof slice is committed as `9508a8b` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future framebuffer attachment lifetime fields (`native_shadow_framebuffer_attachment_owned`, `native_shadow_framebuffer_attachment_reuse_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only fields; the current active descriptor attachment lifetime state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled lifetime markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not claim framebuffer attachment ownership at runtime, reuse native shadow attachments, validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow framebuffer attachment lifetime artifact: `logs/gpu_native_shadow_framebuffer_attachment_lifetime_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_framebuffer_attachment_lifetime_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0 framebuffer_status=none framebuffer_rid_allocated=0 framebuffer_attachment_count=0 framebuffer_pass_compat_status=none framebuffer_pass_compat_error_count=0 framebuffer_depth_only_enabled=0 framebuffer_color_attachment_count=0 framebuffer_attachment_owned=0 framebuffer_attachment_reuse_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.738`, `gpu_compositor_submit_max_ms=0.196`, and `process_wall_p95_ms=0.008`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-framebuffer-attachment-lifetime-descriptor.txt` selected this summary and reported max `native_shadow_framebuffer_attachment_owned=0.000` and `native_shadow_framebuffer_attachment_reuse_count=0.000` with the fresh marker as origin.
- Checks passed for this framebuffer attachment lifetime contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_test_gap` (`PASS` heuristic), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `9508a8b`, docs sync commit `63e55cc`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow framebuffer depth-only contract proof slice is committed as `7f1148e` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future framebuffer depth-only fields (`native_shadow_framebuffer_depth_only_enabled`, `native_shadow_framebuffer_color_attachment_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat fields; the current active descriptor depth-only state is `1/0`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled depth-only markers, and `scripts/gpu_terrain_report.sh` aggregates both maxima. This does not validate framebuffer depth-only behavior, create native shadow color attachments, validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow framebuffer depth-only artifact: `logs/gpu_native_shadow_framebuffer_depth_only_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_framebuffer_depth_only_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0 framebuffer_status=none framebuffer_rid_allocated=0 framebuffer_attachment_count=0 framebuffer_pass_compat_status=none framebuffer_pass_compat_error_count=0 framebuffer_depth_only_enabled=0 framebuffer_color_attachment_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.118`, `gpu_compositor_submit_max_ms=1.043`, and `process_wall_p95_ms=0.050`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-framebuffer-depth-only-descriptor.txt` selected this summary and reported max `native_shadow_framebuffer_depth_only_enabled=0.000` and `native_shadow_framebuffer_color_attachment_count=0.000` with the fresh marker as origin.
- Checks passed for this framebuffer depth-only contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_test_gap` (`PASS` heuristic), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), code commit `7f1148e`, docs sync commit `3af3205`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow framebuffer/pass compatibility contract proof slice is committed as `13475dc` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future framebuffer/pass compatibility fields (`native_shadow_framebuffer_pass_compat_status`, `native_shadow_framebuffer_pass_compat_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer fields; the current active descriptor compatibility state is `not_validated/0`, while disabled runtime resources must report `none/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled compatibility markers, and `scripts/gpu_terrain_report.sh` aggregates compatibility error counts. This does not validate native shadow framebuffer/pass compatibility, create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow framebuffer/pass compatibility artifact: `logs/gpu_native_shadow_framebuffer_pass_compat_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_framebuffer_pass_compat_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0 framebuffer_status=none framebuffer_rid_allocated=0 framebuffer_attachment_count=0 framebuffer_pass_compat_status=none framebuffer_pass_compat_error_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.459`, `gpu_compositor_submit_max_ms=0.240`, and `process_wall_p95_ms=0.052`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-framebuffer-pass-compat-descriptor.txt` selected this summary and reported max `native_shadow_framebuffer_pass_compat_error_count=0.000` with the fresh marker as origin.
- Checks passed for this framebuffer/pass compatibility contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, `./scripts/check.sh fast`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY` after including inline `tests` in expected scope), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS` heuristic), staged MCP `gn_review_diff` (no high-risk findings; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `13475dc`, docs sync commit `e3a3eb8`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow framebuffer contract proof slice is committed as `068c1b5` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future framebuffer fields (`native_shadow_framebuffer_status`, `native_shadow_framebuffer_rid_allocated`, `native_shadow_framebuffer_attachment_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier fields; the current active descriptor framebuffer state is `not_created/0/1`, while disabled runtime resources must report `none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled framebuffer markers, and `scripts/gpu_terrain_report.sh` aggregates framebuffer RID/attachment counts. This does not create native shadow framebuffers or RenderingDevice framebuffer RIDs, issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow framebuffer contract artifact: `logs/gpu_native_shadow_framebuffer_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_framebuffer_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0 framebuffer_status=none framebuffer_rid_allocated=0 framebuffer_attachment_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.004`, `gpu_compositor_submit_max_ms=0.136`, and `process_wall_p95_ms=0.048`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-framebuffer-descriptor.txt` selected this summary and reported max `native_shadow_framebuffer_rid_allocated=0.000` and `native_shadow_framebuffer_attachment_count=0.000` with the fresh marker as origin.
- Checks passed for this framebuffer contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `068c1b5`, docs sync commit `224fa28`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow resource barrier contract proof slice is committed as `9a1677f` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future resource barrier fields (`native_shadow_resource_barrier_status`, `native_shadow_resource_transition_count`, `native_shadow_resource_barrier_error_count`) in addition to the existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer fields; the current active descriptor resource barrier state is `not_issued/0/0`, while disabled runtime resources must report `none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled barrier markers, and `scripts/gpu_terrain_report.sh` aggregates transition/error counts. This does not issue native shadow resource barriers or transitions, bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow resource barrier contract artifact: `logs/gpu_native_shadow_resource_barrier_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_resource_barrier_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0 resource_barrier_status=none resource_transition_count=0 resource_barrier_error_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.196`, `gpu_compositor_submit_max_ms=0.272`, and `process_wall_p95_ms=0.011`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-resource-barrier-descriptor.txt` selected this summary and reported max `native_shadow_resource_transition_count=0.000` and `native_shadow_resource_barrier_error_count=0.000` with the fresh marker as origin.
- Checks passed for this resource barrier contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`FAIL` only because it did not ingest shell executedTests from this session), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `9a1677f`, docs sync commit `5ff7400`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow depth attachment contract proof slice is committed as `ad9f9d2` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future depth attachment fields (`native_shadow_depth_attachment_status`, `native_shadow_depth_attachment_binding_count`, `native_shadow_depth_attachment_clear_count`) in addition to the existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer fields; the current active descriptor depth attachment state is `not_bound/0/0`, while disabled runtime resources must report `none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled depth attachment markers, and `scripts/gpu_terrain_report.sh` aggregates binding/clear counts. This does not bind or clear native shadow depth attachments, record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow depth attachment contract artifact: `logs/gpu_native_shadow_depth_attachment_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_depth_attachment_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 pass_status=none pass_rid_allocated=0 pass_submit_status=none pass_begin_count=0 pass_end_count=0 command_buffer_status=none command_buffer_submit_count=0 command_buffer_error_count=0 depth_attachment_status=none depth_attachment_binding_count=0 depth_attachment_clear_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.660`, `gpu_compositor_submit_max_ms=0.212`, and `process_wall_p95_ms=0.050`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-depth-attachment-descriptor.txt` selected this summary and reported max `native_shadow_depth_attachment_binding_count=0.000` and `native_shadow_depth_attachment_clear_count=0.000` with the fresh marker as origin.
- Checks passed for this depth attachment contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `ad9f9d2`, docs sync commit `79abeef`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow command buffer contract proof slice is committed as `4844b10` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future command buffer fields (`native_shadow_command_buffer_status`, `native_shadow_command_buffer_submit_count`, `native_shadow_command_buffer_error_count`) in addition to the existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission fields; the current active descriptor command buffer state is `not_recorded/0/0`, while disabled runtime resources must report `none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled command buffer markers, and `scripts/gpu_terrain_report.sh` aggregates submit/error counts. This does not record or submit command buffers, submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow command buffer contract artifact: `logs/gpu_native_shadow_command_buffer_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_command_buffer_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 pass_status=none pass_rid_allocated=0 pass_submit_status=none pass_begin_count=0 pass_end_count=0 command_buffer_status=none command_buffer_submit_count=0 command_buffer_error_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.042`, `gpu_compositor_submit_max_ms=0.224`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-command-buffer-descriptor.txt` selected this summary and reported max `native_shadow_command_buffer_submit_count=0.000` and `native_shadow_command_buffer_error_count=0.000` with the fresh marker as origin.
- Checks passed for this command buffer contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY` after expected scope included `GameClient`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `4844b10`, docs sync commit `dd407ce`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow draw submission contract proof slice is committed as `0304f21` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future draw submission fields (`native_shadow_draw_status`, `native_shadow_draw_call_count`, `native_shadow_draw_face_count`) in addition to the existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission fields; the current active descriptor draw submission state is `not_submitted/0/0`, while disabled runtime resources must report `none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled draw submission markers, and `scripts/gpu_terrain_report.sh` aggregates draw call/face counts. This does not submit RenderingDevice draws, begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow draw submission contract artifact: `logs/gpu_native_shadow_draw_submit_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_draw_submit_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 pass_status=none pass_rid_allocated=0 pass_submit_status=none pass_begin_count=0 pass_end_count=0 draw_status=none draw_call_count=0 draw_face_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.244`, `gpu_compositor_submit_max_ms=0.127`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-draw-submit-descriptor.txt` selected this summary and reported max `native_shadow_draw_call_count=0.000` and `native_shadow_draw_face_count=0.000` with the fresh marker as origin.
- Checks passed for this draw submission contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `0304f21`, docs sync commit `0e2f86e`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow pass submission contract proof slice is committed as `a76d3f7` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future pass submission boundary fields (`native_shadow_pass_submit_status`, `native_shadow_pass_begin_count`, `native_shadow_pass_end_count`) in addition to the existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth fields; the current active descriptor submission state is `not_submitted/0/0`, while disabled runtime resources must report `none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled submission markers, and `scripts/gpu_terrain_report.sh` aggregates begin/end counts. This does not begin/end RenderingDevice passes, allocate pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow pass submission contract artifact: `logs/gpu_native_shadow_pass_submit_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_pass_submit_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 pass_status=none pass_rid_allocated=0 pass_submit_status=none pass_begin_count=0 pass_end_count=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.153`, `gpu_compositor_submit_max_ms=0.186`, and `process_wall_p95_ms=0.051`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-pass-submit-descriptor.txt` selected this summary and reported max `native_shadow_pass_begin_count=0.000` and `native_shadow_pass_end_count=0.000` with the fresh marker as origin.
- Checks passed for this pass submission contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `a76d3f7`, docs sync commit `607ab61`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow pass RID contract proof slice is committed as `690416a` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future pass/RID contract fields (`native_shadow_pass_status`, `native_shadow_pass_rid_allocated`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth fields; the current active descriptor pass state is `descriptor_ready/0`, while disabled runtime resources must report `none/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled pass markers, and `scripts/gpu_terrain_report.sh` aggregates `native_shadow_pass_rid_allocated`. This does not allocate RenderingDevice pass/RID resources, compile shaders, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow pass RID contract artifact: `logs/gpu_native_shadow_pass_rid_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_pass_rid_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 pass_status=none pass_rid_allocated=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0 shader_language=none shader_entry=none shader_depth_output_enabled=0 shader_color_output_enabled=0 shader_source_bytes=0 shader_source_checksum=0 shader_module_status=none shader_module_rid_allocated=0 light_source=none light_space=none cascade_count=0 light_matrix_bytes=0 depth_clip_space=none depth_range_source=none depth_near_milli=0 depth_far_chunks=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.945`, `gpu_compositor_submit_max_ms=0.238`, and `process_wall_p95_ms=0.051`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-pass-rid-descriptor.txt` selected this summary and reported max `native_shadow_pass_rid_allocated=0.000` with the fresh marker as origin.
- Checks passed for this pass RID contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `690416a`, docs sync commit `94ec1e0`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow shader module contract proof slice is committed as `033db27` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future shader module contract fields (`native_shadow_shader_module_status`, `native_shadow_shader_module_rid_allocated`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/light/depth fields; the current active descriptor module state is `source_ready/0`, while disabled runtime resources must report `none/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled module markers, and `scripts/gpu_terrain_report.sh` aggregates `native_shadow_shader_module_rid_allocated`. This does not compile shader sources, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow shader module contract artifact: `logs/gpu_native_shadow_shader_module_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_shader_module_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0 shader_language=none shader_entry=none shader_depth_output_enabled=0 shader_color_output_enabled=0 shader_source_bytes=0 shader_source_checksum=0 shader_module_status=none shader_module_rid_allocated=0 light_source=none light_space=none cascade_count=0 light_matrix_bytes=0 depth_clip_space=none depth_range_source=none depth_near_milli=0 depth_far_chunks=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.341`, `gpu_compositor_submit_max_ms=0.276`, and `process_wall_p95_ms=0.053`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-shader-module-descriptor.txt` selected this summary and reported max `native_shadow_shader_module_rid_allocated=0.000` with the fresh marker as origin.
- Checks passed for this shader module contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `033db27`, docs sync commit `967eb73`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow shader source contract proof slice is committed as `bf4c393` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future shader source/module contract fields (`native_shadow_shader_source_bytes`, `native_shadow_shader_source_checksum`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/light/depth fields; the current active descriptor source contract is `59` bytes with checksum `3794449943`, while disabled runtime resources must report `0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled source contract markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric source maxima. This does not compile shader sources, allocate shader modules/RIDs, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow shader source contract artifact: `logs/gpu_native_shadow_shader_source_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_shader_source_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0 shader_language=none shader_entry=none shader_depth_output_enabled=0 shader_color_output_enabled=0 shader_source_bytes=0 shader_source_checksum=0 light_source=none light_space=none cascade_count=0 light_matrix_bytes=0 depth_clip_space=none depth_range_source=none depth_near_milli=0 depth_far_chunks=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.688`, `gpu_compositor_submit_max_ms=0.186`, and `process_wall_p95_ms=0.053`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-shader-source-descriptor.txt` selected this summary and reported max `native_shadow_shader_source_bytes=0.000` and `native_shadow_shader_source_checksum=0.000` with the fresh marker as origin.
- Checks passed for this shader source contract proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY` after expected scope included `GameClient` and `GpuNativeShadowResourceAction`), MCP `gn_verify_diff` (`PASS` after exact executed smoke command), MCP `gn_test_gap` (`FAIL` heuristic only because supplied changed symbols have no graph-linked test ids; targeted Rust test and runtime smoke were executed), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `bf4c393`, docs sync commit `4fb4f00`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow depth/clip descriptor proof slice is committed as `39b5a41` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future depth/clip fields (`native_shadow_depth_clip_space`, `native_shadow_depth_range_source`, `native_shadow_depth_near_milli`, `native_shadow_depth_far_chunks`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/light fields; the current active descriptor plan is `zero_to_one/scene_shadow_radius/100/<shadow_radius_chunks>`, while disabled runtime resources must report `none/none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled depth/clip markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric depth maxima. This does not upload depth ranges, bind light matrices, compile shaders, allocate shader/pass/RID/draw resources, enable a native shadow pass, change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow depth/clip descriptor artifact: `logs/gpu_native_shadow_depth_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_depth_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0 shader_language=none shader_entry=none shader_depth_output_enabled=0 shader_color_output_enabled=0 light_source=none light_space=none cascade_count=0 light_matrix_bytes=0 depth_clip_space=none depth_range_source=none depth_near_milli=0 depth_far_chunks=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.082`, `gpu_compositor_submit_max_ms=0.237`, and `process_wall_p95_ms=0.050`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-depth-descriptor.txt` selected this summary and reported max `native_shadow_depth_near_milli=0.000` and `native_shadow_depth_far_chunks=0.000` with the fresh marker as origin.
- Checks passed for this depth/clip descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS` after exact executed smoke command), MCP `gn_test_gap` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `39b5a41`, docs sync commit `7fdc935`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow light descriptor proof slice is committed as `bed9cd9` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future light/matrix fields (`native_shadow_light_source`, `native_shadow_light_space`, `native_shadow_cascade_count`, `native_shadow_light_matrix_bytes`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader fields; the current active descriptor plan is `directional_light/world_to_shadow/1/64`, while disabled runtime resources must report `none/none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled light markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric light maxima. This does not bind light matrices, does not compile shaders, does not allocate shader/pass/RID/draw resources, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow light descriptor artifact: `logs/gpu_native_shadow_light_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_light_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0 shader_language=none shader_entry=none shader_depth_output_enabled=0 shader_color_output_enabled=0 light_source=none light_space=none cascade_count=0 light_matrix_bytes=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=3.698`, `gpu_compositor_submit_max_ms=0.240`, and `process_wall_p95_ms=0.052`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-light-descriptor.txt` selected this summary and reported max `native_shadow_cascade_count=0.000` and `native_shadow_light_matrix_bytes=0.000` with the fresh marker as origin.
- Checks passed for this light descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `bed9cd9`, docs sync commit `5939589`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow shader descriptor proof slice is committed as `12fff45` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future shader fields (`native_shadow_shader_language`, `native_shadow_shader_entry`, `native_shadow_shader_depth_output_enabled`, `native_shadow_shader_color_output_enabled`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform fields; the current active descriptor plan is `glsl/native_shadow_depth/1/0`, while disabled runtime resources must report `none/none/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled shader markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric shader output maxima. This does not compile shaders, does not allocate shader/pass/RID/draw resources, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow shader descriptor artifact: `logs/gpu_native_shadow_shader_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_shader_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0 shader_language=none shader_entry=none shader_depth_output_enabled=0 shader_color_output_enabled=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.016`, `gpu_compositor_submit_max_ms=0.238`, and `process_wall_p95_ms=0.055`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-shader-descriptor.txt` selected this summary and reported max `native_shadow_shader_depth_output_enabled=0.000` and `native_shadow_shader_color_output_enabled=0.000` with the fresh marker as origin.
- Checks passed for this shader descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `12fff45`, docs sync commit `cbe66c8`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow uniform descriptor proof slice is committed as `08448c2` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future uniform/binding fields (`native_shadow_uniform_set_index`, `native_shadow_face_buffer_binding`, `native_shadow_push_constant_bytes`, `native_shadow_texture_sampling_enabled`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline/draw fields; the current active descriptor plan is `0/0/64/0`, while disabled runtime resources must report `0/0/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled uniform markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric uniform maxima. This does not allocate shader/pass/RID/draw resources, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow uniform descriptor artifact: `logs/gpu_native_shadow_uniform_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_uniform_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0 uniform_set_index=0 face_buffer_binding=0 push_constant_bytes=0 texture_sampling_enabled=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.101`, `gpu_compositor_submit_max_ms=0.143`, and `process_wall_p95_ms=0.051`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-uniform-descriptor.txt` selected this summary and reported max `native_shadow_uniform_set_index=0.000`, `native_shadow_face_buffer_binding=0.000`, `native_shadow_push_constant_bytes=0.000`, and `native_shadow_texture_sampling_enabled=0.000` with the fresh marker as origin.
- Checks passed for this uniform descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `08448c2`, docs sync commit `9193a61`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow draw descriptor proof slice is committed as `6314795` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future draw/input fields (`native_shadow_draw_source`, `native_shadow_draw_primitive`, `native_shadow_draw_face_stride_bytes`, `native_shadow_draw_command_stride_bytes`, `native_shadow_draw_indirect_enabled`) in addition to the existing resource/pass/sampler/depth-bias/viewport/pipeline fields; the current active descriptor plan is `packed_faces/triangles/16/16/1`, while disabled runtime resources must report `none/none/0/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled draw markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric draw maxima. This does not allocate RenderingDevice RIDs or issue native draw calls, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow draw descriptor artifact: `logs/gpu_native_shadow_draw_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_draw_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none draw_source=none draw_primitive=none draw_face_stride_bytes=0 draw_command_stride_bytes=0 draw_indirect_enabled=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.203`, `gpu_compositor_submit_max_ms=0.259`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-draw-descriptor.txt` selected this summary and reported max `native_shadow_draw_face_stride_bytes=0.000`, `native_shadow_draw_command_stride_bytes=0.000`, and `native_shadow_draw_indirect_enabled=0.000` with the fresh marker as origin.
- Checks passed for this draw descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `6314795`, docs sync commit `2708ce9`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow pipeline descriptor proof slice is committed as `5b69f52` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future pipeline fields (`native_shadow_pipeline_depth_test_enabled`, `native_shadow_pipeline_depth_write_enabled`, `native_shadow_pipeline_cull_mode`, `native_shadow_pipeline_front_face`) in addition to the existing resource/pass/sampler/depth-bias/viewport fields; the current active descriptor plan is `1/1/back/clockwise`, while disabled runtime resources must report `0/0/none/none`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled pipeline markers, and `scripts/gpu_terrain_report.sh` aggregates the numeric pipeline flag maxima. This does not allocate RenderingDevice RIDs or pipelines, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow pipeline descriptor artifact: `logs/gpu_native_shadow_pipeline_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_pipeline_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0 pipeline_depth_test_enabled=0 pipeline_depth_write_enabled=0 pipeline_cull_mode=none pipeline_front_face=none`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.035`, `gpu_compositor_submit_max_ms=0.135`, and `process_wall_p95_ms=0.051`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-pipeline-descriptor.txt` selected this summary and reported max `native_shadow_pipeline_depth_test_enabled=0.000` and `native_shadow_pipeline_depth_write_enabled=0.000` with the fresh marker as origin.
- Checks passed for this pipeline descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `5b69f52`, docs sync commit `a47d22c`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`. MCP `gn_test_gap` reported heuristic `FAIL` because inline Rust unit tests and shell smoke are not linked in the graph test index; the executed test evidence is listed above.

- Current Phase 12 native-shadow viewport descriptor proof slice is committed as `3bb4b3a` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future viewport fields (`native_shadow_viewport_x_px`, `native_shadow_viewport_y_px`, `native_shadow_viewport_width_px`, `native_shadow_viewport_height_px`, `native_shadow_viewport_min_depth_milli`, `native_shadow_viewport_max_depth_milli`) in addition to the existing resource/pass/sampler/depth-bias fields; the current active descriptor plan is `0/0/2048/2048/0/1000`, while disabled runtime resources must report `0/0/0/0/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled viewport markers, and `scripts/gpu_terrain_report.sh` aggregates the viewport maxima. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow viewport descriptor artifact: `logs/gpu_native_shadow_viewport_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_viewport_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0 viewport_x_px=0 viewport_y_px=0 viewport_width_px=0 viewport_height_px=0 viewport_min_depth_milli=0 viewport_max_depth_milli=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.112`, `gpu_compositor_submit_max_ms=0.134`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-viewport-descriptor.txt` selected this summary and reported max `native_shadow_viewport_x_px=0.000`, `native_shadow_viewport_y_px=0.000`, `native_shadow_viewport_width_px=0.000`, `native_shadow_viewport_height_px=0.000`, `native_shadow_viewport_min_depth_milli=0.000`, and `native_shadow_viewport_max_depth_milli=0.000` with the fresh marker as origin.
- Checks passed for this viewport descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY` after including adjacent `GpuNativeShadowResourceAction` in expected scope), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `3bb4b3a`, docs sync commit `b65b83c`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`. MCP `gn_test_gap` reported heuristic `FAIL` because inline Rust unit tests and shell smoke are not linked in the graph test index; the executed test evidence is listed above.

- Current Phase 12 native-shadow depth bias descriptor proof slice is committed as `b1721e8` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future depth bias fields (`native_shadow_depth_bias_constant_milli`, `native_shadow_depth_bias_slope_milli`, `native_shadow_depth_bias_clamp_milli`) in addition to the existing resource/pass/sampler fields; the current active descriptor plan is `2/1500/0`, while disabled runtime resources must report `0/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled bias markers, and `scripts/gpu_terrain_report.sh` aggregates the bias maxima. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow depth bias descriptor artifact: `logs/gpu_native_shadow_depth_bias_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_depth_bias_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0 depth_bias_constant_milli=0 depth_bias_slope_milli=0 depth_bias_clamp_milli=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.233`, `gpu_compositor_submit_max_ms=0.141`, and `process_wall_p95_ms=0.051`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-depth-bias-descriptor.txt` selected this summary and reported max `native_shadow_depth_bias_constant_milli=0.000`, `native_shadow_depth_bias_slope_milli=0.000`, and `native_shadow_depth_bias_clamp_milli=0.000` with the fresh marker as origin.
- Checks passed for this depth bias descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `b1721e8`, docs sync commit `cad165f`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`. MCP `gn_test_gap` reported heuristic `FAIL` because inline Rust unit tests and shell smoke are not linked in the graph test index; the executed test evidence is listed above.

- Current Phase 12 native-shadow sampler descriptor proof slice is committed as `74cc460` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future depth sampler fields (`native_shadow_sampler_filter`, `native_shadow_sampler_address`, `native_shadow_sampler_compare_op`, `native_shadow_sampler_compare_enabled`) in addition to the existing resource/pass fields; the current active descriptor plan is `linear/clamp_to_edge/less_equal/1`, while disabled runtime resources must report `none/none/none/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled sampler markers, and `scripts/gpu_terrain_report.sh` aggregates `native_shadow_sampler_compare_enabled`. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow sampler descriptor artifact: `logs/gpu_native_shadow_sampler_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_sampler_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0 sampler_filter=none sampler_address=none sampler_compare_op=none sampler_compare_enabled=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.149`, `gpu_compositor_submit_max_ms=0.138`, and `process_wall_p95_ms=0.049`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-sampler-descriptor.txt` selected this summary and reported max `native_shadow_sampler_compare_enabled=0.000` with the fresh marker as origin.
- Checks passed for this sampler descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY` after including adjacent `GameClient`/`GpuNativeShadowResourceAction` in expected scope), MCP `gn_verify_diff` (`PASS`), staged MCP `gn_review_diff` (no high-risk symbols; degraded only because the staged review ran against a dirty-worktree overlay before commit), commit `74cc460`, docs sync commit `dd51019`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`. MCP `gn_test_gap` reported heuristic `FAIL` because inline Rust unit tests and shell smoke are not linked in the graph test index; the executed test evidence is listed above.

- Current Phase 12 native-shadow pass descriptor proof slice is committed as `8ee1e0b` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future depth pass fields (`native_shadow_pass_load_op`, `native_shadow_pass_store_op`, `native_shadow_pass_clear_depth_milli`) in addition to the existing resource format/extent/byte fields; the current active descriptor plan is `clear/store/1000`, while disabled runtime resources must report `none/none/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled pass markers, and `scripts/gpu_terrain_report.sh` aggregates `native_shadow_pass_clear_depth_milli`. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow pass descriptor artifact: `logs/gpu_native_shadow_pass_descriptor_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_pass_descriptor_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none pass_load_op=none pass_store_op=none pass_clear_depth_milli=0`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.999`, `gpu_compositor_submit_max_ms=0.142`, and `process_wall_p95_ms=0.048`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-pass-descriptor.txt` selected this summary and reported max `native_shadow_pass_clear_depth_milli=0.000` with the fresh marker as origin.
- Checks passed for this pass descriptor proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_test_gap`, MCP `gn_verify_diff`, staged MCP `gn_review_diff`, commit `8ee1e0b`, docs sync commit `867d739`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow resource extent proof slice is committed as `d84f485` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now exposes future shadow-map extent fields (`native_shadow_resource_width`, `native_shadow_resource_height`, `native_shadow_resource_layers`) in addition to the existing map/format/byte fields; the current active descriptor plan is `2048 x 2048 x 1`, while disabled runtime resources must report extent `0/0/0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled extent markers, and `scripts/gpu_terrain_report.sh` aggregates the extent maxima. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow resource extent artifact: `logs/gpu_native_shadow_resource_extent_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_resource_extent_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_width=0 resource_height=0 resource_layers=0 resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=3.008`, `gpu_compositor_submit_max_ms=0.126`, and `process_wall_p95_ms=0.019`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-resource-extent.txt` selected this summary and reported max `native_shadow_resource_width=0.000`, `native_shadow_resource_height=0.000`, and `native_shadow_resource_layers=0.000` with the fresh marker as origin.
- Checks passed for this resource extent proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_test_gap`, MCP `gn_verify_diff`, staged MCP `gn_review_diff`, commit `d84f485`, docs sync commit `c008045`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow resource byte estimate proof slice is committed as `101265c` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now computes a future shadow-map memory estimate from `map_size_px * map_size_px * bytes_per_texel`; the current active descriptor plan is `2048 x 2048 x 4 = 16 MiB`, while disabled runtime resources report `native_shadow_resource_bytes_per_texel=0` and `native_shadow_resource_bytes=0`. `scripts/gpu_terrain_movement_stress.sh` surfaces and validates the disabled byte markers, and `scripts/gpu_terrain_report.sh` aggregates `native_shadow_resource_bytes`. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow resource byte artifact: `logs/gpu_native_shadow_resource_bytes_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_resource_bytes_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_bytes_per_texel=0 resource_bytes=0 resource_format=none resource_usage=none`, zero lifecycle/coverage counters, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.313`, `gpu_compositor_submit_max_ms=0.112`, and `process_wall_p95_ms=0.015`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-resource-bytes.txt` selected this summary and reported max `native_shadow_resource_bytes=0.000` with the fresh marker as origin.
- Checks passed for this resource byte estimate slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_test_gap`, MCP `gn_verify_diff`, staged MCP `gn_review_diff`, commit `101265c`, docs sync commit `a3790ee`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow resource format proof slice is committed as `9127d7b` and behavior-preserving. `GpuNativeShadowResourceDescriptor` now carries explicit future allocation labels for a depth shadow map (`native_shadow_resource_format=d32_sfloat`, `native_shadow_resource_usage=depth_stencil_attachment|sampling`), and `scripts/gpu_terrain_movement_stress.sh` surfaces them in the compact `movement_native_shadow` summary row. Disabled resources report `none` labels. This does not allocate RenderingDevice RIDs, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow resource format artifact: `logs/gpu_native_shadow_resource_format_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_resource_format_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_format=none resource_usage=none resource_creates=0 resource_reuses=0 resource_replaces=0 resource_releases=0 covered_chunks=0 covered_subchunks=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.481`, `gpu_compositor_submit_max_ms=0.226`, and `process_wall_p95_ms=0.046`. Aggregate report check `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-resource-format.txt` selected this summary and found no error patterns.
- Checks passed for this resource format proof slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, the aggregate report above, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_test_gap`, MCP `gn_verify_diff`, staged MCP `gn_review_diff`, commit `9127d7b`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow movement summary fields slice is committed as `841b7ea` and behavior-preserving. `scripts/gpu_terrain_movement_stress.sh` now writes a compact `movement_native_shadow` row from existing native-shadow marker metrics, including requested/active/fallback/implemented state, resource lifecycle counters, and coverage counters. This does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow summary artifact: `logs/gpu_native_shadow_summary_fields_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_summary_fields_20260614`. It passed with `movement_native_shadow requested=1 active=0 fallback=1 implemented=0 resource_status=disabled resource_creates=0 resource_reuses=0 resource_replaces=0 resource_releases=0 covered_chunks=0 covered_subchunks=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.179`, `gpu_compositor_submit_max_ms=0.121`, and `process_wall_p95_ms=0.019`.
- Checks passed for this summary fields slice: `sh -n scripts/gpu_terrain_movement_stress.sh`, the env-on movement smoke above, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-summary-fields.txt`, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_test_gap`, MCP `gn_verify_diff`, staged MCP `gn_review_diff`, commit `841b7ea`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`.

- Current Phase 12 native-shadow coverage marker scaffold slice is behavior-preserving. `client/rust_ext/src/lib.rs` now exposes `native_shadow_covered_chunks` and `native_shadow_covered_subchunks` in perf text from the existing state-only `GpuNativeShadowResources` descriptor. While `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, the descriptor is disabled, so env-on fallback captures must keep both coverage counters at `0`. `scripts/gpu_terrain_movement_stress.sh` validates those zero coverage counters when `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1`, and `scripts/gpu_terrain_report.sh` aggregates their maxima and origins. This does not allocate RenderingDevice resources, does not enable a native shadow pass, and does not change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, or chunk serialization.
- Fresh native-shadow coverage artifact: `logs/gpu_native_shadow_coverage_scaffold_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_coverage_scaffold_20260614`. It passed with `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `native_shadow_resource_status=disabled`, zero native-shadow resource lifecycle counters, `native_shadow_covered_chunks=0`, `native_shadow_covered_subchunks=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.536`, `gpu_compositor_submit_max_ms=0.121`, and `process_wall_p95_ms=0.013`.
- Checks passed for this coverage marker slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, the env-on movement smoke above, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-coverage-scaffold.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_test_gap`, MCP `gn_verify_diff`, and staged MCP `gn_review_diff`. Still commit and reindex OntoIndex.

- Current Phase 12 native-shadow resource lifecycle scaffold slice is committed as `be056bc` and behavior-preserving. `client/rust_ext/src/lib.rs` now has a state-only `GpuNativeShadowResources` lifecycle scaffold that derives create/reuse/replace/release decisions from `native_shadow_active`, GPU-visible terrain state, conservative shadow mode, and scene shadow radius. It does not allocate RenderingDevice RIDs, does not attach a native shadow pass, and remains disabled while `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`. Perf text now emits `native_shadow_resource_status`, radius/map fields, and lifecycle counters. Movement/parity/report scripts require env-on native-shadow fallback captures to keep `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `native_shadow_resource_status=disabled`, and zero native-shadow resource lifecycle counters. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native shadow-map rendering, draw distance, texture quality, protocol, storage, world generation, and chunk serialization are unchanged.
- Fresh native-shadow env-on movement artifact: `logs/gpu_native_shadow_resource_lifecycle_20260614/movement-stress-summary.txt` from `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=180 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=5.0 /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_resource_lifecycle_20260614`. It passed with `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `native_shadow_resource_status=disabled`, `native_shadow_resource_creates=0`, `native_shadow_resource_replaces=0`, `native_shadow_resource_releases=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=1.455`, `gpu_compositor_submit_max_ms=0.127`, and `process_wall_p95_ms=0.048`.
- Fresh parity artifact: `logs/gpu_native_shadow_resource_lifecycle_parity_20260614/parity-summary.txt` from `GODOT_TIMEOUT_SEC=210 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/gpu_terrain_parity_smoke.sh logs/gpu_native_shadow_resource_lifecycle_parity_20260614`, followed by validate-only over the same artifact. It passed with `case_count=17`; the native-shadow fallback case reports `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `native_shadow_resource_status=disabled`, and zero create/replace/release counters. The `pair=native_shadow_fallback` visual comparison against the ordinary GPU marker stayed zero-delta for luma range, terrain samples, color buckets, and chroma samples. This full parity run was used because the parity validator/schema changed; for later marker/lifecycle-only changes, prefer validate-only over this fresh compatible artifact instead of recapturing all cases.
- Aggregate report check: `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-resource-lifecycle.txt` reported max `native_shadow_resource_creates=0.000`, max `native_shadow_resource_replaces=0.000`, and max `native_shadow_resource_releases=0.000` with origins in `logs/gpu_native_shadow_resource_lifecycle_20260614/gpu-terrain-movement-stress.png.txt`. The committed default report `logs/gpu-terrain-report.txt` was refreshed on `be056bc` and now selects `logs/gpu_native_shadow_resource_lifecycle_20260614/movement-stress-summary.txt`.
- Checks passed for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `sh -n scripts/gpu_terrain_parity_smoke.sh scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow -- --nocapture`, env-on movement smoke above, full parity and validate-only above, aggregate report above, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, MCP `gn_test_gap`, staged MCP `gn_review_diff`, commit `be056bc`, and OntoIndex reindex via `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats`. The follow-up report-refresh slice regenerated `logs/gpu-terrain-report.txt` on `be056bc`; no runtime behavior changed.

Fresh 2026-06-13 status:

- Current GPU lighting marker slice is telemetry/report-only over the existing directional lighting shader. `client/rust_ext/src/gpu_terrain.rs` now returns sanitized `GpuTerrainLighting` in `GpuTerrainStats`, `client/rust_ext/src/lib.rs` emits `gpu_light_dir`, `gpu_light_color`, `gpu_light_energy`, and `gpu_light_ambient` in perf text, and `scripts/gpu_terrain_movement_stress.sh` requires/surfaces those fields in movement summaries. `scripts/gpu_terrain_report.sh` surfaces aggregate `gpu_light_energy` and `gpu_light_ambient`. This does not change protocol, storage, world generation, chunk serialization, Godot scenes, native shadow-map behavior, draw distance, texture quality, or visible quality.
- Fresh lighting marker runtime artifact: `logs/gpu_lighting_marker_summary_retry_20260613/movement-stress-summary.txt` from `GODOT_TIMEOUT_SEC=240 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/gpu_terrain_movement_stress.sh logs/gpu_lighting_marker_summary_retry_20260613`. It passed with `gpu_light_dir=-0.355/0.743/0.567`, `gpu_light_color=1.000/1.000/1.000`, `gpu_light_energy=0.450`, `gpu_light_ambient=0.550`, `terrain_queue_max_ms=1.179`, `gpu_compositor_submit_max_ms=0.130`, `process_wall_p95_ms=0.019`, `gpu_upload_fail=0`, and `terrain_samples=384`. A prior retry in `logs/gpu_lighting_marker_summary_20260613` hit a local stale server lifecycle race and captured no chunks; treat it as invalid runtime evidence except for confirming the marker fields existed.
- Checks passed for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`; `sh -n scripts/gpu_terrain_movement_stress.sh scripts/gpu_terrain_report.sh`; `cargo test --manifest-path client/rust_ext/Cargo.toml lighting -- --nocapture`; lighting marker smoke above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`); `sh scripts/gpu_terrain_report.sh logs logs/gpu-terrain-report.txt`; `./scripts/handoff.sh`. Still commit and reindex OntoIndex.

- Current startup reader/main-thread lag telemetry slice is behavior-preserving. `client/rust_ext/src/network.rs` now carries reader elapsed and packet receive timestamps in `PacketReadTiming`/`PacketReadRecord`; `client/rust_ext/src/lib.rs` records main-thread queue lag before processing the first startup chunk. The movement, bootstrap, batch, RAW-vs-RLE encoding, standalone RLE, and combined regression pack summaries now surface `startup_packet_reader_elapsed_ms` and `startup_packet_queue_lag_ms`. This does not change protocol, storage, world generation, chunk serialization, server scheduling defaults, Godot scenes, renderer behavior, default chunk encoding, default batch size, bootstrap default, startup loading policy, or visible quality.
- Runtime evidence: the broad retry in `logs/world_streaming_startup_reader_lag_20260613_retry` passed bootstrap, batch, and RAW-vs-RLE encoding but did not write a top-level regression summary because the final standalone RLE smoke exceeded the terrain queue budget by `0.099ms` (`6.766ms` vs `6.667ms`). The dedicated RLE retry `logs/world_streaming_startup_reader_lag_rle_20260613_retry/world-streaming-rle-summary.txt` passed with `394` chunks, wire percent `0.004168%`, startup packet/reader elapsed/queue lag/chunk decode/first mesh work `70.480ms / 2.891ms / 10.885ms / 0.233ms / 3.965ms`. The passing broader legs showed bootstrap radius `0` reader elapsed `3.172ms` and queue lag `13.247ms`, batch `64` reader elapsed `2.176ms` and queue lag `12.178ms`, and encoding RLE reader elapsed `1.945ms` and queue lag `15.751ms`. This points the next startup optimization toward earlier client connection/reader startup or Godot main-thread scheduling before packet processing, not server first-stream send or chunk decode.
- Checks passed for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `sh -n scripts/gpu_terrain_movement_stress.sh scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh scripts/world_streaming_regression_pack.sh`; `cargo test --manifest-path client/rust_ext/Cargo.toml perf_records_startup_milestones_once -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml receive_ -- --nocapture`; broad regression-pack retry with caveat above; standalone RLE retry above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`); `sh scripts/gpu_terrain_report.sh logs logs/gpu-terrain-report.txt`; `./scripts/handoff.sh`. Still commit and reindex OntoIndex.

- Current startup receive/decode telemetry slice is behavior-preserving. `client/rust_ext/src/network.rs` now exposes `receive_packet_with_timing()` while preserving the old `receive_packet()` API; `client/rust_ext/src/lib.rs` records one-shot startup packet arrival, packet read/decode work, chunk block decode work, chunk insertion, mesh dispatch, first mesh work, collision work, and player spawn. `scripts/gpu_terrain_movement_stress.sh` now requires the startup order `packet <= chunk_inserted <= chunk_loaded <= mesh_queued <= mesh_dispatched <= first_mesh <= collision <= player_spawn` and defaults `GODOT_QUIT_AFTER_FRAMES` to `30000` so the smoke-owned marker save is not preempted. The bootstrap, batch, RAW-vs-RLE encoding, standalone RLE, and combined regression pack summaries now surface the new packet/decode/dispatch fields. This does not change protocol, storage, world generation, chunk serialization, server scheduling defaults, Godot scenes, renderer behavior, default chunk encoding, default batch size, bootstrap default, startup loading policy, or visible quality.
- Fresh startup receive/decode runtime artifact: `logs/world_streaming_startup_receive_breakdown_20260613/world-streaming-regression-summary.txt` from `GODOT_TIMEOUT_SEC=360 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/world_streaming_regression_pack.sh logs/world_streaming_startup_receive_breakdown_20260613`. It passed with bootstrap candidate first stream `1` chunk and startup packet/chunk decode/mesh dispatch/first mesh/work `89.553ms / 0.403ms / 89.553ms / 89.553ms / 5.876ms`, batch `64` streaming `394` chunks over `9` batches with startup `84.343ms / 0.397ms / 84.343ms / 84.343ms / 5.201ms`, encoding RLE `394` chunks with wire percent `0.004168%` and startup `71.415ms / 0.441ms / 71.415ms / 71.415ms / 6.327ms`, and standalone RLE `394` chunks with wire percent `0.004168%` and startup `91.099ms / 0.499ms / 91.099ms / 91.099ms / 5.790ms`.
- Checks passed for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `sh -n scripts/gpu_terrain_movement_stress.sh scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh scripts/world_streaming_regression_pack.sh`; `cargo test --manifest-path client/rust_ext/Cargo.toml perf_records_startup_milestones_once -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml receive_ -- --nocapture`; runtime regression pack command above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`); `sh scripts/gpu_terrain_report.sh logs logs/gpu-terrain-report.txt`; `./scripts/handoff.sh`. Still commit and reindex OntoIndex.

- Current startup first-mesh work telemetry slice is behavior-preserving. `client/rust_ext/src/lib.rs` now records first startup mesh job work duration, phase split, and collision work (`startup_first_mesh_work_ms`, `startup_first_mesh_phase_ms`, `startup_first_mesh_collision_work_ms`) separately from the wall-clock startup readiness timestamp. `scripts/gpu_terrain_movement_stress.sh` requires the work marker and forwards it into `movement_startup`; the world-streaming bootstrap, batch, RAW-vs-RLE encoding, standalone RLE, and combined regression pack summaries now surface the numeric first-mesh work fields. This does not change protocol, storage, world generation, chunk serialization, server scheduling defaults, Godot scenes, renderer behavior, default chunk encoding, default batch size, bootstrap default, startup loading policy, or visible quality.
- Fresh startup first-mesh work runtime artifact: `logs/world_streaming_startup_work_breakdown_20260613_frames_retry/world-streaming-regression-summary.txt` from `GODOT_TIMEOUT_SEC=360 GODOT_QUIT_AFTER_FRAMES=30000 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/world_streaming_regression_pack.sh logs/world_streaming_startup_work_breakdown_20260613_frames_retry`. It passed with bootstrap candidate first stream `1` chunk and startup first mesh/work/collision work `63.932ms / 4.308ms / 3.530ms`, batch `64` streaming `394` chunks over `9` batches with startup first mesh/work/collision work `88.853ms / 4.143ms / 3.418ms`, encoding RLE `394` chunks with wire percent `0.004168%` and startup first mesh/work/collision work `78.148ms / 4.729ms / 3.940ms`, and standalone RLE `394` chunks with wire percent `0.004168%` and startup first mesh/work/collision work `78.337ms / 4.223ms / 3.527ms`. Earlier attempts in `logs/world_streaming_startup_work_breakdown_20260613` and `logs/world_streaming_startup_work_breakdown_20260613_retry` reached motion/capture but exited before PNG marker save; the passing run raised `GODOT_QUIT_AFTER_FRAMES` to avoid early Godot exit.
- Checks passed for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `sh -n scripts/gpu_terrain_movement_stress.sh scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh scripts/world_streaming_regression_pack.sh scripts/gpu_terrain_report.sh scripts/handoff.sh`; `cargo test --manifest-path client/rust_ext/Cargo.toml perf_records_startup_milestones_once -- --nocapture`; runtime regression pack command above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; `sh scripts/gpu_terrain_report.sh logs logs/gpu-terrain-report.txt`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay/docs-sidecar warnings only); MCP `gn_test_gap` (`PASS`). Re-run MCP `gn_verify_diff` with executed tests, then commit and reindex OntoIndex.

- Previous world startup phase telemetry slice is behavior-preserving. `client/rust_ext/src/lib.rs` records one-shot `startup_mesh_queued_ms` and `startup_first_mesh_ms` between the existing chunk-loaded, collision-ready, and player-spawn startup timings. `scripts/gpu_terrain_movement_stress.sh` requires the nondecreasing startup order `chunk_loaded <= mesh_queued <= first_mesh <= collision <= player_spawn`, and the world-streaming bootstrap, batch, RAW-vs-RLE encoding, standalone RLE, and combined regression pack summaries surface the added mesh phase fields. This does not change protocol, storage, world generation, chunk serialization, server scheduling defaults, Godot scenes, renderer behavior, default chunk encoding, default batch size, bootstrap default, or visible quality.
- Fresh startup phase runtime artifact: `logs/world_streaming_startup_phase_regression_20260613_retry/world-streaming-regression-summary.txt` from `GODOT_TIMEOUT_SEC=240 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/world_streaming_regression_pack.sh logs/world_streaming_startup_phase_regression_20260613_retry`. It passed with bootstrap candidate first stream `1` chunk and startup first mesh/player spawn `94.706ms / 94.706ms`, batch `64` streaming `394` chunks over `9` batches with startup first mesh/player spawn `87.334ms / 87.334ms`, encoding RLE `394` chunks with wire percent `0.004168%` and startup first mesh/player spawn `93.806ms / 93.806ms`, and standalone RLE `394` chunks with wire percent `0.004168%` and startup first mesh/player spawn `90.329ms / 90.329ms`. The first attempt in `logs/world_streaming_startup_phase_regression_20260613` hit the known screenshot-capture timeout after reaching chunk `3,2`; the longer-timeout retry passed.

- Current world-loading regression pack slice is harness-only and behavior-preserving. `scripts/world_streaming_regression_pack.sh` builds and ad-hoc signs `server/server` once, runs the existing bootstrap, batch-size, RAW-vs-RLE encoding, and standalone RLE movement gates with nested server rebuilds disabled, and writes one top-level `world-streaming-regression-summary.txt`. Fresh release artifact: `logs/world_streaming_regression_pack_20260613/world-streaming-regression-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/world_streaming_regression_pack.sh logs/world_streaming_regression_pack_20260613`. It passed with bootstrap candidate first stream `1` chunk and startup player spawn `93.318ms`, batch `64` streaming `394` chunks over `9` batches with startup player spawn `83.367ms`, encoding RLE `394` chunks with wire percent `0.004168%` and startup player spawn `95.459ms`, and standalone RLE `394` chunks with wire percent `0.004168%` and startup player spawn `87.231ms`. This does not change protocol, storage, world generation, chunk serialization, server scheduling defaults, Godot scenes, renderer behavior, Rust client behavior, default chunk encoding, default batch size, bootstrap default, or visible quality.
- Checks passed for the world-loading regression pack slice: `sh -n` for world-streaming shell wrappers including `scripts/world_streaming_regression_pack.sh`; runtime regression pack command above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current aggregate GPU report refresh slice is report-only and behavior-preserving. `logs/gpu-terrain-report.txt` was regenerated with `sh scripts/gpu_terrain_report.sh logs logs/gpu-terrain-report.txt`, and should be regenerated after the final docs commit so the default handoff snapshot does not treat the full-log report as stale. The report selected the current RLE world-streaming movement summary from `logs/world_streaming_encoding_startup_timing_20260613/rle/movement-stress-summary.txt`, including startup chunk/collision/player spawn `84.197ms / 84.197ms / 84.197ms`, and still records aggregate `gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=13344`, max draw command occupancy `30.298%`, and transparent fixture overlay metadata `5/5`. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, renderer behavior, Rust client behavior, default chunk encoding, default batch size, bootstrap default, or visible quality.
- Checks passed for the aggregate GPU report refresh slice: report generation command above; `sh -n scripts/gpu_terrain_report.sh`; `sh -n scripts/handoff.sh`; `./scripts/handoff.sh` freshness check against the regenerated report; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming encoding startup timing summary slice is behavior-preserving. `scripts/world_streaming_rle_movement_smoke.sh` and `scripts/world_streaming_chunk_encoding_compare.sh` now surface startup chunk-loaded, collision-ready, and player-spawn timings in RAW/RLE per-run summaries and the top-level encoding comparison artifact. This makes encoding comparisons include time-to-ready evidence without changing protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, Rust client behavior, or visible quality.
- Fresh encoding startup timing runtime artifact: `logs/world_streaming_encoding_startup_timing_20260613/world-streaming-encoding-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=0 /bin/sh scripts/world_streaming_chunk_encoding_compare.sh logs/world_streaming_encoding_startup_timing_20260613`. It passed with RAW/RLE `394` chunks each, raw bytes `413,138,944` each, RAW payload/wire `413,138,944 / 413,148,011`, RLE payload/wire `7,362 / 17,219`, RAW startup chunk/collision/player spawn `91.070ms / 91.070ms / 91.070ms`, and RLE startup chunk/collision/player spawn `84.197ms / 84.197ms / 84.197ms`.
- Checks passed for the encoding startup timing summary slice: `sh -n scripts/world_streaming_chunk_encoding_compare.sh`; `sh -n scripts/world_streaming_rle_movement_smoke.sh`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming batch startup timing summary slice is behavior-preserving. `scripts/world_streaming_batch_compare.sh` now surfaces startup chunk-loaded, collision-ready, and player-spawn timings in each per-batch summary and in the top-level batch comparison artifact. This makes batch-size comparisons include time-to-ready evidence without changing protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, Rust client behavior, or visible quality.
- Fresh batch startup timing runtime artifact: `logs/world_streaming_batch_startup_timing_20260613/world-streaming-batch-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=0 /bin/sh scripts/world_streaming_batch_compare.sh logs/world_streaming_batch_startup_timing_20260613`. It passed with rollback batch `6` streaming `133` chunks over `23` batches and default/candidate batch `64` streaming `394` chunks over `9` batches. Batch `6` had startup chunk/collision/player spawn `97.389ms / 97.389ms / 97.389ms`, `terrain_queue_max_ms=2.225`, `process_wall_p95_ms=0.050`, `gpu_compositor_submit_max_ms=0.225`; batch `64` had startup chunk/collision/player spawn `76.650ms / 76.650ms / 76.650ms`, `terrain_queue_max_ms=2.052`, `process_wall_p95_ms=0.054`, `gpu_compositor_submit_max_ms=0.140`.
- Checks passed for the batch startup timing summary slice: `sh -n scripts/world_streaming_batch_compare.sh`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming shell wrapper slice is behavior-preserving. `scripts/world_streaming_batch_compare.sh`, `scripts/world_streaming_chunk_encoding_compare.sh`, and `scripts/world_streaming_rle_movement_smoke.sh` now invoke nested project shell scripts through `/bin/sh`, matching `scripts/world_streaming_bootstrap_compare.sh` and avoiding the local macOS `/usr/bin/env` dyld-start hang observed during redirected harness runs. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, Rust client behavior, or visible quality.
- Fresh shell wrapper runtime artifact: `logs/world_streaming_shell_wrapper_compare_20260613/world-streaming-encoding-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=0 /bin/sh scripts/world_streaming_chunk_encoding_compare.sh logs/world_streaming_shell_wrapper_compare_20260613`. It passed with RAW/RLE `394` chunks each, raw bytes `413,138,944` each, RAW payload/wire `413,138,944 / 413,148,011`, RLE payload/wire `7,362 / 17,219`, RAW payload/wire percent `100.000000 / 100.002195`, and RLE payload/wire percent `0.001782 / 0.004168`.
- Checks passed for the shell wrapper slice: `for f in scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh scripts/world_streaming_bootstrap_compare.sh scripts/gpu_terrain_movement_stress.sh; do sh -n "$f" || exit 1; done`; direct executable nested world-streaming script call scan; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming startup timing order gate slice is behavior-preserving and supports the radius `0` default. `scripts/gpu_terrain_movement_stress.sh` now requires startup timings to be ordered as `startup_chunk_loaded_ms <= startup_collision_ms <= startup_player_spawn_ms`, so the movement/bootstrap gates catch misplaced instrumentation or early player-spawn regressions. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, Rust client behavior, or visible quality.
- Fresh startup timing order gate runtime artifact: `logs/world_streaming_startup_timing_order_gate_20260613/world-streaming-bootstrap-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=0 /bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_startup_timing_order_gate_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had startup chunk/collision/player spawn `103.887ms / 103.887ms / 103.887ms`, `8` batches, `terrain_queue_max_ms=2.091`, `process_wall_p95_ms=0.050`, `gpu_compositor_submit_max_ms=0.220`; default bootstrap radius `0` had startup chunk/collision/player spawn `74.247ms / 74.247ms / 74.247ms`, `9` batches, `terrain_queue_max_ms=2.175`, `process_wall_p95_ms=0.053`, `gpu_compositor_submit_max_ms=0.115`.
- Checks passed for the startup timing order gate slice: `sh -n scripts/gpu_terrain_movement_stress.sh`; `sh -n scripts/world_streaming_bootstrap_compare.sh`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming startup metric gate slice is behavior-preserving and supports the radius `0` default. `scripts/gpu_terrain_movement_stress.sh` now fails if `startup_chunk_loaded_ms`, `startup_collision_ms`, or `startup_player_spawn_ms` is missing or non-positive, and `scripts/world_streaming_bootstrap_compare.sh` enforces the same requirement before writing per-run summaries. The final bootstrap compare summary now includes base/candidate startup chunk-loaded, collision-ready, and player-spawn timings. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, Rust client behavior, or visible quality.
- Fresh startup metric gate runtime artifact: `logs/world_streaming_startup_metric_gate_20260613/world-streaming-bootstrap-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=0 /bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_startup_metric_gate_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had startup chunk/collision/player spawn `120.459ms / 120.459ms / 120.459ms`, `8` batches, `terrain_queue_max_ms=2.151`, `process_wall_p95_ms=0.052`, `gpu_compositor_submit_max_ms=0.208`; default bootstrap radius `0` had startup chunk/collision/player spawn `78.157ms / 78.157ms / 78.157ms`, `9` batches, `terrain_queue_max_ms=4.510`, `process_wall_p95_ms=0.053`, `gpu_compositor_submit_max_ms=0.132`.
- Checks passed for the startup metric gate slice: `sh -n scripts/gpu_terrain_movement_stress.sh`; `sh -n scripts/world_streaming_bootstrap_compare.sh`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS` after rerun with executed tests); MCP `gn_test_gap` (`PASS`).

- Current world streaming startup timing telemetry slice is behavior-preserving and supports the radius `0` default. `client/rust_ext/src/lib.rs` now records one-shot `startup_chunk_loaded_ms`, `startup_collision_ms`, and `startup_player_spawn_ms` from client runtime time and exposes them through `get_perf_text`. `scripts/gpu_terrain_movement_stress.sh` surfaces those fields in `movement_startup`, and `scripts/world_streaming_bootstrap_compare.sh` includes startup timing in the per-run and compare summaries. The bootstrap compare wrapper now invokes movement stress via `/bin/sh` instead of executable shebang to avoid a local macOS `/usr/bin/env` dyld-start hang observed when stdout/stderr were redirected. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, or visible quality.
- Fresh startup timing runtime artifact: `logs/world_streaming_startup_timing_default0_20260613/world-streaming-bootstrap-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=0 /bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_startup_timing_default0_20260613` after an explicit `cargo build --manifest-path client/rust_ext/Cargo.toml --release`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had startup player spawn `105.392ms`, `8` batches, `terrain_queue_max_ms=2.139`, `process_wall_p95_ms=0.050`, `gpu_compositor_submit_max_ms=0.140`; default bootstrap radius `0` had startup player spawn `70.746ms`, `9` batches, `terrain_queue_max_ms=2.151`, `process_wall_p95_ms=0.051`, `gpu_compositor_submit_max_ms=0.159`.
- Checks passed for the startup timing telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `sh -n scripts/gpu_terrain_movement_stress.sh`; `sh -n scripts/world_streaming_bootstrap_compare.sh scripts/gpu_terrain_movement_stress.sh`; `cargo test --manifest-path client/rust_ext/Cargo.toml perf_records_startup_milestones_once -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml initial_player_startup_contract_targets_current_chunk -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml`; `cargo build --manifest-path client/rust_ext/Cargo.toml --release`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming pre-spawn queue hint slice is behavior-preserving and supports the radius `0` default. `client/rust_ext/src/lib.rs` now uses the startup chunk contract as the mesh/collision queue hint while `current_player_chunk` is still unavailable, and the no-player collision/shadow fallback now targets `initial_player_chunk()` instead of a hardcoded origin literal. It intentionally does not set `current_player_chunk` before `Player` exists, so the first real player-position refresh path remains available. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, bootstrap default, or visible quality.
- Fresh pre-spawn queue hint runtime artifact: `logs/world_streaming_startup_queue_hint_default0_20260613/world-streaming-bootstrap-compare-summary.txt` from `/bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_startup_queue_hint_default0_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=0.915`, `process_wall_p95_ms=0.019`, `gpu_compositor_submit_max_ms=0.124`; default bootstrap radius `0` had `9` batches, `terrain_queue_max_ms=1.487`, `process_wall_p95_ms=0.022`, `gpu_compositor_submit_max_ms=0.158`.
- Checks passed for the pre-spawn queue hint slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `cargo test --manifest-path client/rust_ext/Cargo.toml initial_player_startup_contract_targets_current_chunk -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml mesh_queue_uses_initial_chunk_hint_before_player_chunk_updates -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming initial-player contract slice is behavior-preserving and supports the radius `0` default. `client/rust_ext/src/lib.rs` now derives the initial `ClientPosition` packet, spawned player position, initial player chunk, and the two pre-spawn mesh subchunks from one startup contract instead of separate `(16,68,16)` and `(0,0)` literals. A new Rust unit test locks that the initial position maps to chunk `(0,0)` and that the pre-spawn subchunks need collision before `current_player_chunk` is available. This does not change protocol, storage, world generation, chunk serialization, server scheduling, Godot scenes, default chunk encoding, default batch size, or visible quality.
- Fresh initial contract runtime artifact: `logs/world_streaming_initial_contract_default0_20260613/world-streaming-bootstrap-compare-summary.txt` from `/bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_initial_contract_default0_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=1.008`, `process_wall_p95_ms=0.022`, `gpu_compositor_submit_max_ms=0.339`; default bootstrap radius `0` had `9` batches, `terrain_queue_max_ms=1.340`, `process_wall_p95_ms=0.020`, `gpu_compositor_submit_max_ms=0.195`.
- Checks passed for the initial-player contract slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `cargo test --manifest-path client/rust_ext/Cargo.toml initial_player_startup_contract_targets_current_chunk -- --nocapture`; `cargo test --manifest-path client/rust_ext/Cargo.toml`; runtime comparison gate above; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; `git diff --check`; added-line ASCII scan; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming bootstrap-default-0 slice changes `server/pkg/network` default `RUMPELMC_SERVER_BOOTSTRAP_RADIUS` behavior from `1` to `0`; `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=1` restores the previous default startup stream, `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=2` restores the earlier wider startup stream, and `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=full` restores full view-distance startup streaming. Invalid bootstrap env values now fall back to `0` clamped to view distance. `scripts/world_streaming_bootstrap_compare.sh` now uses candidate radius `0` by default. This changes only server startup stream scheduling and the comparison gate default; protocol, storage, world generation, chunk serialization, Godot scenes, Rust decode behavior, default chunk encoding, and default batch size are unchanged.
- Fresh post-default radius `0` comparison artifact: `logs/world_streaming_bootstrap_default0_compare_20260613/world-streaming-bootstrap-compare-summary.txt` from `/bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_bootstrap_default0_compare_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=1.989`, `process_wall_p95_ms=0.033`, `gpu_compositor_submit_max_ms=0.224`; default bootstrap radius `0` had `9` batches, `terrain_queue_max_ms=1.728`, `process_wall_p95_ms=0.044`, `gpu_compositor_submit_max_ms=0.102`.
- Checks passed for the bootstrap-default-0 slice: `go test ./pkg/network -run 'TestConfiguredBootstrapRadius|TestSendChunksAroundWithRadiusLimitsBootstrapArea|TestHandleInitialClientPacketUsesBootstrapRadius|TestConfiguredChunksPerUpdate' -v`; `sh -n scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh`; post-default radius `0` comparison gate above; `go test ./pkg/network ./pkg/world`; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming bootstrap-radius-0 evidence slice keeps the server default at `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=1`, but verifies radius `0` as the next startup-stream candidate. Radius `0` sends only the current chunk in the first post-connect stream before normal full view-distance updates continue. This changes docs/evidence only; protocol, storage, world generation, chunk serialization, Godot scenes, Rust decode behavior, default chunk encoding, default batch size, and the current bootstrap default are unchanged.
- Fresh bootstrap radius `0` comparison artifact: `logs/world_streaming_bootstrap_radius0_compare_20260613/world-streaming-bootstrap-compare-summary.txt` from `RUMPELMC_WORLD_STREAMING_BOOTSTRAP_COMPARE_CANDIDATE=0 /bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_bootstrap_radius0_compare_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and candidate bootstrap first stream `radius=0`, `1` chunk. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=2.367`, `process_wall_p95_ms=0.050`, `gpu_compositor_submit_max_ms=0.128`; candidate bootstrap radius `0` had `9` batches, `terrain_queue_max_ms=2.107`, `process_wall_p95_ms=0.053`, `gpu_compositor_submit_max_ms=0.155`.
- Checks passed for the bootstrap-radius-0 evidence slice: comparison gate above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming bootstrap-default-1 slice changes `server/pkg/network` default `RUMPELMC_SERVER_BOOTSTRAP_RADIUS` behavior from `2` to `1`; `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=2` restores the previous default startup stream, and `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=full` restores full view-distance startup streaming. Invalid bootstrap env values now fall back to `1` clamped to view distance. `scripts/world_streaming_bootstrap_compare.sh` now uses candidate radius `1` by default. This changes only server startup stream scheduling and the comparison gate default; protocol, storage, world generation, chunk serialization, Godot scenes, Rust decode behavior, default chunk encoding, and default batch size are unchanged.
- Fresh post-default radius `1` comparison artifact: `logs/world_streaming_bootstrap_default1_compare_20260613/world-streaming-bootstrap-compare-summary.txt` from `/bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_bootstrap_default1_compare_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=1`, `5` chunks. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=1.276`, `process_wall_p95_ms=0.036`, `gpu_compositor_submit_max_ms=0.162`; default bootstrap radius `1` had `9` batches, `terrain_queue_max_ms=1.508`, `process_wall_p95_ms=0.039`, `gpu_compositor_submit_max_ms=0.129`.
- Checks passed for the bootstrap-default-1 slice: `go test ./pkg/network -run 'TestConfiguredBootstrapRadius|TestSendChunksAroundWithRadiusLimitsBootstrapArea|TestHandleInitialClientPacketUsesBootstrapRadius|TestConfiguredChunksPerUpdate' -v`; `sh -n scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh`; post-default radius `1` comparison gate above; `go test ./pkg/network ./pkg/world`; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS` after rerun without manual symbol override).

- Current world streaming bootstrap-radius-1 evidence slice keeps the server default at `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=2`, but verifies radius `1` as the next startup-stream candidate. This changes docs/evidence only; protocol, storage, world generation, chunk serialization, Godot scenes, Rust decode behavior, default chunk encoding, default batch size, and the current bootstrap default are unchanged.
- Fresh bootstrap radius `1` comparison artifact: `logs/world_streaming_bootstrap_radius1_compare_20260613/world-streaming-bootstrap-compare-summary.txt` from `RUMPELMC_WORLD_STREAMING_BOOTSTRAP_COMPARE_CANDIDATE=1 /bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_bootstrap_radius1_compare_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and candidate bootstrap first stream `radius=1`, `5` chunks. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=1.995`, `process_wall_p95_ms=0.039`, `gpu_compositor_submit_max_ms=0.251`; candidate bootstrap radius `1` had `9` batches, `terrain_queue_max_ms=1.758`, `process_wall_p95_ms=0.045`, `gpu_compositor_submit_max_ms=0.109`.
- Checks passed for the bootstrap-radius-1 evidence slice: comparison gate above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming bootstrap-default slice makes `RUMPELMC_SERVER_BOOTSTRAP_RADIUS` default to `2`; `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=full` restores the previous full view-distance startup stream. Invalid bootstrap env values now fall back to `2` clamped to view distance. This changes only server startup stream scheduling; protocol, storage, world generation, chunk serialization, Godot scenes, Rust decode behavior, default chunk encoding, and default batch size are unchanged.
- Fresh post-default bootstrap comparison artifact: `logs/world_streaming_bootstrap_default2_compare_20260613/world-streaming-bootstrap-compare-summary.txt` from `/bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_bootstrap_default2_compare_20260613`. It passed with rollback full startup first stream `radius=10`, `64` chunks and default/candidate bootstrap first stream `radius=2`, `13` chunks. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Rollback full startup had `8` batches, `terrain_queue_max_ms=1.436`, `process_wall_p95_ms=0.038`, `gpu_compositor_submit_max_ms=0.291`; default bootstrap radius `2` had `9` batches, `terrain_queue_max_ms=1.426`, `process_wall_p95_ms=0.037`, `gpu_compositor_submit_max_ms=0.139`.
- Checks passed for the bootstrap-default slice: `go test ./pkg/network -run 'TestConfiguredBootstrapRadius|TestSendChunksAroundWithRadiusLimitsBootstrapArea|TestHandleInitialClientPacketUsesBootstrapRadius|TestConfiguredChunksPerUpdate' -v`; `go test ./pkg/network ./pkg/world`; `sh -n scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh`; post-default bootstrap comparison gate above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming bootstrap-compare slice adds `scripts/world_streaming_bootstrap_compare.sh`, a reproducible full-startup-vs-bootstrap-radius movement gate. It rebuilds and ad-hoc signs `server/server`, refuses occupied port `25565`, runs movement stress first with full startup and then with candidate `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=2`, validates movement/collision/ground/upload markers, requires the candidate first stream to be smaller, summarizes stream metrics and client perf markers, and cleans up local servers. It does not change default server startup behavior.
- Fresh bootstrap comparison artifact: `logs/world_streaming_bootstrap_compare_20260613/world-streaming-bootstrap-compare-summary.txt` from `/bin/sh scripts/world_streaming_bootstrap_compare.sh logs/world_streaming_bootstrap_compare_20260613`. It passed with full startup first stream `radius=10`, `64` chunks and candidate bootstrap first stream `radius=2`, `13` chunks. Both runs streamed `394` chunks total with `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`. Full startup had `8` batches, `terrain_queue_max_ms=1.183`, `process_wall_p95_ms=0.037`, `gpu_compositor_submit_max_ms=0.126`; bootstrap radius `2` had `9` batches, `terrain_queue_max_ms=1.577`, `process_wall_p95_ms=0.034`, `gpu_compositor_submit_max_ms=0.105`.
- Checks passed for the bootstrap-compare slice: `sh -n scripts/world_streaming_bootstrap_compare.sh scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh`; bootstrap comparison gate above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming bootstrap-radius slice adds opt-in `RUMPELMC_SERVER_BOOTSTRAP_RADIUS` startup scheduling in `server/pkg/network`. Unset preserves the current full view-distance startup stream; setting `0..viewDistance` limits only the first post-connect stream around the initial client position before normal full-radius position updates continue. This does not change protocol, storage, world generation, chunk serialization, Godot scenes, Rust decode behavior, default chunk encoding, or default batch size.
- Fresh opt-in bootstrap artifact: `logs/world_streaming_bootstrap_radius2_20260613/world-streaming-bootstrap-radius-summary.txt` from `RUMPELMC_SERVER_BOOTSTRAP_RADIUS=2 RUMPELMC_SERVER_CHUNK_STREAM_METRICS=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=140 GODOT_QUIT_AFTER_FRAMES=9000 scripts/gpu_terrain_movement_stress.sh logs/world_streaming_bootstrap_radius2_20260613`. It passed with first stream `radius=2`, `13` chunks, raw/payload/wire bytes `13,631,488 / 413 / 701`, elapsed `39.793ms`, then normal `radius=10` updates completed `394` chunks over `9` batches with full-run raw/payload/wire bytes `413,138,944 / 7,362 / 17,219`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, `gpu_upload_fail=0`, and `chunk_initial=394`.
- Checks passed for the bootstrap-radius slice: `go test ./pkg/network -run 'TestConfiguredBootstrapRadius|TestSendChunksAroundWithRadiusLimitsBootstrapArea|TestHandleInitialClientPacketUsesBootstrapRadius|TestConfiguredChunksPerUpdate' -v`; opt-in bootstrap movement smoke above; `go test ./pkg/network ./pkg/world`; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming batch-default slice changes `server/pkg/network` default `RUMPELMC_SERVER_CHUNKS_PER_UPDATE` behavior from `6` to `64`; `RUMPELMC_SERVER_CHUNKS_PER_UPDATE=6` remains the explicit conservative rollback path. This changes only server batch sizing; protocol, storage, world generation, chunk serialization, Godot scenes, and Rust decode behavior are unchanged.
- Fresh default batch `64` artifact: `logs/world_streaming_default_batch64_20260613/world-streaming-default-batch64-summary.txt` from `env -u RUMPELMC_SERVER_CHUNKS_PER_UPDATE -u RUMPELMC_SERVER_CHUNK_ENCODING RUMPELMC_SERVER_CHUNK_STREAM_METRICS=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=140 GODOT_QUIT_AFTER_FRAMES=9000 scripts/gpu_terrain_movement_stress.sh logs/world_streaming_default_batch64_20260613`. It passed with `8` batches, `394` chunks, raw bytes `413,138,944`, payload bytes `7,362`, wire bytes `17,219`, payload percent `0.001782`, wire percent `0.004168`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, `gpu_upload_fail=0`, and server log `batch=64`.
- Fresh rollback-vs-default comparison after changing the default: `logs/world_streaming_batch_default64_compare_20260613/world-streaming-batch-compare-summary.txt` from `/bin/sh scripts/world_streaming_batch_compare.sh logs/world_streaming_batch_default64_compare_20260613`. It passed with rollback batch `6` streaming `132` chunks over `22` batches and default/candidate batch `64` streaming `394` chunks over `8` batches. Batch `64` passed with `terrain_queue_max_ms=1.903`, `process_wall_p95_ms=0.038`, `gpu_compositor_submit_max_ms=0.159`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`.
- Checks passed for the batch-default slice: `go test ./pkg/network -run 'TestConfiguredChunksPerUpdate|TestRLEChunkBatchShrinksPayloadAndWireBytes|TestSendChunkCanUseRLEPayload' -v`; default-unset batch `64` movement smoke above; post-default rollback-vs-default batch comparison above; `go test ./pkg/network ./pkg/world`; `sh -n scripts/world_streaming_batch_compare.sh scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh`; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming batch-evidence slice adds `scripts/world_streaming_batch_compare.sh`. It rebuilds and ad-hoc signs `server/server`, refuses occupied port `25565`, runs movement stress twice with default RLE encoding and `RUMPELMC_SERVER_CHUNKS_PER_UPDATE` set to base `6` then candidate `64`, validates movement/collision/ground/upload markers, summarizes stream metrics and client perf markers, and cleans up local servers. It does not change the default server batch size.
- Fresh batch comparison artifact: `logs/world_streaming_batch_compare_20260613_retry/world-streaming-batch-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release /bin/sh scripts/world_streaming_batch_compare.sh logs/world_streaming_batch_compare_20260613_retry`. It passed with batch `6` streaming `132` chunks over `22` batches and batch `64` streaming `394` chunks over `8` batches. Batch `64` kept RLE payload/wire at `0.001782 / 0.004168` percent of raw and passed with `terrain_queue_max_ms=1.560`, `process_wall_p95_ms=0.036`, `gpu_compositor_submit_max_ms=0.109`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and `gpu_upload_fail=0`.
- Checks passed for the batch-evidence slice: `sh -n scripts/world_streaming_batch_compare.sh`; `go test ./pkg/network ./pkg/world`; batch comparison gate above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`.

- Current world streaming default-on slice makes RLE the server chunk encoding when `RUMPELMC_SERVER_CHUNK_ENCODING` is unset or set to `rle`; `RUMPELMC_SERVER_CHUNK_ENCODING=raw` remains the explicit full-chunk rollback path. Invalid encoding env values now log and fall back to RLE. This changes only server chunk payload selection and docs/tests; protocol fields, storage serialization, world generation, Godot scenes, and Rust decode behavior are unchanged.
- The same slice also hardens visual/world-streaming harnesses: `client/main.gd` now waits for the locally spawned Go server to listen before creating `GameClient`, and the world-streaming wrapper scripts ad-hoc sign freshly built `server/server` with `codesign -s - --force` when available. This avoids local macOS dyld/linker-signed startup hangs after `go build`.
- Fresh default-on artifact: `logs/world_streaming_default_rle_20260613/world-streaming-default-rle-summary.txt` from `env -u RUMPELMC_SERVER_CHUNK_ENCODING RUMPELMC_SERVER_CHUNK_STREAM_METRICS=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=140 GODOT_QUIT_AFTER_FRAMES=9000 scripts/gpu_terrain_movement_stress.sh logs/world_streaming_default_rle_20260613`. It passed with `22` batches, `132` chunks, raw bytes `138,412,032`, payload bytes `2,646`, wire bytes `5,640`, payload percent `0.001912`, and wire percent `0.004075`.
- Fresh RAW-vs-RLE comparison artifact after the default-on slice: `logs/world_streaming_encoding_compare_default_final2_20260613/world-streaming-encoding-compare-summary.txt` from `/bin/sh scripts/world_streaming_chunk_encoding_compare.sh logs/world_streaming_encoding_compare_default_final2_20260613` with release Rust extension env. It passed with RAW/RLE `138` chunks each, raw bytes `144,703,488` each, RAW payload/wire `144,703,488 / 144,706,330`, RLE payload/wire `2,754 / 5,874`, RAW payload/wire percent `100.000000 / 100.001964`, and RLE payload/wire percent `0.001903 / 0.004059`.
- Checks passed for the default-on slice: `go test ./pkg/network -run 'TestConfiguredChunkEncoding|TestRLEChunkBatchShrinksPayloadAndWireBytes|TestSendChunkCanUseRLEPayload' -v`; `go test ./pkg/api ./pkg/network ./pkg/world`; `cargo test --manifest-path client/rust_ext/Cargo.toml chunk -- --nocapture`; `sh -n scripts/world_streaming_chunk_encoding_compare.sh scripts/world_streaming_rle_movement_smoke.sh`; default-on movement smoke; RAW-vs-RLE comparison gate; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty-worktree overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming comparison slice adds `scripts/world_streaming_chunk_encoding_compare.sh`, a reproducible RAW-vs-RLE movement gate. It rebuilds `server/server`, refuses to run if port `25565` is occupied, runs the same movement stress first with `RUMPELMC_SERVER_CHUNK_ENCODING=raw` and then through the RLE wrapper, requires both runs to stream the same chunk count and raw byte total, compares payload/wire totals, writes `world-streaming-encoding-compare-summary.txt`, and cleans up the local server.
- Fresh comparison artifact: `logs/world_streaming_encoding_compare_20260613/world-streaming-encoding-compare-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=140 GODOT_QUIT_AFTER_FRAMES=9000 scripts/world_streaming_chunk_encoding_compare.sh logs/world_streaming_encoding_compare_20260613`. It passed with `132` chunks, raw bytes `138,412,032`, RAW payload/wire `138,412,032 / 138,414,760`, RLE payload/wire `2,646 / 5,640`, and RLE payload/wire percent of RAW `0.001912 / 0.004075`.
- This comparison slice does not make RLE default-on and does not change protocol, storage, world generation, Godot scenes, or Rust runtime behavior.
- Checks passed for this comparison slice: `sh -n scripts/world_streaming_chunk_encoding_compare.sh`; `sh -n scripts/world_streaming_rle_movement_smoke.sh`; comparison smoke command above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty-worktree overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming wrapper slice adds `scripts/world_streaming_rle_movement_smoke.sh`, a reproducible RLE movement smoke gate. It rebuilds `server/server`, refuses to run if port `25565` is already occupied, launches the existing movement stress with `RUMPELMC_SERVER_CHUNK_ENCODING=rle` and `RUMPELMC_SERVER_CHUNK_STREAM_METRICS=1`, validates the movement marker, validates that the Rust client decoded streamed chunks to `blocks=1048576`, requires RLE payload and framed wire bytes to stay below `1%` of raw bytes, writes `world-streaming-rle-summary.txt`, and cleans up the local server.
- Fresh wrapper artifact: `logs/world_streaming_rle_wrapper_20260613/world-streaming-rle-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=140 GODOT_QUIT_AFTER_FRAMES=9000 scripts/world_streaming_rle_movement_smoke.sh logs/world_streaming_rle_wrapper_20260613`. It passed with `22` batches, `132` chunks, raw bytes `138,412,032`, payload bytes `2,646`, wire bytes `5,640`, payload percent `0.001912`, and wire percent `0.004075`.
- This wrapper slice does not make RLE default-on and does not change protocol, storage, world generation, Godot scenes, or Rust runtime behavior.
- Checks passed for this wrapper slice: `sh -n scripts/world_streaming_rle_movement_smoke.sh`; wrapper smoke command above; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty-worktree overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).

- Current world streaming handshake slice fixes the invalid early probe behavior that previously produced `Failed to send initial chunks: ... broken pipe` before visual evidence could be trusted. `client/rust_ext/src/lib.rs` now sends an initial `ClientPosition` packet immediately after connecting. `server/pkg/network` waits briefly for that first packet before starting the initial stream, treats closed probes as closed probes, and keeps the old `(0,0)` initial stream fallback when no first packet arrives before the timeout.
- New framing tests cover the handshake path: `TestReceiveInitialClientPacketIgnoresClosedProbe` and `TestReceiveInitialClientPacketReadsHandshakePosition`. The existing RLE guard still reports RAW payload `3,145,728`, RLE payload `54`, RAW wire `3,145,786`, and RLE wire `118` for three generated flat chunks.
- Valid visual movement evidence now exists for both default RAW and opt-in RLE using direct Godot release-profile launches. RAW marker/log: `logs/world_streaming_raw_visual_20260613/movement.godot.log`. RLE marker: `logs/world_streaming_rle_visual_20260613/movement.godot.log`. RLE server metrics: `logs/world_streaming_rle_visual_20260613/godot.log`.
- Both RAW and RLE movement runs passed `smoke_err=0`, `motion_steps=4`, `motion_chunks=4`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_hits=9`, and ended at `current_chunk="3,2"`. The RLE client decoded streamed chunks to `blocks=1048576` before the normal terrain path consumed them.
- Movement byte totals for the same 132 streamed chunks: RAW raw/payload/wire `138,412,032 / 138,412,032 / 138,414,760`; RLE raw/payload/wire `138,412,032 / 2,646 / 5,640`. In this generated-world smoke, RLE framed wire bytes were about `0.0041%` of RAW framed wire bytes.
- Checks passed for this handshake/visual slice: `go test ./pkg/network -run 'TestReceiveInitialClientPacket|TestRLEChunkBatchShrinksPayloadAndWireBytes|TestSendChunkCanUseRLEPayload' -v`; `go test ./pkg/api ./pkg/network ./pkg/world`; `cargo test --manifest-path client/rust_ext/Cargo.toml chunk -- --nocapture`; `git diff --check`; added-line ASCII scan; `./scripts/check.sh fast`; `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty-worktree overlay warning only); MCP `gn_verify_diff` (`PASS`); MCP `gn_test_gap` (`PASS`).
- Next useful world-loading step after final checks: consider making RLE the default only after one more broad fast-check plus a normal wrapper-based movement smoke, or continue with startup scheduling/backpressure now that the RLE transport path has visual evidence.

- Current world streaming protocol slice adds an opt-in RLE chunk payload path while keeping raw chunks as the default rollback behavior. `api/schema/packets.proto` now has `ChunkEncoding`, `ChunkData.encoding` tag `4`, and `ChunkData.uncompressed_size` tag `5`; Go protocol code was regenerated with `protoc-gen-go` v1.34.2. Do not hand-edit `server/pkg/api/packets.pb.go`.
- `server/pkg/network` now reads `RUMPELMC_SERVER_CHUNK_ENCODING`: unset/`raw` sends the existing full raw `ChunkData.blocks`, and `rle` sends `world.EncodeSerializedChunkRLE` output with `CHUNK_ENCODING_RLE` plus the raw byte size. Stream metrics include both `raw_bytes` and `payload_bytes`, so raw and RLE batches can be compared without changing the log shape.
- `client/rust_ext/src/lib.rs` now validates and decodes both raw and RLE `ChunkData` payloads before dirty-update, meshing, collision, and GPU upload logic. Malformed raw/RLE chunks are rejected with a debug log instead of being inserted into `chunk_blocks`.
- Checks passed for this protocol slice so far: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `gofmt` on changed Go tests/server files; `cargo check --manifest-path client/rust_ext/Cargo.toml`; `cargo test --manifest-path client/rust_ext/Cargo.toml chunk -- --nocapture`; `go test ./pkg/api ./pkg/network ./pkg/world`.
- Next useful world-loading step: run a real movement/visual smoke with default raw, then with `RUMPELMC_SERVER_CHUNK_ENCODING=rle RUMPELMC_SERVER_CHUNK_STREAM_METRICS=1`, compare `payload_bytes`, `wire_bytes`, startup latency, and terrain/collision markers before considering RLE default-on.

- Current world streaming metrics slice is wire-behavior-preserving. `server/pkg/network` now has opt-in batch logging behind `RUMPELMC_SERVER_CHUNK_STREAM_METRICS=1`, reporting chunks, raw bytes, framed protobuf wire bytes, elapsed milliseconds, and chunks/sec for non-empty `sendChunksAround` batches. The default is off, and `ChunkData.blocks` remains raw.
- Tests added/updated for stream metric env parsing, framed packet size accounting, and batch stat aggregation. `docs/WORLD_STREAMING.md` documents the metric line and keeps raw chunk behavior as the rollback/default path.
- Checks passed for this metrics slice so far: `gofmt` on `pkg/network/server.go` and `pkg/network/server_test.go`; `go test ./pkg/network ./pkg/world`.
- Protocol generator block: `/Users/daniil/go/bin/protoc-gen-go` installed successfully but hung when invoked directly and through `protoc`, so the schema change for encoded chunks was not committed. Do not hand-edit `server/pkg/api/packets.pb.go`; fix or replace the generator before adding encoded chunk protocol fields.
- Next useful world-loading step: repair protobuf generation, then add an env-gated encoded chunk protocol path using new field numbers or a new packet variant, plus Rust decode and raw/RLE integration tests.

- Current world streaming RLE foundation slice is storage/wire-behavior-preserving. `server/pkg/world` now has `EncodeSerializedChunkRLE` and `DecodeSerializedChunkRLE` over the existing serialized chunk bytes, with tests for flat chunk round-trip, edited chunk preservation, wrong-length rejection, malformed run rejection, and benchmarks for raw serialize/RLE encode/RLE decode. `world.Chunk.Serialize()` and persisted chunk payloads remain unchanged, and `server/pkg/network` still sends raw `ChunkData.blocks`.
- Current RLE evidence for the flat generated world: raw chunk payload is `1,048,576` bytes; the RLE test requires the encoded flat chunk to be below `64` bytes. Local Apple M4 benchmark evidence: raw serialize about `431597 ns/op`, RLE encode about `421359 ns/op`, and RLE decode about `268477 ns/op`.
- `docs/WORLD_STREAMING.md` now records the staged rollout: keep raw as default/rollback, benchmark RLE, add a compatible protocol field or packet variant only with client/server tests, then gate encoded chunk streaming behind an explicit env flag.
- Checks passed for this slice so far: `gofmt` on the new world codec files, `go test ./pkg/world`, and `go test ./pkg/world -bench 'Benchmark(ChunkSerializeFlat|EncodeSerializedChunkRLEFlat|DecodeSerializedChunkRLEFlat)' -benchtime=100ms -run '^$'`.
- Next useful world-loading step: install/use `protoc-gen-go`, add a compatible encoded chunk protocol path without changing `ChunkData.blocks` semantics in place, implement Rust decode for that opt-in path, and gate server use behind an env flag.

- Current transparent fixture overlay fixed metadata-entry slice is behavior-preserving and keeps transparent rendering disabled. `client/rust_ext/src/lib.rs` now carries a fixed client-only overlay metadata list with five role/offset entries: `front_transparent`, `behind_wall_transparent`, `opaque_depth_occluder`, `adjacent_same_material_pair`, and `collision_probe`. The metadata still only feeds `transparent_fixture_overlay_roles=5` and `transparent_fixture_overlay_blocks=5` when `RUMPELMC_GPU_TERRAIN_TRANSPARENT_FIXTURE_OVERLAY=1` is requested.
- The new unit test `transparent_fixture_overlay_metadata_is_client_only_and_fixed` locks the five roles, unique block offsets, `5/5` metadata counters, and the inactive overlay gate while `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`. This slice does not create overlay geometry, mutate `ChunkData`, send `BlockAction`, add block IDs/assets, touch protocol/storage/worldgen, change shaders/render passes/sorting, edit `.tscn`, or change default runtime behavior.
- Checks passed for this fixed metadata-entry slice so far: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `cargo test --manifest-path client/rust_ext/Cargo.toml transparent_fixture_overlay_metadata_is_client_only_and_fixed`.
- Next useful GPU step: add a reportable metadata checksum or marker for the fixed overlay role/offset contract, still behind the overlay request flag and still metadata-only, so fixture logs can prove they are using the expected five-entry overlay contract before any geometry consumer exists.

- Current transparent fixture overlay runtime metadata slice is behavior-preserving and keeps transparent rendering disabled. `client/rust_ext/src/lib.rs` now has a fixed five-role client-only fixture metadata list and emits `transparent_fixture_overlay_roles=5` plus `transparent_fixture_overlay_blocks=5` when `RUMPELMC_GPU_TERRAIN_TRANSPARENT_FIXTURE_OVERLAY=1` is requested. With the flag off, both metadata counters stay `0`. `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` still keeps `transparent_active=0`, `transparent_fixture_overlay_active=0`, and all transparent workload counters at `0`.
- The fresh release fixture scene smoke passed in `logs/gpu_transparent_fixture_scene_smoke` with `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `transparent_blocks=0`, `transparent_faces=0`, `transparent_draws=0`, `transparent_subchunks=0`, `transparent_fixture_overlay_requested=1`, `transparent_fixture_overlay_active=0`, `transparent_fixture_overlay_fallback=1`, `transparent_fixture_overlay_roles=5`, `transparent_fixture_overlay_blocks=5`, `terrain_samples=488`, `terrain_color_buckets=8`, `current_chunk_collision=2`, `ground_misses=0`, `terrain_queue_max_ms=2.617`, `process_wall_p95_ms=0.030`, `gpu_compositor_submit_max_ms=0.311`, and `gpu_upload_fail=0`.
- `scripts/gpu_terrain_movement_stress.sh` and `scripts/gpu_terrain_transparent_fixture_scene_smoke.sh` now require the overlay metadata counts when the overlay flag is requested. `scripts/gpu_terrain_report.sh` aggregates max `transparent_fixture_overlay_roles` and max `transparent_fixture_overlay_blocks`, records metric origins, and the refreshed report at `/tmp/rumpel-gpu-report-transparent-overlay-metadata-fresh.txt` selected the latest fixture scene summary with both counters at `5`.
- Checks passed for this runtime metadata slice so far: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`; `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_transparent_gate_stays_disabled_until_implemented`; shell syntax for `scripts/gpu_terrain_movement_stress.sh`, `scripts/gpu_terrain_transparent_fixture_scene_smoke.sh`, and `scripts/gpu_terrain_report.sh`; `sh scripts/gpu_terrain_transparent_fixture_pack.sh logs/gpu_transparent_fixture_plan` with `overlay_metadata_expected=5/5`; fresh release fixture scene smoke; refreshed overlay design artifact; and refreshed aggregate report. This slice does not create overlay geometry, mutate `ChunkData`, send `BlockAction`, add block IDs/assets, touch protocol/storage/worldgen, change shaders/render passes/sorting, edit `.tscn`, or change default runtime behavior.
- Next useful GPU step: add the smallest client-only fixture overlay data structure that carries fixed coordinates/roles behind `RUMPELMC_GPU_TERRAIN_TRANSPARENT_FIXTURE_OVERLAY=1`, still with `geometry_active=0`, no `ChunkData` mutation, no packets, no block IDs/assets/protocol/storage/worldgen, and no renderer/shader behavior change. Keep it metadata-only until a separate implementation slice consumes it.

- Current transparent fixture overlay fallback/metadata guard slice is script/docs-only and behavior-preserving. The no-render fixture chain now requires overlay env-on fallback markers `transparent_fixture_overlay_requested=1`, `transparent_fixture_overlay_active=0`, and `transparent_fixture_overlay_fallback=1` wherever current transparent env-on fallback guards are validated. It also carries a client-only metadata scaffold with `transparent_fixture_overlay_roles=5`, `transparent_fixture_overlay_blocks=5`, `geometry_active=0`, and `chunk_data_mutation=no` through plan, harness, check, smoke-plan, scene checklist/harness/check, report-check, acceptance, default-off, final-report, scene-implementation checklist, scene-implementation gate-check, pack summaries, and docs/trends.
- The regenerated no-render pack passed at `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` with `env_on_expected=1/0/1`, `overlay_env_on_expected=1/0/1`, `overlay_metadata_expected=5/5`, `contract_tokens=26`, all fixture chain checks pass, and `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` still enforced. This slice does not create overlay geometry, mutate `ChunkData`, send `BlockAction`, add block IDs/assets, touch protocol/storage/worldgen, change shaders/render passes/sorting, edit `.tscn`, or change default runtime behavior.
- Checks passed for this overlay fallback/metadata guard slice: shell syntax for transparent fixture scripts; `sh scripts/gpu_terrain_transparent_fixture_pack.sh logs/gpu_transparent_fixture_plan`; standalone smoke-plan/acceptance/default-off/scene-implementation gate checks to `/tmp`; `sh scripts/gpu_terrain_transparent_fixture_overlay_design.sh docs/GPU_TRANSPARENT_PATH.md /tmp/rumpel-transparent-fixture-overlay-design.txt`; `git diff --check`; ASCII scan over changed docs/scripts; `./scripts/check.sh fast` (Go cached, Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit` (`READY`, dirty-worktree overlay warning only); MCP `gn_verify_diff` (`PASS` after executed test evidence); and MCP `gn_test_gap` (`PASS`).
- `docs/AI_TASK_TEMPLATE.md` already contains the requested no-fixed-size-cap rule: "Do not impose fixed file-count or line-count caps; split work by risk, reviewability, and testability instead." No extra template edit was needed despite this slice exceeding 5 files and 300 lines.

- Current transparent fixture scene fallback smoke slice adds the first runtime harness while preserving opaque-only behavior. `client/main.gd` now supports `RUMPELMC_VISUAL_SMOKE_POSE=transparent_fixture`, and `scripts/gpu_terrain_transparent_fixture_scene_smoke.sh` launches the existing Godot visual smoke path with `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1`, then validates `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, `smoke_err=0`, zero transparent workload markers, and non-sky terrain samples. The fresh release capture passed in `logs/gpu_transparent_fixture_scene_smoke` with `terrain_samples=496`, `terrain_color_buckets=8`, `gpu_subchunks=120`, `gpu_draws=120`, `terrain_queue_max_ms=2.243`, `process_wall_p95_ms=0.039`, and `gpu_compositor_submit_max_ms=0.193`.
- Current aggregate-report slice surfaces that runtime fixture scene fallback smoke in `scripts/gpu_terrain_report.sh`. The latest report run selected `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt` and includes `status=pass`, `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `transparent_blocks=0`, `transparent_faces=0`, `transparent_draws=0`, `transparent_subchunks=0`, `gpu_upload_fail=0`, and `terrain_samples=496`.
- Current transparent workload marker scaffold adds `transparent_blocks`, `transparent_faces`, `transparent_draws`, and `transparent_subchunks` to runtime perf markers, report origins, and fixture fallback guards. They are expected to remain `0` while `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`.
- Current no-render fixture pack sync makes plan/smoke/checklist/harness/acceptance/default-off/implementation gate artifacts require those same zero workload markers in current env-on fallback guard lines. `sh scripts/gpu_terrain_transparent_fixture_pack.sh logs/gpu_transparent_fixture_plan` passed with `env_on_expected=1/0/1`.
- Current blocker: adding a real `transparent_test_glass` fixture identity cannot be done safely as a local-only edit without either a production block ID/asset/protocol/storage/worldgen change or a new client-only fixture overlay path. The current runtime consumes serialized `u16` block IDs and block definitions for texture/solid behavior.
- Current client-only overlay design slice defines the safe path for `transparent_test_glass`: smoke-owned overlay metadata, explicit future env gate, one-run lifetime, no `ChunkData` mutation, no `BlockAction`, no protocol/storage/worldgen/asset/renderer changes, and future overlay marker names. `scripts/gpu_terrain_transparent_fixture_overlay_design.sh` validates the design and writes `transparent-fixture-overlay-design.txt`; aggregate GPU reports surface it when present.
- Current transparent fixture overlay marker scaffold adds `RUMPELMC_GPU_TERRAIN_TRANSPARENT_FIXTURE_OVERLAY` and runtime/report markers `transparent_fixture_overlay_requested`, `transparent_fixture_overlay_active`, and `transparent_fixture_overlay_fallback`. Fresh release fixture scene smoke passed with overlay markers `1/0/1`, `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, zero transparent workload markers, `gpu_upload_fail=0`, and no overlay geometry/chunk mutation. `scripts/gpu_terrain_report.sh` now caches the summary file list and an indexed `path:line` scan, reducing full historical `logs` report generation to about 3 seconds; `sh scripts/gpu_terrain_transparent_fixture_pack.sh logs/gpu_transparent_fixture_plan` now passes end-to-end again in about 33 seconds.
- This runtime harness does not add fixture block/material identity, shader alpha, blending, transparent pass, sorting, block IDs, atlas assets, protocol, storage, worldgen, `.tscn` changes, or default render behavior. `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` remains the active implementation gate.
- Next useful GPU step: sync the remaining no-render fixture pack acceptance/checklist artifacts to explicitly require the overlay `1/0/1` fallback markers, then consider a client-only metadata list for fixed fixture overlay roles.

Previous snapshot:

Date: 2026-06-13

Fresh 2026-06-13 status:

- Current transparent fixture scene implementation planning slice is docs-only and behavior-preserving. `docs/GPU_TRANSPARENT_PATH.md` now says the next implementation step should stop extending the no-render guard chain and move to an env-driven fixture scene harness using the existing `main.gd` visual smoke path. The first runtime slice should add a `transparent_fixture` pose/wrapper while keeping `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` and current env-on fallback markers. It explicitly avoids new `.tscn` rewrites, shader alpha, blending, transparent pass, block IDs, atlas assets, protocol, storage, worldgen, or default behavior changes.
- Next useful GPU step: implement the documented first runtime slice by adding the env-driven `transparent_fixture` visual smoke pose and wrapper script, then run the fallback capture with `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1` and assert `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, `smoke_err=0`, and non-sky terrain samples.

Previous snapshot:

Date: 2026-06-13

Fresh 2026-06-13 status:

- Current GPU long-horizon planning and Week 1 baseline slice is docs/artifact-only and behavior-preserving. `docs/GPU_ROADMAP.md` now includes a 100-week rolling long-horizon plan with 4-week re-check expectations and weekly artifact/trend checkpoints. No renderer, shader, runtime, protocol, storage, worldgen, asset, or Godot scene behavior changed.
- Fresh isolated Week 1 baseline artifacts live under `logs/week1_gpu_baseline_20260613`. The scoped aggregate report is `logs/week1_gpu_baseline_20260613/gpu-terrain-report.txt` and intentionally uses that isolated log root so fresh metrics are not mixed with historical logs. A historical full-log report was also refreshed at `logs/gpu-terrain-report.txt`.
- Fresh Week 1 baseline results: movement stress passed with `gpu_upload_fail=0`, `terrain_queue_max_ms=1.146`, `process_wall_p95_ms=0.017`, and `gpu_compositor_submit_max_ms=0.112`; standard workload matrix passed all four cases with max `terrain_queue_max_ms=2.155`, max `gpu_compositor_submit_max_ms=0.165`, `gpu_upload_fail=0`, and terrain samples present; fill stress repeats `1/4/8/16` all passed with `gpu_upload_fail=0`, `smoke_err=0`, `terrain_samples=384`, max `gpu_effective_draws=13344`, and max fill `terrain_queue_max_ms=2.125`.
- Week 1 caveat/blocker: `scripts/gpu_terrain_perf_baseline.sh logs/week1_gpu_baseline_20260613/perf_baseline` exited nonzero because the CPU ArrayMesh control reported `terrain_queue_max_ms=16.862 > 6.667`; the GPU terrain side of the same perf baseline passed with `terrain_queue_max_ms=1.003`, `gpu_upload_fail=0`, and `gpu_compositor_submit_max_ms=0.312`. Treat the CPU control spike as inconclusive baseline evidence, not a GPU terrain regression. Local visual FPS and `gpu_compositor_gpu_us/ms` remain report-only on macOS/Metal.
- Next useful GPU step: continue Week 2 by stopping the no-render transparent guard chain and beginning the real transparent fixture scene/harness plan while keeping `transparent_active=0` and `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`, or continue Week 1 optional heavy baseline only if a stronger draw-pressure sample is needed. External profiler evidence is still required before shader, shadow, or transparent default-on decisions.

Previous snapshot:

Date: 2026-06-12

Fresh 2026-06-12 status:

- Current Phase 12 transparent fixture scene-implementation gate-check pack/report integration slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` now runs `scripts/gpu_terrain_transparent_fixture_scene_implementation_gate_check.sh`, refreshes the aggregate report so it includes `Selected Transparent Fixture Scene Implementation Gate Check`, and emits a final pack with `scene_implementation_gate_check`, `scene_implementation_gate_steps=scene_implementation_gate_check/report_refresh`, and `transparent_fixture_scene_implementation_gate_check_status=pass`. `scripts/gpu_terrain_report.sh` now surfaces this gate-check artifact. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-implementation gate-check pack/report integration slice: syntax checks for changed shell scripts; real-log pack generation confirming `scene_implementation_gate_steps=scene_implementation_gate_check/report_refresh`, `transparent_fixture_scene_implementation_gate_check_status=pass`, and `Selected Transparent Fixture Scene Implementation Gate Check`; standalone gate-check over the final checklist; isolated `/tmp/rumpel-transparent-scene-implementation-gate-chain` pack generation with a minimal seed summary; negative-path report regeneration without `transparent-fixture-scene-implementation-gate-check.txt` confirming the selected gate-check `Source:` line is absent; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard or stop the no-render guard chain and begin a fixture-scene plan while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from scene-implementation gate-check artifacts.

- Current Phase 12 transparent fixture scene-implementation gate-check slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_scene_implementation_gate_check.sh` consumes `transparent-fixture-scene-implementation-checklist.txt`, validates the linked final no-render pack and aggregate report, and requires `transparent_implementation_gate=false`, `scene_implementation_steps=scene_implementation_checklist/report_refresh`, current env-on fallback `env_on_expected=1/0/1`, future active-path gate `future_active_expected=1/0/0`, unchanged runtime/ordinary-world visibility, and non-goals. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-implementation gate-check slice: `sh -n scripts/gpu_terrain_transparent_fixture_scene_implementation_gate_check.sh`; real-log gate-check generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-implementation-gate-check.txt` (`transparent_fixture_scene_implementation_gate_check_status=pass`, `transparent_implementation_gate=false`, `future_active_expected=1/0/0`); isolated `/tmp/rumpel-transparent-scene-implementation-gate-check` pack plus gate-check generation; negative-path gate flip rejection; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: integrate `transparent-fixture-scene-implementation-gate-check.txt` into the fixture pack/report chain or add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from scene-implementation gate-check artifacts.

- Current Phase 12 transparent fixture scene-implementation-checklist pack/report integration slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` now runs `scripts/gpu_terrain_transparent_fixture_scene_implementation_checklist.sh`, refreshes the aggregate report so it includes `Selected Transparent Fixture Scene Implementation Checklist`, and emits a final pack with `scene_implementation_checklist`, `scene_implementation_steps=scene_implementation_checklist/report_refresh`, and `transparent_fixture_scene_implementation_checklist_status=pending_scene_implementation`. `scripts/gpu_terrain_report.sh` now surfaces this checklist artifact. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-implementation-checklist pack/report integration slice: syntax checks for changed shell scripts; real-log pack generation confirming `scene_implementation_steps=scene_implementation_checklist/report_refresh`, `transparent_fixture_scene_implementation_checklist_status=pending_scene_implementation`, and `Selected Transparent Fixture Scene Implementation Checklist`; standalone scene-implementation checklist over the final pack; isolated `/tmp/rumpel-transparent-scene-implementation-chain` pack generation with a minimal seed summary; negative-path report regeneration without `transparent-fixture-scene-implementation-checklist.txt` confirming the selected scene implementation checklist `Source:` line is absent; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard or fixture-scene harness checklist while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from scene-implementation checklist artifacts.

- Current Phase 12 transparent fixture scene-implementation-checklist slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_scene_implementation_checklist.sh` consumes the final no-render `transparent-fixture-pack.txt`, validates the linked smoke-plan, scene checklist, scene harness, scene harness-check, acceptance, default-off, and final-report artifacts, then emits `transparent-fixture-scene-implementation-checklist.txt` with the required future fixture-scene roles, current env-on fallback guard, and future active-path gates. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-implementation-checklist slice: `sh -n scripts/gpu_terrain_transparent_fixture_scene_implementation_checklist.sh`; real-log checklist generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-implementation-checklist.txt` (`transparent_fixture_scene_implementation_checklist_status=pending_scene_implementation`); isolated `/tmp/rumpel-transparent-scene-implementation-checklist` pack plus checklist generation; negative-path missing `final_report_steps=final_report_check/report_refresh` rejection; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: integrate `transparent-fixture-scene-implementation-checklist.txt` into the fixture pack/report chain or add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from this checklist.

- Current Phase 12 transparent fixture final-report pack/report integration slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` now runs `scripts/gpu_terrain_transparent_fixture_final_report_check.sh`, refreshes the aggregate report so it includes `Selected Transparent Fixture Final Report Check`, and emits a final pack with `final_report_check`, `final_report_steps=final_report_check/report_refresh`, and `transparent_fixture_final_report_check_status=pass`. `scripts/gpu_terrain_report.sh` now surfaces this final-report artifact. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture final-report pack/report integration slice: syntax checks for changed shell scripts; real-log pack generation confirming `final_report_steps=final_report_check/report_refresh`, `transparent_fixture_final_report_check_status=pass`, and `Selected Transparent Fixture Final Report Check`; standalone final-report check over the final pack; isolated `/tmp/rumpel-transparent-final-report-chain` pack generation with a minimal seed summary; negative-path report regeneration without `transparent-fixture-final-report-check.txt` confirming the selected final-report `Source:` line is absent; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard or fixture-scene implementation checklist while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from final-report artifacts.

- Current Phase 12 transparent fixture final-report guard slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_final_report_check.sh` consumes the final no-render `transparent-fixture-pack.txt`, validates linked report/report-check/acceptance/default-off artifacts, requires `acceptance_steps=acceptance_check/report_refresh` plus `default_off_steps=default_off_check/report_refresh`, and verifies the aggregate report contains `Selected Transparent Fixture Acceptance Check` and `Selected Transparent Fixture Default-Off Check` sections sourced from those linked artifacts with pass summaries. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture final-report guard slice: `sh -n scripts/gpu_terrain_transparent_fixture_final_report_check.sh`; real-log final-report check generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-final-report-check.txt` (`transparent_fixture_final_report_check_status=pass`); isolated `/tmp/rumpel-transparent-final-report-check` pack plus final-report check generation; negative-path missing selected default-off report section rejection; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: integrate `transparent-fixture-final-report-check.txt` into the fixture pack/report chain or add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from final-report artifacts.

- Current Phase 12 transparent fixture default-off pack/report integration slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` now writes an intermediate acceptance pack, runs `scripts/gpu_terrain_transparent_fixture_default_off_check.sh`, refreshes the aggregate report so it includes `Selected Transparent Fixture Default-Off Check`, and emits a final pack with `default_off_check`, `default_off_steps=default_off_check/report_refresh`, and `transparent_fixture_default_off_status=pass`. `scripts/gpu_terrain_report.sh` now surfaces this default-off artifact. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture default-off pack/report integration slice: `sh -n scripts/gpu_terrain_transparent_fixture_pack.sh`; `sh -n scripts/gpu_terrain_report.sh`; real-log pack generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` with `default_off_steps=default_off_check/report_refresh` and `transparent_fixture_default_off_status=pass`; report grep confirming `Selected Transparent Fixture Default-Off Check`; standalone default-off check over the final pack; compatibility acceptance-check and smoke-plan generation over the final pack; isolated `/tmp/rumpel-transparent-default-off-chain` pack generation with a minimal seed summary; stripped-pack grep check confirming `default_off_steps` is the final-pack marker; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from default-off pack/report artifacts.

- Current Phase 12 transparent fixture default-off guard slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_default_off_check.sh` consumes the final no-render `transparent-fixture-pack.txt`, validates the linked acceptance/smoke/scene harness-check artifacts, checks the Rust `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` gate and requested/active/fallback decisions, checks the movement-stress env-on fallback guard, and emits `transparent-fixture-default-off-check.txt` with `transparent_fixture_default_off_status=pass`. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture default-off guard slice: `sh -n scripts/gpu_terrain_transparent_fixture_default_off_check.sh`; real-log default-off generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-default-off-check.txt` (`transparent_fixture_default_off_status=pass`, `transparent_fixture_acceptance_status=pass`, `env_on_expected=1/0/1`, `future_active_expected=1/0/0`); isolated `/tmp/rumpel-transparent-default-off` pack plus default-off generation with a minimal seed summary; negative-path missing `acceptance_steps=acceptance_check/report_refresh` rejection; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: integrate `transparent-fixture-default-off-check.txt` into the fixture pack/report chain or add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from default-off artifacts.

- Current Phase 12 transparent fixture acceptance pack/report integration slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` now emits `transparent-fixture-acceptance-check.txt`, refreshes the aggregate report after that artifact exists, and includes `transparent_fixture_acceptance_status=pass` in the final pack summary. `scripts/gpu_terrain_report.sh` now surfaces `Selected Transparent Fixture Acceptance Check`, and `scripts/gpu_terrain_transparent_fixture_acceptance_check.sh` validates that final packs with `acceptance_steps=acceptance_check/report_refresh` have that selected report section. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` records the checkpoint.
- Checks passed for this transparent fixture acceptance pack/report integration slice: `sh -n` for `scripts/gpu_terrain_transparent_fixture_pack.sh`, `scripts/gpu_terrain_report.sh`, and `scripts/gpu_terrain_transparent_fixture_acceptance_check.sh`; real-log pack generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` with `acceptance_steps=acceptance_check/report_refresh` and `transparent_fixture_acceptance_status=pass`; report grep confirming `Selected Transparent Fixture Acceptance Check`; repeat standalone acceptance-check over the final pack; compatibility `scripts/gpu_terrain_transparent_fixture_smoke_plan.sh` over the final pack; negative-path missing selected acceptance report section rejection; isolated `/tmp/rumpel-transparent-fixture-pack-acceptance-chain` pack plus acceptance generation; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from acceptance pack/report artifacts.

- Current Phase 12 transparent fixture acceptance-check slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_acceptance_check.sh` consumes `transparent-fixture-pack.txt`, validates the linked no-render pack/report-check/smoke-plan/scene harness-check artifacts, requires current requested-but-fallback gates, future fixture scene/workload/active gates, and non-goals, then emits `transparent-fixture-acceptance-check.txt` with `transparent_fixture_acceptance_status=pass`. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture acceptance-check slice: `sh -n scripts/gpu_terrain_transparent_fixture_acceptance_check.sh`; real-log acceptance generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-acceptance-check.txt` (`transparent_fixture_acceptance_status=pass`, `future_active_expected=1/0/0`, `env_on_expected=1/0/1`); isolated `/tmp/rumpel-transparent-fixture-acceptance` pack plus acceptance generation; negative-path missing `future_active_expected=1/0/0` rejection; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: integrate `transparent-fixture-acceptance-check.txt` into the pack/report chain or add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from acceptance-check artifacts.

- Current Phase 12 transparent fixture pack/report scene-chain slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` now regenerates `transparent-fixture-smoke-plan.txt`, `transparent-fixture-scene-checklist.txt`, `transparent-fixture-scene-harness.txt`, and `transparent-fixture-scene-harness-check.txt` as part of the no-render pack before regenerating the aggregate report and report-check. `scripts/gpu_terrain_report.sh` surfaces the scene checklist, scene harness, and scene harness-check selected sections, and `scripts/gpu_terrain_transparent_fixture_report_check.sh` now requires those sections plus `transparent_fixture_scene_harness_check_status=pass`. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` records the checkpoint.
- Checks passed for this transparent fixture pack/report scene-chain slice: `sh -n` for `scripts/gpu_terrain_transparent_fixture_pack.sh`, `scripts/gpu_terrain_report.sh`, and `scripts/gpu_terrain_transparent_fixture_report_check.sh`; real-log pack generation to `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` with `scene_steps=smoke_plan/scene_checklist/scene_harness/scene_harness_check` and `transparent_fixture_scene_harness_check_status=pass`; report grep confirming all three selected scene sections; isolated `/tmp/rumpel-transparent-fixture-pack-scene-chain` pack generation; compatibility `scripts/gpu_terrain_transparent_fixture_smoke_plan.sh` over the new pack; repeat report-check generation; negative-path missing scene harness-check report section rejection; `git diff --check`; ASCII check; `./scripts/check.sh fast` (Rust `110/110`); `./scripts/diff_guard.sh`; MCP `gn_pre_commit_audit`; MCP `gn_verify_diff`; and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only report guard or acceptance validator while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from these pack/report artifacts.

- Current Phase 12 transparent fixture scene-harness-check slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_scene_harness_check.sh` consumes `transparent-fixture-scene-checklist.txt` plus `transparent-fixture-scene-harness.txt`, validates fixture/material/status agreement, all five role checks, env-off/env-on fallback gates, future workload/active gates, non-goals, and the `roles=5` harness summary, then emits `transparent-fixture-scene-harness-check.txt` with `transparent_fixture_scene_harness_check_status=pass`. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-harness-check slice: `sh -n scripts/gpu_terrain_transparent_fixture_scene_harness_check.sh`, scene harness-check generation from `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-checklist.txt` plus `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-harness.txt` to `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-harness-check.txt` (`transparent_fixture_scene_harness_check_status=pass`, `roles=5`, `env_on_expected=1/0/1`), direct executable scene-harness-check generation under `/tmp/rumpel-transparent-fixture-scene-harness-check`, negative-path bad `roles=4` rejection, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: integrate the scene checklist, scene harness, and scene harness check into the fixture pack/report chain while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from scene-harness-check artifacts.

- Current Phase 12 transparent fixture scene-harness slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_scene_harness.sh` consumes `transparent-fixture-scene-checklist.txt`, validates the fixed camera/light/chunk lines, all five scene roles, current env-off/env-on fallback gates, future workload/active gates, and non-goals, then emits `transparent-fixture-scene-harness.txt` with placeholder role checks. It does not launch Godot, create scenes/assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-harness slice: `sh -n scripts/gpu_terrain_transparent_fixture_scene_harness.sh`, scene harness generation from `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-checklist.txt` to `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-harness.txt` (`transparent_fixture_scene_harness_status=placeholder`, `roles=5`, `env_on_expected=1/0/1`), direct executable scene-harness generation under `/tmp/rumpel-transparent-fixture-scene-harness`, negative-path missing collision solidity rejection, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only validator or integrate this no-render scene harness into the fixture pack/report chain while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from scene-harness artifacts.

- Current Phase 12 transparent fixture scene-checklist slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_scene_checklist.sh` consumes `transparent-fixture-smoke-plan.txt`, validates current fallback gates plus future fixture-scene/workload/active-path gates, then emits `transparent-fixture-scene-checklist.txt` with fixed future roles for front transparent, behind-wall transparent, opaque depth occluder, adjacent same-material pair, and collision probe checks. It does not launch Godot, create assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture scene-checklist slice: `sh -n scripts/gpu_terrain_transparent_fixture_scene_checklist.sh`, scene checklist generation from `logs/gpu_transparent_fixture_plan/transparent-fixture-smoke-plan.txt` to `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-checklist.txt` (`transparent_fixture_scene_checklist_status=pending_scene_harness`, `env_on_expected=1/0/1`), direct executable scene-checklist generation under `/tmp/rumpel-transparent-fixture-scene-checklist`, negative-path missing collision probe rejection, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only harness/checklist while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from scene-checklist artifacts.

- Current Phase 12 transparent fixture smoke-plan slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_smoke_plan.sh` consumes `transparent-fixture-pack.txt`, verifies the generated plan/harness/check/report-check artifacts and pack summary, then emits `transparent-fixture-smoke-plan.txt` with current env-off and env-on fallback smoke commands plus future fixture-scene, workload-marker, and active-path gates. It does not launch Godot, create assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture smoke-plan slice: `sh -n scripts/gpu_terrain_transparent_fixture_smoke_plan.sh`, smoke-plan generation from `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` to `logs/gpu_transparent_fixture_plan/transparent-fixture-smoke-plan.txt` (`transparent_fixture_smoke_plan_status=pending_fixture_scene`, `env_on_expected=1/0/1`), direct executable smoke-plan generation under `/tmp/rumpel-transparent-fixture-pack`, negative-path bad pack status rejection, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard or fixture-scene implementation checklist while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from smoke-plan/checklist artifacts.

- Current Phase 12 transparent fixture no-render pack slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_pack.sh` regenerates the transparent fixture plan, harness, check, aggregate report, and report-check artifacts in one command, then emits `transparent_fixture_pack_status=pass` only when the generated summaries agree on `fixture_plan_status=pending_fixture_harness`, `transparent_fixture_harness_status=placeholder`, `transparent_fixture_check_status=pass`, `transparent_fixture_report_check_status=pass`, and `env_on_expected=1/0/1`. It does not launch Godot, create assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture no-render pack slice: `sh -n scripts/gpu_terrain_transparent_fixture_pack.sh`, pack generation under `logs/gpu_transparent_fixture_plan` (`transparent_fixture_pack_status=pass`, `transparent_fixture_report_check_status=pass`, `transparent_fixture_check_status=pass`, `env_on_expected=1/0/1`), direct executable pack generation under `/tmp/rumpel-transparent-fixture-pack` with a minimal `seed-summary.txt`, negative-path missing and empty log dir rejection, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard or fixture smoke design checkpoint while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from pack/checklist artifacts.

- Current Phase 12 transparent fixture report guard slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_report_check.sh` validates that an aggregate GPU report includes `Selected Transparent Fixture Plan`, `Selected Transparent Fixture Harness`, and `Selected Transparent Fixture Check` sections sourced from `logs/gpu_transparent_fixture_plan`, then emits `transparent_fixture_report_check_status=pass` only when the selected artifacts, pass summary, and requested/active/fallback triplet match. It does not launch Godot, create assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture report guard slice: `sh -n scripts/gpu_terrain_transparent_fixture_report_check.sh`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-fixture-real.txt`, report guard generation to `/tmp/rumpel-transparent-fixture-report-check.txt` (`transparent_fixture_report_check_status=pass`, `transparent_fixture_check_status=pass`, `env_on_expected=1/0/1`), direct executable report guard generation, negative-path missing selected check section rejection, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add a one-command no-render fixture pack or the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from report/checklist artifacts.

- Current Phase 12 transparent fixture real-log artifact slice is artifact/docs-only and behavior-preserving. The ignored local directory `logs/gpu_transparent_fixture_plan` now contains generated `transparent-fixture-plan.txt`, `transparent-fixture-harness.txt`, and `transparent-fixture-check.txt` artifacts from the existing transparent fixture scripts. `scripts/gpu_terrain_report.sh` over `logs` surfaces all three selected transparent fixture sections, with the check artifact reporting `transparent_fixture_check_status=pass` and `env_on_expected=1/0/1`. These artifacts remain validators/checklists, not Godot captures or transparent render evidence. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, Godot scene, or render behavior changed.
- Checks passed for this transparent fixture real-log artifact slice: plan+harness+check generation under `logs/gpu_transparent_fixture_plan` (`transparent_fixture_check_status=pass`, `env_on_expected=1/0/1`), `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-fixture-real.txt` with selected plan/harness/check sections sourced from `logs/gpu_transparent_fixture_plan`, ignored-log status check, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from checklist artifacts.

- Current Phase 12 transparent fixture check report surfacing slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now includes the latest `transparent-fixture-check.txt` under the selected log directory as `Selected Transparent Fixture Check`, next to the existing plan and harness sections. These artifacts remain validators/checklists, not Godot captures or transparent render evidence. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, Godot scene, or render behavior changed.
- Checks passed for this transparent fixture check report surfacing slice: `sh -n scripts/gpu_terrain_report.sh`, temp transparent fixture plan+harness+check generation under `/tmp/rumpel-transparent-report-fixture-check`, `sh scripts/gpu_terrain_report.sh /tmp/rumpel-transparent-report-fixture-check /tmp/rumpel-gpu-report-transparent-fixture-check.txt` with selected transparent fixture check section and expected `transparent_fixture_check_status=pass` summary, normal `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-fixture-check-current.txt` showing no matching transparent fixture artifacts under current `logs`, `git diff --check`, ASCII check, `./scripts/check.sh fast`, `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit`, MCP `gn_verify_diff`, and MCP `gn_test_gap`.
- Next useful GPU step after checkpointing this slice: generate ignored local `logs/gpu_transparent_fixture_plan` artifacts through the existing plan+harness+check scripts, or add the next fixture-only guard while keeping all runtime transparent behavior disabled. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from report-only artifact surfacing.

- Current Phase 12 transparent fixture artifact validator slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_check.sh` validates matching generated `transparent-fixture-plan.txt` and `transparent-fixture-harness.txt` artifacts, enforces the fixture identity, material label, runtime/ordinary-world status, env-on requested/active/fallback triplet, future active triplet, workload marker placeholders, and non-goals, then emits a line-oriented `transparent-fixture-check.txt` pass artifact. It does not launch Godot, create assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture artifact validator slice: `sh -n scripts/gpu_terrain_transparent_fixture_check.sh`, temp plan+harness+check generation under `/tmp/rumpel-transparent-fixture-check` (`transparent_fixture_check_status=pass`, `env_on_expected=1/0/1`), direct executable check generation, negative-path harness `env_on_expected` validation, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: surface `transparent-fixture-check.txt` in `scripts/gpu_terrain_report.sh` or generate ignored local `logs/gpu_transparent_fixture_plan` artifacts through the existing plan+harness+check scripts. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from validator artifacts.

- Current Phase 12 transparent fixture report surfacing slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now includes selected optional sections for the latest `transparent-fixture-plan.txt` and `transparent-fixture-harness.txt` under the selected log directory. These artifacts remain checklists/placeholders, not Godot captures or transparent render evidence. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, Godot scene, or render behavior changed.
- Checks passed for this transparent fixture report surfacing slice: `sh -n scripts/gpu_terrain_report.sh`, temp transparent fixture plan+harness generation under `/tmp/rumpel-transparent-report-fixture`, `sh scripts/gpu_terrain_report.sh /tmp/rumpel-transparent-report-fixture /tmp/rumpel-gpu-report-transparent-fixture.txt` with selected transparent fixture sections and expected summaries, normal `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-fixture-current.txt` showing no matching transparent fixture artifacts under current `logs`, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add validation around generated transparent fixture plan/harness summaries or generate ignored local `logs/gpu_transparent_fixture_plan` artifacts through the existing plan+harness scripts. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from report-only artifact surfacing.

- Current Phase 12 transparent fixture harness placeholder slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_harness.sh` consumes a generated `transparent-fixture-plan.txt`, validates its fixture identity, material label, runtime/ordinary-world status, env-on fallback triplet, future active triplet, workload marker placeholders, and non-goals, then emits a placeholder `transparent-fixture-harness.txt` artifact. It does not launch Godot, create assets, enable transparent terrain materials, or change runtime behavior. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint.
- Checks passed for this transparent fixture harness placeholder slice: `sh -n scripts/gpu_terrain_transparent_fixture_harness.sh`, plan generation to `/tmp/rumpel-transparent-fixture-plan.txt`, harness generation to `/tmp/rumpel-transparent-fixture-harness.txt` (`transparent_fixture_harness_status=placeholder`, `env_on_expected=1/0/1`), direct executable harness generation to `/tmp/rumpel-transparent-fixture-harness-exec.txt`, negative-path missing env-on fallback gate check, `git diff --check`, ASCII check, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: surface the transparent fixture plan/harness artifacts in `scripts/gpu_terrain_report.sh` or add a small validator for their generated summaries. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, Godot scene changes, or render behavior from these placeholder artifacts alone.

- Current Phase 12 transparent fixture plan guard slice is script/docs-only and behavior-preserving. `scripts/gpu_terrain_transparent_fixture_plan.sh` validates the transparent fixture contract tokens in `docs/GPU_TRANSPARENT_PATH.md`, verifies that `scripts/gpu_terrain_movement_stress.sh` still has the env-on requested/active/fallback guard, and emits a line-oriented `transparent-fixture-plan.txt` checklist for the next no-render harness slice. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, Godot scene, or render behavior changed.
- Checks passed for this transparent fixture plan guard slice: `sh -n scripts/gpu_terrain_transparent_fixture_plan.sh`, `sh scripts/gpu_terrain_transparent_fixture_plan.sh docs/GPU_TRANSPARENT_PATH.md /tmp/rumpel-transparent-fixture-plan.txt` (`contract_tokens=21`, `fallback_guard=present`), direct executable plan generation to `/tmp/rumpel-transparent-fixture-plan-exec.txt`, negative-path missing-token check, `git diff --check`, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add a fixture-only harness placeholder that consumes `transparent-fixture-plan.txt` and preserves env-off ordinary terrain plus env-on requested-but-fallback marker gates. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, or render behavior from the plan alone.

- Current Phase 12 transparent fixture contract slice is docs-only and behavior-preserving. `docs/GPU_TRANSPARENT_PATH.md` now defines the first fixture-only transparent material contract: a stable non-production fixture identity, render opacity separated from collision solidity, a fixed depth/collision scene shape, required future workload markers, fallback and active acceptance gates, and explicit non-goals for shader alpha, transparent pass, sorting, block IDs, assets, protocol, storage, worldgen, and render behavior. `docs/GPU_TRENDS.md` records the checkpoint.
- Checks passed for this transparent fixture contract slice: `git diff --check`, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add a fixture-only harness placeholder or docs-backed smoke plan that references the contract while preserving env-off ordinary terrain and env-on requested-but-fallback marker gates. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, storage, worldgen, or render behavior until that fixture harness exists.

- Current Phase 12 transparent fallback harness guard slice is script-only and behavior-preserving. `scripts/gpu_terrain_movement_stress.sh` now checks `RUMPELMC_GPU_TERRAIN_TRANSPARENT` using the same enabled values as Rust (`1`, `true`, `yes`, `on`) and, only when that env flag is enabled, requires `transparent_requested=1`, `transparent_active=0`, and `transparent_fallback=1` in the generated movement marker. Normal movement smoke runs with the transparent env unset keep their existing gates. No Rust runtime, shader, Godot scene, transparent face buffer, alpha blending, sorting, block ID, atlas asset, protocol, storage, worldgen, shadow quality, or render behavior changed.
- Fresh transparent fallback guard artifact: `logs/gpu_transparent_fallback_guard_capture` from `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_transparent_fallback_guard_capture`. It passed with `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, `smoke_err=0`, `terrain_samples=384`, `ground_hits=9`, `current_chunk_loaded=1`, `current_chunk_submeshes=2`, `current_chunk_collision=2`, `terrain_queue_max_ms=3.306`, `gpu_compositor_submit_max_ms=0.197`, and `process_wall_p95_ms=0.053`.
- Checks passed for this transparent fallback harness guard slice: `sh -n scripts/gpu_terrain_movement_stress.sh`, env-on release movement smoke above, marker grep for `transparent_requested=1 transparent_active=0 transparent_fallback=1`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-guard.txt`, `git diff --check`, `./scripts/check.sh fast` (Rust `110/110`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: plan the first real transparent fixture/material contract before any renderer work, or add report-only documentation for how that fixture will prove depth/collision behavior. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, or render behavior until the fixture contract exists.

- Current Phase 12 transparent marker scaffold slice is telemetry/test-only and behavior-preserving. `client/rust_ext/src/lib.rs` reserves `RUMPELMC_GPU_TERRAIN_TRANSPARENT` plus `transparent_requested`, `transparent_active`, and `transparent_fallback` marker fields. `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false` keeps the requested flag inactive, so current runtime behavior remains opaque-only even if the env flag is set. `scripts/gpu_terrain_report.sh` now aggregates the transparent marker fields and metric origins when fresh captures include them. `docs/GPU_TRANSPARENT_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No transparent face buffer, alpha blending, sort policy, shader alpha path, Godot transparent material, block ID, atlas asset, protocol, storage, worldgen, shadow quality, or render behavior changed.
- Fresh transparent fallback artifact: `logs/gpu_transparent_fallback_capture` from `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_transparent_fallback_capture`. It passed with `rust_ext_profile=release`, `shadow_path=godot_proxy`, `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, `smoke_err=0`, `terrain_samples=384`, `ground_hits=9`, `current_chunk_loaded=1`, `current_chunk_submeshes=2`, `current_chunk_collision=2`, `terrain_queue_max_ms=4.140`, `gpu_compositor_submit_max_ms=0.129`, and `process_wall_p95_ms=0.055`. The aggregate report over `logs` resolves max `transparent_requested=1.000`, max `transparent_active=0.000`, and max `transparent_fallback=1.000`, with origins pointing at the fresh marker.
- Checks passed for this transparent marker scaffold slice: source inspection of native-shadow marker patterns in `client/rust_ext/src/lib.rs` and `scripts/gpu_terrain_report.sh`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_transparent_gate_stays_disabled_until_implemented`, `sh -n scripts/gpu_terrain_report.sh`, `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1` release movement smoke above, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-transparent-fallback.txt`, `git diff --check`, `./scripts/check.sh fast` (Rust `110/110`), and `./scripts/diff_guard.sh`.
- Next useful GPU step after checkpointing this slice: add a tiny report/harness assertion for transparent requested/active/fallback marker captures, or start a transparent fixture plan only after a real transparent block/material contract exists. Do not implement blending, sorting, shader alpha, block IDs, atlas assets, protocol, or render behavior from this marker scaffold alone.

- Current Phase 12 transparent terrain path slice is docs-only and behavior-preserving. `docs/GPU_TRANSPARENT_PATH.md` records the rollout for a future transparent terrain path: keep the current opaque pass as default rollback, split render opacity from collision solidity, preserve opaque face visibility next to transparent blocks, render future transparent terrain after opaque terrain with an explicit sort policy, and require requested/active/fallback telemetry plus visual/depth/collision parity and external profiler evidence before default enablement. `docs/GPU_ROADMAP.md`, `docs/GPU_PROFILING.md`, and `docs/GPU_TRENDS.md` link/record the checkpoint. No Rust runtime, shader, Godot scene, atlas, protocol, block ID, storage, worldgen, shadow quality, or render behavior changed.
- Checks passed for this transparent path design slice: `sh scripts/handoff.sh`, OntoIndex `gn_explore` for transparent/opaque terrain entry points, source inspection of `client/rust_ext/src/blocks.rs`, `client/shaders/mesher.glsl`, `client/shaders/gpu_terrain_render.glsl`, `client/rust_ext/src/lib.rs`, and `client/rust_ext/src/gpu_terrain.rs`, `LC_ALL=C rg` non-ASCII check, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add transparent requested/active/fallback marker scaffolding behind an implementation gate, while keeping current opaque rendering inactive for transparency until a fixture, parity gates, and profiler evidence exist.

- Current handoff GPU report freshness slice is behavior-preserving. `scripts/handoff.sh` no longer prints `logs/gpu-terrain-report.txt` as if it were current when the report's recorded commit differs from HEAD; by default it prints a stale-report notice with the report commit and current commit. Setting `RUMPELMC_HANDOFF_REFRESH_GPU_REPORT=1` opts into generating a fresh temporary report during handoff. This avoids misleading continuation snapshots when ignored log artifacts are stale, while keeping ordinary handoff runs lightweight. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this handoff freshness slice: `sh -n scripts/handoff.sh`, standalone fresh report generation to `/tmp/rumpel-gpu-report-handoff-freshness.txt` showing `Git commit: 5b47072`, no profiler results summary, and the pending real-log capture pack with `results_file_status=missing`, default `sh scripts/handoff.sh >/tmp/rumpel-handoff-freshness-default.txt` showing stale report detection for `logs/gpu-terrain-report.txt` (`d270c2a` vs current `5b47072`), `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step remains external Metal/Xcode profiler capture for `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-capture-pack.txt`; locally, do not cite pending capture-pack rows as profiler evidence.

- Current Phase 12 real-log shadow profiler capture-pack slice is artifact-only and behavior-preserving. `scripts/gpu_terrain_shadow_profiler_capture_pack.sh` was run over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` and wrote the ignored local artifacts `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-plan.txt`, `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-results-template.txt`, and `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-capture-pack.txt`. The pack reports `rows=4`, `results_file_status=missing`, and `capture_pack_status=pending_external_profiler`; it is a checklist only, not profiler evidence. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this real-log capture-pack slice: real-log capture pack generation above, `scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-profiler-real-pack.txt` showing no `Selected Shadow Profiler Results Summary` but showing `Selected Shadow Profiler Capture Pack` with `results_file_status=missing` and `capture_pack_status=pending_external_profiler`, negative-path validation confirming the generated `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-results-template.txt` is rejected by `scripts/gpu_terrain_shadow_profiler_results_check.sh` with `results have no external_profile_status rows`, `git status --short --ignored=matching` confirming the generated log artifacts remain ignored under `logs/`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: capture real Metal/Xcode profiler data for the pack rows, write only real captured rows to `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-results.txt`, then run `scripts/gpu_terrain_shadow_profiler_results_check.sh` before citing any profiler numbers.

- Current Phase 12 shadow profiler capture-pack report surfacing slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now includes a `Selected Shadow Profiler Capture Pack` section sourced from the latest `shadow-radius-profiler-capture-pack.txt` under the selected log directory. This is intentionally separate from `Selected Shadow Profiler Results Summary`: a capture pack is pending handoff/checklist state, not external profiler evidence. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this capture-pack report surfacing slice: `sh -n scripts/gpu_terrain_report.sh`, temp report fixture with `shadow-radius-profiler-capture-pack.txt` showing the new selected capture-pack section and no results summary, normal report over `logs` to `/tmp/rumpel-gpu-report-shadow-profiler-current.txt` showing no matching capture-pack artifact yet, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: run the capture pack into the real log directory before external profiling, then fill and validate `shadow-radius-profiler-results.txt` only from real Metal/Xcode capture data.

- Current Phase 12 shadow profiler capture pack slice is behavior-preserving and external-profiler-handoff-only. `scripts/gpu_terrain_shadow_profiler_capture_pack.sh` reads `shadow-radius-profiler-manifest.txt`, regenerates the pending `shadow-radius-profiler-plan.txt`, regenerates the commented TODO `shadow-radius-profiler-results-template.txt`, and writes a `shadow-radius-profiler-capture-pack.txt` checklist with the exact validate/report commands for future real profiler rows. The pack records `capture_pack_status=pending_external_profiler` and is not profiler evidence. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this capture pack slice: `sh -n scripts/gpu_terrain_shadow_profiler_capture_pack.sh`, `sh -n` over the existing profiler plan/template/results scripts, pack generation over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` to `/tmp/rumpel-shadow-profiler-capture-pack.txt` producing `rows=4 capture_pack_status=pending_external_profiler`, negative-path validation confirming the generated `/tmp/shadow-radius-profiler-results-template.txt` is rejected by `scripts/gpu_terrain_shadow_profiler_results_check.sh` with `results have no external_profile_status rows`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: run the capture pack into the real log directory before external profiling, fill `shadow-radius-profiler-results.txt` only from a real Metal/Xcode capture, validate it, and then surface the validated summary through `scripts/gpu_terrain_report.sh`.

- Current Phase 12 shadow profiler results template slice is behavior-preserving and external-profiler-handoff-only. `scripts/gpu_terrain_shadow_profiler_results_template.sh` reads a pending `shadow-radius-profiler-plan.txt` and writes a fillable `shadow-radius-profiler-results-template.txt` whose candidate `external_profile_status=captured` rows are commented and contain `TODO` placeholders. The existing results validator ignores those commented rows and rejects the template until real `profiler_tool`, `profiler_artifact`, and positive `gpu_shadow_pass_ms` values are recorded. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this results template slice: `sh -n scripts/gpu_terrain_shadow_profiler_results_template.sh`, `sh -n` over the existing profiler plan/results scripts, plan generation over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` to `/tmp/rumpel-shadow-profiler-plan.txt` producing `rows=4 usable=3 rejected=1`, template generation to `/tmp/rumpel-shadow-profiler-results-template.txt` producing `summary rows=4 template_status=todo`, negative-path validation confirming `scripts/gpu_terrain_shadow_profiler_results_check.sh` rejects the commented template with `results have no external_profile_status rows`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: fill `shadow-radius-profiler-results.txt` only from a real Metal/Xcode capture, then run `scripts/gpu_terrain_shadow_profiler_results_check.sh` and let `scripts/gpu_terrain_report.sh` surface the validated summary. Do not cite the template rows as evidence.

- Current Phase 12 shadow profiler report surfacing slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now includes a `Selected Shadow Profiler Results Summary` section sourced from the latest `shadow-radius-profiler-results-summary.txt` under the selected log directory. This only surfaces already-validated external profiler summaries; it does not create profiler evidence, capture Metal/Xcode data, or change runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, or production shadow decisions.
- Checks passed for this report surfacing slice: `sh -n scripts/gpu_terrain_report.sh`, normal report over `logs` to `/tmp/rumpel-gpu-report-shadow-profiler-current.txt` showing no matching profiler summary yet, temp report fixture with `shadow-radius-profiler-results-summary.txt` showing the new selected summary section, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), and `./scripts/diff_guard.sh`.
- Next useful GPU step: use this report section only with real validated `shadow-radius-profiler-results-summary.txt` artifacts, not synthetic `/tmp` rows.

- Current Phase 12 shadow profiler results validator slice is behavior-preserving and external-profiler-recording-only. `scripts/gpu_terrain_shadow_profiler_results_check.sh` validates a future `shadow-radius-profiler-results.txt` against `shadow-radius-profiler-plan.txt`: result rows must start with `external_profile_status=captured`, match a planned `priority`/`radius`/`artifact`, include non-placeholder `profiler_tool` and `profiler_artifact`, and include positive `gpu_shadow_pass_ms`. By default all planned rows must be captured; `RUMPELMC_SHADOW_PROFILER_RESULTS_ALLOW_PARTIAL=1` allows partial handoff validation while listing missing rows. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed so far for this results validator slice: `sh -n scripts/gpu_terrain_shadow_profiler_results_check.sh`, synthetic captured results validation against `/tmp/rumpel-shadow-profiler-plan.txt` producing `planned_rows=4 captured_rows=4 missing_rows=0`, negative-path validation rejecting `external_profile_status=pending`, strict partial validation rejecting missing rows, and explicit partial handoff validation with `RUMPELMC_SHADOW_PROFILER_RESULTS_ALLOW_PARTIAL=1` listing `missing_rows=3`.
- Next useful GPU step: run the standard checks, then use this results validator only after a real Metal/Xcode capture has produced actual `profiler_artifact` and `gpu_shadow_pass_ms` values. Do not cite the synthetic `/tmp` result rows as evidence.

- Current Phase 12 shadow profiler plan guard slice is behavior-preserving and external-profiler-handoff-only. `scripts/gpu_terrain_shadow_profiler_plan.sh` reads `shadow-radius-profiler-manifest.txt`, validates required row fields plus referenced artifact paths, requires `profiler_required=external_metal`, sorts rows by priority, and writes a line-oriented `shadow-radius-profiler-plan.txt` with `external_profile_status=pending`. It maps `shadow_normal_total_savings_evidence=usable` to `normal_total_decision=may_cite_after_external_profiler`, `rejected` to `do_not_cite`, and `unknown` to `needs_review`. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, native-shadow gate behavior, and production shadow decisions are unchanged.
- Checks passed for this profiler plan guard slice: `sh -n scripts/gpu_terrain_shadow_profiler_plan.sh`, plan generation over `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` to `/tmp/rumpel-shadow-profiler-plan.txt` producing `rows=4 usable=3 rejected=1`, negative-path manifest validation rejecting invalid `shadow_normal_total_savings_evidence=bad`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: use `/tmp/rumpel-shadow-profiler-plan.txt` or a regenerated `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-plan.txt` as the exact external Metal/Xcode capture checklist. Do not treat any `external_profile_status=pending` row as profiler evidence.

- Current Phase 12 shadow normal-total evidence guard slice is behavior-preserving. `scripts/gpu_terrain_shadow_radius_matrix.sh` now derives `shadow_normal_total_savings_evidence` from `shadow_normal_total_status`: `reduced` becomes `usable`, `increased` becomes `rejected`, and anything else becomes `unknown`. Both `shadow-radius-matrix-summary.txt` and `shadow-radius-profiler-manifest.txt` include this field and a note that `rejected` rows must not be cited as normal-total savings evidence. The wrapper also avoids reusing the function-scoped `summary_path` name for the top-level matrix output, so POSIX `sh` global variable semantics cannot move the matrix summary into the last radius case's child summary path. Report-mode regenerated `logs/gpu_shadow_radius_matrix_wide` without new screenshots; the wide artifact now marks `scene`, radius `2`, and radius `4` as `usable`, while radius `1` is `rejected` because its normal total increased. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Checks passed for this evidence guard slice: `sh -n scripts/gpu_terrain_shadow_radius_matrix.sh`, matrix radius `0` guard check, report-mode regeneration over `logs/gpu_shadow_radius_matrix_wide` producing top-level `shadow-radius-matrix-summary.txt` and `shadow-radius-profiler-manifest.txt`, grep checks for `radius=1` `shadow_normal_total_savings_evidence=rejected` and `scene`/`2`/`4` `usable`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-evidence.txt` with no error patterns, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: use `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` for external Metal profiling; locally, do not cite `radius=1` as normal-total savings evidence unless a future artifact changes its evidence field to `usable`.

- Current Phase 12 shadow-radius profiler manifest slice is behavior-preserving. `scripts/gpu_terrain_shadow_radius_matrix.sh` now writes `shadow-radius-profiler-manifest.txt` next to `shadow-radius-matrix-summary.txt`. The manifest is line-oriented and lists each radius row's priority, profiler role, artifact directory, full/compact screenshot paths, compact marker path, compact proxy counters, visual luma/sample fields, mesh max timings, and `profiler_required=external_metal`. Report-mode regenerated `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` without new screenshots. Priorities are `scene` = `1`/`default_high_proxy`, radius `2` = `2`/`mid_proxy_control`, radius `1` = `3`/`low_proxy_control`, and remaining positive radii = `4`/`extra_proxy_control`. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Checks passed for this manifest slice: `sh -n scripts/gpu_terrain_shadow_radius_matrix.sh`, matrix radius `0` guard check, report-mode regeneration over `logs/gpu_shadow_radius_matrix_wide` producing both `shadow-radius-matrix-summary.txt` and `shadow-radius-profiler-manifest.txt`, `test -s logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-manifest.txt` with no error patterns, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: use the manifest as the capture checklist for external Metal profiling. If continuing locally only, the next safe code slice is to add a small parser/check that refuses to treat `shadow_normal_total_status=increased` rows as savings evidence.

- Current Phase 12 wide shadow-radius matrix capture is behavior-preserving and uses the committed matrix wrapper without code changes. Fresh artifact: `logs/gpu_shadow_radius_matrix_wide/shadow-radius-matrix-summary.txt` from `RUMPELMC_SHADOW_RADIUS_MATRIX_CAPTURE=1 RUMPELMC_SHADOW_RADIUS_MATRIX_RADII="scene 1 2 4" RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_shadow_radius_matrix.sh logs/gpu_shadow_radius_matrix_wide`. It reused the pre-existing server on `25565` and left it running. All rows passed with full/compact `avg_luma=0.2805` and `terrain_samples=472`. Compact row signals: `scene` `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3932`; radius `1` `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`; radius `2` `compact_shadow_proxy=58`, `compact_shadow_normals_saved=1492`; radius `4` `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3932`.
- The wide matrix validates `scene`, `1`, `2`, and `4` as profiler controls, not production shadow-distance choices. Radius `1` again reported `shadow_normal_total_status=increased`, so cross-row/cross-run `normals_total` deltas remain unreliable; use per-marker `compact_shadow_normals_saved`, `compact_shadow_proxy`, visual parity fields, and external Metal profiler data for decisions.
- Checks passed for this wide matrix capture: fresh release `scene 1 2 4` matrix capture above, report-mode regeneration over `logs/gpu_shadow_radius_matrix_wide`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-matrix-wide.txt` with no error patterns, port check confirming the reused server PID `96670` still listens, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step: take external Metal profiler captures for the wide matrix rows, with priority on `scene`, `radius-2`, and `radius-1` as the high/default, middle, and low proxy-load controls. Do not change production shadow behavior from the local proxy counters alone.

- Current Phase 12 shadow-radius matrix wrapper slice is behavior-preserving. `scripts/gpu_terrain_shadow_radius_matrix.sh` now runs or re-reports multiple compact proxy shadow-radius controls into one `shadow-radius-matrix-summary.txt`. It delegates all capture and marker validation to `scripts/gpu_terrain_compact_proxy_benchmark.sh`, accepts `RUMPELMC_SHADOW_RADIUS_MATRIX_RADII` entries of `scene` or positive integers, rejects radius `0` before launch because that remains the benchmark's shadow-disabled control, and supports `RUMPELMC_SHADOW_RADIUS_MATRIX_CAPTURE=0` report-mode validation over existing case directories. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Fresh release matrix artifact: `logs/gpu_shadow_radius_matrix/shadow-radius-matrix-summary.txt` from `RUMPELMC_SHADOW_RADIUS_MATRIX_CAPTURE=1 RUMPELMC_SHADOW_RADIUS_MATRIX_RADII="scene 1" RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_shadow_radius_matrix.sh logs/gpu_shadow_radius_matrix`. It reused the pre-existing server on `25565` and left it running. The `scene` row passed with full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3956`, and `shadow_normal_total_delta=5538` with status `reduced`. The radius `1` row passed with full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`, and `shadow_normal_total_delta=732` with status `reduced`.
- Checks passed for this matrix wrapper slice: `sh -n scripts/gpu_terrain_shadow_radius_matrix.sh`, matrix radius `0` guard check, fresh release `scene 1` matrix capture above, report-mode regeneration over `logs/gpu_shadow_radius_matrix`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius-matrix.txt`, port check confirming the reused server PID `96670` still listens, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: take an external Metal profiler capture against `logs/gpu_shadow_radius_matrix/scene` and `logs/gpu_shadow_radius_matrix/radius-1`, or use the matrix wrapper to add one more positive radius control if the profiler needs a wider sweep. Do not change production shadow distance or start a native-shadow default path from these local CPU/proxy counters alone.

- Current Phase 12 explicit shadow-radius benchmark slice is behavior-preserving. `scripts/gpu_terrain_compact_proxy_benchmark.sh` now accepts `RUMPELMC_COMPACT_PROXY_BENCH_SHADOW_RADIUS` for the full/compact shadow-casting cases, rejects radius `0` because that is the existing shadow-disabled control case, records `shadow_radius_override` in `compact-proxy-benchmark-summary.txt`, and supports `RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1` for intentional captures against an already-running local server without killing it afterward. Runtime defaults, Rust renderer behavior, Godot scene/shadow quality, CPU proxy fallback, and native-shadow gate behavior are unchanged.
- Fresh release radius-1 shadow proxy artifact: `logs/gpu_shadow_proxy_radius1_capture/compact-proxy-benchmark-summary.txt` from `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1 RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1 RUMPELMC_COMPACT_PROXY_BENCH_SHADOW_RADIUS=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_compact_proxy_benchmark.sh logs/gpu_shadow_proxy_radius1_capture`. It reused the pre-existing server on `25565` and left it running. The capture passed with `shadow_radius_override=1`, `gpu_upload_fail=0`, full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`, `mesh_max_ms=3.23`, and `coll_avg_ms=0.51`. The summary also reports `shadow_normal_total_status=increased`, so cross-run `normals_total` deltas are not reliable savings evidence in this low-radius/reuse-server control; use per-marker `compact_shadow_normals_saved` plus external profiler data instead.
- Checks passed for this explicit-radius benchmark slice: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, report-mode validation over `logs/gpu_shadow_proxy_focused_capture`, radius `0` guard check, fresh radius-1 release capture above, report-mode regeneration over `logs/gpu_shadow_proxy_radius1_capture`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-radius1.txt`, port check confirming the reused server PID `96670` still listens, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: either capture the current compact/full proxy workload with an external Metal profiler using the explicit-radius artifact controls, or start the first tiny native-shadow renderer proof only with the existing `godot_proxy` rollback and parity gates intact.

- Current Phase 12 native-shadow fallback parity gate slice is behavior-preserving. `scripts/gpu_terrain_parity_smoke.sh` now normalizes relative output directories to the repo root, prepares the requested Godot Rust extension profile before full captures, requires the matching `rust_ext_profile` in fresh marker files, and adds `gpu-terrain-native-shadow-fallback-parity` as a 14th case. The new case runs with `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1` while `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, so it must keep `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, and `native_shadow_fallback=1`. Validate-only mode remains compatible with older 13-case parity artifact directories.
- Fresh release parity artifact: `logs/gpu_native_shadow_fallback_parity/parity-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=4000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_parity_smoke.sh logs/gpu_native_shadow_fallback_parity`. It passed with `case_count=14`; the fallback marker reports `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `gpu_upload_fail=0`, `gpu_frames=239`, `gpu_subchunks=88`, `gpu_faces=314`, `compact_shadow_proxy=191`, and `compact_collision_proxy=0`. The `pair=native_shadow_fallback` visual comparison against the ordinary GPU marker reports `avg_luma delta=0.0000`, `terrain_luma_range delta=0.0000`, `terrain_samples delta=0`, `terrain_color_buckets delta=0`, and `terrain_chroma_samples delta=0`.
- Checks passed for this fallback parity gate slice: `sh -n scripts/gpu_terrain_parity_smoke.sh`, validate-only over old 13-case `logs/gpu_shader_branchless_corners_parity`, validate-only over fresh 14-case `logs/gpu_native_shadow_fallback_parity`, full release parity run above, marker/summary grep for the fallback triplet, `sips` dimension check for the fallback screenshot (`1280x720`), `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: stop adding native-shadow marker scaffolding unless a renderer proof needs a new invariant; either take an external Metal profiler capture for the current compact proxy path, or start the first tiny native-shadow renderer proof behind the still-false implementation gate with `godot_proxy` rollback and this parity gate intact.

- Current Phase 12 native-shadow proxy-radius contract slice is behavior-preserving. `client/rust_ext/src/lib.rs` now routes Godot shadow-proxy refresh decisions through `terrain_godot_shadow_proxy_chunk_distance`: while `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, the current `godot_proxy` path keeps the same proxy radius; if a future implementation makes `native_shadow_active=true`, `terrain_shadow_path_decision` can report `gpu_native_shadow` while the Godot shadow-proxy refresh radius drops to `0`, so future renderer proof cannot keep building Godot shadow proxies under a native marker. `docs/GPU_SHADOW_PATH.md` and `docs/GPU_TRENDS.md` record the contract. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this proxy-radius contract slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml native_shadow_active_separates_renderer_path_from_godot_proxy_radius`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_shadow_path_keeps_production_and_diagnostic_paths_explicit`, `cargo test --manifest-path client/rust_ext/Cargo.toml cpu_proxy_refresh_tracks_collision_and_shadow_radius_edges`, `git diff --check`, `./scripts/check.sh fast` (Rust `109/109`), `./scripts/diff_guard.sh` (expected Rust extension notice only), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: either take the external Metal profiler capture for the current compact proxy path, or start the first real native renderer experiment behind the still-false implementation gate only after wiring parity/fallback checks to this proxy-radius contract.

- Current Phase 12 native-shadow fallback marker slice is behavior-preserving. `client/rust_ext/src/lib.rs` now emits `native_shadow_fallback=1` only when `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` is requested but the implementation is not active; the current implementation gate remains `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, so even with the env flag set runtime shadow path stays `godot_proxy`. `scripts/gpu_terrain_report.sh` surfaces `native_shadow_fallback` and its metric origin. Fresh env-on release movement stress wrote `logs/gpu_native_shadow_fallback_capture/gpu-terrain-movement-stress.png.txt` and reported `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `gpu_upload_fail=0`, `terrain_queue_work_ms=0.325/0.534/1.340`, `gpu_compositor_submit_ms=0.013/0.018/0.206`, and `process_wall_p95_ms=0.036`. Aggregate report `/tmp/rumpel-gpu-report-native-shadow-fallback.txt` now reports max `native_shadow_requested=1.000`, max `native_shadow_active=0.000`, and max `native_shadow_fallback=1.000` with origins pointing at the fresh marker. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this fallback marker slice so far: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `sh -n scripts/gpu_terrain_report.sh`, `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_fallback_capture`, marker grep for `shadow_path=godot_proxy native_shadow_requested=1 native_shadow_active=0 native_shadow_fallback=1`, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-fallback.txt`.
- Next useful GPU step after checkpointing this slice: stop adding native-shadow marker scaffolding unless a renderer proof needs a new invariant; either take an external Metal profiler capture for the current compact proxy path, or start the first tiny renderer proof behind the still-false implementation gate with `godot_proxy` rollback and parity gates intact.

- Current Phase 12 requested/active native-shadow capture is complete and behavior-preserving. Fresh release movement stress reused the already-running local server on `127.0.0.1:25565` and wrote `logs/gpu_native_shadow_requested_active_capture/gpu-terrain-movement-stress.png.txt`; the marker reports `shadow_path=godot_proxy`, `native_shadow_requested=0`, `native_shadow_active=0`, `shadow_mode=conservative`, `shadow_mesh=compact`, `gpu_upload_fail=0`, `terrain_queue_work_ms=0.421/0.545/1.824`, `gpu_compositor_submit_ms=0.017/0.018/0.368`, and `process_wall_p95_ms=0.036`. Aggregate report `/tmp/rumpel-gpu-report-native-shadow-requested-active-fresh.txt` now resolves max `native_shadow_requested=0.000` and max `native_shadow_active=0.000` with metric origins pointing at that fresh marker instead of `n/a`. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this capture/docs slice: `env -u RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=8000 SMOKE_DELAY_SEC=5.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_native_shadow_requested_active_capture`, marker grep for `shadow_path=godot_proxy native_shadow_requested=0 native_shadow_active=0`, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-requested-active-fresh.txt`.
- Next useful GPU step after checkpointing this capture: either take an external Metal profiler capture for the current compact proxy path, or start a very small renderer proof with `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` still behind `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false` until parity and rollback evidence exist.

- Current Phase 12 GPU-native shadow requested/active telemetry slice is behavior-preserving. `client/rust_ext/src/lib.rs` now splits `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` into `native_shadow_requested` and `native_shadow_active` marker fields; requested reflects the env flag, while active still requires `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=true`, so current runtime behavior stays on `godot_proxy`. `scripts/gpu_terrain_report.sh` surfaces both metrics and metric origins; existing historical logs report them as `n/a` until fresh captures include the new marker fields. `docs/GPU_SHADOW_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this requested/active telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_shadow_path_keeps_production_and_diagnostic_paths_explicit`, `sh -n scripts/gpu_terrain_report.sh`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-native-shadow-requested-active.txt` (new fields present as `n/a` on old logs), `git diff --check`, `./scripts/check.sh fast` (Rust `108/108`), `./scripts/diff_guard.sh` (expected sensitive-path notice only), staged MCP `gn_review_diff` (no high-risk findings; dirty overlay only), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS` in file-scope mode; symbol-name mode is heuristic without graph ids for the new dirty helper), and post-commit OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` plus `bash ./scripts/ontoindex.sh status` (HEAD up-to-date; native graph writer unavailable/non-blocking).
- Follow-up capture for this slice is recorded above; next useful GPU step is either external profiling of the current compact proxy path or a tiny renderer proof with `godot_proxy` rollback intact.

- Current Phase 12 GPU-native shadow marker scaffold is behavior-preserving. `client/rust_ext/src/lib.rs` now reserves `GpuTerrainShadowPath::GpuNativeShadow` and the marker token `shadow_path=gpu_native_shadow`, adds `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW`, and keeps that env flag inert until a future implementation flips `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=true`; current runtime markers therefore stay on `godot_proxy` by default and even with the flag set. `terrain_shadow_path_decision` tests now cover production, disabled, diagnostic, and future native paths plus the implementation gate. `scripts/gpu_terrain_compact_proxy_benchmark.sh` now validates shadow-casting paths through a helper that accepts only `godot_proxy` and future `gpu_native_shadow`, keeping them separate from `scene_shadows_disabled` and `diagnostic_no_shadow_proxy`. `docs/GPU_SHADOW_PATH.md` and `docs/GPU_TRENDS.md` record the checkpoint. No renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed.
- Checks passed for this marker scaffold slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml terrain_shadow_path_keeps_production_and_diagnostic_paths_explicit`, `cargo test --manifest-path client/rust_ext/Cargo.toml gpu_native_shadow_gate_stays_disabled_until_implemented`, `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, report-mode `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=0 sh scripts/gpu_terrain_compact_proxy_benchmark.sh logs/gpu_shadow_proxy_focused_capture`, `git diff --check`, `./scripts/check.sh fast` (Rust `108/108`), `./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`c43171e`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS` in file-scope mode; symbol-name mode is heuristic without graph ids for the new dirty helper).
- Next useful GPU step after checkpointing this slice: add explicit requested-vs-active native-shadow telemetry/report fields, or start a tiny renderer proof only after preserving `godot_proxy` rollback and the existing shadow parity gates.

- Current Phase 12 GPU-native shadow path slice is docs-only and behavior-preserving. `docs/GPU_SHADOW_PATH.md` now records the rollout for a future native terrain shadow path: keep `godot_proxy` as the production default, keep `scene_shadows_disabled` and `diagnostic_no_shadow_proxy` as diagnostics only, require an explicit off-by-default rollback flag for any prototype, reuse the current `SunLight.SHADOW_MAX_DISTANCE` radius and chunk-role refresh rules, keep collision proxies independent, and require visual parity plus an external profiler comparison before default enablement. `docs/GPU_ROADMAP.md`, `docs/GPU_PROFILING.md`, and `docs/GPU_TRENDS.md` link/record the checkpoint. No Rust runtime, shader, Godot scene, shadow quality, capture harness, or artifact-generation behavior changed.
- Checks passed for this design slice: `git diff --check`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`705f6b9`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS` after explicit untracked-doc file override), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: add telemetry/test scaffolding for an off-by-default `gpu_native_shadow` marker path, or take an external Metal profiler capture for the current compact proxy artifact before writing renderer code.

- Current Phase 12 focused shadow proxy capture slice is harness/report-only and behavior-preserving. `scripts/gpu_terrain_compact_proxy_benchmark.sh` now normalizes relative output directories to the repo root, exact-parses metric tokens so fields like `cpu_proxy` are not confused with `max_mesh_cpu_proxy`, writes `compact-proxy-benchmark-summary.txt`, and requires/cleans a free local `25565` server port in capture mode. No renderer, Rust runtime, shadow quality, or production shadow-path behavior changed.
- Fresh focused shadow proxy artifact: `logs/gpu_shadow_proxy_focused_capture/compact-proxy-benchmark-summary.txt` from `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1 RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=10000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_compact_proxy_benchmark.sh logs/gpu_shadow_proxy_focused_capture`. It captured full, compact, shadow-disabled, and collision-only proxy rows. The compact shadow row preserved the same `avg_luma=0.2812` as full, reported `compact_shadow_proxy=189`, `shadow_normals_saved=3158`, and reduced `normals_total` from `49834` to `45472` (`shadow_normal_total_reduction=8.8%`). The shadow-disabled and collision-only rows both used `compact_collision_proxy=13`, with collision normal payload reductions of `1.5%`; all four markers passed benchmark validation and the cleanup check left port `25565` free.
- Checks passed for this focused capture slice: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, report-mode validation over `logs/gpu_terrain_visual_smoke/parity`, fresh capture command above, report-mode regeneration over `logs/gpu_shadow_proxy_focused_capture`, manual `lsof -nP -iTCP:25565 -sTCP:LISTEN` cleanup verification, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-proxy-focused-capture.txt`, `git diff --check`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`447e2d5`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: do not change shadow behavior from these local counters alone; either take an external Metal profiler capture for the focused artifact or design the GPU-native terrain shadow path behind a rollback flag.

- Current Phase 12 shadow proxy report slice is report-only and behavior-preserving. `scripts/gpu_terrain_report.sh` now surfaces existing compact shadow/collision proxy savings counters and metric origins: `compact_shadow_normals_saved`, `compact_collision_proxy`, `compact_collision_normals_saved`, and `fast_proxy`, alongside existing `mesh_shadow_only`, `proxy_shadow`, `proxy_shadow_only`, `compact_shadow_proxy`, and `proxy_refresh_reuse`. No renderer, runtime, shadow-quality, capture, or artifact-generation behavior changed.
- Fresh shadow proxy report artifact: `/tmp/rumpel-gpu-report-shadow-proxy-cost.txt` from `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-proxy-cost.txt`. It reported max `mesh_shadow_only=262`, `proxy_shadow=262`, `proxy_shadow_only=252`, `compact_shadow_proxy=949`, `compact_shadow_normals_saved=1579008`, `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `fast_proxy=949`, and `proxy_refresh_reuse=267`, with no summary error patterns. These are historical cumulative marker maxima, not fresh per-frame shadow-cost measurements.
- Checks passed for this report slice: `sh -n scripts/gpu_terrain_report.sh`, `git diff --check`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shadow-proxy-cost.txt`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`3ee1b22`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` in file-scope mode (`PASS`; symbol-name mode is heuristic without graph ids for shell functions).
- Next useful GPU step after checkpointing this slice: take a fresh focused capture or external profiler measurement before changing shadow behavior; the current slice only makes already-recorded proxy/savings counters visible in the aggregate report.

- Current Phase 11 lighting/depth/shadow contract slice is test-only and behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_lighting_contract_keeps_vertex_lighting_and_fragment_handoff`, which locks the render shader assumption that lighting is computed in the vertex stage from `face_normal(face_idx)` plus sanitized push constants, passed through `lighting_out`/`lighting_in`, and only applied to the sampled atlas texel in the fragment stage with forced opaque alpha. This complements the existing reverse-Z `GREATER_OR_EQUAL` depth-state guard and the existing shadow proxy mode/path tests; no GLSL runtime path, renderer setting, or artifact producer changed.
- Checks passed for this contract slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_lighting_contract_keeps_vertex_lighting_and_fragment_handoff`, `git diff --check`, `./scripts/check.sh fast` (Rust `107/107`), `./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`541cf55`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: stop adding shader hot-path guards unless a new assumption is about to change; either take an external Metal profiler capture for the current branchless shader or move to the next roadmap area with fresh measured evidence.

- Current Phase 11 parity reporting slice is report-only. `scripts/gpu_terrain_parity_smoke.sh` now writes `parity-summary.txt` after successful full or validate-only parity validation. The summary records the output directory, thresholds, all 13 parity cases, per-case smoke/terrain/shadow metadata, and visual metric deltas for `default`, `atlas_depth`, `lighting_shadow`, `lighting_shadow_compact`, and `texture_stand`.
- Fresh parity summary artifact: `logs/gpu_shader_branchless_corners_parity/parity-summary.txt` from `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh scripts/gpu_terrain_parity_smoke.sh /Users/daniil/Documents/RUMPELMC/logs/gpu_shader_branchless_corners_parity`. It passed and reported tight deltas for the existing branchless-corner parity artifacts: `default avg_luma delta=0.0076`, `atlas_depth avg_luma delta=0.0141`, `lighting_shadow avg_luma delta=0.0137`, `lighting_shadow_compact avg_luma delta=0.0000`, and `texture_stand avg_luma delta=0.0075`; all are under the current `max_avg_luma_delta=0.16` threshold.
- Checks passed for this parity-summary slice: `sh -n scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh scripts/gpu_terrain_parity_smoke.sh /Users/daniil/Documents/RUMPELMC/logs/gpu_shader_branchless_corners_parity`, `git diff --check`, `./scripts/check.sh fast` (Rust `106/106`), `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`770d613`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: use `parity-summary.txt` in future Phase 11 lighting/depth/shadow decisions, then either add shader contract guards for lighting/depth assumptions or take an external Metal profiler capture before more shader rewrites.

- Current Phase 11 shader face-corner geometry slice is behavior-preserving. `client/shaders/gpu_terrain_render.glsl` replaces the `face_corner` per-face `if` chain and local `vec3 corners[4]` arrays with 32-entry `FACE_CORNER_BASES`, `FACE_CORNER_EXTENT_X_FACTORS`, and `FACE_CORNER_EXTENT_Y_FACTORS` lookup tables. The lookup still builds each vertex as `base + extent.x * factor + extent.y * factor`, and rows `6/7` preserve the previous front-face fallback for masked nonstandard face indices. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_uses_branchless_face_corner_tables` to guard the table contract and absence of the old local arrays.
- `scripts/gpu_terrain_parity_smoke.sh` now parses marker metrics by exact `key=value` token instead of suffix matching. Full parity exposed this pre-existing harness ambiguity because `collision` could be read from a later token such as `max_array_mesh_collision_bodies=0`; the parser fix is test infrastructure only and does not change runtime behavior.
- Fresh shader corner artifact: `logs/gpu_shader_branchless_corners` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_shader_branchless_corners`. It passed with `smoke_err=0`, `terrain_samples=384`, `terrain_color_buckets=6`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.007`, `gpu_compositor_submit_max_ms=0.212`, `process_wall_p95_ms=0.035`, and `gpu_scene_target_replace=0`. The smoke-started server PID `96079` was stopped and port `25565` was cleared.
- Fresh geometry parity artifact: `logs/gpu_shader_branchless_corners_parity` from `RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_TIMEOUT_SEC=120 GODOT_QUIT_AFTER_FRAMES=4000 SMOKE_DELAY_SEC=4.0 sh scripts/gpu_terrain_parity_smoke.sh /Users/daniil/Documents/RUMPELMC/logs/gpu_shader_branchless_corners_parity`. It passed all 13 CPU/GPU terrain parity cases and produced 26 files (13 PNGs plus 13 markers). A first relative-output attempt wrote under `client/logs/...` and failed path validation; that generated directory was removed before the absolute-output pass. The parity-started server PID `20822` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-corners.txt` selected `logs/gpu_shader_branchless_corners/movement-stress-summary.txt` as the current movement summary and reported no error patterns.
- Checks passed so far for this shader corner slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_uses_branchless_face_corner_tables`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (106/106), `sh -n scripts/gpu_terrain_parity_smoke.sh`, release movement stress, full terrain parity smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-corners.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`dc1324b`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` in file-scope mode (`PASS`; symbol-name mode remains heuristic without graph ids), and MCP `gn_review_diff` staged review (no high-risk symbols; degraded only by dirty overlay/no embeddings metadata).
- Next useful GPU step after checkpointing this slice: with `face_normal`, `face_uv`, and `face_corner` now table-driven, stop broad geometry rewrites unless a profiler points at shader ALU/control flow. Continue Phase 11 with lighting/depth/shadow parity assumptions, shader contract guards, or an external profiler capture of the current branchless shader.

- Current Phase 11 shader UV hot-path slice is behavior-preserving. `client/shaders/gpu_terrain_render.glsl` replaces the `face_uv` branch/local-array chain with a 32-entry `FACE_UV_FACTORS` lookup table indexed by `(face_idx & 7u) * 4u + corner_idx`, then multiplies by the existing face extent. The table preserves the previous UV order for left/right, bottom/top, back, front, and the fallback rows for masked indices `6/7`. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_uses_branchless_face_uv_table` to guard the table and absence of old `vec2 uvs[4]` branches.
- Fresh shader UV artifact: `logs/gpu_shader_branchless_uv` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_shader_branchless_uv`. It passed with `smoke_err=0`, `terrain_samples=384`, `terrain_color_buckets=6`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.265`, `gpu_compositor_submit_max_ms=0.225`, `process_wall_p95_ms=0.050`, and `gpu_scene_target_replace=0`. The smoke-started server PID `2631` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-uv.txt` selected `logs/gpu_shader_branchless_uv/movement-stress-summary.txt` as the current movement summary and reported no error patterns.
- Checks passed so far for this shader UV slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_uses_branchless_face_uv_table`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (105/105), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-uv.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`f332190`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), and MCP `gn_review_diff` staged review (no high-risk symbols; degraded only by staged dirty-overlay/embeddings metadata).
- Next useful GPU step after checkpointing this slice: leave `face_corner` for a separate geometry-focused pass with stronger parity evidence. The UV table removed shader branching without changing atlas layout or fragment sampling, but `face_corner` changes would affect vertex positions directly.

- Current Phase 11 shader hot-path slice is behavior-preserving. `client/shaders/gpu_terrain_render.glsl` replaces the `face_normal` `if` chain with an 8-entry `FACE_NORMALS` lookup table; indices `5`, `6`, and `7` still resolve to `+Z`, preserving the old fallback behavior for masked or unexpected face indices. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_uses_branchless_face_normal_table` to guard the branchless lookup and fallback entries.
- Fresh shader artifact: `logs/gpu_shader_branchless_normals` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_shader_branchless_normals`. It passed with `smoke_err=0`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.417`, `gpu_compositor_submit_max_ms=0.195`, `process_wall_p95_ms=0.050`, `gpu_scene_target_replace=0`, `gpu_uniform_set_create=1`, `gpu_push_constant_avg_bytes=112.0`, and `terrain_samples=384`. The smoke-started server PID `29998` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-normals.txt` selected `logs/gpu_shader_branchless_normals/movement-stress-summary.txt` as the current movement summary and reported no error patterns.
- Checks passed so far for this shader slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_uses_branchless_face_normal_table`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (104/104), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-shader-branchless-normals.txt`, `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`a3761de`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), MCP `gn_test_gap` (`PASS`), and MCP `gn_review_diff` staged review (no high-risk symbols; degraded only by staged dirty-overlay/embeddings metadata).
- Next useful GPU step after checkpointing this slice: continue Phase 11 with shader branch audit for `face_corner` / `face_uv`, but keep any larger geometry/UV table rewrite behind focused parity evidence. The current slice only removed the cheap normal branch chain and did not change lighting inputs or atlas UV math.

- Current Phase 10 binding invariant guard is test-only and behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now has `render_shader_binding_layout_matches_uniform_set_contract`, which locks the render shader contract that set 0 binding 0 is `FaceBuffer`, set 0 binding 1 is the atlas `sampler2D` in both vertex/fragment stages, vertex reads `face_buffer.faces[face_instance]`, and fragment samples `atlas_texture`. No renderer runtime path, quality setting, artifact producer, or telemetry field changed.
- Checks passed so far for this guard slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml render_shader_binding_layout_matches_uniform_set_contract`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (103/103), `./scripts/check.sh fast`, `git diff --check`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`ab54ae6`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this guard: keep the atlas layout in push constants unless a focused benchmark/profiler capture shows the constants phase matters. The current fresh frame-data evidence still shows constants work as tiny (`0.004ms` max constants component), so moving atlas data into immutable uniform data should be justified before changing behavior.

- Current Phase 10 frame-data telemetry slice is behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now counts actual `draw_list_set_push_constant` updates, total pushed bytes, average bytes per update, and exposes the fixed camera/lighting/atlas byte split. `client/rust_ext/src/lib.rs` publishes `gpu_push_constant_updates`, `gpu_push_constant_total_bytes`, `gpu_push_constant_avg_bytes`, `gpu_push_constant_camera_bytes`, `gpu_push_constant_lighting_bytes`, and `gpu_push_constant_atlas_bytes`; movement/workload summaries and the aggregate GPU report surface the same fields.
- Fresh frame-data artifact: `logs/gpu_frame_data_push_constants` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_frame_data_push_constants`. It passed with `gpu_frames=851`, `gpu_push_constant_updates=852`, `gpu_push_constant_total_bytes=95424`, `gpu_push_constant_avg_bytes=112.0`, byte split `gpu_push_constant_camera_bytes=64`, `gpu_push_constant_lighting_bytes=32`, `gpu_push_constant_atlas_bytes=16`, `gpu_scene_target_create=1`, `gpu_scene_target_reuse=850`, `gpu_scene_target_replace=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.463`, `gpu_compositor_submit_max_ms=0.151`, `process_wall_p95_ms=0.045`, `current_chunk_collision=2`, and `ground_misses=0`. The extra push constant update over `gpu_frames` is the one-time offscreen debug draw. The smoke-started server PID `31149` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-frame-data-push-constants.txt` now reports max `gpu_push_constant_updates=852`, max `gpu_push_constant_total_bytes=95424`, max `gpu_push_constant_avg_bytes=112.000`, and byte split `64/32/16`, with Metric Origins pointing to `logs/gpu_frame_data_push_constants/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for this Phase 10 frame-data telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (102/102), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-frame-data-push-constants.txt`, `git diff --check`, `./scripts/check.sh fast`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`c56a746`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: decide whether to move the 16-byte atlas layout out of per-frame push constants into immutable uniform data or leave it until profiler evidence shows constants cost matters. Current measured constants phase stayed tiny (`gpu_compositor_submit_max_parts_ms` constants component `0.004` in the fresh run), so a behavior change should be justified by a focused follow-up, not assumed.

- Current Phase 10 binding/frame-data telemetry slice is behavior-preserving. `client/rust_ext/src/gpu_terrain.rs` now tracks scene target create/reuse/replace counts, immutable uniform-set/atlas texture/sampler creation counts, and last push-constant byte size. `client/rust_ext/src/lib.rs` exposes these as `gpu_scene_target_create`, `gpu_scene_target_reuse`, `gpu_scene_target_replace`, `gpu_uniform_set_create`, `gpu_atlas_texture_create`, `gpu_atlas_sampler_create`, and `gpu_push_constant_bytes`; movement/workload summaries and the aggregate GPU report now surface the same fields.
- Fresh binding-churn artifact: `logs/gpu_binding_churn_movement` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_binding_churn_movement`. It passed with `gpu_scene_target_create=1`, `gpu_scene_target_reuse=852`, `gpu_scene_target_replace=0`, `gpu_uniform_set_create=1`, `gpu_atlas_texture_create=1`, `gpu_atlas_sampler_create=1`, `gpu_push_constant_bytes=112`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.859`, `gpu_compositor_submit_max_ms=0.087`, `process_wall_p95_ms=0.036`, `current_chunk_collision=2`, and `ground_misses=0`. The smoke-started server PID `47199` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-binding-churn.txt` now reports max `gpu_scene_target_create=1`, max `gpu_scene_target_reuse=852`, max `gpu_scene_target_replace=0`, max immutable binding creation counts `1`, and max `gpu_push_constant_bytes=112`, with Metric Origins pointing to `logs/gpu_binding_churn_movement/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the Phase 10 telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_report.sh`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (102/102), release movement stress, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-binding-churn.txt`, `git diff --check`, `./scripts/check.sh fast`, `./scripts/diff_guard.sh`, OntoIndex `bash ./scripts/ontoindex.sh analyze --skip-agents-md --no-stats` and `bash ./scripts/ontoindex.sh status` (`161c950`, up-to-date; native graph writer unavailable/non-blocking), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS` in file-diff mode).
- Next useful GPU step after checkpointing this slice: stay in Phase 10 and inspect per-frame camera/light/constant updates now that scene-target and immutable binding churn are measured as stable. Do not change binding caching unless a fresh artifact shows `gpu_scene_target_replace` or immutable creation counts increasing unexpectedly.

- Current Phase 9 report-headroom slice is report-only. `scripts/gpu_terrain_report.sh` now derives `gpu_draw_cmd_occupancy_pct` and `gpu_draw_cmd_headroom_bytes` from existing `gpu_draw_cmd_bytes` / `gpu_draw_cmd_capacity_bytes` markers, and records the max occupancy source in Metric Origins. No runtime, renderer, workload, or artifact format producer changed.
- Fresh report artifact: `/tmp/rumpel-gpu-report-draw-command-occupancy.txt` from `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-occupancy.txt`. It reports max `gpu_draw_cmd_occupancy_pct=30.298`, min `gpu_draw_cmd_headroom_bytes=91360.000`, and points the occupancy source to `logs/gpu_draw_command_buffer_workload_heavy_radius16/extended/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the report-headroom slice: `sh -n scripts/gpu_terrain_report.sh`, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-occupancy.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh`, OntoIndex reindex/status via `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings` and `bash ./scripts/ontoindex.sh status` (`8bd11dc`, up-to-date, 1,922 nodes / 5,068 edges / 68 clusters / 168 flows), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this report-only slice: move to Phase 10 binding/frame-data metrics, starting with uniform-set/scene-target recreation frequency, because current draw-command occupancy and compositor-submit data do not justify grouped draw submission yet.

- Current Phase 9 follow-up locks the indirect draw command ABI in code. `client/rust_ext/src/gpu_terrain.rs` now documents why the four `DrawIndirectCommand` fields stay present even when `first_vertex` is always `0`, and adds a shader contract test proving the render shader consumes `gl_InstanceIndex` for the face-buffer range and `gl_VertexIndex` only for generated quad corners. This is behavior-preserving and completes the "remove or justify unused draw command fields" audit without changing render output.
- Fresh heavy draw-command telemetry artifact: `logs/gpu_draw_command_buffer_workload_heavy_radius16` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=480 RUMPELMC_WORKLOAD_MATRIX_CASE_SET=heavy RUMPELMC_WORKLOAD_MATRIX_SERVER_VIEW_DISTANCE=16 RUMPELMC_WORKLOAD_MATRIX_CLIENT_KEEP_CHUNK_DISTANCE=16 RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0 sh scripts/gpu_terrain_workload_matrix.sh logs/gpu_draw_command_buffer_workload_heavy_radius16`. It passed all six radius-16 cases with `gpu_upload_fail=0` and `terrain_samples=384`. Matrix maxima were `gpu_draws=2482`, `gpu_draw_cmd_bytes=39712`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `gpu_faces=3256`, `terrain_queue_max_ms=3.781`, `gpu_compositor_submit_max_ms=0.161`, and `process_wall_p95_ms=0.055`. Peak command-buffer occupancy is about 30.3%, so current evidence does not justify grouped draw submission on capacity pressure alone. The smoke-started server PID `23125` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-heavy-radius16.txt` now points `gpu_draw_cmd_bytes=39712` to `logs/gpu_draw_command_buffer_workload_heavy_radius16/extended/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the Phase 9 follow-up: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (102/102), release heavy radius-16 workload matrix, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-heavy-radius16.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh` (expected sensitive-path notice only), OntoIndex reindex/status via `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings` and `bash ./scripts/ontoindex.sh status` (`99fae04`, up-to-date, 1,922 nodes / 5,067 edges / 68 clusters / 168 flows), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this follow-up: avoid grouped draw submission until profiler or heavier evidence shows submit pressure. A smaller next Phase 9 slice could add a draw-command occupancy/headroom guard to reports, or move to the next roadmap phase if no draw-submit bottleneck appears.

- Current draw-submission telemetry slice is behavior-preserving. It adds explicit indirect draw command buffer byte telemetry (`gpu_draw_cmd_bytes`, `gpu_draw_cmd_capacity_bytes`, `gpu_draw_cmd_stride`) plus an ABI/layout unit test for the 16-byte `IndirectDrawCommand`; render behavior, draw count, culling/front-face state, quality settings, and rollback flags are unchanged.
- Changed by this slice so far: `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_movement_stress.sh`, `scripts/gpu_terrain_workload_matrix.sh`, `scripts/gpu_terrain_report.sh`, `docs/AGENT_HANDOFF.md`, and `docs/GPU_TRENDS.md`.
- Fresh draw command telemetry movement artifact: `logs/gpu_draw_command_buffer_telemetry` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_draw_command_buffer_telemetry`. It passed with `gpu_draws=311`, `gpu_draw_cmd_bytes=4976`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `terrain_queue_max_ms=2.093`, `gpu_compositor_submit_max_ms=0.120`, `process_wall_p95_ms=0.044`, `current_chunk_collision=2`, `ground_misses=0`, and `gpu_upload_fail=0`. The smoke-started server was stopped and port `25565` was cleared.
- Fresh draw command telemetry standard workload artifact: `logs/gpu_draw_command_buffer_workload_standard` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=360 RUMPELMC_WORKLOAD_MATRIX_CASE_SET=standard RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_workload_matrix.sh logs/gpu_draw_command_buffer_workload_standard`. It passed `short`, `long`, `long-filled`, and `max-resident` with `gpu_upload_fail=0` and `terrain_samples=384` in all cases. Matrix maxima were `gpu_draw_cmd_bytes=15936`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `gpu_draws=996`, `gpu_faces=1522`, `terrain_queue_max_ms=2.324`, `gpu_compositor_submit_max_ms=0.177`, and `process_wall_p95_ms=0.048`. The smoke-started server PID `32480` was stopped and port `25565` was cleared.
- Fresh aggregate report command `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-buffer-workload.txt` now includes aggregate maxima for `gpu_draw_cmd_bytes`, `gpu_draw_cmd_capacity_bytes`, and `gpu_draw_cmd_stride`, and points `gpu_draw_cmd_bytes=15936` to `logs/gpu_draw_command_buffer_workload_standard/long-filled/gpu-terrain-movement-stress.png.txt`.
- Checks passed so far for the draw command telemetry slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (101/101), `sh -n` for changed GPU shell scripts, release movement telemetry smoke, release standard workload matrix, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-draw-command-buffer-workload.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh` (expected sensitive-path notices only), OntoIndex reindex/status via `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings` and `bash ./scripts/ontoindex.sh status` (`dad073e`, up-to-date, 1,921 nodes / 5,062 edges / 68 clusters / 168 flows), MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`).
- Next useful GPU step after checkpointing this slice: use the explicit draw command buffer byte telemetry to decide whether Phase 9 should target grouped/merged draw submission or only unused command-field cleanup behind a flag. Do not optimize draw submission solely from this telemetry until a heavier resident workload or profiler evidence shows the submit path moving toward budget.

- Current local refactor slice is behavior-preserving Rust/GPU terrain hotspot cleanup. Changed by this slice: `client/rust_ext/src/lib.rs`, `client/rust_ext/src/gpu_terrain.rs`, plus project instruction cleanup in `AGENTS.md` and `docs/PARALLEL_DEVELOPMENT_MONTH_PLAN.md` removing the old `5 files / 300 lines` stop rule at the user's request.
- Rust cleanup details: `GameClient.render_subchunk_mesh` now delegates proxy-refresh reuse, GPU upload, mesh data construction, ArrayMesh surface construction, and mesh-node/collision application to local helpers; `GameClient.get_perf_text` now delegates GPU-terrain perf text construction; `gpu_terrain::build_packed_faces` now uses a face-neighbor offset table instead of six repeated `push_visible_face` blocks. Intended behavior, env flags, GPU/CPU fallback paths, collision, shadows, and perf marker field names/order are unchanged.
- Checks passed for the refactor slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (97/97), `./scripts/check.sh fast`, and `git diff --check`.
- Focused post-refactor GPU smoke passed in `logs/gpu_refactor_smoke` using `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_refactor_smoke`. It reported `smoke_err=0`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_misses=0`, `terrain_queue_max_ms=2.036`, `gpu_compositor_submit_max_ms=0.136`, and `gpu_upload_fail=0`.
- OntoIndex was refreshed at current commit `55b9a07` through `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings`; `bash ./scripts/ontoindex.sh status` reports up-to-date with 1,904 nodes, 5,029 edges, 68 clusters, 167 flows, and semantic query loads the CPU embedding model successfully. Native graph writer remains unavailable, which is non-blocking for this repo.
- Current dirty edge-edit smoke slice extends `scripts/gpu_terrain_block_edit_stress.sh` with optional expected edge/bounds/partial/neighbor-refresh/collision/shadow assertions and adds `scripts/gpu_terrain_edge_block_edit_stress.sh`. The wrapper defaults to a toggle edit at global `127,64,95`, local chunk bounds `31,64,31:31,64,31`, expected edges `pos_x,pos_z`, opt-in `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1`, expected loaded-neighbor refresh of two edge chunks / four subchunks for the last update, and positive collision/shadow/proxy evidence.
- Fresh edge collision/shadow artifact: `logs/gpu_edge_collision_shadow_refresh_stress` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_edge_block_edit_stress.sh logs/gpu_edge_collision_shadow_refresh_stress`. It passed with `block_edit_dirty_observed=1`, `chunk_replace=2`, `dirty_chunks=2`, `dirty_blocks=2`, `dirty_edge_chunks=2`, `dirty_last_edges=pos_x,pos_z`, `dirty_last_bounds=31,64,31:31,64,31`, `dirty_edge_neighbor_chunks=4`, `dirty_edge_neighbor_subchunks=8`, `dirty_last_edge_neighbor_chunks=2`, `dirty_last_edge_neighbor_subchunks=4`, `dirty_partial_chunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_rebuilt=36`, `collision_refresh_last_rebuilt=1`, `mesh_shadow_only=200`, `proxy_shadow=200`, `proxy_shadow_only=190`, `compact_shadow_proxy=602`, `proxy_refresh_reuse=23`, `terrain_queue_max_ms=2.195`, `gpu_compositor_submit_max_ms=0.405`, `gpu_upload_fail=0`, and `ground_misses=0`. The smoke-started local server PID `29771` was stopped manually and port `25565` was cleared.
- Fresh full-vs-partial edge compare artifact: `logs/gpu_edge_dirty_compare` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_edge_dirty_compare.sh logs/gpu_edge_dirty_compare`. It runs the same edge edit once with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` and once with `=1`, requiring a clean port before start and stopping the smoke-started server between runs. The compare passed with matching dirty surface (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, bounds `31,64,31:31,64,31`, edges `pos_x,pos_z`), full control `dirty_partial_subchunks=0`, `dirty_edge_neighbor_subchunks=0`, partial candidate `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `dirty_edge_neighbor_subchunks=8`, both `current_chunk_collision=2`, both `collision_refresh_last_rebuilt=1`, both `proxy_shadow=162`, both `proxy_refresh_reuse=21`, full `terrain_queue_max_ms=2.304`, partial `terrain_queue_max_ms=5.039`, and both `gpu_upload_fail=0`.
- Fresh partial edge repeat artifact: `logs/gpu_edge_dirty_repeat` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_edge_dirty_repeat.sh logs/gpu_edge_dirty_repeat`. It runs the same opt-in partial edge edit three times in fresh server sessions, requires a clean port before start, and stops the smoke-started server after each run. The repeat sample passed with stable dirty surface and neighbor refresh evidence: each run had `dirty_edge_neighbor_subchunks=8`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=21`, and `gpu_upload_fail=0`. Aggregate maxima stayed under the 6.667ms 150 FPS CPU-side budget: `terrain_queue_max_ms_max=4.905`, `gpu_compositor_submit_max_ms_max=0.140`, and `process_wall_p95_ms_max=0.052`.
- Fresh single-edge partial artifact: `logs/gpu_single_edge_block_edit_stress` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_block_edit_stress.sh logs/gpu_single_edge_block_edit_stress`. It covers a non-corner `pos_x` edit at global `127,64,80` with exact edge validation enabled. The smoke passed with `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`, `dirty_edge_neighbor_chunks=2`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_chunks=1`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `compact_shadow_proxy=659`, `proxy_refresh_reuse=21`, `terrain_queue_max_ms=3.494`, `gpu_compositor_submit_max_ms=0.148`, `process_wall_p95_ms=0.052`, `ground_misses=0`, and `gpu_upload_fail=0`. The smoke-started local server PID `92444` was stopped manually and port `25565` was cleared.
- Fresh single-edge full-vs-partial compare artifact: `logs/gpu_single_edge_dirty_compare` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_dirty_compare.sh logs/gpu_single_edge_dirty_compare`. It reuses `scripts/gpu_terrain_edge_dirty_compare.sh` with global edit `127,64,80`, exact expected `dirty_last_edges=pos_x`, and bounds `31,64,16:31,64,16`. The compare passed with matching dirty surface (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`), full control `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, partial candidate `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, both `current_chunk_collision=2`, both `collision_refresh_last_rebuilt=1`, both `proxy_shadow=162`, full `terrain_queue_max_ms=2.222`, partial `terrain_queue_max_ms=3.541`, queue delta `1.319ms`, and both `gpu_upload_fail=0`.
- Fresh single-edge partial repeat artifact: `logs/gpu_single_edge_dirty_repeat` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_dirty_repeat.sh logs/gpu_single_edge_dirty_repeat`. It reuses the now-parameterized `scripts/gpu_terrain_edge_dirty_repeat.sh` with global edit `127,64,80`, exact expected `dirty_last_edges=pos_x`, bounds `31,64,16:31,64,16`, and last-neighbor threshold `1` edge chunk / `2` subchunks. The repeat sample passed three fresh server sessions with stable dirty evidence: every run had `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `proxy_refresh_reuse=23`, and `gpu_upload_fail=0`. Aggregate maxima stayed under the 6.667ms budget: `terrain_queue_max_ms_max=3.059`, `gpu_compositor_submit_max_ms_max=0.140`, and `process_wall_p95_ms_max=0.035`.
- Current default-on partial dirty slice changes `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` from opt-in to default-on. Absence of the env var now enables partial dirty upload; explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` remains the rollback/full-rebuild control. `scripts/gpu_terrain_movement_stress.sh` now forwards default `1`, matching the Rust default.
- Fresh default-on sanity artifact: `logs/gpu_default_partial_dirty_block_edit` from `env -u RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 ... sh scripts/gpu_terrain_block_edit_stress.sh logs/gpu_default_partial_dirty_block_edit` with the single-edge edit `127,64,80`. It passed without the partial env var present, proving the new default path: `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `compact_shadow_proxy=607`, `proxy_refresh_reuse=23`, `terrain_queue_max_ms=1.635`, `gpu_compositor_submit_max_ms=0.284`, `process_wall_p95_ms=0.040`, `ground_misses=0`, and `gpu_upload_fail=0`. The smoke-started local server PID `46591` was stopped manually and port `25565` was cleared.
- Fresh default-on rollback-control artifact: `logs/gpu_single_edge_dirty_compare_default_on` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_single_edge_dirty_compare.sh logs/gpu_single_edge_dirty_compare_default_on`. It passed after the default flip, proving explicit rollback still works: full control with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` had `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `terrain_queue_max_ms=2.217`, and `gpu_upload_fail=0`; partial candidate with `=1` had `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `terrain_queue_max_ms=1.980`, and `gpu_upload_fail=0`. Dirty surface matched in both modes (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`).
- Fresh default-on movement sanity artifact: `logs/gpu_default_partial_movement_stress` from `env -u RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_default_partial_movement_stress`. It passed the normal movement path with wrapper-default partial dirty enabled but no block edit: `block_edit_dirty_observed=0`, `dirty_chunks=0`, `dirty_blocks=0`, `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `current_chunk_loaded=1`, `current_chunk_collision=2`, `ground_misses=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.202`, `gpu_compositor_submit_max_ms=0.147`, `process_wall_p95_ms=0.052`, `gpu_draws=336`, `gpu_faces=637`, `gpu_fragmentation_pct=0.0`, and `gpu_upload_fail=0`. The smoke-started local server PID `30702` was stopped manually and port `25565` was cleared.
- Fresh default-on standard workload artifact: `logs/gpu_default_partial_workload_standard` from `env -u RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=360 RUMPELMC_WORKLOAD_MATRIX_CASE_SET=standard RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_workload_matrix.sh logs/gpu_default_partial_workload_standard`. It passed `short`, `long`, `long-filled`, and `max-resident` under the new wrapper default. All four cases had `gpu_upload_fail=0` and `terrain_samples=384`; maxima across the matrix were `terrain_queue_max_ms=2.196`, `gpu_compositor_submit_max_ms=0.143`, `process_wall_p95_ms=0.055`, `gpu_draws=996`, and `gpu_faces=1522`. The smoke-started local server PID `74314` was stopped manually and port `25565` was cleared.
- Checks passed for the edge collision/shadow slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (98/98), `sh -n` for `scripts/gpu_terrain_block_edit_stress.sh`, `scripts/gpu_terrain_edge_block_edit_stress.sh`, and `scripts/gpu_terrain_report.sh`, release edge collision/shadow smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-edge-collision-shadow.txt`, `./scripts/check.sh fast`, `git diff --check`, `sh ./scripts/diff_guard.sh`, OntoIndex reindex/status, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), and MCP `gn_verify_diff` (`PASS`) with explicit `changedFiles` including the untracked edge wrapper.
- Additional checks passed for the compare slice: `sh -n scripts/gpu_terrain_edge_dirty_compare.sh`, release full-vs-partial compare smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-edge-dirty-compare.txt`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (98/98), `./scripts/check.sh fast`, and `git diff --check`.
- Additional checks passed for the repeat slice: `sh -n` for `scripts/gpu_terrain_block_edit_stress.sh`, `scripts/gpu_terrain_edge_block_edit_stress.sh`, `scripts/gpu_terrain_edge_dirty_compare.sh`, `scripts/gpu_terrain_edge_dirty_repeat.sh`, and `scripts/gpu_terrain_report.sh`; release partial edge repeat smoke; `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-edge-dirty-repeat.txt`; `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`; `cargo test --manifest-path client/rust_ext/Cargo.toml` (98/98); `./scripts/check.sh fast`; and `git diff --check`.
- Additional checks passed for the single-edge slice: `sh -n` for `scripts/gpu_terrain_block_edit_stress.sh`, `scripts/gpu_terrain_single_edge_block_edit_stress.sh`, `scripts/gpu_terrain_edge_block_edit_stress.sh`, `scripts/gpu_terrain_edge_dirty_compare.sh`, `scripts/gpu_terrain_edge_dirty_repeat.sh`, and `scripts/gpu_terrain_report.sh`; release single-edge partial smoke; and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-single-edge.txt`.
- Additional checks passed for the single-edge compare slice: `sh -n scripts/gpu_terrain_edge_dirty_compare.sh`, `sh -n scripts/gpu_terrain_single_edge_dirty_compare.sh`, release single-edge full-vs-partial compare, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-single-edge-compare.txt`.
- Additional checks passed for the single-edge repeat slice: `sh -n scripts/gpu_terrain_edge_dirty_repeat.sh`, `sh -n scripts/gpu_terrain_single_edge_dirty_repeat.sh`, release single-edge partial repeat, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-single-edge-repeat.txt`.
- Additional checks passed so far for the default-on slice: `sh -n` for all changed GPU scripts, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, default-on release single-edge sanity smoke, `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-default-partial-dirty.txt`, `./scripts/check.sh fast` with Rust `99/99`, `git diff --check`, `sh ./scripts/diff_guard.sh`, MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS`) with explicit `changedFiles` including untracked GPU wrappers.
- Additional checks passed for the default-on rollback-control slice: release single-edge full-vs-partial compare after default flip, and `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-default-on-rollback-compare.txt`.
- Additional checks passed for the default-on movement sanity slice: release normal movement stress with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset; release standard workload matrix with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset; `sh scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-default-on-movement.txt`; `cargo test --manifest-path client/rust_ext/Cargo.toml` (99/99); `git diff --check`; `sh ./scripts/diff_guard.sh`; `bash ./scripts/ontoindex.sh analyze --force --skip-agents-md --no-stats --embeddings`; `bash ./scripts/ontoindex.sh status` (up-to-date, 1,911 nodes / 5,039 edges / 67 clusters / 167 flows); MCP `gn_pre_commit_audit` (`READY`, dirty overlay warning only), MCP `gn_verify_diff` (`PASS`), and MCP `gn_test_gap` (`PASS` in file-scope mode; symbol-name mode is heuristic and lacks graph ids for supplied names).
- Next useful GPU step: checkpoint this dirty-update/default-on slice before starting the next roadmap area, or run a heavier radius workload only if more confidence is needed before checkpointing.

- User retired the multi-chat worker plan for now. Continue GPU work locally/sequentially from this HQ chat; do not poll, resume, or coordinate the old worker chats unless the user explicitly re-enables them.
- First parallel worker round was reviewed in HQ and integrated as adapted main commits, not direct worktree merges, because all worker worktrees were detached/dirty and behind current `main`.
- Integrated commits from the worker round: `58543c6` strengthens `scripts/diff_guard.sh` artifact warnings; `f0bbb79` adds world/chunk invariant tests; `5a96759` adds protocol docs plus packet/framing compatibility tests; `ad449e4` documents/tests current RocksDB key invariants; `724dd66` adds Rust client packet length guards and graceful stream-clone failure.
- Do not continue worker edits from the old detached worktrees without an explicit user request and a fresh sync plan. Storage/World/Protocol/Client Rust/Review QA worktrees may still contain their original dirty worker patches, some of which are now obsolete or adapted differently in `main`.
- Client Rust worker was based on an old pre-GPU-resident tree. Its meshing patch was not applied because current `main` already has a different shader/Rust path with capacity guards; only the current-main-safe packet guard and clone handling were integrated.

- Current chunk-streaming stability slice addresses player-visible chunk holes after fast fly-out/fly-back travel. Root cause is client-side immediate chunk unload outside `RUMPELMC_CLIENT_KEEP_CHUNK_DISTANCE` while the server/client reload path is still batch/queue limited; with fly noclip toggled off over a not-yet-rebuilt chunk, the player can fall through missing collision. This is not a worldgen/RocksDB cut by default.
- The client now keeps recently seen chunks for `RUMPELMC_CLIENT_CHUNK_UNLOAD_GRACE_SEC` seconds before unloading them. Default is `20.0`, max clamp is `120.0`, and `0` restores immediate unload behavior for rollback/control runs. Identical resent chunks refresh the residency timestamp without re-enqueueing full geometry rebuilds. This increases transient resident chunk memory during fast travel but does not reduce draw distance, lighting, shadows, texture quality, or visible quality.
- Checks passed for the chunk-unload grace slice: `cargo test --manifest-path client/rust_ext/Cargo.toml` (97/97) and `./scripts/check.sh fast`.
- Current flyback stress slice uses visual-smoke motion `chunk_fly_snap_back` by default. It moves diagonally into negative Z faster than the client's `0.5s` position-send cadence, then snaps back to origin and waits only `0.65s` for one normal position/collision tick. The wrapper is correctness-first: terrain/process/compositor budgets are report-only by default, while final chunk visibility, chunk collision, and a 3x3 downward ground raycast grid around the return point are enforced. Redundant single-ray ground assertions were removed; `ground_hit`/`ground_distance` remain logged as diagnostics.
- Fresh flyback artifact: `logs/gpu_flyback_snap_back_default` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_flyback_stress.sh logs/gpu_flyback_snap_back_default`. It passed with `motion_steps=9`, `motion_chunks=8`, `current_chunk="0,0"`, `current_chunk_loaded=1`, `current_chunk_submeshes=2`, `current_chunk_collision=2`, `ground_samples=9`, `ground_hits=9`, `ground_misses=0`, `ground_max_distance=12.000`, `terrain_samples=378`, `terrain_queue_max_ms=2.257`, `gpu_compositor_submit_max_ms=0.124`, and `gpu_upload_fail=0`. The local server was stopped and port `25565` was cleared.
- Control artifact: `logs/gpu_flyback_snap_back_grace0` from `RUMPELMC_CLIENT_CHUNK_UNLOAD_GRACE_SEC=0 ... sh scripts/gpu_terrain_flyback_stress.sh logs/gpu_flyback_snap_back_grace0` also passed the 3x3 ground grid with `ground_misses=0`, but logs show heavy unload/reload churn and lower residency (`chunks="53 loaded, 42 submeshes, 6 collision"`, `queue_max=192`, `queue_stale=14`, `gpu_draws=102`, `mem=53.0MB`) compared with the default grace run (`chunks="102 loaded, 94 submeshes, 10 collision"`, `queue_max=182`, `queue_stale=2`, `gpu_draws=200`, `mem=102.0MB`).
- Next useful stability step: add a manual/gameplay validation path for the fly-mode safety guard, then continue reducing residency churn without lowering draw distance, lighting, shadows, or texture quality.

- Latest GPU upload-frame telemetry slice adds Rust perf fields `terrain_queue_gpu_uploads=last/avg/max` and `terrain_queue_gpu_upload_kb=last/avg/max` for successful GPU uploads completed during terrain mesh-queue frames. `scripts/gpu_terrain_movement_stress.sh` surfaces those fields in movement summaries, and `scripts/gpu_terrain_report.sh` aggregates them from markers/reports. This is telemetry-only; upload behavior, draw distance, lighting, shadows, and texture quality are unchanged.
- Fresh release movement artifact: `logs/gpu_terrain_queue_upload_frame/movement-stress-summary.txt` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 ./scripts/gpu_terrain_movement_stress.sh logs/gpu_terrain_queue_upload_frame`. It passed with `terrain_queue_gpu_uploads=1/0.84/1`, `terrain_queue_gpu_upload_kb=0.0/0.0/1.5`, `gpu_upload_ms=0.014/0.005/0.136`, `gpu_upload_fail=0`, `gpu_fragmented_free_faces=18`, `gpu_fragmentation_pct=0.0`, `smoke_err=0`, `terrain_queue_max_ms=3.660`, `process_wall_p95_ms=0.037`, and `gpu_compositor_submit_max_ms=0.169`. Visual FPS was display-paced at 60 Hz on macOS/Metal. The local server left on port `25565` after the stress run was stopped.
- Checks passed for the slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (90/90), `sh -n` for the changed GPU scripts, `./scripts/check.sh fast` (required an unsandboxed rerun because Go/sccache cache access was blocked in sandbox), `./scripts/gpu_terrain_report.sh logs /tmp/rumpel-gpu-report-upload-frame-full.txt`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Fresh heavy radius-16 upload artifact: `logs/gpu_terrain_upload_heavy_radius16` from `RUMPELMC_WORKLOAD_MATRIX_CASE_SET=heavy`, `RUMPELMC_WORKLOAD_MATRIX_SERVER_VIEW_DISTANCE=16`, `RUMPELMC_WORKLOAD_MATRIX_CLIENT_KEEP_CHUNK_DISTANCE=16`, `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0`, `GODOT_QUIT_AFTER_FRAMES=36000`, `GODOT_TIMEOUT_SEC=480`, run through `sh scripts/gpu_terrain_workload_matrix.sh ...`. Use `sh scripts/...` for these GPU scripts in this environment if direct `./scripts/...` invocation stalls before script body.
- Heavy radius-16 result: 5/6 cases passed. Successful cases stayed stable at `terrain_queue_gpu_uploads_max=1`, `terrain_queue_gpu_upload_kb_max=1.5`, `gpu_upload_fail=0`, `gpu_upload_ms_max=0.062`, `gpu_effective_draws=1584`, `gpu_faces=2246`, `terrain_queue_max_ms=1.928`, `gpu_compositor_submit_max_ms=0.165`, and `process_wall_p95_ms=0.051`. `extended-filled` reached `current_chunk="11,8"` but failed/hung during screenshot capture; a single-case retry also reached motion completion and then hung post-capture, was killed, and port `25565` was cleared.
- `scripts/gpu_terrain_report.sh` now aggregates `movement_terrain_queue ... max_ms=...` as `terrain_queue_max_ms`, so movement-only heavy artifacts no longer report `n/a` for terrain queue max. Verified report output at `/tmp/rumpel-gpu-report-heavy-radius16-upload-after.txt`.
- Current GPU upload breakdown slice adds marker/report fields for `gpu_upload_encode_ms`, `gpu_upload_stage_ms`, and `gpu_upload_update_ms` as `last/avg/max` triplets. This is telemetry-only and does not change upload behavior.
- Fresh upload-breakdown artifact: `logs/gpu_upload_breakdown_frame` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_upload_breakdown_frame`. It passed with `gpu_upload_ms=0.046/0.006/0.052`, `gpu_upload_encode_ms=0.002/0.000/0.004`, `gpu_upload_stage_ms=0.009/0.001/0.014`, `gpu_upload_update_ms=0.034/0.004/0.043`, `terrain_queue_max_ms=2.042`, `gpu_compositor_submit_max_ms=0.154`, and `gpu_upload_fail=0`. The local server left by the smoke run was stopped and port `25565` was cleared.
- Current dirty-update telemetry slice adds chunk diff counters to the Rust perf marker and unified report: `chunk_initial`, `chunk_replace`, `dirty_chunks`, `dirty_blocks`, changed/rebuild subchunk totals, last dirty masks, bounds, and edge labels. This is telemetry-only; chunk updates still use the existing full-subchunk geometry rebuild path.
- Fresh dirty telemetry artifact: `logs/gpu_dirty_update_telemetry_frame` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_movement_stress.sh logs/gpu_dirty_update_telemetry_frame`. It passed with `chunk_initial=168`, `chunk_replace=0`, `dirty_chunks=0`, `dirty_blocks=0`, `dirty_last_rebuild_subchunks=0`, `terrain_queue_max_ms=2.032`, `gpu_compositor_submit_max_ms=0.170`, and `gpu_upload_fail=0`; no block edits were exercised in this movement-only run. The local server was stopped and port `25565` was cleared.
- Current block-edit stress slice adds an env-driven visual-smoke block edit path plus `scripts/gpu_terrain_block_edit_stress.sh`. The default action is `toggle` at `112,64,80` with block id `1`, so repeated runs return the block to air while still producing a dirty chunk update.
- Fresh block-edit artifact: `logs/gpu_block_edit_stress` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_block_edit_stress.sh logs/gpu_block_edit_stress`. It passed with `block_edit_dirty_observed=1`, `chunk_replace=2`, `dirty_chunks=2`, `dirty_blocks=2`, `dirty_last_blocks=1`, `dirty_last_changed_subchunks=1`, `dirty_last_rebuild_subchunks=2`, `dirty_last_bounds=16,64,16:16,64,16`, `dirty_last_edges=none`, `terrain_queue_max_ms=2.231`, `gpu_compositor_submit_max_ms=0.229`, and `gpu_upload_fail=0`. The local server was stopped and port `25565` was cleared.
- Current partial dirty-upload slice adds opt-in `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1`. When enabled for a dirty chunk replacement, the client queues only the dirty `rebuild_subchunk_mask` plus touched edge-neighbor subchunks; the existing full chunk replacement enqueue path remains the default rollback/control path. Rust perf text and `scripts/gpu_terrain_report.sh` now surface `dirty_partial_chunks`, `dirty_partial_subchunks`, and `dirty_partial_saved_subchunks`.
- Fresh partial dirty artifact: `logs/gpu_partial_dirty_block_edit_retry` from `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1 RUMPELMC_GODOT_RUST_EXT_PROFILE=release RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1 GODOT_QUIT_AFTER_FRAMES=36000 GODOT_TIMEOUT_SEC=300 SMOKE_DELAY_SEC=8.0 sh scripts/gpu_terrain_block_edit_stress.sh logs/gpu_partial_dirty_block_edit_retry`. It passed with `dirty_partial_chunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `terrain_queue_max_ms=2.685`, `process_wall_p95_ms=0.044`, `gpu_compositor_submit_max_ms=0.194`, and `gpu_upload_fail=0`. The local server was stopped and port `25565` was cleared.
- Caveat: `logs/gpu_partial_dirty_block_edit_clean` was a failed clean retry with the same partial flag; visual and dirty counters passed, but `terrain_queue_max_ms=7.514` exceeded the 6.667 ms budget. Treat it as a transient spike/regression sample to watch before making partial dirty upload default.
- Next useful GPU step: add edge-edit smoke coverage for dirty updates near chunk borders, then verify neighbor face refresh before widening partial dirty upload beyond opt-in.

Fresh 2026-06-11 status:

- Long-running GPU backlog is now local/sequential, not delegated to worker chats. `docs/GPU_ROADMAP.md` defines 120 small GPU iterations across stabilization, stress gates, workload scaling, memory/residency, upload, dirty updates, draw submission, bindings, shader hot path, and larger GPU directions. `docs/GPU_PROFILING.md` defines trusted local metrics and macOS/Windows profiler workflows.
- GPU worker chats created during the interrupted delegation attempt were archived and should not be treated as active sources of changes; continue this backlog locally/sequentially from the main HQ thread unless the user explicitly re-enables delegation.
- Completed checkpoints: `2635006` enables GPU terrain back-face culling; `c5dad7f` adds the GPU roadmap/profiling docs. Next sequential step is to add the unified report script from the roadmap measurement phase, then continue with visual/stress gates.
- `scripts/gpu_terrain_report.sh` now aggregates existing GPU terrain summaries and markers into `logs/gpu-terrain-report.txt` by default. It is report-only, scans historical artifacts under the selected log dir, and marks local visual FPS/GPU timestamp caveats explicitly.
- Current marker-visibility slice adds `gpu_cull` and `gpu_front_face` to GPU terrain perf markers, movement-stress summaries, and the unified report's rasterization-state section. Fresh artifact: `logs/gpu_terrain_cull_summary/movement-stress-summary.txt` reports `gpu_cull=back gpu_front_face=clockwise`, `smoke_err=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=4.705`, `gpu_compositor_submit_max_ms=0.866`, and `fps_p05=144.0`. A first short validation run missed screenshot capture with `GODOT_QUIT_AFTER_FRAMES=12000`; rerunning with `36000` passed, though that run reported a very large `post_draw_wait_ms`, so capture-window stability remains worth watching.
- `scripts/gpu_terrain_fill_stress.sh` supports `RUMPELMC_FILL_STRESS_REPORT_ONLY_REPEATS`, intended for heavy cases such as repeat `16`. Normal repeats still fail the script on missing markers or movement-stress errors; report-only repeats write `status=failed` and continue. During validation, an attempted empty-repeat no-op exposed that `${VAR:-default}` ignored an explicitly empty `RUMPELMC_FILL_STRESS_REPEATS`; the script now uses `${VAR-default}` so empty repeats are respected for wrapper tests. The accidental test run was stopped and port `25565` was cleared.
- Fill-stress pass lines now include `status=pass`, `smoke_err`, `terrain_samples`, and `terrain_color_buckets`, so correctness and workload pressure are visible from `fill-stress-summary.txt` without opening the full marker. A normal repeat=1 validation in `logs/gpu_terrain_fill_summary_fields` reached chunk `11,8` but missed screenshot capture; the server was stopped afterward. Report-only failure behavior was validated with `GODOT_TIMEOUT_SEC=1`, producing `repeat=1 status=failed reason=movement_stress_failed` and exit 0 under `/tmp/rumpel_fill_report_only_fail`.
- Fill-stress report-only failure lines now include `run_status` and `marker_present`, making capture/window failures diagnosable directly from `fill-stress-summary.txt`.
- GPU residency telemetry now derives `gpu_fragmented_free_faces` and `gpu_fragmentation_pct` from allocator free ranges. This is report-only; allocator behavior is unchanged. `scripts/gpu_terrain_report.sh` includes both fields in aggregate signals when artifacts contain them.
- `scripts/gpu_terrain_report.sh` now includes a Metric Origins section for max workload/cost fields, so future agents can jump from an aggregate spike to the exact source summary or marker file.
- GPU upload telemetry now reports `gpu_upload_ms=last/avg/max` for successful `upload_subchunk` calls. Upload queue depth/latency was not added because the current code does not have a separate upload queue; adding one would be a broader behavior change.
- `docs/GPU_TRENDS.md` now records durable GPU optimization checkpoints and caveats. Update it after meaningful successful stress/profiling runs instead of burying all trend history in this handoff file.
- `scripts/handoff.sh` now includes GPU roadmap/profiling/trends in Read First and prints existing GPU trends/report snippets when available.

- Current weak-spot slice enables GPU terrain back-face culling by default in `client/rust_ext/src/gpu_terrain.rs`. The rasterization state now uses `PolygonCullMode::BACK` with `PolygonFrontFace::CLOCKWISE`; `RUMPELMC_GPU_TERRAIN_CULL_MODE=disabled|none|off` is a rollback control and `front` is available for diagnostics. This reduces hidden/back-face GPU raster work without reducing draw distance, lighting, shadows, texture quality, or visible quality.
- Fresh correctness artifact: `logs/gpu_terrain_cull_back_smoke/movement-stress-summary.txt` passed radius-16 extended movement with `gpu_upload_fail=0`, `gpu_draws=1582`, `gpu_effective_draws=1582`, `gpu_faces=2244`, `terrain_queue_max_ms=3.152`, `gpu_compositor_submit_max_ms=0.210`, and `smoke_err=0`.
- Fresh fill-stress artifacts: `logs/gpu_terrain_fill_stress_cull_back/fill-stress-summary.txt` and control `logs/gpu_terrain_fill_stress_cull_disabled_control/fill-stress-summary.txt`. In the current macOS session both cull-back and cull-disabled repeat=4 runs report `fps_p05=60.0`, so this run is display/driver-limited and is not a clean fps comparison against the earlier repeat=4 `fps_p05=144.0` result. Error scans were clean and `gpu_upload_fail=0`.
- Next useful weak-spot step: keep culling as a safe default if checks pass, then add a platform-specific GPU profiling path or a stronger synthetic stress/report gate that avoids the current 60 Hz visual limiter. Do not reduce visible quality to chase the 150 FPS target.

- Latest synthetic fill-stress slice adds `RUMPELMC_GPU_TERRAIN_COMPOSITOR_DRAW_REPEAT` (default `1`, clamp `64`) to repeat the same GPU terrain indirect draw inside the compositor pass. This is opt-in and keeps visible terrain quality unchanged while increasing GPU draw/fill pressure. Perf text now reports `gpu_effective_draws` and `gpu_draw_repeat`; movement stress summaries include the same fields. `scripts/gpu_terrain_fill_stress.sh` runs radius-16 extended movement with configurable repeat values.
- Fresh fill-stress artifacts: `logs/gpu_terrain_fill_stress_probe/fill-stress-summary.txt` and `logs/gpu_terrain_fill_stress_repeat8/fill-stress-summary.txt`. At radius-16/extended, `1x` produced `1582` effective draws and `fps_p05=144.0`; `4x` produced `6328` effective draws and still `fps_p05=144.0`; `8x` produced `12656` effective draws and dropped back to `fps_p05=60.0`. CPU-side submit stayed low (`gpu_compositor_submit_max_ms <= 0.148`) and `gpu_upload_fail=0`.
- A manual `16x` run in `logs/gpu_terrain_fill_stress_high/repeat-16.run.log` reached `Visual smoke motion complete` but did not save a screenshot before Godot exited, so treat `16x` as above the current automated smoke window rather than a clean pass. The fill-stress script was fixed so future movement-stress failures are not hidden by `tee`.
- Current conclusion: synthetic compositor overdraw gives a useful GPU/fill pressure knob. The visible automated marker can distinguish `4x` from `8x`, but CPU submit/terrain queue metrics still do not expose GPU cost; next useful step is a report-mode fill-stress gate around `4x/8x` or a platform GPU profiler path for macOS/Windows.

- Latest stress-profiling run pushed the opt-in heavy workload beyond the initial radius-12 slice without code changes. Fresh artifacts: `logs/gpu_terrain_workload_heavy_radius14/workload-matrix-summary.txt` and `logs/gpu_terrain_workload_heavy_radius16/workload-matrix-summary.txt`.
- Radius-14 heavy passed cleanly with peak resident load `1222` GPU draws / `1816` faces, `gpu_upload_fail=0`, worst `terrain_queue_max_ms=2.414`, worst `gpu_compositor_submit_max_ms=0.157`, and worst `process_wall_p95_ms=0.062`.
- Radius-16 heavy passed cleanly with peak resident load `1588` GPU draws / `2246` faces, `gpu_upload_fail=0`, worst `terrain_queue_max_ms=2.421`, worst `gpu_compositor_submit_max_ms=0.221`, and worst `process_wall_p95_ms=0.056`. Error scans were clean and the local server was stopped after each run.
- Current conclusion: within the existing opt-in stress clamp, terrain queue work and GPU compositor submit are not the limiting factor for the 150 FPS target. Local visual FPS is still display/driver-paced around 60 Hz on macOS, and Godot GPU timestamp values still report `0.0us`, so the next useful step is either a platform GPU profiler path or a heavier synthetic draw/fill-rate stress that does not reduce visible quality.

- Latest heavy-radius workload slice adds opt-in stress radius controls without changing default gameplay radius: server `RUMPELMC_SERVER_VIEW_DISTANCE` defaults to `10` and clamps to `16`; client `RUMPELMC_CLIENT_KEEP_CHUNK_DISTANCE` defaults to `10` and clamps to `16`. `scripts/gpu_terrain_workload_matrix.sh` keeps `RUMPELMC_WORKLOAD_MATRIX_CASE_SET=standard` by default, while `case_set=heavy` defaults both stress radii to `12`.
- The same slice adds visual-smoke motion `chunk_walk_extended` ending at chunk `11,8`, plus heavy matrix cases `extended` and `extended-filled`. This raises test pressure without reducing draw distance, lighting, shadows, texture quality, or visible quality; it only asks the server/client to hold more chunks during opt-in stress runs.
- Fresh radius-12 artifact: `logs/gpu_terrain_workload_heavy_radius12/workload-matrix-summary.txt` from `RUMPELMC_WORKLOAD_MATRIX_CASE_SET=heavy`, `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0`, `GODOT_QUIT_AFTER_FRAMES=36000`, `GODOT_TIMEOUT_SEC=420`. It raised resident load from the previous `~628` draws / `~1090` faces to `876` draws / `1402` faces. Worst CPU-side metrics still pass the 150 FPS budget (`6.667ms`): `extended terrain_queue_max_ms=2.405`, `extended gpu_compositor_submit_max_ms=0.213`, `short process_wall_p95_ms=0.067`, and all cases had `gpu_upload_fail=0`. Error scan was clean and the local server was stopped afterward.
- Review note: the local review pass caught an accidental replacement of existing `configuredChunksPerUpdate` tests in `server/pkg/network/server_test.go`; those tests were restored and new view-distance tests were added beside them.
- Next likely optimization step: push the opt-in radius to `14` or `16` only if needed, then optimize the first metric that approaches budget. Current radius-12 results do not justify changing visible quality or rewriting renderer code.

- Latest repeat workload stability slice adds phase logs around visual-smoke capture in `client/main.gd`: capture start, motion completion, post-draw completion, and image-save timing. This diagnosed a repeat-run failure where `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0` made Godot reach `--quit-after 8000` immediately after motion completion, before `RenderingServer.frame_post_draw`; use a larger `GODOT_QUIT_AFTER_FRAMES` for delayed/heavy captures.
- `scripts/gpu_terrain_workload_matrix.sh` now passes `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC` through to movement stress as `SMOKE_DELAY_SEC`. Default remains `5.0`; the fresh heavier repeat used `20.0` plus `GODOT_QUIT_AFTER_FRAMES=20000`.
- Fresh repeat artifact: `logs/gpu_terrain_workload_repeat_2/workload-repeat-summary.txt` from `RUMPELMC_WORKLOAD_MATRIX_REPEAT_COUNT=2`, `RUMPELMC_WORKLOAD_MATRIX_SMOKE_DELAY_SEC=20.0`, `GODOT_QUIT_AFTER_FRAMES=20000`, `GODOT_TIMEOUT_SEC=240`, batch `64`. Repeat summary passed with worst reported values: `long terrain_queue_max_ms=3.791`, `max-resident gpu_compositor_submit_max_ms=0.167`, `long process_wall_p95_ms=0.077`, and `gpu_upload_fail=0` in both per-run matrix summaries. Error scans over repeat logs were clean. The local server process was stopped afterward.
- Next likely optimization step: increase resident/load pressure beyond the current `~628` GPU draws / `~1090` faces before optimizing more code; current repeated batch=64 metrics are still comfortably under the 150 FPS CPU-side budget (`6.667ms`) and the local visual FPS remains display-paced at 60 Hz.

- Latest compositor-submit breakdown slice adds Rust perf fields `gpu_compositor_submit_parts=setup/target/constants/draw` for the last compositor frame and `gpu_compositor_submit_max_parts=setup/target/constants/draw` for the same frame that produced `gpu_compositor_submit_ms` max. Movement stress and workload matrix summaries now print `gpu_compositor_submit_max_parts_ms=setup/target/constants/draw`.
- `scripts/gpu_terrain_workload_matrix.sh` now also supports optional repeat runs via `RUMPELMC_WORKLOAD_MATRIX_REPEAT_COUNT=N`; it writes `workload-repeat-summary.txt` with min/avg/max for `gpu_compositor_submit_max_ms`, `terrain_queue_max_ms`, and `process_wall_p95_ms` per case. Default remains a single run.
- Fresh breakdown artifacts: `logs/gpu_compositor_submit_breakdown/movement-stress-summary.txt` and `logs/gpu_compositor_submit_breakdown_matrix/workload-matrix-summary.txt`. Movement stress passed with `gpu_compositor_submit_max_ms=0.120` and max parts `0.026/0.000/0.028/0.065`; matrix passed with representative max parts like `short 0.023/0.000/0.031/0.089` and `long-filled 0.029/0.000/0.019/0.102`. Error scans were clean.
- Checks for this breakdown slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (84/84), `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_movement_stress.sh`, release movement stress capture, release workload matrix capture with shortened max-resident settle, log error scans, `./scripts/check.sh fast`, and `git diff --check` all passed. The local server process started for captures was stopped afterward.
- Next likely step is to rerun a heavier batch=64 workload with `RUMPELMC_WORKLOAD_MATRIX_REPEAT_COUNT=2` or `3` after starting a fresh server, then inspect whether any repeated submit spike is dominated by setup, target, constants, or draw.

- Latest server streaming test-mode slice adds `RUMPELMC_SERVER_CHUNKS_PER_UPDATE` to the Go server. Default behavior stays unchanged at `6` chunks per update; invalid, zero, or negative env values fall back to the default. `scripts/gpu_terrain_workload_matrix.sh` now passes `RUMPELMC_WORKLOAD_MATRIX_SERVER_CHUNKS_PER_UPDATE` (default `64`) through to Godot/server-started runs and prints `server_reused=0/1` per case so reused already-running servers are obvious.
- Fresh batch-aware workload artifact after rebuilding the local server binary and starting a fresh server: `logs/gpu_terrain_workload_matrix_batch_fresh/workload-matrix-summary.txt`. The short case reports `server_reused=0` and server logs confirm `batch=64`; later cases reuse that fresh server. Batch streaming raises short-route load from the prior `~260` draws to `gpu_draws=536`, while max-resident reaches `gpu_draws=628`, `gpu_faces=1090`, `gpu_upload_fail=0`. Gates still pass: max-resident `terrain_queue_max_ms=1.822`, `process_wall_p95_ms=0.042`, `gpu_compositor_submit_max_ms=0.124`. The long case had one compositor submit spike at `5.277ms`, still under the 150 FPS budget (`6.667ms`) and worth watching in future heavier runs.
- Checks for this server streaming slice: `go test ./pkg/network ./pkg/world`, `sh -n scripts/gpu_terrain_workload_matrix.sh`, release workload matrix capture, matrix log error scan, `./scripts/check.sh fast`, and `git diff --check` all passed.
- The local batch=64 server process was stopped after the capture so a hidden test-mode server is not left running. Next likely step: push the resident set higher than ~630 draws/faces ~1090, or repeat batch=64 with a larger view/load path if upload or compositor submit spikes start approaching budget.

- Latest max-resident workload slice extends `scripts/gpu_terrain_workload_matrix.sh` with a `max-resident` case. It reuses `chunk_walk_long` and a configurable `RUMPELMC_WORKLOAD_MATRIX_MAX_RESIDENT_SETTLE_SEC` defaulting to `30.0`, so the current player radius has more time to fill without reducing draw distance, lighting, shadows, texture quality, or visible quality.
- Fresh max-resident artifact: `logs/gpu_terrain_workload_matrix_max/workload-matrix-summary.txt`. The new case raises simultaneous GPU terrain load to `gpu_draws=634`, `gpu_subchunks=634`, `gpu_faces=1064` with `gpu_upload_fail=0`. It still passes refresh-independent gates: `terrain_queue_max_ms=1.961`, `process_wall_p95_ms=0.045`, `gpu_compositor_submit_max_ms=0.131`; local visual `frame_p95_ms` remains `16.667` due 60 Hz pacing and GPU timestamp max remains `0.0us` on macOS/Metal.
- Checks for this max-resident slice: `sh -n scripts/gpu_terrain_workload_matrix.sh`, `git diff --check`, release workload matrix capture, matrix log error scan, and `./scripts/check.sh fast` all passed.
- Next likely step is to push the resident set higher or make the server/client streaming fill radius faster in test mode, then optimize only if `gpu_compositor_submit_max_ms`, `terrain_queue_max_ms`, or upload failures start moving toward budget.

- Latest workload-scaling slice adds `RUMPELMC_VISUAL_SMOKE_MOTION=chunk_walk_long`, parameterizes `scripts/gpu_terrain_movement_stress.sh` with `RUMPELMC_MOVEMENT_STRESS_MOTION` and `RUMPELMC_MOVEMENT_STRESS_EXPECTED_CHUNK`, and adds `scripts/gpu_terrain_workload_matrix.sh` to compare short, long, and long-filled movement captures without reducing draw distance, lighting, shadows, texture quality, or visible quality.
- Fresh workload matrix artifact: `logs/gpu_terrain_workload_matrix/workload-matrix-summary.txt`. The `long` path alone does not increase simultaneous GPU load because far chunks are unloaded around the current player chunk, but `long-filled` gives the server more settle time at the final chunk and raises GPU terrain load from `short gpu_draws=261 gpu_faces=537` to `long-filled gpu_draws=440 gpu_faces=769`. Under that higher load, real terrain queue/process/submit metrics still pass: `terrain_queue_max_ms=1.737`, `process_wall_p95_ms=0.061`, `gpu_compositor_submit_max_ms=0.144`, `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` due local 60 Hz display/driver pacing, and GPU timestamp max remains `0.0us` on the current macOS/Metal backend.
- Checks for this workload slice: `sh -n scripts/gpu_terrain_workload_matrix.sh`, `sh -n scripts/gpu_terrain_movement_stress.sh`, `git diff --check`, release workload matrix capture, log error scan for matrix artifacts, and `./scripts/check.sh fast` all passed.
- Next likely optimization target is no longer basic compositor submit or terrain queue under 440 draws; use the workload matrix to push to a heavier simultaneous loaded set or add an explicit no-quality-loss stress mode for max resident terrain, then optimize the first metric that grows past budget.

- Latest GPU compositor timestamp probe slice adds report-first Godot `RenderingDevice` timestamp capture around the GPU terrain compositor draw list. Rust perf text now includes `gpu_compositor_gpu_samples`, `gpu_compositor_gpu_ms=last/avg/max`, and `gpu_compositor_gpu_us=last/avg/max`; baseline and movement stress summaries print the same data.
- Timestamp budget enforcement is intentionally opt-in for now: `RUMPELMC_PERF_BASELINE_GPU_TIMESTAMP_BUDGET_MODE=report` and `RUMPELMC_MOVEMENT_STRESS_GPU_TIMESTAMP_BUDGET_MODE=report` are the defaults. On the current macOS/Metal Godot run, timestamp samples are captured but GPU delta is reported as `0.0us`, so this probe must not be treated as a reliable GPU-time gate locally.
- Fresh timestamp artifacts: `logs/gpu_compositor_timestamp_baseline/perf-baseline-summary.txt` and `logs/gpu_compositor_timestamp_us/movement-stress-summary.txt`. Static GPU baseline passes the real terrain queue/process/submit gates with `terrain_queue_max_ms=1.957`, `process_wall_p95_ms=0.043`, `gpu_compositor_submit_max_ms=0.120`, `gpu_compositor_gpu_samples=356`, `gpu_compositor_gpu_max_us=0.0`, and `gpu_upload_fail=0`. Movement stress passes with `terrain_queue_max_ms=1.875`, `process_wall_p95_ms=0.076`, `gpu_compositor_submit_max_ms=0.140`, `gpu_compositor_gpu_samples=673`, `gpu_compositor_gpu_max_us=0.0`, and `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` due local 60 Hz display/driver pacing.
- Checks for this timestamp slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `sh -n scripts/gpu_terrain_perf_baseline.sh && sh -n scripts/gpu_terrain_movement_stress.sh`, `git diff --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (84/84), release movement stress capture, release static baseline capture, and `./scripts/check.sh fast` all passed. Error scans for the fresh timestamp movement and baseline logs had no `ObjectDB`, leaked-instance, panic, script-error, failed, or exceeds matches.
- Next optimization should not trust the local timestamp gate as proof of GPU time. Prefer a workload-scaling stress matrix that increases visible GPU terrain load/draws/faces without reducing quality, or a platform GPU profiler path for Windows/macOS, then optimize whichever metric actually moves under load.

- Latest GPU compositor submit gate slice adds `gpu_compositor_submit=count` and `gpu_compositor_submit_ms=last/avg/max` to Rust perf text, perf baseline summaries, and movement stress summaries. The gate measures CPU callback submit/setup time inside the Godot compositor path; it is not a GPU timestamp query.
- Fresh compositor submit artifacts: `logs/gpu_compositor_submit_baseline/perf-baseline-summary.txt` and `logs/gpu_compositor_submit_gate/movement-stress-summary.txt`. Static GPU baseline passes: `gpu_compositor_submit_avg_ms=0.029`, `gpu_compositor_submit_max_ms=0.121`, `terrain_queue_max_ms=2.017`, `process_wall_p95_ms=0.045`, `gpu_upload_fail=0`. Movement stress passes: `gpu_compositor_submit_avg_ms=0.033`, `gpu_compositor_submit_max_ms=0.128`, `terrain_queue_max_ms=2.051`, `process_wall_p95_ms=0.053`, `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` due local 60 Hz display/driver pacing.

- Latest process-wall gate slice adds refresh-independent `_process` wall-time gates to terrain perf scripts. `scripts/gpu_terrain_perf_baseline.sh` now defaults `RUMPELMC_PERF_BASELINE_PROCESS_WALL_BUDGET_MODE=enforce`; `scripts/gpu_terrain_movement_stress.sh` now defaults `RUMPELMC_MOVEMENT_STRESS_PROCESS_WALL_BUDGET_MODE=enforce` and prints `process_wall_p95_ms` in `movement-stress-summary.txt`.
- Fresh process-wall movement artifact: `logs/terrain_queue_max_parts/movement-stress-summary.txt`. Release GPU movement stress passes both real queue and process-wall gates: `terrain_queue_avg_ms=0.874`, `terrain_queue_max_ms=1.977`, `max_mesh_ms=1.977`, `max_coll_ms=0.000`, `process_wall_p95_ms=0.042`, `budget_status=pass`, `gpu_upload_fail=0`. Visual `frame_p95_ms` remains `16.667` because the current macOS/Metal visual smoke is display/driver paced at 60 Hz.

- Latest real queue breakdown slice adds `terrain_queue_work_max_parts=mesh/collision` to Rust perf text and both terrain perf summaries. This records the mesh and collision portions from the same frame that produced `terrain_queue_work_ms` max, so future optimization targets the real peak instead of the independent `mesh_max + coll_max` pessimistic sum.
- Fresh queue breakdown artifact: `logs/terrain_queue_max_parts/movement-stress-summary.txt`. GPU movement stress passes the 150 FPS real queue budget: `terrain_queue_avg_ms=0.981`, `terrain_queue_max_ms=2.111`, `max_mesh_ms=2.111`, `max_coll_ms=0.000`, `budget_status=pass`, while independent maxima were `mesh_max_ms=4.490` and `coll_max_ms=3.540` in different frames. The fresh marker has `terrain_queue_work_max_parts=2.111/0.000`, `gpu_upload_fail=0`, and no run-log error matches.

- Latest terrain queue budget gate slice makes the real `terrain_queue_work_ms` max enforceable by default in both static and movement terrain perf scripts. `scripts/gpu_terrain_perf_baseline.sh` still keeps visual `frame_p95_ms` in report mode by default because the local automated visual FPS metric is display/driver paced at 60 Hz, but `RUMPELMC_PERF_BASELINE_TERRAIN_QUEUE_BUDGET_MODE=enforce` is now the default for CPU and GPU terrain queue max against the 150 FPS budget. `scripts/gpu_terrain_movement_stress.sh` also defaults to `RUMPELMC_MOVEMENT_STRESS_BUDGET_MODE=enforce`.
- Fresh terrain queue gate artifact: `logs/terrain_queue_budget_gate/movement-stress-summary.txt`. GPU terrain movement stress passes the real queue budget: `terrain_queue_avg_ms=0.945`, `terrain_queue_max_ms=1.921`, `budget_status=pass`, `mesh_max_ms=4.960`, `coll_max_ms=3.890`, `frame_p95_ms=16.667`, `fps_p05=60.0`, `gpu_upload_fail=0`, `current_chunk="3,2"`. Static validate-only baseline against `logs/indexed_cpu_array_mesh` also passes the new queue gate: CPU `terrain_queue_max_ms=2.698`, GPU `terrain_queue_max_ms=1.985`, budget `6.667ms`.

- Latest movement-stress gate slice updates `scripts/gpu_terrain_movement_stress.sh` to normalize relative output dirs to repo-root absolute paths and write `movement-stress-summary.txt` with `terrain_queue_avg_ms`, `terrain_queue_max_ms`, `mesh_*`, `coll_*`, and 150 FPS budget status.
- Fresh movement artifact: `logs/movement_queue_work/movement-stress-summary.txt`. GPU terrain movement stress over `chunk_walk` passes the real queue budget: `terrain_queue_avg_ms=0.934`, `terrain_queue_max_ms=1.936`, `budget_status=pass`, `mesh_max_ms=4.820`, `coll_max_ms=3.840`, `gpu_upload_fail=0`, `current_chunk="3,2"`.
- Checks for this slice: `sh -n scripts/gpu_terrain_movement_stress.sh`, release movement stress capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/movement_queue_work/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest CPU ArrayMesh spike slice changes packed-face CPU fallback meshes to use 4 quad vertices plus 6 indices per unit quad instead of 6 duplicated vertices. UV tiling and triangle order are preserved, and visible quality/draw distance/lighting/shadows are unchanged.
- Fresh release artifact: `logs/indexed_cpu_array_mesh/perf-baseline-summary.txt`. CPU real queue work now passes the 150 FPS budget even at max: `terrain_queue_avg_ms=1.085`, `terrain_queue_max_ms=2.698`, `terrain_queue_budget_status=pass`. The previous CPU ArrayMesh spike was `terrain_queue_max_ms=16.271` with `max_mesh_phase array_mesh_ms=15.89`; after indexing, CPU `max_mesh_phase array_mesh_ms=1.13`, `mesh_max_ms=4.520`, `coll_max_ms=3.520`. GPU remains within real queue budget: `terrain_queue_avg_ms=0.872`, `terrain_queue_max_ms=1.985`.
- Checks for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (82/82), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/indexed_cpu_array_mesh/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest collision-refresh weak-spot slice makes collision refresh build direct packed collision faces from chunk data when cached faces are missing, instead of falling back to Godot `MeshInstance3D.create_trimesh_collision()`. It also adds `collision_refresh_phase_last=faces/clear/create/count/node_counts` and `collision_refresh_phase_max=...` to Rust perf text, and makes the `mesh` perf triplet reflect full `render_subchunk_mesh` job time including ArrayMesh/collision/node work.
- Fresh release artifact: `logs/collision_refresh_direct_faces/perf-baseline-summary.txt`. CPU collision max dropped from the previous `coll_max_ms=23.460` to `coll_max_ms=3.240`; CPU terrain average remains within 150 FPS budget (`avg_ms=1.840`, `budget_status=pass`). The remaining CPU max is now clearly an ArrayMesh surface creation spike: `terrain_queue_max_ms=16.271`, `mesh_max_ms=16.270`, `max_mesh_phase array_mesh_ms=15.89`. GPU remains within real queue budget: `terrain_queue_avg_ms=0.876`, `terrain_queue_max_ms=2.095`.
- Checks for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (80/80), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/collision_refresh_direct_faces/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest CPU fallback weak-spot slice adds `terrain_queue_work_ms=last/avg/max` and `terrain_queue_work_frames` to Rust perf text, and `scripts/gpu_terrain_perf_baseline.sh` now reports `terrain_queue_avg_ms`, `terrain_queue_max_ms`, and `terrain_queue_budget_status`. This gives a real per-frame mesh+collision queue max instead of relying only on independent mesh/collision maxima.
- CPU ArrayMesh fallback now uses the existing packed-face ArrayMesh path by default, with `RUMPELMC_CPU_ARRAY_MESH_PACKED_FACES=0` as a control/rollback switch for old compute/readback meshing. No draw distance, lighting, shadows, texture quality, or visible quality settings were reduced.
- Fresh release artifact: `logs/cpu_packed_faces_baseline/perf-baseline-summary.txt`. CPU terrain work improved from the previous queue-work run `avg_ms=9.340`, `mesh_max_ms=19.660`, `terrain_queue_avg_ms=7.594` to `avg_ms=1.290`, `mesh_max_ms=1.570`, `terrain_queue_avg_ms=1.575`. CPU average now passes the 150 FPS budget, but CPU `terrain_queue_max_ms=23.482` still fails due to collision/ArrayMesh spike (`coll_max_ms=23.460`, `max_array_mesh_phase array_mesh_ms=15.91` in the marker). GPU remains comfortably within real queue budget: `terrain_queue_avg_ms=0.899`, `terrain_queue_max_ms=2.388`.
- Checks for this slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (79/79), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/cpu_packed_faces_baseline/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest terrain spike-smoothing slice runs collision refresh before mesh queue work and skips mesh queue processing in frames where collision actually rebuilt. This keeps near-player collision updates responsive while avoiding stacked mesh+collision terrain work in the same frame.
- `scripts/gpu_terrain_perf_baseline.sh` now normalizes relative output dirs to repo-root absolute paths before launching Godot, matching the pacing matrix behavior and preventing markers from being written under `client/logs/...`.
- The perf baseline summary now reports `max_pessimistic_ms` plus per-component max fields (`mesh_max_ms`, `coll_max_ms`, `draw_rebuild_max_ms`, `draw_patch_max_ms`) instead of a plain ambiguous `max_ms`; the pessimistic max is still a sum of independent component maxima, not a same-frame timing sample.
- Fresh release spike-smoothing artifact: `logs/gpu_terrain_spike_smoothing/perf-baseline-summary.txt`. GPU terrain work: `avg_ms=1.005`, `max_pessimistic_ms=5.561`, `capacity_avg_fps=995.0`, `budget_status=pass`, `max_pessimistic_budget_status=pass` for the 150 FPS budget (`6.667ms`), `mesh_max_ms=1.660`, `coll_max_ms=3.870`. CPU fallback remains over budget: `avg_ms=8.650`, `max_pessimistic_ms=42.400`.
- Checks for the spike-smoothing slice: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (77/77), `sh -n scripts/gpu_terrain_perf_baseline.sh`, release baseline capture, `./scripts/check.sh fast`, `git diff --check`, and `./scripts/diff_guard.sh` all passed. `logs/gpu_terrain_spike_smoothing/run.log` had no `ObjectDB`, leaked-instance, panic, or script-error matches.

- Latest leak-cleanup slice adds explicit visual-smoke shutdown for `GameClient`, detaches the GPU terrain compositor before freeing its RIDs, and makes `ComputeMesher` free its local `RenderingDevice` through `Gd::free()` after releasing compute pipeline/shader RIDs.
- Fresh verbose smoke artifact `logs/gpu_terrain_leak_fix/run.log` no longer reports `ObjectDB instances leaked at exit`; the previous leak was `RenderingDevice` plus two internal `Object` instances from the local compute mesher device lifetime. Release wrapper smoke also passed in `logs/gpu_terrain_leak_fix_release/run.log`.
- Latest weak-spot cleanup adds `scripts/fix_godot_userdata_case.sh`, a safe local maintenance helper for the repeated macOS Godot userdata warning where `project.godot` uses `config/name="rumpelgame"` but the local app userdata directory existed as `RUMPELGAME`.
- The helper renames through a temporary path when both names resolve to the same directory on a case-insensitive filesystem. If both variants are real different directories, it refuses to merge or delete user data automatically.
- `scripts/gpu_terrain_pacing_matrix.sh` now normalizes relative output directories to repo-root absolute paths before launching Godot, so screenshot markers are not accidentally written under `client/`.
- Current pacing investigation slice adds `RUMPELMC_VISUAL_SMOKE_MAX_FPS` and `scripts/gpu_terrain_pacing_matrix.sh`.
- Fresh pacing matrix artifacts: `logs/gpu_terrain_pacing_matrix/gpu-maxfps-30.png.txt`, `gpu-maxfps-0.png.txt`, `gpu-maxfps-150.png.txt`, `gpu-maxfps-240.png.txt`, and `pacing-matrix-summary.txt`.
- Fresh pacing matrix result: `engine_max_fps=30` produces `fps_avg=30.0`, proving the Godot FPS cap works downward. `engine_max_fps=0`, `150`, and `240` all produce `fps_avg=60.0`, `vsync_mode=0`, and `screen_refresh_hz=60.000`, proving the current automated visual FPS metric is capped by display/driver pacing above the 60 Hz screen refresh.
- Godot 4.6.2 command-line help says `--disable-vsync` does not override driver-level V-Sync enforcement. This matches the matrix result and explains why `Engine.max_fps` above refresh is not enough on the current macOS/Metal run.

- Stabilization commit created on `main`: `58ebe46` (`Stabilize GPU terrain perf baseline`). It captured the previous broad GPU terrain/perf slice before starting the next profiler slice.
- Current post-commit dirty slice is intentionally small: `client/main.gd`, `scripts/gpu_terrain_perf_baseline.sh`, and `docs/AGENT_HANDOFF.md`.
- The new slice adds visual-smoke end-to-end profiler marker fields: `process_wall_samples`, `process_wall_avg_ms`, `process_wall_p95_ms`, `process_wall_max_ms`, `post_draw_wait_ms`, `image_read_ms`, `image_save_ms`, and `image_metrics_ms`. The perf baseline summary now prints these fields.
- Fresh release e2e profile artifacts: `logs/gpu_terrain_e2e_profile/cpu-arraymesh-baseline.png.txt` and `logs/gpu_terrain_e2e_profile/gpu-terrain-baseline.png.txt`. Current GPU marker: `fps_avg=60.0`, `process_wall_avg_ms=0.030`, `process_wall_p95_ms=0.044`, `post_draw_wait_ms=31.802`, `screen_refresh_hz=60.000`, `gpu_terrain_work avg_ms=0.976`, `capacity_avg_fps=1024.6`. This points at frame/display pacing rather than GDScript `_process` or terrain update work.

- The committed `58ebe46` slice covered `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `client/shaders/gpu_terrain_render.glsl`, `client/main.gd`, `scripts/gpu_terrain_perf_baseline.sh`, and `scripts/gpu_terrain_movement_stress.sh`.
- The committed GPU terrain slice adds greedy packed-face merging, face extents in the GPU terrain shader, direct/cached collision-face accounting, and collision timing perf records.
- A review pass found and fixed a CPU ArrayMesh fallback cap bug: merged faces can expand into many unit quads for UV parity, so CPU fallback must enforce `MAX_CPU_ARRAY_MESH_VERTICES` against expanded vertices, not packed face count. Guard test added.
- Fresh checks passed: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh`, `./scripts/gpu_terrain_compact_proxy_benchmark.sh`, `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, and `cargo test --manifest-path client/rust_ext/Cargo.toml` with 76/76 tests.
- Fresh release perf baseline passed after building the release Rust GDExtension with `RUMPELMC_GODOT_RUST_EXT_BUILD_RELEASE=1`. Artifacts: `logs/gpu_terrain_perf_baseline/cpu-arraymesh-baseline.png.txt` and `logs/gpu_terrain_perf_baseline/gpu-terrain-baseline.png.txt`.
- Fresh release movement stress passed. Artifact: `logs/gpu_terrain_visual_smoke/movement_stress/gpu-terrain-movement-stress.png.txt`.
- Fresh perf numbers: CPU fallback `frame_p95_ms=12.500`, `fps_p05=80.0`; GPU terrain `frame_p95_ms=8.333`, `fps_p05=120.0`; movement stress GPU `frame_p95_ms=8.333`, `fps_p05=120.0`, `current_chunk="3,2"`, `gpu_upload_fail=0`.
- The 150 FPS target is still not proven. The latest GPU release captures are exactly 120 FPS, which may be a renderer/display cap rather than pure workload limit; the next step should verify or bypass that cap before chasing smaller CPU/GPU optimizations.
- FPS cap investigation added visual-smoke runtime fields to marker logs: `engine_max_fps`, `vsync_mode`, and `screen_refresh_hz`. `RUMPELMC_VISUAL_SMOKE_FORCE_UNCAPPED=1` now forces `Engine.max_fps=0` and `DisplayServer.VSYNC_DISABLED` from `client/main.gd`; the perf baseline and movement stress scripts pass that flag through.
- Fresh forced-uncapped release baseline artifacts: `logs/gpu_terrain_fps_cap/cpu-arraymesh-baseline.png.txt` and `logs/gpu_terrain_fps_cap/gpu-terrain-baseline.png.txt`. Both reported `engine_max_fps=0`, `vsync_mode=0`, `screen_refresh_hz=60.000`, and exactly ~60 FPS (`frame_p95_ms=16.667`). This confirms the current visual smoke FPS metric is display/frame-pacing limited on this run, not a reliable proof of terrain renderer capacity above monitor refresh.
- In the forced-uncapped GPU marker the terrain work itself is light: `mesh_avg_ms=0.82`, `mesh_max_ms=1.26`, `coll_avg_ms=0.16`, `gpu_upload_fail=0`, `gpu_subchunks=132`, `gpu_draws=132`, `gpu_faces=338`.
- `scripts/gpu_terrain_perf_baseline.sh` now writes `logs/gpu_terrain_fps_cap/perf-baseline-summary.txt` with refresh-independent `*_terrain_work` lines. Current derived results: CPU terrain work `avg_ms=9.930`, `capacity_avg_fps=100.7`, `budget_status=fail`; GPU terrain work `avg_ms=0.986`, `capacity_avg_fps=1014.2`, `budget_status=pass` for a 150 FPS budget (`6.667ms`). Treat this as a terrain update/work proxy, not a full end-to-end FPS claim.
- Current post-commit profiler slice passes `./scripts/diff_guard.sh`; the previous 1005-line warning was resolved by committing the broad GPU terrain slice as `58ebe46`.

Goal:

- Continue performance optimization toward stable high FPS without reducing draw distance, lighting, shadows, or visible quality.
- Current technical direction is GPU-resident terrain on Godot RenderingDevice, compatible with macOS and Windows.

Done:

- Added GPU terrain upload/render prototype behind `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Added Godot compositor integration that attaches to the player camera and draws GPU terrain into the scene color target.
- Added GPU terrain texture atlas sampling from `res://assets/textures/blocks/block_texture_atlas.png`.
- Kept the current GPU terrain solid-block pass opaque; atlas alpha is not used for block opacity until transparent block metadata and a dedicated transparency path exist.
- Moved GPU terrain atlas layout from a hard-coded shader column count into Rust-validated atlas metadata pushed to the render shader; the current atlas is validated as 64px tiles, 10 columns, 1 row.
- Made the GPU terrain atlas texture prefer `R8G8B8A8_SRGB` sampling for closer albedo parity with Godot materials, with a guarded fallback to `R8G8B8A8_UNORM` if the backend rejects sRGB sampling.
- Made the opt-in GPU terrain compositor path depth-compatible by requesting resolved depth, binding a color+depth framebuffer, and enabling depth test/write for scene rendering.
- Fixed the GPU compositor depth test for Godot 4.6 reverse-Z by using `GREATER_OR_EQUAL`; `LESS_OR_EQUAL` could produce sky-only screenshots even while GPU draw counters advanced.
- Added a lighting-aware GPU terrain slice: the Rust client reads the scene `SunLight` direction/color/energy, passes it through GPU terrain push constants, and the GPU terrain shader shades block faces from per-face normals with directional diffuse plus ambient.
- Added a conservative shadow-compatibility slice: terrain `MeshInstance3D` shadow casting is explicitly configured as double-sided, so the retained ArrayMesh terrain can serve as the Godot shadow-map proxy while the opt-in GPU RD compositor path handles visible terrain rendering.
- Added an opt-in GPU render bridge where CPU ArrayMesh subchunk nodes switch to Godot `SHADOWS_ONLY` after the GPU compositor is attached and the GPU render pipeline is ready, preserving fallback visibility if GPU render setup fails.
- Added the first safe CPU ArrayMesh reduction step for the opt-in GPU path: after GPU upload, distant subchunks that do not need local collision or a Godot shadow proxy drop their CPU `MeshInstance3D` while keeping the GPU slot intact.
- Added a delayed visible-path transition: CPU fallback/proxy nodes are not hidden or removed until the GPU compositor has produced at least one frame, and a subchunk keeps CPU fallback if its GPU upload did not produce a slot.
- Added a GPU-visible refresh so existing CPU subchunk nodes are re-evaluated once the GPU compositor becomes the confirmed visible terrain path, instead of waiting for later player movement or chunk updates.
- Added a faster CPU shadow/collision proxy path for opt-in GPU terrain: after successful GPU upload and confirmed GPU visible rendering, retained CPU proxy `ArrayMesh` geometry is built directly from `PackedFaceBatch` vertices/normals instead of rerunning the full compute mesher/readback/UV path.
- Added CPU proxy reason counters to the Rust client perf text: `proxy_coll`, `proxy_shadow`, `proxy_both`, and `proxy_shadow_only`. These counters explain why retained CPU `SubchunkMesh_*` nodes still exist in the opt-in GPU terrain path.
- Added opt-in CPU shadow proxy strategy selection through `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MODE`. The default remains `conservative`; `collision_only` drops shadow-only CPU proxies while preserving nearby collision proxies. Rust perf text now includes `shadow_mode=...`.
- Made compact shadow-only CPU proxy meshes the default for GPU-visible shadow-only proxies. `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=full` remains an explicit opt-out/control mode; collision proxies plus CPU fallback meshes stay on the full ArrayMesh path. Rust perf text includes `shadow_mesh=...` and `compact_shadow_proxy=...`.
- Added compact proxy payload measurement to Rust perf text: `compact_shadow_normals_saved=...`, plus `normals last=... total=...`. The parity gate now requires zero saved normals outside compact mode and positive saved normals in the compact shadow-proxy case.
- Extended the parity gate with a compact `lighting_shadow` pose case. It compares compact shadow-only proxy rendering against the full GPU `lighting_shadow` marker using the same visual metric envelopes before compact can be considered for default use.
- Added `scripts/gpu_terrain_compact_proxy_benchmark.sh` as a report-first benchmark helper for compact shadow-only and collision-only proxy payload/runtime metrics. By default it reads the existing parity artifacts; direct Godot captures are opt-in with `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1` because shell-launched Godot wrappers can still stall in the current Codex PTY environment.
- Kept visible CPU fallback behavior unchanged: CPU-only rendering and failed GPU uploads still use the existing compute mesher with UVs/materials.
- Added visual smoke capture hooks in `client/main.gd` through `RUMPELMC_VISUAL_SMOKE_PATH`, `RUMPELMC_VISUAL_SMOKE_DELAY_SEC`, and `RUMPELMC_VISUAL_SMOKE_HIDE_HUD`.
- Strengthened visual smoke validation with `terrain_samples` and `smoke_err`; sky-only frames now fail even if the image is bright and GPU counters are nonzero.
- Extended visual smoke markers with region, color-bucket, chroma, and terrain-luma-range metrics so parity can catch atlas/color/lighting/depth distribution regressions beyond a basic non-sky frame.
- Added multi-pose visual smoke support through `RUMPELMC_VISUAL_SMOKE_POSE=default|atlas_depth|lighting_shadow`; capture waits for `RenderingServer.frame_post_draw` after applying the pose so the screenshot reflects the fixed camera.
- Added `scripts/gpu_terrain_visual_smoke.sh` as a CPU/GPU smoke wrapper; in the current Codex PTY environment direct Godot commands are the reliable gate because the wrapper can stall before project logs appear.
- Added `scripts/gpu_terrain_parity_smoke.sh` as a CPU/GPU/radius=1 parity gate. It validates marker files for `smoke_err=0`, terrain samples, region coverage, atlas/color diversity, terrain luma range, CPU fallback counters, CPU proxy reason counters, GPU counters, fast proxy usage, radius=1 proxy/collision parity, and conservative CPU/GPU visual envelopes across default, atlas/depth, and lighting/shadow poses.
- Extended the parity gate with a GPU `shadow_radius=0` conservative case that validates `shadow_path=scene_shadows_disabled` and keeps retained CPU proxy work collision-only when scene shadows are disabled.
- Extended the parity gate with a `collision_only` GPU case that verifies shadow proxy counters stay at zero while CPU proxy count still matches collision body count.
- Added `RUMPELMC_VISUAL_SMOKE_DISABLE_PLAYER_INPUT=1` for automated visual smokes so player input does not accidentally break/place blocks and mutate the world during capture.
- Added GPU terrain log/perf visibility, including `gpu_frames`.
- Kept ArrayMesh terrain fallback active.
- Added this handoff system: `docs/HANDOFF.md`, `docs/AGENT_HANDOFF.md`, and `scripts/handoff.sh`.

Relevant files:

- `client/rust_ext/src/gpu_terrain.rs`
- `client/rust_ext/src/lib.rs`
- `client/rust_ext/src/player.rs`
- `client/shaders/gpu_terrain_render.glsl`
- `scripts/gpu_terrain_visual_smoke.sh`
- `scripts/gpu_terrain_parity_smoke.sh`
- `scripts/gpu_terrain_compact_proxy_benchmark.sh`
- `logs/gpu_terrain_visual_smoke/direct-cpu.png`
- `logs/gpu_terrain_visual_smoke/direct-cpu.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-cpu-smoke-gate.png`
- `logs/gpu_terrain_visual_smoke/direct-cpu-smoke-gate.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z-proxy-radius1.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-reverse-z-proxy-radius1.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-final.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-final.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-radius1-final.png`
- `logs/gpu_terrain_visual_smoke/direct-gpu-fast-proxy-radius1-final.png.txt`
- `logs/gpu_terrain_visual_smoke/direct-cpu-fast-proxy-final.png`
- `logs/gpu_terrain_visual_smoke/direct-cpu-fast-proxy-final.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/cpu-arraymesh-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/cpu-arraymesh-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-radius1-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-radius1-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-collision-only-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-collision-only-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-shadow-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-shadow-parity.png.txt`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-lighting-shadow-parity.png`
- `logs/gpu_terrain_visual_smoke/parity/gpu-terrain-compact-lighting-shadow-parity.png.txt`
- `logs/godot-gpu-terrain-smoke.log`
- `logs/godot-gpu-terrain-atlas-smoke.log`
- `logs/godot-gpu-terrain-depth-smoke.log`
- `logs/godot-gpu-terrain-lighting-smoke.log`
- `logs/godot-gpu-terrain-shadow-proxy-smoke.log`
- `logs/godot-gpu-terrain-opaque-smoke.log`
- `logs/godot-gpu-terrain-atlas-layout-smoke.log`
- `logs/godot-gpu-terrain-atlas-layout-shadow-proxy-smoke.log`
- `logs/godot-gpu-terrain-srgb-atlas-shadow-proxy-smoke.log`
- `docs/HANDOFF.md`
- `scripts/handoff.sh`

Checks:

- `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the shadow-proxy slice.
- `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the lighting-aware GPU terrain slice.
- `./scripts/check.sh full` passed after the GPU compositor changes.
- `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after atlas/depth work.
- `./scripts/check.sh full` with sccache failed locally with `sccache: error: Operation not permitted (os error 1)`; rerun without sccache passed.
- `cargo fmt -- --check`, `cargo build`, `cargo check`, and `cargo test` passed in `client/rust_ext`; Rust tests: 7/7.
- `cargo build --manifest-path client/rust_ext/Cargo.toml` passed.
- Godot smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot atlas smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot depth smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot lighting smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot shadow-proxy smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot opaque solid-block smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot atlas-layout smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot atlas-layout plus shadow-proxy bridge smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- Godot sRGB atlas plus shadow-proxy bridge smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`; no UNORM fallback warning was observed on the local Metal backend.
- Direct CPU visual smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=0`, screenshot and marker saved under `logs/gpu_terrain_visual_smoke/`.
- Direct GPU visual smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`, screenshot and marker saved under `logs/gpu_terrain_visual_smoke/`; marker included nonzero `gpu_frames`, `gpu_subchunks`, `gpu_draws`, and `gpu_faces`.
- Direct GPU visual smoke passed after the reverse-Z depth fix: `direct-gpu-reverse-z.png`, `terrain_samples=576`, `smoke_err=0`, `gpu_frames=160`, `gpu_subchunks=88`, `gpu_faces=143452`.
- Direct GPU proxy-reduction smoke passed with `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE=1`: `direct-gpu-reverse-z-proxy-radius1.png`, `cpu_proxy=10`, `collision=10`, `gpu_subchunks=92`, `terrain_samples=288`, `smoke_err=0`.
- Direct CPU visual smoke passed after the strengthened visual gate: `direct-cpu-smoke-gate.png`, `terrain_samples=288`, `smoke_err=0`, `current_chunk="0,0"`.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the reverse-Z/fallback-transition/smoke-gate fixes; `golangci-lint` is not installed locally and was skipped by the script.
- Latest Rust fast-proxy checks passed in `client/rust_ext`: `cargo fmt -- --check`, `cargo check`, `cargo test` (10/10), and `cargo build`.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the fast CPU proxy slice; `golangci-lint` is not installed locally and was skipped by the script.
- Direct GPU fast-proxy smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1`: `direct-gpu-fast-proxy-final.png`, `terrain_samples=288`, `smoke_err=0`, `gpu_frames=190`, `gpu_subchunks=90`, `gpu_faces=145506`, `fast_proxy=163`.
- Direct GPU fast-proxy radius smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=1` and `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE=1`: `direct-gpu-fast-proxy-radius1-final.png`, `cpu_proxy=10`, `collision=10`, `fast_proxy=20`, `terrain_samples=288`, `smoke_err=0`, `gpu_frames=195`.
- Direct CPU fallback smoke passed with `RUMPELMC_GPU_TERRAIN_RENDER=0`: `direct-cpu-fast-proxy-final.png`, `terrain_samples=288`, `smoke_err=0`, `fast_proxy=0`, `current_chunk="0,0"`.
- Latest parity smoke artifacts passed on the final Rust build with player input disabled:
  - CPU fallback: `parity/cpu-arraymesh-parity.png`, `terrain_samples=256`, `avg_luma=0.3517`, `smoke_err=0`, `fast_proxy=0`, `collision=10`.
  - GPU terrain: `parity/gpu-terrain-parity.png`, `terrain_samples=256`, `avg_luma=0.3442`, `smoke_err=0`, `gpu_frames=68`, `gpu_subchunks=48`, `gpu_faces=80994`, `fast_proxy=62`, `collision=10`.
  - GPU radius=1: `parity/gpu-terrain-radius1-parity.png`, `terrain_samples=256`, `avg_luma=0.3442`, `smoke_err=0`, `gpu_frames=74`, `cpu_proxy=10`, `collision=10`, `fast_proxy=19`.
- `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the latest parity artifacts.
- `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
- `sh -n scripts/gpu_terrain_visual_smoke.sh` passed after adding wrapper checks for `smoke_err=0` and nonzero `terrain_samples`.
- `scripts/gpu_terrain_visual_smoke.sh` passed `sh -n`, but can time out in this Codex PTY environment before Godot project logs appear. Prefer the direct `/opt/homebrew/bin/timeout ... /usr/bin/env ... /opt/homebrew/bin/godot --path client ...` commands for the current gate.
- `sh -n scripts/handoff.sh` passed.
- `./scripts/handoff.sh` ran successfully and printed the continuation snapshot.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the parity smoke slice; `golangci-lint` is not installed locally and was skipped by the script.
- `./scripts/diff_guard.sh` passed after the dirty-tree cleanup baseline was committed.
- Latest direct parity captures passed after the extended visual metrics slice:
  - CPU fallback: `parity/cpu-arraymesh-parity.png`, `terrain_samples=256`, `terrain_mid_samples=64`, `terrain_bottom_samples=192`, `terrain_left_samples=128`, `terrain_right_samples=128`, `terrain_color_buckets=8`, `terrain_chroma_samples=256`, `terrain_luma_range=0.3655`, `smoke_err=0`, `fast_proxy=0`, `collision=10`.
  - GPU terrain: `parity/gpu-terrain-parity.png`, `terrain_samples=256`, `terrain_color_buckets=8`, `terrain_chroma_samples=256`, `terrain_luma_range=0.3532`, `smoke_err=0`, `gpu_frames=143`, `gpu_subchunks=81`, `gpu_faces=133218`, `fast_proxy=127`, `collision=10`.
  - GPU radius=1: `parity/gpu-terrain-radius1-parity.png`, `terrain_samples=256`, `terrain_color_buckets=8`, `terrain_chroma_samples=256`, `terrain_luma_range=0.3532`, `smoke_err=0`, `gpu_frames=149`, `gpu_subchunks=82`, `gpu_faces=135266`, `cpu_proxy=10`, `collision=10`, `fast_proxy=20`.
- Latest `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the extended parity artifacts.
- Latest `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed after the extended visual metrics slice; `golangci-lint` is not installed locally and was skipped by the script.
- Latest `./scripts/diff_guard.sh` passed on the small 3-file slice before commit.
- Latest proxy-reason counter slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (11/11), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Parity captures were refreshed with the new perf counters and `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed.
  - CPU fallback marker: `cpu_proxy=80`, `proxy_coll=10`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `collision=10`.
  - GPU default marker: `cpu_proxy=82`, `proxy_coll=10`, `proxy_shadow=82`, `proxy_both=10`, `proxy_shadow_only=72`, `collision=10`.
  - GPU radius=1 marker: `cpu_proxy=10`, `proxy_coll=10`, `proxy_shadow=10`, `proxy_both=10`, `proxy_shadow_only=0`, `collision=10`.
- Latest multi-pose parity slice passed:
  - Default CPU/GPU/radius captures were refreshed with `pose="default"`.
  - Atlas/depth pose captures passed: CPU `terrain_samples=510`, `terrain_color_buckets=10`, `terrain_luma_range=0.3378`; GPU `terrain_samples=510`, `terrain_color_buckets=9`, `terrain_luma_range=0.3294`.
  - Lighting/shadow pose captures passed: CPU `terrain_samples=457`, `terrain_color_buckets=8`, `terrain_luma_range=0.3654`; GPU `terrain_samples=453`, `terrain_color_buckets=10`, `terrain_luma_range=0.3570`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed across all seven marker files.
  - `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest shadow-proxy mode slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (12/12), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Fresh direct parity captures were produced with `RUMPELMC_VISUAL_SMOKE_DELAY_SEC=1.5` because the shell wrapper can time out in the current Codex PTY before Godot writes markers.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed across eight marker files.
  - Collision-only marker: `cpu_proxy=10`, `proxy_coll=10`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `shadow_mode=collision_only`, `collision=10`.
  - Conservative radius=1 marker: `cpu_proxy=9`, `proxy_coll=9`, `proxy_shadow=9`, `proxy_both=9`, `proxy_shadow_only=0`, `shadow_mode=conservative`, `collision=9`.
- Latest compact shadow-proxy mesh slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (15/15), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
  - Fresh direct parity captures were produced across nine marker files, including the compact shadow-only proxy case.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the latest parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Compact marker: `cpu_proxy=19`, `proxy_coll=10`, `proxy_shadow=19`, `proxy_both=10`, `proxy_shadow_only=9`, `shadow_mode=conservative`, `shadow_mesh=compact`, `compact_shadow_proxy=9`, `collision=10`, `gpu_frames=32`.
  - Conservative radius=1 marker after rerun with `RUMPELMC_VISUAL_SMOKE_DELAY_SEC=3.0`: `cpu_proxy=10`, `proxy_coll=10`, `proxy_shadow=10`, `proxy_both=10`, `proxy_shadow_only=0`, `shadow_mesh=full`, `compact_shadow_proxy=0`, `gpu_frames=131`.
- Latest compact proxy measurement slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (16/16), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
  - Fresh direct parity captures were produced across nine marker files with the new `compact_shadow_normals_saved` and `normals last/total` perf fields.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the latest parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Compact marker: `cpu_proxy=70`, `proxy_coll=10`, `proxy_shadow=70`, `proxy_both=10`, `proxy_shadow_only=60`, `shadow_mesh=compact`, `compact_shadow_proxy=98`, `compact_shadow_normals_saved=1327104`, `collision=10`, `gpu_frames=145`.
  - Full/default GPU marker: `shadow_mesh=full`, `compact_shadow_proxy=0`, `compact_shadow_normals_saved=0`, `normals last=18432 total=1575360`.
- Latest compact shadow visual parity slice passed:
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` passed.
  - Direct compact lighting/shadow capture passed: `gpu-terrain-compact-lighting-shadow-parity.png`, `pose="lighting_shadow"`, `terrain_samples=453`, `terrain_color_buckets=11`, `terrain_luma_range=0.3570`, `shadow_mesh=compact`, `compact_shadow_proxy=97`, `compact_shadow_normals_saved=1320960`, `collision=10`, `gpu_frames=141`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the full parity artifact set, including the compact-vs-full `lighting_shadow` visual metric pair.
  - `./scripts/check.sh fast` passed; Rust tests remained 16/16.
- Latest compact proxy benchmark helper slice passed:
  - `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh` passed.
  - `./scripts/gpu_terrain_compact_proxy_benchmark.sh` passed in default report mode against current parity artifacts.
  - `./scripts/check.sh fast` passed; Rust tests remained 16/16.
  - Benchmark summary from current artifacts: full `normals_total=1593792`, `mesh_avg_ms=10.18`, `mesh_max_ms=38.49`, `coll_avg_ms=0.73`, `gpu_frames=144`; compact `normals_total=260544`, `compact_shadow_proxy=97`, `compact_shadow_normals_saved=1320960`, `mesh_avg_ms=9.32`, `mesh_max_ms=34.15`, `coll_avg_ms=0.79`, `gpu_frames=141`.
  - Reported `normal_total_delta=1333248` and `normal_total_reduction=83.7%` for the compact `lighting_shadow` marker pair.
- Latest compact-default slice passed:
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (16/16), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `sh -n scripts/gpu_terrain_parity_smoke.sh` and `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh` passed.
  - Direct baseline GPU capture with empty `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=` passed: `shadow_mesh=compact`, `compact_shadow_proxy=95`, `compact_shadow_normals_saved=1284096`, `collision=10`, `gpu_frames=138`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the full parity artifact set; explicit `full` markers remain as control/opt-out cases.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest collision-only render-state slice passed:
  - Collision-only CPU proxies now use `visible=false` and `ShadowCastingSetting::OFF` when a subchunk has a GPU slot but does not need a shadow proxy. CPU fallback meshes stay visible/double-sided if a subchunk is not actually backed by a GPU slot.
  - Rust perf text now reports terrain mesh render-state counters: `mesh_visible`, `mesh_shadow_off`, `mesh_shadow_double`, and `mesh_shadow_only`. The parity gate uses them to catch `collision_only` regressions where retained collision proxies become visible or cast shadows again.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (17/17), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Direct collision-only capture passed after adding render-state counters: `mesh_visible=0`, `mesh_shadow_off=10`, `mesh_shadow_double=0`, `mesh_shadow_only=0`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `collision=10`, `gpu_frames=104`, `terrain_samples=256`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the full parity artifact set.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain render flag safety slice passed:
  - `RUMPELMC_GPU_TERRAIN_RENDER` remains opt-in by default; the Rust decision helper now has a unit test that default/no env stays disabled, explicit true enables, and explicit false disables.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (19/19), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest opaque solid GPU terrain shader guard slice passed:
  - Added a Rust unit test that reads the included GPU terrain render shader and verifies the solid pass forces fragment alpha to `1.0` and does not use `texel.a`. This protects the prior grass/solid block transparency regression.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (20/20), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain shader push-constant layout guard slice passed:
  - Added a Rust unit test that reads the included GPU terrain render shader and verifies the GLSL push-constant order matches the Rust byte layout: clip matrix, lighting block, then atlas layout.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (21/21), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain reverse-Z depth-state guard slice passed:
  - Added a Rust unit test for the pure depth-state config used by the RD render pipeline: scene depth keeps test/write paired with `enable_depth`, and the compare operator stays `GREATER_OR_EQUAL` for Godot 4.6 reverse-Z.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (22/22), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
- Latest GPU terrain shadow-proxy radius slice passed:
  - `GameClient::terrain_shadow_proxy_chunk_distance()` now delegates to a pure helper covered by unit tests. Explicit radius overrides are clamped, finite Godot shadow distance is converted to chunk radius, disabled scene shadows return radius `0`, unavailable scene data uses the conservative default distance, and non-finite distances fall back to `DEFAULT_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE`.
  - This keeps the default conservative shadow-proxy behavior when the `SunLight` cannot be inspected, while avoiding unnecessary shadow-only CPU proxies when Godot shadows are explicitly disabled.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (25/25), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest CPU proxy decision guard slice passed:
  - `GameClient::subchunk_needs_collision()`, `subchunk_needs_shadow_proxy()`, and `subchunk_needs_cpu_proxy()` now delegate to pure helpers covered by unit tests. The tests lock the fallback rule, nearby collision radius, conservative-vs-collision-only shadow mode, shadow radius behavior, and the rule that GPU-visible subchunks without collision or shadow reasons may drop their CPU proxy.
  - The shadow helper now treats `radius <= 0` as no shadow proxy even before `current_player_chunk` is known, matching disabled Godot shadows and avoiding an origin-only shadow proxy in that state.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (28/28), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest CPU proxy refresh guard slice passed:
  - `GameClient::enqueue_cpu_proxy_refresh()` now delegates its per-chunk refresh condition to pure helpers covered by a unit test. The test locks start-up refresh with no previous player chunk, collision radius enter/leave edges, shadow-only refresh outside collision radius, and disabled shadow radius `0`.
  - Behavior is unchanged: the refresh still queues loaded chunks that were or are inside the collision radius or the active conservative shadow-proxy radius.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (29/29), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU attach refresh guard slice passed:
  - `GameClient::refresh_cpu_proxies_after_gpu_attach()` now delegates the list of chunks to refresh to a pure helper covered by a unit test. The test locks the intentional behavior: no refresh before the GPU visible path is active, and all loaded chunks are refreshed after GPU visibility is confirmed.
  - This protects the distant ArrayMesh removal path from a future optimization that only refreshes nearby chunks and leaves old CPU terrain nodes alive after the first GPU frame.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (30/30), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU terrain mesh-build plan guard slice passed:
  - `GameClient::render_subchunk_mesh()` now delegates the CPU mesh path choice to a pure `TerrainMeshBuildPlan` helper covered by a unit test. The test locks the three production-sensitive outcomes: remove the CPU node when uploaded GPU terrain has no CPU proxy reason, build a packed-face CPU proxy only after the GPU visible path is active and packed faces are available, and fall back to the full ArrayMesh path otherwise.
  - This preserves the ArrayMesh fallback for failed GPU uploads, pre-confirmation GPU state, and missing packed-face data while making future distant CPU mesh removal changes easier to review.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (31/31), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU terrain upload flag guard slice passed:
  - `gpu_terrain_upload_enabled()` now delegates its decision to a pure helper covered by a unit test. The test locks that upload stays off by default, explicit upload enables it, and opt-in GPU render also enables upload so visible GPU terrain cannot run without GPU face uploads.
  - This preserves the current invariant that `RUMPELMC_GPU_TERRAIN_RENDER` itself remains opt-in while keeping the render/upload dependency explicit and reviewable.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (32/32), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest GPU terrain visible activation guard slice passed:
  - `GameClient::gpu_terrain_visible_render_active()` now delegates its decision to a pure helper covered by a unit test. The test locks the delayed transition invariant: CPU fallback/proxy nodes are only allowed to switch after the render flag is enabled, the compositor is attached, and the GPU terrain buffer pool has confirmed at least one visible render frame.
  - This protects the visible-path transition from hiding or removing ArrayMesh fallback too early when GPU setup is only partially complete.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (33/33), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest terrain mesh render-state counter guard slice passed:
  - `NodePerfCounts::record_mesh_render_state()` now delegates the visible/shadow bucket updates to a pure helper covered by a unit test. The test locks the `mesh_visible`, `mesh_shadow_off`, `mesh_shadow_double`, and `mesh_shadow_only` buckets that the parity gate uses for collision-only and proxy render-state regressions.
  - Behavior is unchanged for real `MeshInstance3D` nodes; the helper only makes the counter mapping reviewable without constructing Godot scene nodes in unit tests.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (34/34), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest terrain proxy-reason counter guard slice passed:
  - `NodePerfCounts` now records CPU proxy reason buckets through a pure `record_cpu_proxy_reasons()` helper covered by a unit test. The test locks `proxy_coll`, `proxy_shadow`, `proxy_both`, and `proxy_shadow_only` mapping for collision-only, shadow-only, both, and no-reason cases.
  - Behavior is unchanged for real terrain nodes; `GameClient::current_node_perf_counts()` still derives the same collision and shadow proxy reasons before recording the counters.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (35/35), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh` passed against the current parity artifacts.
- Latest terrain shadow-path visibility slice passed:
  - Rust perf text now includes `shadow_path=...`, with a pure `terrain_shadow_path_decision()` helper covered by a unit test. The values make the current shadow strategy explicit: `arraymesh` for CPU/fallback terrain, `godot_proxy` for the conservative GPU-visible production path, `scene_shadows_disabled` when scene shadows resolve to radius `0`, and `diagnostic_no_shadow_proxy` for `collision_only`.
  - `scripts/gpu_terrain_parity_smoke.sh` now requires the expected `shadow_path` in CPU, GPU conservative, compact proxy, radius, multi-pose, and collision-only markers. This keeps `collision_only` visibly diagnostic and protects the current Godot shadow-proxy production path from accidental removal.
  - Behavior is unchanged: GPU terrain render remains opt-in, conservative Godot shadow proxies remain active, and ArrayMesh fallback remains intact.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (36/36), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Fresh direct parity captures passed across all ten marker files; examples include `shadow_path=arraymesh` for CPU fallback, `shadow_path=godot_proxy` for conservative GPU cases, and `shadow_path=diagnostic_no_shadow_proxy` for the collision-only diagnostic case.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh` passed against the fresh artifacts. Direct executable wrapper launch can still stall in this Codex PTY environment, matching the known wrapper issue; use explicit `sh` or direct Godot commands for the reliable local gate.
- Latest collision-only compact proxy slice passed:
  - Collision-only CPU proxy meshes now use the same vertex-only compact payload as compact shadow-only proxies. This applies only when a GPU-visible proxy needs collision and does not need a Godot shadow proxy; conservative collision+shadow proxies and ArrayMesh fallback stay on the full mesh path.
  - Rust perf text now includes `compact_collision_proxy=...` and `compact_collision_normals_saved=...`. `fast_proxy` remains the total packed-face CPU proxy counter, while the new compact collision counters make the collision-only diagnostic payload reduction explicit.
  - `scripts/gpu_terrain_parity_smoke.sh` now requires zero compact-collision payload in CPU fallback and conservative shadow-proxy cases, and requires `compact_collision_proxy == fast_proxy` in the `collision_only` diagnostic marker.
  - Rust: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (37/37), and `cargo build --manifest-path client/rust_ext/Cargo.toml`.
  - Full check: `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full` passed; `golangci-lint` is not installed locally and was skipped by the script.
  - Fresh direct parity captures passed across all ten marker files. The collision-only marker included `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `fast_proxy=20`, `mesh_visible=0`, `mesh_shadow_off=10`, and `shadow_path=diagnostic_no_shadow_proxy`.
  - `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh` passed against the fresh artifacts.
- Latest compact proxy benchmark report slice passed:
  - `scripts/gpu_terrain_compact_proxy_benchmark.sh` now validates and reports the new collision-only compact proxy counters alongside compact shadow proxy counters. Conservative full/compact rows are required to keep `compact_collision_proxy=0`, while the collision-only diagnostic row requires `compact_collision_proxy == fast_proxy`.
  - The benchmark summary now prints `compact_collision_proxy`, `collision_normals_saved`, `shadow_normal_total_reduction`, and `collision_normal_payload_reduction`.
  - Checks passed: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/check.sh fast` (Rust tests 37/37), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, and `git diff --check`.
  - Current artifact report: full `normals_total=1243584`, compact `normals_total=260544`, collision-only `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `shadow_normal_total_reduction=79.0%`, and `collision_normal_payload_reduction=71.5%`.
- Latest shadow-disabled parity guard slice passed:
  - `scripts/gpu_terrain_parity_smoke.sh` now captures and validates `gpu-terrain-shadow-disabled-parity` with `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_CHUNK_DISTANCE=0`, conservative mode, and `shadow_mesh=full`.
  - The new marker is required to report `shadow_path=scene_shadows_disabled`, `proxy_shadow=0`, `proxy_both=0`, `proxy_shadow_only=0`, `mesh_visible=0`, and `mesh_shadow_off == cpu_proxy`, while keeping `compact_collision_proxy == fast_proxy`.
  - Direct Godot capture passed for the new marker: `cpu_proxy=10`, `collision=10`, `compact_collision_proxy=20`, `compact_collision_normals_saved=192336`, `fast_proxy=20`, `mesh_shadow_off=10`, `gpu_frames=172`, and `terrain_samples=256`.
  - Checks passed: `sh -n scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `./scripts/check.sh fast` (Rust tests 37/37), and `git diff --check`.
- Latest compact proxy benchmark shadow-disabled report slice passed:
  - `scripts/gpu_terrain_compact_proxy_benchmark.sh` now validates and reports the production-safe `shadow_path=scene_shadows_disabled` marker alongside the diagnostic `collision_only` marker.
  - Report mode now prints a `shadow_disabled` row and separate collision normal payload reductions for `shadow_disabled` and `collision_only`, so disabled-scene-shadow savings are visible without treating `collision_only` as production behavior.
  - Checks passed: `sh -n scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/gpu_terrain_compact_proxy_benchmark.sh`, `./scripts/check.sh fast` (Rust tests 37/37), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, and `git diff --check`.
  - Current artifact report: `shadow_disabled compact_collision_proxy=20`, `shadow_disabled compact_collision_normals_saved=192336`, `shadow_disabled_collision_normal_payload_reduction=72.1%`; `collision_only_collision_normal_payload_reduction=71.5%`.
- Latest GPU-visible CPU-node removal guard slice passed:
  - `terrain_mesh_build_plan()` now requires `gpu_visible_render_active` before returning `RemoveCpuNode`, even if a future caller incorrectly reports no CPU proxy reason before the first confirmed GPU terrain frame.
  - The existing mesh-build-plan unit test now locks that uploaded GPU terrain still falls back to `FullArrayMesh` before the GPU visible path is active.
  - Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (37/37), `./scripts/check.sh fast`, `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Latest GPU-backed CPU proxy activation guard slice passed:
  - Added pure `terrain_cpu_proxy_mesh_active()` and routed both mesh-build planning and render-mode refresh through it. CPU nodes are treated as GPU-backed proxy meshes only when the GPU visible path is confirmed and the specific subchunk has a GPU slot.
  - Added a unit test that locks all four active/inactive combinations, protecting fallback meshes from being switched to proxy render modes without an actual GPU subchunk.
  - Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo check --manifest-path client/rust_ext/Cargo.toml`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (38/38), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Latest GPU upload-failure fallback guard slice passed:
  - Extended the mesh-build-plan unit test so an active GPU-visible path still returns `FullArrayMesh` when the specific subchunk has no GPU slot, even if there is no CPU proxy reason. This protects fallback rendering for per-subchunk upload failures.
  - Checks passed: `cargo fmt --manifest-path client/rust_ext/Cargo.toml -- --check`, `cargo test --manifest-path client/rust_ext/Cargo.toml` (38/38), `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 sh ./scripts/gpu_terrain_parity_smoke.sh`, `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, `git diff --check`, and `./scripts/diff_guard.sh`.
- Latest controlled low-angle lighting smoke slice passed:
  - `client/main.gd` now records `lighting_variant` in visual smoke markers and adds smoke-only pose `RUMPELMC_VISUAL_SMOKE_POSE=lighting_low_angle`, which changes only the visual smoke `SunLight` direction/energy.
  - `scripts/gpu_terrain_movement_stress.sh` now passes through `RUMPELMC_VISUAL_SMOKE_POSE`, validates the requested pose marker, and surfaces `smoke_pose` plus `lighting_variant` in `movement-stress-summary.txt`.
  - Fresh release smoke passed in `logs/gpu_lighting_low_angle_smoke_20260613_retry/movement-stress-summary.txt` with `lighting_variant="low_angle"`, `gpu_light_dir=-0.775/0.407/0.484`, `gpu_light_energy=0.700`, `gpu_light_ambient=0.550`, `terrain_queue_max_ms=1.200`, `gpu_compositor_submit_max_ms=0.174`, `gpu_upload_fail=0`, and `terrain_samples=509`.
  - First attempt hit a stale local server listener and failed before chunk load; the clean retry started its own server and passed.
- Latest low-angle lighting parity gate slice passed:
  - `scripts/gpu_terrain_parity_smoke.sh` now includes CPU/GPU `lighting_low_angle` captures, requires `lighting_variant="low_angle"` on both markers, and writes the pair into `parity-summary.txt`.
  - Fresh full parity passed in `logs/gpu_lighting_low_angle_parity_20260613/parity-summary.txt` with `case_count=16`, low-angle pair `avg_luma=0.2865/0.2671 delta=0.0194`, `terrain_luma_range=0.3664/0.3357 delta=0.0307`, `terrain_samples=498/498`, and `terrain_color_buckets=8/9`.
  - The GPU low-angle marker reported `gpu_light_dir=-0.775/0.407/0.484`, `gpu_light_energy=0.700`, `gpu_upload_fail=0`, `smoke_err=0`, and `gpu_frames=235`.
- Latest low-angle compact shadow parity gate slice passed:
  - `scripts/gpu_terrain_parity_smoke.sh` now includes `gpu-terrain-compact-lighting-low-angle-parity`, requires `lighting_variant="low_angle"` on the compact marker, and validates compact-vs-full shadow proxy parity for the low-angle pose.
  - Fresh full parity passed in `logs/gpu_lighting_low_angle_compact_parity_20260613/parity-summary.txt` with `case_count=17`, `lighting_low_angle_compact` pair `avg_luma=0.2671/0.2671 delta=0.0000`, `terrain_luma_range=0.3357/0.3357 delta=0.0000`, `terrain_samples=498/498`, and `terrain_color_buckets=9/9`.
  - The compact low-angle marker reported `shadow_mesh=compact`, `shadow_path=godot_proxy`, `gpu_light_energy=0.700`, `gpu_upload_fail=0`, `smoke_err=0`, and `gpu_frames=234`.

Useful log lines:

- `GPU terrain compositor initialized`
- `GPU terrain compositor attached to player camera`
- `GPU terrain compositor draw: size=1280x720 views=1 draws=2 faces=7242`
- `GPU terrain compositor draw: size=1280x720 views=1 depth=true draws=2 faces=7242`
- `Visual smoke screenshot saved ... current_chunk="0,0"`
- `perf="... gpu_subchunks=66 gpu_draws=66 gpu_faces=116824 gpu_frames=116 ..."`
- `GPU terrain visible path confirmed`
- `Visual smoke screenshot saved ... terrain_samples=576 ... smoke_err=0 ... gpu_frames=160 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... cpu_proxy=10 collision=10 ... gpu_subchunks=92 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... current_chunk="0,0"` for CPU fallback.
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... fast_proxy=163 ... gpu_frames=190 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... cpu_proxy=10 collision=10 fast_proxy=20 ... gpu_frames=195 ...`
- `Visual smoke screenshot saved ... terrain_samples=288 ... smoke_err=0 ... fast_proxy=0 ... current_chunk="0,0"` for CPU fallback.
- `Visual smoke screenshot saved ... parity/cpu-arraymesh-parity.png ... avg_luma=0.3517 ... terrain_samples=256 ... smoke_err=0 ... fast_proxy=0 collision=10 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-parity.png ... avg_luma=0.3442 ... terrain_samples=256 ... smoke_err=0 ... fast_proxy=62 ... gpu_frames=68 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-radius1-parity.png ... terrain_samples=256 ... smoke_err=0 ... cpu_proxy=10 fast_proxy=19 collision=10 ... gpu_frames=74 ...`
- `Visual smoke screenshot saved ... parity/cpu-arraymesh-parity.png ... terrain_color_buckets=8 terrain_chroma_samples=256 terrain_luma_range=0.3655 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-parity.png ... terrain_color_buckets=8 terrain_chroma_samples=256 terrain_luma_range=0.3532 ... gpu_frames=143 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-radius1-parity.png ... terrain_color_buckets=8 terrain_chroma_samples=256 terrain_luma_range=0.3532 ... cpu_proxy=10 fast_proxy=20 ... gpu_frames=149 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-parity.png ... cpu_proxy=82 proxy_coll=10 proxy_shadow=82 proxy_both=10 proxy_shadow_only=72 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-radius1-parity.png ... cpu_proxy=10 proxy_coll=10 proxy_shadow=10 proxy_both=10 proxy_shadow_only=0 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-atlas-depth-parity.png pose="atlas_depth" ... terrain_samples=510 terrain_color_buckets=9 terrain_luma_range=0.3294 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-lighting-shadow-parity.png pose="lighting_shadow" ... terrain_samples=453 terrain_color_buckets=10 terrain_luma_range=0.3570 ...`
- `Visual smoke screenshot saved ... parity/gpu-terrain-compact-lighting-shadow-parity.png pose="lighting_shadow" ... shadow_mesh=compact compact_shadow_proxy=97 compact_shadow_normals_saved=1320960 ...`
- `Visual smoke screenshot saved ... pose="lighting_low_angle" lighting_variant="low_angle" ... terrain_samples=509 terrain_color_buckets=9 terrain_luma_range=0.3357 ... gpu_light_dir=-0.775/0.407/0.484 gpu_light_energy=0.700 ...`
- `pair=lighting_low_angle left=cpu-arraymesh right=gpu-terrain avg_luma=0.2865/0.2671 delta=0.0194 terrain_luma_range=0.3664/0.3357 delta=0.0307 terrain_samples=498/498 delta=0 ...`

Known limitations:

- GPU terrain render is still opt-in via `RUMPELMC_GPU_TERRAIN_RENDER=1`.
- GPU terrain now samples the real block texture atlas and applies directional lighting, but visual parity still needs shadow integration and visual tuning.
- GPU terrain solid blocks force opaque alpha in the shader; do not infer grass/solid block transparency from the RGBA atlas alpha.
- GPU terrain shader atlas UVs now use runtime atlas layout push constants instead of a hard-coded column count.
- GPU terrain atlas texture now prefers sRGB sampling; color parity should still be visually compared against the ArrayMesh fallback before deleting more CPU rendering.
- Depth-compatible scene rendering is wired and smoke-tested with Godot 4.6 reverse-Z depth compare.
- GPU terrain is now directional-light aware, and retained ArrayMesh terrain is explicitly configured as a double-sided Godot shadow caster proxy.
- With `RUMPELMC_GPU_TERRAIN_RENDER=1`, CPU ArrayMesh nodes become `SHADOWS_ONLY` only after the GPU visible render path has rendered at least one compositor frame; this avoids double visible terrain while preserving fallback if GPU setup fails or a subchunk fails GPU upload.
- Distant CPU terrain node removal is also gated by the confirmed GPU-visible path inside `terrain_mesh_build_plan()`, not only by the current caller's proxy decision.
- CPU terrain nodes are considered GPU-backed proxies only when the confirmed GPU-visible path and the per-subchunk GPU slot are both present.
- With `RUMPELMC_GPU_TERRAIN_RENDER=1`, CPU ArrayMesh nodes are still kept where needed for nearby collision and the conservative Godot shadow-map proxy. Distant CPU `MeshInstance3D` removal has started, but the retained proxy radius is intentionally conservative.
- With `RUMPELMC_GPU_TERRAIN_RENDER=1`, retained CPU proxy meshes can now be built from packed GPU terrain faces after the GPU visible path is confirmed. These proxy meshes intentionally omit UVs because they are used for collision and `SHADOWS_ONLY`, not visible terrain.
- `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MODE=collision_only` is diagnostic only for now. It confirms how much CPU work is shadow-proxy-only. In this mode retained collision CPU proxies are invisible and do not cast shadows, but it must not become the default until GPU terrain has a dedicated shadow-compatible path or native Godot shadow-map participation.
- `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=compact` is now the default for GPU-visible shadow-only CPU proxy meshes. Use `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=full` for a control run or opt-out. This is still a Godot shadow proxy and does not replace native RD shadow-map participation.
- `scripts/gpu_terrain_parity_smoke.sh` can validate existing artifacts with `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1`. It now expects markers produced by the extended visual smoke metrics and multi-pose support in `client/main.gd`, plus proxy reason counters, terrain mesh render-state counters, `shadow_mode=...`, `shadow_mesh=...`, and `compact_shadow_normals_saved=...` from `client/rust_ext/src/lib.rs`. It validates CPU/GPU pose parity, compact-vs-full `lighting_shadow` parity, disabled-shadow-radius behavior, and `collision_only` proxy render-state invariants. In this Codex PTY environment, launching Godot through shell wrappers can still stall before project logs appear, so direct Godot commands plus validate-only are the reliable local gate.
- `scripts/gpu_terrain_compact_proxy_benchmark.sh` defaults to report mode over existing parity artifacts and now includes both the disabled-shadow-radius marker and the collision-only diagnostic marker. Use `RUMPELMC_COMPACT_PROXY_BENCH_CAPTURE=1` only when the local shell/Godot wrapper is known to be reliable, or prefer direct Godot captures followed by benchmark report mode.
- The custom RD compositor draw still does not natively participate in Godot shadow maps as a real shadow caster/receiver. Full shadow parity without ArrayMesh proxy needs a separate render integration plan.
- Final ArrayMesh replacement for distant terrain is not finished.
- Keep the working tree small and commit completed slices; `diff_guard` is expected to stay green after the dirty-tree cleanup baseline.

Next steps:

1. After any GPU terrain edit, run the parity gate: direct CPU/GPU/radius=1 visual smoke captures with `RUMPELMC_VISUAL_SMOKE_DISABLE_PLAYER_INPUT=1`, then `RUMPELMC_PARITY_SMOKE_VALIDATE_ONLY=1 ./scripts/gpu_terrain_parity_smoke.sh`.
2. Keep compact shadow-only proxy meshes as the default unless fresh parity or visual shadow checks show a regression; use `RUMPELMC_GPU_TERRAIN_SHADOW_PROXY_MESH=full` for comparison runs.
3. Decide the production shadow path next: either build a dedicated lower-cost shadow proxy beyond ArrayMesh, or make the RD terrain participate in Godot shadow maps. The new `collision_only` mode is useful for measurement, not a production default.
4. Do not make `collision_only` default or remove conservative shadow proxies until multi-pose parity and visible shadow behavior are explicitly verified.
5. Treat `lighting_low_angle` parity as the current controlled lighting gate before changing shader lighting or native shadow behavior.
6. Continue reducing CPU ArrayMesh generation for distant chunks while preserving nearby collision meshes, shadow proxy requirements, and the fallback path.
7. Run `RUMPELMC_USE_SCCACHE=0 ./scripts/check.sh full`, parity visual smoke validation, and `./scripts/diff_guard.sh` before handing off again.
