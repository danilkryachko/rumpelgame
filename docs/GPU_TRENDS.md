# GPU Trends

Use this file for durable GPU optimization checkpoints. Keep entries short and factual; link the detailed artifact directory instead of copying long logs.

## Reading The Table

- `visual_fps` is report-only when the run is display/driver paced.
- `gpu_time` is report-only on the current macOS/Metal path because Godot timestamp samples report `0.0us`.
- Prefer `terrain_queue_max_ms`, `process_wall_p95_ms`, `gpu_compositor_submit_max_ms`, `gpu_upload_fail`, draw counts, and external GPU profiler captures for decisions.

## 2026-06-16

| Commit | Slice | Artifact | Key Result | Caveat |
| --- | --- | --- | --- | --- |
| `workspace` | GPU buffer residency budget | `logs/gpu_buffer_residency_budget_current/gpu-buffer-residency-budget-summary.txt`, `logs/gpu_stress_artifact_index_current/gpu-stress-artifact-index-summary.txt`, `scripts/gpu_buffer_residency_budget.sh` | Added a summary-only buffer residency budget over current mass-load, stage-pool, grouped-draw, and cutout-pressure evidence. Fresh summary passed with `residency_pressure_class=high`, `residency_proof_status=partial`, configured buffers `67239936` bytes / `95.709%`, active face bytes `100672` / `2.400%`, subchunks/logical records/submitted records `2482/2482/2482`, faces `6292`, draw-command occupancy/headroom `30.298%/91360`, queue/process/submit `2.214/0.059/5.677ms`, upload failures `0`, stage-pool reuses `3128`, and grouped saved records `2174`. Stress index now has `24` rows and `15` required passes and surfaces the buffer pressure fields. | summary/governance slice only; allocator/free-range memory-budget evidence is missing in this checkout and explicitly blocks default decisions; local FPS/Godot GPU timestamps remain report-only; external profiler plus macOS/Windows/Vulkan/Direct3D validation remain required before repack, eviction, or streaming-policy defaults |
| `workspace` | GPU chunk unload churn diagnosis | `logs/gpu_chunk_unload_churn_diagnosis_current/gpu-chunk-unload-churn-diagnosis-summary.txt`, `scripts/gpu_chunk_unload_churn_diagnosis.sh` | Added a summary-only unload/reload churn diagnosis over current chunk-boundary evidence. Fresh summary passed with `max_chunk_unload_total=0`, `max_chunk_unload_grace_kept=48984`, `max_chunk_unload_neighbor_refreshes=0`, `max_chunk_unload=0`, queue/process/submit `2.258/0.049/0.124ms`, packet lag `27.437ms`, upload failures `0`, ground misses `0`, and render/collision not-ready cases `0`. | current chunk-boundary evidence only; older default/immediate teleport-only control summaries are missing in this checkout and remain optional unless `RUMPELMC_GPU_CHUNK_UNLOAD_REQUIRE_CONTROLS=1`; no unload policy, quality, protocol, storage, worldgen, or chunk serialization change |
| `workspace` | GPU stress artifact index | `logs/gpu_stress_artifact_index_current/gpu-stress-artifact-index-summary.txt`, `logs/gpu_stress_artifact_index_current/gpu-stress-artifact-index.txt`, `scripts/gpu_stress_artifact_index.sh` | Added a focused current-evidence index over required GPU streaming/residency/upload/draw/transparent rows plus optional governance/profiler gap rows. Fresh summary now passes with `23` rows, `14` required passes, `8` optional missing rows, zero upload-failure/ground-miss/default-runtime-change violations, max GPU subchunks/draws/faces `2482/2482/6292`, draw-command occupancy `30.298%`, queue/process/submit `3.928/0.059/5.677ms`, packet lag `27.437ms`, cutout uploads `265`, stage-pool reuses `3128`, and grouped saved records `2174`. | summary/index infrastructure only; optional missing governance rows remain visible, local FPS/Godot GPU timestamps remain report-only, and external profiler plus Windows/Vulkan/Direct3D validation remain pending |
| `workspace` | GPU rapid camera-turn stress gate | `logs/gpu_terrain_rapid_camera_turn_stress_current/rapid-camera-turn-stress-summary.txt`, `logs/gpu_terrain_rapid_camera_turn_stress_current/movement/movement-stress-summary.txt`, `scripts/gpu_terrain_rapid_camera_turn_stress.sh` | Added a standalone `chunk_fast_turn` gate for rapid camera orientation changes inside one current chunk. Fresh release evidence passed with `motion_steps=8`, `motion_chunks=1`, final chunk `2,2`, render/collision readiness, `ground_misses=0`, `terrain_samples=416`, `gpu_upload_fail=0`, unload total/neighbor refreshes `0`, max packet drain `51`, and queue/process/submit `1.535/0.049/0.100ms`. Aggregate reports and the test strategy gate now surface the summary. | local macOS/Metal runtime/gate evidence only; local FPS/GPU timestamps remain report-only, pop-in counters remain telemetry, this does not lower camera far plane/draw distance/visibility/lighting/shadows/texture quality, and external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | GPU chunk-boundary stress gate | `logs/gpu_terrain_chunk_boundary_stress_current/chunk-boundary-stress-summary.txt`, `logs/gpu_terrain_chunk_boundary_stress_current/high-pressure-suite/world-load-suite-summary.txt`, `scripts/gpu_terrain_chunk_boundary_stress.sh` | Added a focused chunk enter/exit gate over `long-move`, `spiral`, `fast-turn`, `teleport-snap`, and bounded `high-resident` workload evidence. Fresh release suite passed all movement cases with correct final chunks, render/collision readiness, `ground_misses=0`, `gpu_upload_fail=0`, unload total/neighbor refreshes `0`, max packet drain `54`, max queue/process/submit `2.258/0.049/0.124ms`, and bounded resident smoke `998` subchunks/draws / `1445` faces. The first wrapper pass correctly failed when it incorrectly applied strict load-scaling thresholds to this bounded residency lane; the source suite was then revalidated with the gate-specific `900/900/1200` residency thresholds. | local macOS/Metal runtime/gate evidence only; bounded `high-resident` is not a replacement for strict `gpu_terrain_load_scaling` or mass chunk-load thousands-of-draws evidence; pop-in counters remain telemetry, local FPS/GPU timestamps remain report-only, and external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | Cutout sort/build cost gate | `logs/transparent_cutout_sort_build_cost_current/transparent-cutout-sort-build-cost-summary.txt`, `logs/gpu_transparent_cutout_fixture_scene_smoke_current/transparent-cutout-fixture-acceptance-summary.txt`, `logs/gpu_terrain_cutout_pressure_load_scaling_current/gpu-terrain-cutout-pressure-load-scaling-summary.txt` | Added focused cost evidence for the default-off cutout prototype. Fixture and pressure summaries now agree on `transparent_sort_policy=opaque_depth_alpha_test_no_sort`, `transparent_sort_active=0`, `transparent_sort_keys=0`, `transparent_sort_ms=0.000`, and `transparent_build_cost_source=cutout_in_opaque_mesh_phase`; the fresh cost gate passed with fixture build `17` faces / `2` subchunks / `2.121ms` envelope and pressure build `1590` faces / `265` subchunks / `1.994ms` envelope, with matching full-payload upload counters. | CPU-side marker/gate/report evidence only; no blended transparency, split transparent pass, default-on decision, external profiler artifact, Windows validation, protocol/storage/worldgen/chunk serialization change, draw distance, lighting, shadows, texture quality, visible quality, or default behavior change |
| `workspace` | Cutout upload telemetry and gates | `logs/gpu_transparent_cutout_upload_block_edit_pass/transparent-cutout-prototype-acceptance-summary.txt`, `logs/gpu_transparent_cutout_fixture_scene_smoke_current/transparent-cutout-fixture-acceptance-summary.txt`, `logs/gpu_terrain_cutout_pressure_load_scaling_current/gpu-terrain-cutout-pressure-load-scaling-summary.txt`, `client/rust_ext/src/lib.rs`, `client/rust_ext/src/gpu_terrain.rs` | Added cutout upload telemetry for the default-off leaf/cutout prototype. Runtime markers now expose `transparent_cutout_uploads`, full upload payload bytes, cutout face counts, logical cutout-face bytes, and last-upload values; terrain queue telemetry exposes cutout upload slot/count KiB plus logical cutout-face KiB; workload/resident/load-scaling summaries, block-edit/fixture/pressure gates, and V1/V2 reports propagate and validate those fields. Fresh block-edit acceptance passed with `1/80/5/80`, fixture acceptance passed with `2/352/17/272`, and pressure gate passed with `265/25440/1590/25440` for uploads/bytes/faces/face-bytes. | telemetry/gate/report slice only; upload bytes are existing packed terrain-buffer payloads for subchunks that contain cutout faces, not a separate transparent buffer; no blended transparency, split transparent pass, sorting, default-on decision, external profiler artifact, Windows validation, protocol/storage/worldgen/chunk serialization change, draw distance, lighting, shadows, texture quality, visible quality, or default behavior change |
| `workspace` | Cutout same-material seam/culling gate | `logs/gpu_transparent_cutout_fixture_scene_smoke_current/transparent-cutout-fixture-scene-smoke-summary.txt`, `logs/gpu_transparent_cutout_fixture_scene_smoke_current/transparent-cutout-fixture-acceptance-summary.txt`, `client/rust_ext/src/gpu_terrain.rs` | Tightened the cutout fixture scene smoke into a same-material adjacent-pair sentinel. Fresh release-profile evidence requires adjacent-pair markers `cutout_fixture_adjacent_pair_blocks=2`, `cutout_fixture_adjacent_pair_block_id=5`, `cutout_fixture_adjacent_pair_same_material=1`, `cutout_fixture_adjacent_pair_neighbor=1`, `cutout_fixture_adjacent_pair_collision_hits=2`, exact cutout workload `transparent_blocks=4`, `transparent_faces=17`, `transparent_draws=2`, `transparent_subchunks=2`, and acceptance emits `transparent_cutout_seam_culling_status=pass` with `same_material_seam_policy=cutout_pair_visible_faces`. The new Rust unit `cutout_prototype_keeps_same_material_adjacent_seam_faces_visible` locks two adjacent leaves at default `face_count/cutout=6/0` and cutout `8/8`, proving internal cutout seam faces are retained while renderer culling remains unchanged. | local macOS/Metal cutout-only seam/culling evidence; exact `transparent_faces=17` is a fixture sentinel, not a general transparent-face budget; no blended transparency, split transparent pass, sorting, default-on decision, external profiler artifact, Windows validation, protocol/storage/worldgen/chunk serialization change, draw distance, lighting, shadows, texture quality, or visible quality change |
| `workspace` | Cutout fixture scene smoke and acceptance gate | `logs/gpu_transparent_cutout_fixture_scene_smoke_current/transparent-cutout-fixture-scene-smoke-summary.txt`, `logs/gpu_transparent_cutout_fixture_scene_smoke_current/transparent-cutout-fixture-acceptance-summary.txt`, `scripts/gpu_terrain_cutout_fixture_scene_smoke.sh`, `scripts/gpu_terrain_cutout_fixture_acceptance_gate.sh` | Added a fixed cutout fixture scene smoke for the default-off leaf/cutout prototype. Fresh release-profile evidence used isolated RocksDB, `RUMPELMC_GPU_TERRAIN_CUTOUT_PROTOTYPE=1`, and no full-transparent env request; it placed four cutout leaves plus one opaque occluder and passed with `cutout_fixture_roles=5`, `cutout_fixture_leaf_blocks=4`, `cutout_fixture_opaque_blocks=1`, `cutout_fixture_dirty_observed=1`, `cutout_fixture_collision_hits=5`, `cutout_fixture_collision_misses=0`, `cutout_fixture_occlusion_probe_hit=1`, `cutout_fixture_queue_drained=1`, `transparent_requested=1`, `transparent_active=1`, `transparent_fallback=0`, `transparent_blocks=4`, `transparent_faces=17`, `transparent_draws=2`, `transparent_subchunks=2`, `gpu_upload_fail=0`, `terrain_queue_work_ms` max `2.471`, `process_wall_p95_ms=0.046`, and `gpu_compositor_submit_ms` max `0.137`. The acceptance gate validates the smoke, source contracts, and aggregate report surfacing. | local macOS/Metal cutout-only depth/collision smoke; occlusion is a physics probe, not blended transparency sorting; no split transparent pass, default-on decision, external profiler artifact, Windows validation, protocol/storage/worldgen/chunk serialization change, draw distance, lighting, shadows, texture quality, or visible quality change |
| `workspace` | Cutout pressure load-scaling gate | `logs/gpu_terrain_cutout_pressure_load_scaling_current/gpu-terrain-cutout-pressure-load-scaling-summary.txt`, `logs/gpu_terrain_cutout_pressure_load_scaling_current/load-scaling/resident-set-growth/resident-set-growth-summary.txt`, `scripts/gpu_terrain_cutout_pressure_load_scaling_gate.sh` | Added a high resident-set pressure gate for the default-off leaf cutout prototype. Fresh evidence used `pressure` + `chunk_disc` + block ID `5` and passed with `max_gpu_subchunks=1859`, `max_gpu_draws=1859`, `max_gpu_faces=3712`, `gpu_draw_cmd_occupancy_pct=22.693`, `transparent_requested=1`, `transparent_active=1`, `transparent_fallback=0`, `transparent_blocks=709`, `transparent_faces=1590`, `transparent_draws=265`, `transparent_subchunks=265`, `transparent_cutout_uploads=265`, `transparent_cutout_upload_bytes=25440`, `transparent_cutout_upload_faces=1590`, `transparent_cutout_upload_face_bytes=25440`, `gpu_upload_fail=0`, `max_terrain_queue_ms=1.994`, `max_process_wall_p95_ms=0.009`, and `max_gpu_compositor_submit_ms=0.193`. Workload, resident-set, and load-scaling summaries now propagate fixture block ID plus transparent workload/upload maxima, and aggregate reports surface the selected cutout pressure summary. | default-off local macOS/Metal cutout-only pressure evidence; no blended transparency, split transparent pass, sorting, new block ID, atlas asset change, protocol/storage/worldgen/chunk serialization change, draw distance, lighting, shadows, texture quality, visible quality, or default behavior change; external profiler and Windows validation remain required before default-on |
| `workspace` | Cutout prototype acceptance/report gate | `logs/gpu_transparent_cutout_prototype_current/transparent-cutout-prototype-acceptance-summary.txt`, `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt`, `logs/transparent_fixture_acceptance_suite_current/transparent-fixture-acceptance-suite-summary.txt`, `scripts/gpu_terrain_cutout_prototype_acceptance_gate.sh` | Added a gate that accepts the default-off leaf cutout prototype only when the block-edit smoke placed block ID `5`, reports `transparent_requested=1`, `transparent_active=1`, `transparent_fallback=0`, nonzero `transparent_blocks/faces/draws/subchunks`, `gpu_upload_fail=0`, the Rust full-transparent implementation gate remains false, and the aggregate report surfaces the selected cutout acceptance summary. Report V1/V2 now surface transparent workload maxima, the default-off guard understands the cutout-aware decision signatures, and the Rust flag parser accepts `enabled/disabled` consistently with the GPU mesher. Fresh fixture scene smoke and acceptance suite passed with legacy full-transparent fallback still `1/0/1`, overlay metadata `5/5`, zero transparent workload, and `gpu_upload_fail=0`. | guard/report/docs slice plus flag-parser consistency only; no blended transparency, separate transparent pass, sorting, new block ID, atlas asset change, protocol/storage/worldgen/chunk serialization change, draw distance, lighting, shadows, texture quality, visible quality, default behavior change, or default-on decision |
| `workspace` | Default-off leaf cutout prototype | `logs/gpu_transparent_cutout_prototype_current/block-edit-stress-summary.txt`, `client/rust_ext/src/gpu_terrain.rs`, `client/shaders/gpu_terrain_render.glsl`, `scripts/gpu_terrain_block_edit_stress.sh` | Added `RUMPELMC_GPU_TERRAIN_CUTOUT_PROTOTYPE=1` as the first transparent-family runtime slice: leaf metadata marks cutout alpha-test blocks, packed GPU faces carry a cutout bit without changing the 16-byte face layout, the shader discards low-alpha texels only for flagged faces and still writes opaque alpha, and fresh release block-edit smoke placed block ID `5` with `transparent_requested=1`, `transparent_active=1`, `transparent_fallback=0`, `transparent_blocks=1`, `transparent_faces=5`, `transparent_draws=1`, `transparent_subchunks=1`, and `gpu_upload_fail=0`. | default-off cutout-only local macOS/Metal evidence; no blended transparency, separate transparent pass, sorting, new block ID, atlas asset change, protocol/storage/worldgen/chunk serialization change, default behavior change, or default-on decision; external macOS/Windows profiler evidence remains separate |
| `workspace` | Transparent prototype shape decision | `/tmp/rumpel-transparent-prototype-shape-pass/transparent-prototype-shape-decision-summary.txt`, `/tmp/rumpel-gpu-report-transparent-prototype-shape.txt`, `scripts/transparent_prototype_shape_decision_gate.sh` | Added a summary gate that consumes transparent active-path preflight, sorting/depth, fixture acceptance, and block-material metadata summaries. The current expected decision is `cutout_only_first`, with `active_prototype_allowed=0`, `default_runtime_change_allowed=0`, zero transparent workload, and split/full blended alpha deferred until active workload, sorting/depth, and profiler evidence exist. Aggregate report surfacing now includes the decision summary. | gate/report/docs slice only; synthetic summaries are not Godot captures or external profiler evidence, and no renderer, shader alpha path, Rust runtime, Godot scene, block ID, atlas asset, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, visible quality, or default behavior changed |
| `workspace` | Shader profiler results validator | `/tmp/rumpel-shader-profiler-results-summary.txt`, `/tmp/rumpel-gpu-report-shader-profiler-results.txt`, `scripts/gpu_shader_profiler_results_check.sh` | Added a validator for captured macOS/Windows shader profiler rows. Synthetic captured rows matching the current shader profiler manifest produced `planned_rows=3`, `captured_rows=3`, `missing_rows=0`, `macos_rows=1`, `windows_rows=1`, `cross_platform_rows=1`, `external_profile_status=captured`, and aggregate report surfacing now includes shader profiler results plus the capture pack. | validator/report/docs slice only; synthetic rows are not real Xcode, PIX, RenderDoc, or vendor profiler evidence, and no renderer, shader, runtime default, draw distance, lighting, shadows, texture quality, protocol, storage, worldgen, chunk serialization, or visible quality changed |
| `workspace` | Shadow proxy cost decision gate | `/tmp/rumpel-shadow-proxy-cost-pass/shadow-proxy-cost-decision-summary.txt`, `/tmp/rumpel-gpu-report-shadow-cost-decision.txt`, `scripts/shadow_proxy_cost_decision_gate.sh` | Added a summary gate that consumes shadow quality parity, the shadow-radius matrix, the external-profiler capture pack, and optional validated profiler results. The positive fixture passes with `decision=defer_runtime_change`, `default_runtime_change_allowed=0`, `radius_rows=4`, `radius_usable_rows=3`, `max_compact_shadow_proxy=201`, and `max_compact_shadow_normals_saved=3932`; aggregate report surfacing now includes the decision summary. | gate/report/docs slice only; synthetic fixtures are not Godot captures, no external Metal or Windows profiler trace is recorded, and no renderer, shader, shadow default, draw distance, lighting, texture quality, protocol, storage, worldgen, chunk serialization, or visible quality changed |
| `workspace` | Flat per-face shader varyings | `logs/gpu_shader_flat_varyings_current/movement-stress-summary.txt`, `logs/gpu_shader_profiler_capture_pack_current/shader-profiler-capture-pack.txt`, `client/shaders/gpu_terrain_render.glsl` | Split the GPU terrain shader varying ABI so only face UV remains interpolated, while atlas tile offsets and terrain lighting use `flat` vertex-to-fragment varyings. Fresh release movement on Godot 4.6.2/Metal compiled and rendered the new interface with `smoke_err=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_color_buckets=7`, `gpu_effective_draws=788`, `gpu_faces=1167`, `terrain_queue_max_ms=3.928`, `process_wall_p95_ms=0.057`, `current_chunk_collision=2`, and `ground_misses=0`. The shader profiler capture pack passed against the same artifact and records `flat_tile_offset=1` plus `flat_lighting=1`. | shader interface hot-path cleanup only; no packed-face layout, atlas layout, push-constant layout, protocol, storage, worldgen, chunk serialization, draw distance, lighting values, shadows, texture quality, visible quality, or default behavior changed; local GPU timestamps remain report-only and external macOS/Windows profiler evidence is still pending |
| `workspace` | Shader profiler capture pack | `logs/gpu_shader_profiler_capture_pack_current/shader-profiler-capture-pack.txt`, `logs/gpu_shader_profiler_capture_pack_current/shader-profiler-manifest.txt`, `scripts/gpu_shader_profiler_capture_pack.sh` | Added a source-contract and artifact guard for the current branchless GPU terrain render shader path. The generated pack validates the latest movement artifact with `smoke_err=0`, `gpu_upload_fail=0`, nonzero terrain samples/draws/faces, checks that atlas tile offsets stay vertex-precomputed while fragment tile-index `mod`/`floor` remain absent, and emits pending rows for macOS Metal/Xcode plus Windows PIX/RenderDoc/vendor profiler captures. | capture-pack/checklist only; no external Metal or Windows profiler trace is recorded, no GPU timing is claimed, and no shader/runtime/default behavior changed |
| `workspace` | Vertex-precomputed atlas tile offsets | `logs/gpu_shader_atlas_offset_current/movement-stress-summary.txt`, `client/shaders/gpu_terrain_render.glsl`, Rust shader contract tests | Moved terrain atlas tile `col/row` calculation from fragment to vertex output `uv_tile_out.zw`, so the fragment shader keeps merged-face `fract(tile_uv)` repetition but no longer performs tile-index `mod`/`floor` per pixel. Also moved triangle corner index selection to the global `TRIANGLE_CORNER_INDICES` table instead of a local `main()` array. Fresh release movement compiled/rendered the new varying layout with `smoke_err=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_color_buckets=7`, `terrain_queue_max_ms=2.136`, `process_wall_p95_ms=0.054`, `current_chunk_collision=2`, and `ground_misses=0`. | shader hot-path cleanup only; no packed-face layout, atlas layout, push-constant layout, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, visible quality, or default behavior changed; local macOS/Metal smoke had a report-only `gpu_compositor_submit_max_ms=3.737` spike still below the 6.667ms target budget, and external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | Shader hot-path branch and lighting cleanup | `logs/gpu_shader_hot_path_current/movement-stress-summary.txt`, `client/shaders/gpu_terrain_render.glsl`, Rust shader contract tests | Removed the remaining signed-coordinate `if` from the GPU terrain vertex shader by replacing `unpack_signed_i16()` with `int(low & 32767u) - int(low & 32768u)`. The shader also now trusts Rust-sanitized lighting push constants and skips redundant per-vertex `normalize`, `clamp`, and `max` guards for light direction/ambient/color/energy. Rust tests lock both contracts and verify invalid lighting inputs are sanitized before push-constant packing. Fresh release movement compiled/rendered the shader with `smoke_err=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_color_buckets=7`, `gpu_light_dir=-0.355/0.743/0.567`, `gpu_light_energy=0.450`, `gpu_light_ambient=0.550`, `terrain_queue_max_ms=2.030`, `process_wall_p95_ms=0.054`, and `gpu_compositor_submit_max_ms=0.149`. | shader hot-path cleanup only; no packed-face layout, atlas layout, push-constant layout, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, visible quality, or default behavior changed; local macOS/Metal evidence only, and external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | Fixed-size GPU push-constant packing | `logs/gpu_push_constants_fixed_array_current/movement-stress-summary.txt`, `client/rust_ext/src/gpu_terrain.rs` | Replaced the per-frame GPU terrain push-constant `Vec<u8>` builder with a fixed `112`-byte stack array while preserving the existing camera/lighting/atlas byte layout. Fresh release movement passed with `gpu_push_constant_bytes=112`, `gpu_push_constant_updates=1694`, `gpu_push_constant_total_bytes=189728`, byte split `64/32/16`, `gpu_scene_target_create=1`, `gpu_scene_target_reuse=1692`, `gpu_scene_target_replace=0`, `gpu_uniform_set_create=1`, `gpu_atlas_texture_create=1`, `gpu_atlas_sampler_create=1`, constants submit part max `0.030ms`, `gpu_compositor_submit_max_ms=0.141`, `terrain_queue_max_ms=2.079`, `process_wall_p95_ms=0.050`, `gpu_upload_fail=0`, `current_chunk_collision=2`, `ground_misses=0`, and `terrain_samples=384`. | local macOS/Metal runtime evidence; no shader layout, atlas layout, binding set, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, visible quality, or default behavior changed; external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | Default-off grouped indirect draw records | `logs/gpu_terrain_grouped_draws_current/gpu-terrain-grouped-draws-summary.txt`, `logs/gpu_terrain_grouped_draws_current/baseline/workload-matrix-summary.txt`, `logs/gpu_terrain_grouped_draws_current/grouped/workload-matrix-summary.txt` | Added `RUMPELMC_GPU_TERRAIN_GROUPED_DRAWS=1` to rebuild indirect draw commands as contiguous face-range records while keeping the default path unchanged. Fresh full release gate passed with matching pressure fixture blocks `709`, logical records `2289`, subchunks `2289`, faces `6292`, subchunk/face deltas `0/0`, upload failures `0`, baseline grouped disabled, grouped enabled, real grouped records reduced from `2289` to `115`, saved records `2174`, draw-command bytes reduced from `36624` to `1840`, `terrain_queue_max_ms=2.083`, `process_wall_p95_ms=0.034`, `gpu_compositor_submit_max_ms=0.625`, current collision `3`, ground misses `0`, and terrain samples `386`. | default-off local macOS/Metal evidence only; `gpu_draws` in grouped mode is real grouped indirect records, so use `gpu_draw_records_logical` for workload comparisons; no protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, visible quality, upload capacity, allocator policy, or default behavior changed; external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | Pressure dirty full-vs-partial gate | `logs/gpu_terrain_pressure_dirty_compare_current/gpu-terrain-pressure-dirty-compare-summary.txt`, `scripts/gpu_terrain_pressure_dirty_compare.sh`, `docs/GPU_TERRAIN_LOAD_SCALING.md` | Added a deterministic high-pressure dirty-update compare over isolated full-rebuild and partial-dirty lanes. Both lanes observed `terrain_pressure_fixture_blocks=709` and `dirty_blocks=709`; full control stayed partial-disabled with `dirty_partial_subchunks=0` and `dirty_partial_saved_subchunks=0`, while partial dirty reported `dirty_partial_subchunks=709`, `dirty_partial_saved_subchunks=1418`, `dirty_edge_neighbor_chunks=1418`, `dirty_edge_neighbor_subchunks=2836`, `dirty_last_edges=pos_x,pos_z`, upload failures `0`, `terrain_queue_max_ms=4.777`, `process_wall_p95_ms=0.033`, `gpu_compositor_submit_max_ms=0.158`, `current_chunk_collision=3`, `ground_misses=0`, and `terrain_samples=385`. | local macOS/Metal runtime gate only; does not change protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, visible quality, upload capacity, allocator policy, or default in-place upload behavior; external Windows/Vulkan/Direct3D profiler validation remains separate |
| `workspace` | Stage-pool high resident-set gate | `logs/gpu_terrain_upload_stage_pool_load_scaling_current/gpu-terrain-upload-stage-pool-load-scaling-summary.txt`, `scripts/gpu_terrain_upload_stage_pool_load_scaling_gate.sh`, `docs/GPU_UPLOAD_STAGE_POOL.md`, `docs/GPU_TERRAIN_LOAD_SCALING.md` | Added deterministic `pressure` workload support with a visual-smoke `chunk_disc` terrain fixture, propagated workload/fixture/upload/stage-pool counters through workload matrix, resident-set growth, and load-scaling summaries, and passed the baseline-vs-pooled stage-pool load-scaling gate. Baseline and pooled both reached `2289` subchunks/draws, `6292` faces, draw-command occupancy `27.942%`, and zero upload failures; pooled reported stage pool enabled with entries `9`, bytes `816`, creates/reuses `9/3128`. Old summary-only radius16 evidence is rejected with `missing source_case_set`. | default-off stage-pool evidence only; pressure fixture is stress-only through existing block-place updates, no protocol/storage/worldgen/chunk serialization/draw-distance/visible-quality change, and Windows/Vulkan/Direct3D plus external profiler validation remain separate before rollout |
| `workspace` | GPU upload stage pool | `logs/gpu_terrain_upload_stage_pool_current/gpu-terrain-upload-stage-pool-summary.txt`, `docs/GPU_UPLOAD_STAGE_POOL.md` | Added default-off `RUMPELMC_GPU_TERRAIN_UPLOAD_STAGE_POOL=1` to reuse exact-size `PackedByteArray` upload staging arrays before `RenderingDevice::buffer_update`, plus marker/report/gate coverage for pool enabled state, entries, bytes, creates, and reuses. Fresh release compare gate now covers movement and in-place dirty upload: movement baseline `850` uploads with pool enabled/creates/reuses `0/0/0`, movement pooled `851` uploads with entries `8`, bytes `720`, creates/reuses `8/843`; in-place baseline `853` uploads with `1` in-place upload and pool `0/0/0`, in-place pooled `853` uploads with `1` in-place upload and entries/bytes/creates/reuses `8/720/8/845`. Upload failures and retry/backoff stayed `0`/`none`; pooled dirty upload summary had `terrain_queue_max_ms=2.141`, `process_wall_p95_ms=0.054`, and `gpu_compositor_submit_max_ms=0.148`. | opt-in CPU-side staging-pool prototype only; no default upload behavior, upload capacity, allocator policy, retry/backoff policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed; local macOS/Metal evidence only, and high resident-set/load-scaling plus Windows/Vulkan/Direct3D profiler validation remain separate |
| `workspace` | GPU upload failure fallback gate | `logs/gpu_upload_failure_fallback_current/gpu-upload-failure-fallback-summary.txt`, `docs/GPU_UPLOAD_FAILURE_FALLBACK_GATE.md` | Added opt-in `RUMPELMC_GPU_TERRAIN_UPLOAD_FAILURE_INJECTION=1`, surfaced `gpu_upload_fail_injected`, and added a movement wrapper that requires forced failures to keep CPU ArrayMesh visual readiness, `shadow_path=arraymesh`, double-sided shadow markers, current collision readiness, zero ground misses, and non-sky terrain samples. Fresh release gate passed with `gpu_upload_fail=1052`, `gpu_upload_fail_injected=1052`, capacity/fragmentation failures `0/0`, `gpu_uploads=0`, `gpu_subchunks=0`, `mesh_visible=788`, `mesh_shadow_double=788`, `shadow_path=arraymesh`, current render/collision readiness `1/1`, `current_chunk_collision=2`, `ground_misses=0`, and `terrain_samples=384`. | diagnostic failure-injection slice only; it is not real capacity/fragmentation pressure, does not add retry/backoff, and does not change default upload behavior, allocator policy, draw distance, lighting, shadows, texture quality, protocol, storage, world generation, or chunk serialization; terrain queue budget is report-only in this forced CPU-fallback gate |
| `workspace` | GPU upload failure recovery guard | `client/rust_ext/src/lib.rs`, `docs/GPU_UPLOAD_FAILURE_RECOVERY.md` | Verified the current fallback-first upload recovery contract: if a GPU-visible path is active but a subchunk has no confirmed GPU slot, mesh planning stays on `FullArrayMesh`, and proxy-refresh reuse stays on `BuildMesh` until `existing_gpu_slot=true`. Targeted tests `gpu_upload_failure_recovery_keeps_cpu_fallback_until_slot_exists` and `terrain_mesh_build_plan_preserves_gpu_proxy_and_fallback_paths` passed. | unit-contract slice only; no failure injection runtime, retry loop, runtime backoff, upload capacity, allocator policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed |
| `workspace` | GPU upload retry/backoff telemetry | `logs/gpu_upload_retry_backoff_movement_current/movement-stress-summary.txt`, `logs/gpu_upload_retry_backoff_in_place_current/gpu-in-place-upload-summary.txt`, `logs/gpu_upload_retry_backoff_budget_current/gpu-terrain-upload-budget-summary.txt`, `/tmp/rumpel-gpu-report-upload-retry-backoff.txt` | Added explicit no-retry/no-backoff telemetry to GPU terrain runtime markers, movement/in-place summaries, aggregate report, and the upload budget gate. Fresh release movement passed with `gpu_upload_fail=0`, `gpu_upload_retry_policy=none`, all retry/backoff counters `0`, `terrain_queue_max_ms=1.950`, `process_wall_p95_ms=0.029`, `gpu_compositor_submit_max_ms=0.086`, upload max `1`, and new/replacement slot maxima `1/1`. Fresh in-place gate passed with `gpu_in_place_uploads=1`, `gpu_uploads=852`, `gpu_upload_fail=0`, retry/backoff `none/0`, `terrain_queue_max_ms=1.953`, `process_wall_p95_ms=0.023`, and `gpu_compositor_submit_max_ms=0.084`. Fresh upload-budget gate passed with `reason=within_budget` and retry/backoff `none/0` for movement plus in-place. Aggregate report surfaces latest `gpu_upload_retry_policy=none` and retry/backoff sums/maxima all `0`. | telemetry/gate slice only; no retry loop, runtime backoff, upload capacity, allocator policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed; local macOS/Metal GPU timestamps remain report-only and Windows profiler validation is still separate |
| `workspace` | GPU mass chunk-load gate | `logs/gpu_terrain_mass_chunk_load_current/gpu-terrain-mass-chunk-load-summary.txt`, `logs/gpu_terrain_load_scaling_radius16_summary_check/gpu-terrain-load-scaling-summary.txt`, `logs/gpu_terrain_upload_budget_current/gpu-terrain-upload-budget-summary.txt` | Added `scripts/gpu_terrain_mass_chunk_load_gate.sh` and `docs/GPU_TERRAIN_MASS_CHUNK_LOAD.md` as a summary gate that requires high resident-set load-scaling pressure together with the current per-frame upload budget. Fresh heavy load-scaling passed with `2482` GPU subchunks/draws, `3165` faces, draw-command occupancy `30.298%`, headroom `91360` bytes, `terrain_queue_max_ms=2.214`, `process_wall_p95_ms=0.059`, `gpu_compositor_submit_max_ms=5.677`, and zero upload failures. Fresh mass-load gate passed with `reason=mass_load_within_budget`, carrying upload-budget evidence of movement uploads `1/1`, movement payload `0.2/2.0 KiB`, and movement/in-place new/replacement slot maxima `1/1`. Negative validation with `RUMPELMC_GPU_MASS_LOAD_MIN_GPU_SUBCHUNKS=999999` failed as expected with `reason=mass_subchunk_pressure`. | governance/evidence slice only; local macOS/Metal GPU timestamps remain report-only; no runtime upload throttling, upload capacity, allocator policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed |
| `workspace` | GPU upload budget gate | `logs/gpu_terrain_upload_budget_current/gpu-terrain-upload-budget-summary.txt`, `/tmp/rumpel-gpu-report-upload-budget-lanes.txt` | Added `scripts/gpu_terrain_upload_budget.sh` and `docs/GPU_TERRAIN_UPLOAD_BUDGETING.md` as a summary-only per-frame upload budget over movement and in-place lane artifacts. Fresh gate passed with `status=pass`, `reason=within_budget`, `movement_uploads_max=1/1`, movement upload payload `0.2/2.0 KiB`, movement new/replace max `1/1`, in-place new/replace max `1/1`, new/replace max payloads `0.2/0.1 KiB`, and movement plus in-place upload failures `0`. Negative validation with `RUMPELMC_GPU_UPLOAD_BUDGET_MAX_UPLOAD_KB_PER_FRAME=0.05` failed as expected with `reason=movement_upload_kb_budget`. Aggregate report now surfaces max `terrain_queue_gpu_upload_new_slots`, `terrain_queue_gpu_upload_replace_slots`, `terrain_queue_gpu_upload_new_slot_kb`, and `terrain_queue_gpu_upload_replace_slot_kb`. | governance/report slice only; no runtime upload throttling, upload capacity, allocator policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed |
| `workspace` | GPU upload lane split telemetry | `logs/gpu_upload_lane_split_movement_current/movement-stress-summary.txt`, `logs/gpu_upload_lane_split_in_place_current/gpu-in-place-upload-summary.txt` | Terrain queue perf text and movement/in-place summaries now split GPU upload pressure into `terrain_queue_gpu_upload_new_slots`/`new_slot_kb` and `terrain_queue_gpu_upload_replace_slots`/`replace_slot_kb`, while keeping the existing total upload markers. Fresh release movement passed with `gpu_upload_fail=0`, `terrain_queue_max_ms=2.053`, `process_wall_p95_ms=0.056`, `gpu_compositor_submit_max_ms=0.148`, `terrain_queue_gpu_upload_new_slots=0/0.46/1`, `terrain_queue_gpu_upload_replace_slots=0/0.04/1`, new/replace max payload `0.2/0.1 KiB`, `gpu_draws=788`, `gpu_faces=1167`, and current render/collision readiness `1/1`. Fresh in-place gate passed with `gpu_in_place_uploads=1`, `gpu_in_place_upload_misses=63`, `gpu_uploads=852`, `gpu_upload_fail=0`, `terrain_queue_new_slot_uploads_max=1`, `terrain_queue_replace_slot_uploads_max=1`, `terrain_queue_max_ms=2.475`, `process_wall_p95_ms=0.054`, and `gpu_compositor_submit_max_ms=0.200`. | telemetry/gate slice only; no upload capacity, allocator policy, protocol, storage, worldgen, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed; local macOS/Metal GPU timestamps remain report-only and Windows profiler validation is still separate |
| `workspace` | Reproducible in-place upload gate | `logs/gpu_terrain_in_place_upload_gate_current/gpu-in-place-upload-summary.txt` | Added `scripts/gpu_terrain_in_place_upload_gate.sh` to run the opt-in in-place subchunk upload path against a clean isolated RocksDB path, preventing persisted server state from turning block-edit validation into a no-op. Fresh release gate passed with `gpu_in_place_upload_enabled=1`, `gpu_in_place_uploads=1`, `gpu_in_place_upload_misses=3`, `gpu_uploads=699`, `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_upload_ms_max=0.038`, `gpu_upload_stage_ms_max=0.013`, `gpu_upload_update_ms_max=0.034`, `gpu_draw_patch_ms_max=0.013`, `terrain_queue_max_ms=1.461`, `process_wall_p95_ms=0.043`, `gpu_compositor_submit_max_ms=0.157`, `dirty_partial_saved_subchunks=1`, `dirty_last_rebuild_subchunks=2`, `current_chunk_loaded=1`, and `current_chunk_collision=2`. | gate/evidence slice only; path is still opt-in, validated on local macOS/Metal, and Windows/Vulkan/Direct3D profiler captures plus broader upload-pressure/load-scaling remain separate work |
| `workspace` | Default-off in-place GPU subchunk uploads | `logs/gpu_in_place_upload_single_edge_place_y63_wood/block-edit-stress-summary.txt` | Added `RUMPELMC_GPU_TERRAIN_IN_PLACE_SUBCHUNK_UPLOAD=1` to update an existing GPU subchunk at the same face count without freeing/reallocating its face range or patching its indirect draw command. Release single-edge solid-to-solid block-edit smoke passed with `smoke_err=0`, `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_in_place_upload_enabled=1`, `gpu_in_place_uploads=2`, `gpu_in_place_upload_misses=2`, `gpu_uploads=693`, `gpu_upload_ms_max=0.042`, `gpu_upload_stage_ms_max=0.016`, `gpu_draw_patch_ms_max=0.038`, `gpu_compositor_submit_max_ms=0.228`, `terrain_queue_max_ms=1.391`, `process_wall_p95_ms=0.033`, `dirty_partial_saved_subchunks=1`, `dirty_last_rebuild_subchunks=2`, and current render/collision readiness `1/1`. | opt-in only; validates same-face-count dirty updates on local macOS/Metal, while broad upload-pressure, resident-set/load-scaling, and Windows/Vulkan/Direct3D profiler captures still need separate gates |
| `workspace` | GPU upload scratch-buffer reuse | `logs/gpu_upload_scratch_reuse_movement_retry3/movement-stress-summary.txt` | Reused a CPU scratch buffer for packed-face upload encoding and switched single indirect draw-command patches to fixed stack bytes, reducing hot-path heap churn without changing GPU data layout, draw distance, lighting, shadows, texture quality, protocol, storage, worldgen, or chunk serialization. Fresh release movement passed with `smoke_err=0`, `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `terrain_queue_max_ms=1.676`, `process_wall_p95_ms=0.047`, `gpu_compositor_submit_max_ms=0.137`, `gpu_upload_ms_max=0.050`, `gpu_upload_stage_ms_max=0.013`, `gpu_draw_patch_ms_max=0.023`, `gpu_draws=544`, `gpu_faces=544`, and current render/collision readiness `1/1`. | first safe CPU-side upload-prep slice only; high resident-set/load-scaling and upload-pressure gates were not rerun for this patch, and local `frame_p95_ms`, `fps_p05`, and Godot GPU timestamps remain report-only on macOS/Metal |

## 2026-06-15

| Commit | Slice | Artifact | Key Result | Caveat |
| --- | --- | --- | --- | --- |
| `workspace` | Clean resident-set 16/18 matrix | `logs/world_streaming_resident_set_matrix_radius16_18_clean/resident-set-matrix-summary.txt` | Explicit heavy run after clearing a stale local server produced passing source rows for radii `16` and `18` with zero upload failures, but the top-level matrix failed the pressure gate: best radius `16`, `max_gpu_draws=1859`, `max_gpu_subchunks=1859`, `max_gpu_faces=2243`, draw-command occupancy `22.693%`, and headroom `101328` bytes | negative pressure evidence; do not use this run to activate GPU repack, change draw capacity, alter default draw distance, or adjust unload/allocator policy |
| `workspace` | External profiling campaign | `logs/external_profiling_campaign_current/external-profiling-campaign-summary.txt`, `logs/external_profiling_campaign_current/external-profiling-campaign-plan.txt` | Campaign gate passed with macOS/Metal shadow profiler lane prepared from the existing 4-row capture pack, Windows GPU lane pending external machine/tool access, and Linux/Vulkan marked optional/deferred | no real external profiler trace has been captured; `external_profile_status=pending_external_profiler` is a blocker state, not measured GPU timing evidence |
| `workspace` | Resident-set matrix wrapper | `logs/world_streaming_resident_set_matrix_current/resident-set-matrix-summary.txt` | Added a summary-first trend wrapper for resident-set growth evidence. The current radius-16 source should validate `max_gpu_draws=2482`, `max_gpu_subchunks=2482`, draw-command occupancy about `30.298%`, and zero upload failures without rerunning heavy Godot captures | wrapper/gate slice only; fresh heavier radii require explicit `RUMPELMC_RESIDENT_SET_MATRIX_RUN=1`, and no default draw distance, unload policy, allocator, repack, protocol, storage, worldgen, chunk serialization, lighting, shadows, texture quality, or visible quality changes |
| `post-5e9fdfe` | World load/upload measurement | `logs/world_load_upload_measurement_20260615/movement`, `logs/world_load_upload_measurement_20260615/workload_matrix`, `logs/world_load_upload_measurement_20260615/gpu-terrain-report.txt` | Fresh release movement plus standard workload matrix passed with `gpu_upload_fail=0`, max `gpu_effective_draws=996`, max `gpu_faces=1562`, max draw-command occupancy `12.158%`, max `terrain_queue_max_ms=2.151`, max `process_wall_p95_ms=0.053`, max `gpu_compositor_submit_max_ms=0.212`, and max per-frame upload payload `terrain_queue_gpu_upload_kb=1.5`; the loaded resident set still has large headroom (`gpu_draw_cmd_headroom_bytes=115136`) | measurement/artifact slice only; local visual FPS and Godot GPU timestamps remain report-only on macOS/Metal, and this points the next optimization toward world streaming/resident-set growth rather than active GPU buffer repack |

## 2026-06-14

| Commit | Slice | Artifact | Key Result | Caveat |
| --- | --- | --- | --- | --- |
| `post-76f0954` | GPU repack final-swap guard | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added a disabled final-swap guard after apply preview. It exposes `gpu_repack_final_swap_ready=0`, `gpu_repack_final_swap_blocked=1`, and `gpu_repack_final_swap_slots` when apply readiness is valid, making the runtime mutation boundary explicit | guard only; no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, slot mutation, allocator mutation, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-fb3420b` | GPU repack apply scaffold | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added a disabled apply preview that consumes the commit proof, verifies future operation order, staged slot mirror, face/draw byte lengths, and allocator free-range mirror, and exposes `gpu_repack_apply_ready`, `gpu_repack_apply_steps`, and `gpu_repack_apply_slots` | apply scaffold only; no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, slot mutation, allocator mutation, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-f6a6a1c` | GPU repack commit proof | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added a disabled commit-point proof over the staged swap preview. It validates the future operation order, contiguous replacement slot map, draw-byte count, and allocator tail rebuild, and exposes `gpu_repack_commit_ready`, `gpu_repack_commit_steps`, and `gpu_repack_commit_tail_free` | commit proof only; no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, slot mutation, allocator mutation, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-6f582bf` | GPU repack staged swap guard | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added an all-or-nothing staged swap preview that collects compact replacement face bytes, replacement indirect draw bytes, deterministic replacement slot map, and the tail free range before any active renderer state can be replaced. Perf/report markers now include `gpu_repack_stage_ready`, `gpu_repack_stage_slots`, and `gpu_repack_stage_bytes` | staged guard only; no active face-buffer RID swap, uniform-set swap, indirect-buffer upload/swap, slot mutation, allocator mutation, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-d2f05b4` | GPU repack draw remap preview | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added CPU-side replacement indirect draw command preview in current draw order using compacted `first_instance` offsets. Perf/report markers now include `gpu_repack_draw_ready` and `gpu_repack_draw_bytes`, and remap failures are classified as `draw_rebuild_error` | draw remap preview only; no indirect-buffer upload/swap, active render binding swap, slot mutation, allocator policy change, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-ec277bb` | GPU repack binding preview | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Extended the explicit one-shot replacement upload sample with temporary uniform-set creation against the replacement face buffer and existing shader/atlas resources. Perf/report markers now include `gpu_repack_bind_ready` and `gpu_repack_bind_ms`, with temporary binding failure classified as `draw_rebuild_error` | binding preview only; temporary uniform set and buffer are freed immediately, with no active render binding swap, slot mutation, allocator policy change, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-527d2b5` | GPU repack replacement upload preview | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added explicit one-shot temporary replacement storage-buffer upload sampling from the deterministic compact repack payload behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK_UPLOAD_PREVIEW=1`. Perf/report markers now include `gpu_repack_upload_ready`, `gpu_repack_upload_bytes`, and `gpu_repack_upload_ms`, with invalid temporary buffer creation classified as `upload_error` | upload preview only; temporary buffer is freed immediately, with no render binding swap, slot mutation, allocator policy change, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-b21da0b` | GPU repack payload preview | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added deterministic compact replacement payload assembly from resident CPU source bytes behind `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`. Perf/report markers now include `gpu_repack_payload_ready` and `gpu_repack_payload_bytes`, with `source_size_mismatch` failure classification covered by unit tests | payload preview only; no RenderingDevice buffer upload, buffer swap, slot mutation, allocator policy change, capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-52b6e7e` | GPU repack resident source mirror | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added CPU-owned encoded packed-face source bytes for resident GPU terrain subchunks when `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1` is enabled. Perf/report markers now include `gpu_repack_source_subchunks`, `gpu_repack_source_bytes`, and `gpu_repack_source_missing`, and missing resident source data maps to `gpu_repack_failure_reason=missing_source` | source-readiness slice only; no runtime buffer replacement, slot mutation, allocator policy change, buffer capacity change, protocol/storage/worldgen/chunk serialization change, or visible quality change |
| `post-8a71daa` | GPU buffer repack marker prototype | `client/rust_ext/src/gpu_terrain.rs`, `client/rust_ext/src/lib.rs`, `scripts/gpu_terrain_report.sh` | Added default-off `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1` parsing, marker-only `gpu_repack_*` perf fields, read-only deterministic compaction planner, report aggregation for numeric repack fields plus latest `gpu_repack_failure_reason`, and planner/flag telemetry unit coverage | prototype foundation only; `gpu_repack_active=0`, no runtime buffer swap, no slot mutation, no allocator policy change, no buffer capacity change, and no protocol/storage/worldgen/chunk serialization or visible quality changes |
| `post-e3d1e14` | GPU buffer repack design | `docs/GPU_BUFFER_REPACK.md` | Documented a behavior-preserving repack design behind explicit `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1` rollback flag, with deterministic planning, no GPU readback in the first prototype, all-or-nothing buffer swap preference, marker/active telemetry, trigger policy, and allocator/profile gates | design/docs only; no allocator policy, buffer capacity, runtime renderer behavior, protocol, storage, world generation, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed |
| `post-4abc7d4` | GPU allocator stress gate | `scripts/gpu_terrain_allocator_stress_gate.sh`, `logs/week2_gpu_allocator_telemetry_20260614/gpu-allocator-stress-gate-summary.txt`, `logs/week2_gpu_allocator_telemetry_20260614/gpu-terrain-report.txt` | Added a scoped allocator stress gate that refreshes the GPU terrain report and fails on report error patterns, nonzero `gpu_upload_fail`, nonzero `gpu_upload_fail_capacity`, nonzero `gpu_upload_fail_fragmented`, missing/too-low `gpu_free_faces` or `gpu_largest_free`, and `gpu_fragmentation_pct` over the configured threshold. Fresh allocator artifact passed with `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_free_ranges=2.000`, `gpu_free_faces=4193006.000`, `gpu_largest_free=4193004.000`, `gpu_fragmented_free_faces=2.000`, `gpu_fragmentation_pct=0.000`, and `terrain_queue_max_ms=2.036`; synthetic nonzero upload failure was rejected | gate/report slice only; no allocator policy, buffer capacity, runtime renderer behavior, protocol, storage, world generation, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed |
| `post-b3cc816` | GPU allocator telemetry refresh | `logs/week2_gpu_allocator_telemetry_20260614/movement`, `logs/week2_gpu_allocator_telemetry_20260614/gpu-terrain-report.txt`, `/tmp/rumpel-allocator-full-report.txt`, `/tmp/rumpel-allocator-full-report-refresh.out` | Refreshed allocator telemetry propagation: movement, workload matrix, fill stress, and aggregate report summaries now surface upload failure causes (`gpu_upload_fail_capacity`, `gpu_upload_fail_fragmented`) plus allocator free-range state (`gpu_free_ranges`, `gpu_free_faces`, `gpu_largest_free`, `gpu_fragmented_free_faces`, `gpu_fragmentation_pct`). Fresh release movement passed with `gpu_upload_fail=0`, `gpu_upload_fail_capacity=0`, `gpu_upload_fail_fragmented=0`, `gpu_free_ranges=2`, `gpu_free_faces=4193006`, `gpu_largest_free=4193004`, `gpu_fragmented_free_faces=2`, `gpu_fragmentation_pct=0.0`, `terrain_queue_max_ms=2.036`, `gpu_compositor_submit_max_ms=0.213`, and `process_wall_p95_ms=0.049`; full aggregate report still has historical max `gpu_free_ranges=134`, `gpu_fragmented_free_faces=926`, and zero upload failures | telemetry/reporting slice only; no allocator policy, buffer capacity, renderer behavior, protocol, storage, world generation, chunk serialization, draw distance, lighting, shadows, texture quality, or visible quality changed |
| `post-77a7e0a` | Dirty-update default and rollback checkpoint | `docs/AGENT_MEMORY.md`, `docs/GPU_ROADMAP.md`, `docs/AGENT_HANDOFF.md`, `logs/week2_gpu_dirty_default_on_20260614/gpu-terrain-report.txt`, `logs/gpu_single_edge_dirty_compare_default_on` | Checkpointed the current dirty-update status: `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` remains default-on when unset, explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` remains the full-rebuild rollback/control path, fresh default-on single-edge evidence still reports `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, movement `terrain_queue_max_ms=1.885`, and `gpu_upload_fail=0`, while historical rollback compare evidence still shows full-control counters at `dirty_partial_subchunks=0` and `dirty_partial_saved_subchunks=0` | docs/status checkpoint only; no fresh rollback smoke was rerun in this slice, and no runtime, renderer, protocol, storage, world generation, chunk serialization, visible quality, shadow quality, or native-shadow behavior changed |
| `post-288291e` | Dirty report terrain queue diagnostics | `scripts/gpu_terrain_report.sh`, `/tmp/rumpel-dirty-report-parser-fix.txt`, `/tmp/rumpel-full-report-parser-fix.txt` | Fixed aggregate report parsing for indexed `movement_terrain_queue` lines, so dirty-only scoped reports now surface `terrain_queue_max_ms=1.885` and origin `logs/week2_gpu_dirty_default_on_20260614/single_edge/movement-stress-summary.txt`; full historical report still preserves max `terrain_queue_max_ms=28.314` from `logs/gpu_terrain_queue_work/perf-baseline-summary.txt` | report/parser slice only; no runtime, renderer, dirty-update behavior, protocol, storage, world generation, chunk serialization, or visible quality changed |
| `post-90dd09a` | Dirty-update default-on single-edge evidence | `logs/week2_gpu_dirty_default_on_20260614/single_edge/block-edit-stress-summary.txt`, `logs/week2_gpu_dirty_default_on_20260614/gpu-terrain-report.txt` | With `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset, release single-edge toggle at global `127,64,80` passed default-on partial dirty validation: `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`, `chunk_replace=2`, `dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=20`, movement `terrain_queue_max_ms=1.885`, `gpu_compositor_submit_max_ms=0.134`, `process_wall_p95_ms=0.049`, `terrain_samples=384`, and `gpu_upload_fail=0` | default-on evidence for one single-edge dirty shape only; visual FPS and local GPU timestamps remain report-only on macOS/Metal |
| `post-2f151c3` | Fresh movement/workload/fill baselines | `logs/week2_gpu_baseline_20260614/gpu-terrain-report.txt`, `logs/week2_gpu_baseline_20260614/movement`, `logs/week2_gpu_baseline_20260614/workload_standard`, `logs/week2_gpu_baseline_20260614/fill` | Captured fresh release movement, standard workload matrix, and fill-stress repeats `1/4/8`; scoped report records `gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=12752`, draw command occupancy `19.458%`, max `terrain_queue_max_ms=6.367`, max `gpu_compositor_submit_max_ms=0.180`, max `process_wall_p95_ms=0.052`, movement baseline `terrain_queue_max_ms=1.992`, workload max `terrain_queue_max_ms=2.173`, and fill repeat `8` passed with `smoke_err=0` and `terrain_samples=384` | measurement/artifact slice only; local FPS and `gpu_compositor_gpu_us` remain report-only on macOS/Metal, and the fill repeat `8` queue max is close to the `6.667ms` budget but still passed |
| `post-960bbea` | Current-HEAD aggregate GPU report refresh | `logs/gpu-terrain-report.txt` | Refreshed the ignored full-log aggregate report after the native-shadow command-buffer submit readiness slice; the report records `gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=13344`, max draw command occupancy `30.298%`, max `terrain_queue_gpu_uploads=1`, max `terrain_queue_gpu_upload_kb=1.600`, `native_shadow_active=0`, `native_shadow_command_buffer_submit_ready=0`, `native_shadow_command_buffer_submit_error_count=0`, and selects `logs/gpu_native_shadow_command_buffer_submit_contract_20260614/movement-stress-summary.txt` as the latest GPU Terrain Movement Stress source | report refresh only; `logs/gpu-terrain-report.txt` is ignored local evidence and aggregates all historical `logs` summaries, so old outliers such as `terrain_queue_max_ms=28.314`, `gpu_compositor_submit_max_ms=5.277`, and `frame_p95_ms=143.694` remain historical maxima, not new runtime regressions |
| `c98893d` | Native-shadow command buffer submit readiness contract proof | `logs/gpu_native_shadow_command_buffer_submit_contract_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-command-buffer-submit-contract.txt` | Native-shadow resource descriptors now expose future command buffer submit readiness markers (`native_shadow_command_buffer_submit_ready`, `native_shadow_command_buffer_submit_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness/pass-validation/pass-lifecycle/record-readiness fields; targeted unit coverage locks the active descriptor submit-readiness contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.162`, `gpu_compositor_submit_max_ms=0.458`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no command buffer submission, command buffer recording, pass begin/end, pass descriptor validation, framebuffer binding to RenderDevice passes, framebuffer descriptor validation, framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, RenderingDevice draw submission, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `b7ddd62` | Native-shadow command buffer record readiness contract proof | `logs/gpu_native_shadow_command_buffer_record_contract_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-command-buffer-record-contract.txt` | Native-shadow resource descriptors now expose future command buffer record readiness markers (`native_shadow_command_buffer_record_ready`, `native_shadow_command_buffer_record_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness/pass-validation/pass-lifecycle fields; targeted unit coverage locks the active descriptor record-readiness contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=3.248`, `gpu_compositor_submit_max_ms=0.224`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no command buffer recording, command buffer submission, pass begin/end, pass descriptor validation, framebuffer binding to RenderDevice passes, framebuffer descriptor validation, framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, RenderingDevice draw submission, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `7ae1ec7` | Native-shadow pass lifecycle readiness contract proof | `logs/gpu_native_shadow_pass_lifecycle_contract_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-pass-lifecycle-contract.txt` | Native-shadow resource descriptors now expose future pass lifecycle readiness markers (`native_shadow_pass_lifecycle_ready`, `native_shadow_pass_lifecycle_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness/pass-validation fields; targeted unit coverage locks the active descriptor lifecycle contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.085`, `gpu_compositor_submit_max_ms=0.187`, and `process_wall_p95_ms=0.007`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no pass begin/end readiness execution, pass descriptor validation, framebuffer binding to RenderDevice passes, framebuffer descriptor validation, framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `2f49973` | Native-shadow pass descriptor validation contract proof | `logs/gpu_native_shadow_pass_descriptor_contract_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-pass-descriptor-contract.txt` | Native-shadow resource descriptors now expose future pass descriptor validation markers (`native_shadow_pass_descriptor_valid`, `native_shadow_pass_descriptor_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation/bind-readiness fields; targeted unit coverage locks the active descriptor pass validation contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.106`, `gpu_compositor_submit_max_ms=0.096`, and `process_wall_p95_ms=0.008`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no pass descriptor validation, framebuffer binding to RenderDevice passes, framebuffer descriptor validation, framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `65ac059` | Native-shadow framebuffer bind readiness contract proof | `logs/gpu_native_shadow_framebuffer_bind_contract_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-framebuffer-bind-contract.txt` | Native-shadow resource descriptors now expose future framebuffer bind readiness markers (`native_shadow_framebuffer_bind_ready`, `native_shadow_framebuffer_bind_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime/descriptor-validation fields; targeted unit coverage locks the active descriptor bind readiness contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.026`, `gpu_compositor_submit_max_ms=0.204`, and `process_wall_p95_ms=0.011`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no framebuffer binding to RenderDevice passes, framebuffer descriptor validation, framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `d5eedc1` | Native-shadow framebuffer descriptor validation contract proof | `logs/gpu_native_shadow_framebuffer_descriptor_validation_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-framebuffer-descriptor-validation.txt` | Native-shadow resource descriptors now expose future framebuffer descriptor validation markers (`native_shadow_framebuffer_descriptor_valid`, `native_shadow_framebuffer_descriptor_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only/lifetime fields; targeted unit coverage locks the active descriptor validation contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.029`, `gpu_compositor_submit_max_ms=0.250`, and `process_wall_p95_ms=0.007`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no framebuffer descriptor validation, framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `9508a8b` | Native-shadow framebuffer attachment lifetime contract proof | `logs/gpu_native_shadow_framebuffer_attachment_lifetime_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-framebuffer-attachment-lifetime-descriptor.txt` | Native-shadow resource descriptors now expose future framebuffer attachment lifetime markers (`native_shadow_framebuffer_attachment_owned`, `native_shadow_framebuffer_attachment_reuse_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat/depth-only fields; targeted unit coverage locks the active descriptor attachment lifetime contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.738`, `gpu_compositor_submit_max_ms=0.196`, and `process_wall_p95_ms=0.008`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no framebuffer attachment ownership, attachment reuse, framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `7f1148e` | Native-shadow framebuffer depth-only contract proof | `logs/gpu_native_shadow_framebuffer_depth_only_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-framebuffer-depth-only-descriptor.txt` | Native-shadow resource descriptors now expose future framebuffer depth-only markers (`native_shadow_framebuffer_depth_only_enabled`, `native_shadow_framebuffer_color_attachment_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer/pass-compat fields; targeted unit coverage locks the active descriptor framebuffer contract to `1/0`, while fresh env-on release smoke kept disabled runtime markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.118`, `gpu_compositor_submit_max_ms=1.043`, and `process_wall_p95_ms=0.050`; aggregate report now surfaces both maxima and selected the fresh summary | descriptor/marker/report proof only; no framebuffer depth-only validation, color attachment creation, framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `13475dc` | Native-shadow framebuffer/pass compatibility contract proof | `logs/gpu_native_shadow_framebuffer_pass_compat_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-framebuffer-pass-compat-descriptor.txt` | Native-shadow resource descriptors now expose future framebuffer/pass compatibility markers (`native_shadow_framebuffer_pass_compat_status`, `native_shadow_framebuffer_pass_compat_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier/framebuffer fields; targeted unit coverage locks the active descriptor compatibility state to `not_validated/0`, while fresh env-on release smoke kept disabled runtime compatibility markers at `none/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.459`, `gpu_compositor_submit_max_ms=0.240`, and `process_wall_p95_ms=0.052`; aggregate report now surfaces compatibility error maxima and selected the fresh summary | descriptor/marker/report proof only; no framebuffer/pass compatibility validation, framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `068c1b5` | Native-shadow framebuffer contract proof | `logs/gpu_native_shadow_framebuffer_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-framebuffer-descriptor.txt` | Native-shadow resource descriptors now expose future framebuffer markers (`native_shadow_framebuffer_status`, `native_shadow_framebuffer_rid_allocated`, `native_shadow_framebuffer_attachment_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer/barrier fields; targeted unit coverage locks the active descriptor framebuffer state to `not_created/0/1`, while fresh env-on release smoke kept disabled runtime framebuffer markers at `none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.004`, `gpu_compositor_submit_max_ms=0.136`, and `process_wall_p95_ms=0.048`; aggregate report now surfaces RID/attachment maxima and selected the fresh summary | descriptor/marker/report proof only; no framebuffer/RID creation, resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `9a1677f` | Native-shadow resource barrier contract proof | `logs/gpu_native_shadow_resource_barrier_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-resource-barrier-descriptor.txt` | Native-shadow resource descriptors now expose future resource barrier markers (`native_shadow_resource_barrier_status`, `native_shadow_resource_transition_count`, `native_shadow_resource_barrier_error_count`) alongside existing resource/pass/RID/attachment/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer fields; targeted unit coverage locks the active descriptor resource barrier state to `not_issued/0/0`, while fresh env-on release smoke kept disabled runtime barrier markers at `none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.196`, `gpu_compositor_submit_max_ms=0.272`, and `process_wall_p95_ms=0.011`; aggregate report now surfaces transition/error maxima and selected the fresh summary | descriptor/marker/report proof only; no resource barrier/transition issue, depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `ad9f9d2` | Native-shadow depth attachment contract proof | `logs/gpu_native_shadow_depth_attachment_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-depth-attachment-descriptor.txt` | Native-shadow resource descriptors now expose future depth attachment markers (`native_shadow_depth_attachment_status`, `native_shadow_depth_attachment_binding_count`, `native_shadow_depth_attachment_clear_count`) alongside existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission/command-buffer fields; targeted unit coverage locks the active descriptor depth attachment state to `not_bound/0/0`, while fresh env-on release smoke kept disabled runtime depth attachment markers at `none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.660`, `gpu_compositor_submit_max_ms=0.212`, and `process_wall_p95_ms=0.050`; aggregate report now surfaces binding/clear maxima and selected the fresh summary | descriptor/marker/report proof only; no depth attachment binding/clear, command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `4844b10` | Native-shadow command buffer contract proof | `logs/gpu_native_shadow_command_buffer_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-command-buffer-descriptor.txt` | Native-shadow resource descriptors now expose future command buffer markers (`native_shadow_command_buffer_status`, `native_shadow_command_buffer_submit_count`, `native_shadow_command_buffer_error_count`) alongside existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission fields; targeted unit coverage locks the active descriptor command buffer state to `not_recorded/0/0`, while fresh env-on release smoke kept disabled runtime command buffer markers at `none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.042`, `gpu_compositor_submit_max_ms=0.224`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces command buffer submit/error maxima and selected the fresh summary | descriptor/marker/report proof only; no command buffer recording/submission, RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `0304f21` | Native-shadow draw submission contract proof | `logs/gpu_native_shadow_draw_submit_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-draw-submit-descriptor.txt` | Native-shadow resource descriptors now expose future draw submission markers (`native_shadow_draw_status`, `native_shadow_draw_call_count`, `native_shadow_draw_face_count`) alongside existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth/submission fields; targeted unit coverage locks the active descriptor draw submission state to `not_submitted/0/0`, while fresh env-on release smoke kept disabled runtime draw markers at `none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.244`, `gpu_compositor_submit_max_ms=0.127`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces draw call/face count maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice draw submission, pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `a76d3f7` | Native-shadow pass submission contract proof | `logs/gpu_native_shadow_pass_submit_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-pass-submit-descriptor.txt` | Native-shadow resource descriptors now expose future pass submission boundary markers (`native_shadow_pass_submit_status`, `native_shadow_pass_begin_count`, `native_shadow_pass_end_count`) alongside existing resource/pass/RID/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth fields; targeted unit coverage locks the active descriptor submission state to `not_submitted/0/0`, while fresh env-on release smoke kept disabled runtime submission markers at `none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.153`, `gpu_compositor_submit_max_ms=0.186`, and `process_wall_p95_ms=0.051`; aggregate report now surfaces begin/end maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice pass begin/end, pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `690416a` | Native-shadow pass RID contract proof | `logs/gpu_native_shadow_pass_rid_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-pass-rid-descriptor.txt` | Native-shadow resource descriptors now expose future pass/RID contract markers (`native_shadow_pass_status`, `native_shadow_pass_rid_allocated`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/module/light/depth fields; targeted unit coverage locks the active descriptor pass state to `descriptor_ready/0`, while fresh env-on release smoke kept disabled runtime pass markers at `none/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.945`, `gpu_compositor_submit_max_ms=0.238`, and `process_wall_p95_ms=0.051`; aggregate report now surfaces the numeric pass RID allocation maximum and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice pass/RID allocation, shader compilation, shader module allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `033db27` | Native-shadow shader module contract proof | `logs/gpu_native_shadow_shader_module_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-shader-module-descriptor.txt` | Native-shadow resource descriptors now expose future shader module contract markers (`native_shadow_shader_module_status`, `native_shadow_shader_module_rid_allocated`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/source/light/depth fields; targeted unit coverage locks the active descriptor module state to `source_ready/0`, while fresh env-on release smoke kept disabled runtime module markers at `none/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.341`, `gpu_compositor_submit_max_ms=0.276`, and `process_wall_p95_ms=0.053`; aggregate report now surfaces the numeric RID allocation maximum and selected the fresh summary | descriptor/marker/report proof only; no shader source compilation, shader module/RID allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `bf4c393` | Native-shadow shader source contract proof | `logs/gpu_native_shadow_shader_source_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-shader-source-descriptor.txt` | Native-shadow resource descriptors now expose future shader source/module contract markers (`native_shadow_shader_source_bytes`, `native_shadow_shader_source_checksum`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/light/depth fields; targeted unit coverage locks the active descriptor source contract to `59` bytes and checksum `3794449943`, while fresh env-on release smoke kept disabled runtime source markers at `0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.688`, `gpu_compositor_submit_max_ms=0.186`, and `process_wall_p95_ms=0.053`; aggregate report now surfaces the numeric source contract maxima and selected the fresh summary | descriptor/marker/report proof only; no shader source compilation, shader module/RID allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `39b5a41` | Native-shadow depth/clip descriptor proof | `logs/gpu_native_shadow_depth_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-depth-descriptor.txt` | Native-shadow resource descriptors now expose future depth/clip contract markers (`native_shadow_depth_clip_space`, `native_shadow_depth_range_source`, `native_shadow_depth_near_milli`, `native_shadow_depth_far_chunks`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader/light fields; targeted unit coverage locks active descriptor depth state to `zero_to_one/scene_shadow_radius/100/5`, while fresh env-on release smoke kept disabled runtime depth markers at `none/none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.082`, `gpu_compositor_submit_max_ms=0.237`, and `process_wall_p95_ms=0.050`; aggregate report now surfaces the numeric depth maxima and selected the fresh summary | descriptor/marker/report proof only; no depth-range upload, light matrix binding, shader compilation, RenderingDevice shader/pass/RID allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `bed9cd9` | Native-shadow light descriptor proof | `logs/gpu_native_shadow_light_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-light-descriptor.txt` | Native-shadow resource descriptors now expose future light/matrix markers (`native_shadow_light_source`, `native_shadow_light_space`, `native_shadow_cascade_count`, `native_shadow_light_matrix_bytes`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform/shader fields; targeted unit coverage locks active descriptor light state to `directional_light/world_to_shadow/1/64`, while fresh env-on release smoke kept disabled runtime light markers at `none/none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=3.698`, `gpu_compositor_submit_max_ms=0.240`, and `process_wall_p95_ms=0.052`; aggregate report now surfaces the numeric light maxima and selected the fresh summary | descriptor/marker/report proof only; no light matrix binding, shader compilation, RenderingDevice shader RID allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `12fff45` | Native-shadow shader descriptor proof | `logs/gpu_native_shadow_shader_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-shader-descriptor.txt` | Native-shadow resource descriptors now expose future shader markers (`native_shadow_shader_language`, `native_shadow_shader_entry`, `native_shadow_shader_depth_output_enabled`, `native_shadow_shader_color_output_enabled`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw/uniform fields; targeted unit coverage locks active descriptor shader state to `glsl/native_shadow_depth/1/0`, while fresh env-on release smoke kept disabled runtime shader markers at `none/none/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.016`, `gpu_compositor_submit_max_ms=0.238`, and `process_wall_p95_ms=0.055`; aggregate report now surfaces the numeric shader output maxima and selected the fresh summary | descriptor/marker/report proof only; no shader compilation, RenderingDevice shader RID allocation, native shadow render pass, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `08448c2` | Native-shadow uniform descriptor proof | `logs/gpu_native_shadow_uniform_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-uniform-descriptor.txt` | Native-shadow resource descriptors now expose future uniform/binding markers (`native_shadow_uniform_set_index`, `native_shadow_face_buffer_binding`, `native_shadow_push_constant_bytes`, `native_shadow_texture_sampling_enabled`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline/draw fields; targeted unit coverage locks active descriptor uniform state to `0/0/64/0`, while fresh env-on release smoke kept disabled runtime uniform markers at `0/0/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.101`, `gpu_compositor_submit_max_ms=0.143`, and `process_wall_p95_ms=0.051`; aggregate report now surfaces the numeric uniform maxima and selected the fresh summary | descriptor/marker/report proof only; no shader, native shadow render pass, RenderingDevice RID allocation, RenderingDevice draw submission, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `6314795` | Native-shadow draw descriptor proof | `logs/gpu_native_shadow_draw_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-draw-descriptor.txt` | Native-shadow resource descriptors now expose future draw/input markers (`native_shadow_draw_source`, `native_shadow_draw_primitive`, `native_shadow_draw_face_stride_bytes`, `native_shadow_draw_command_stride_bytes`, `native_shadow_draw_indirect_enabled`) alongside existing resource/pass/sampler/depth-bias/viewport/pipeline fields; targeted unit coverage locks active descriptor draw state to `packed_faces/triangles/16/16/1`, while fresh env-on release smoke kept disabled runtime draw markers at `none/none/0/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.203`, `gpu_compositor_submit_max_ms=0.259`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces the numeric draw maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice draw submission, shadow-map allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `5b69f52` | Native-shadow pipeline descriptor proof | `logs/gpu_native_shadow_pipeline_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-pipeline-descriptor.txt` | Native-shadow resource descriptors now expose future depth pipeline markers (`native_shadow_pipeline_depth_test_enabled`, `native_shadow_pipeline_depth_write_enabled`, `native_shadow_pipeline_cull_mode`, `native_shadow_pipeline_front_face`) alongside existing resource/pass/sampler/depth-bias/viewport fields; targeted unit coverage locks active descriptor pipeline state to `1/1/back/clockwise`, while fresh env-on release smoke kept disabled runtime pipeline markers at `0/0/none/none`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.035`, `gpu_compositor_submit_max_ms=0.135`, and `process_wall_p95_ms=0.051`; aggregate report now surfaces the numeric pipeline flag maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice pipeline allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `3bb4b3a` | Native-shadow viewport descriptor proof | `logs/gpu_native_shadow_viewport_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-viewport-descriptor.txt` | Native-shadow resource descriptors now expose future viewport markers (`native_shadow_viewport_x_px`, `native_shadow_viewport_y_px`, `native_shadow_viewport_width_px`, `native_shadow_viewport_height_px`, `native_shadow_viewport_min_depth_milli`, `native_shadow_viewport_max_depth_milli`) alongside existing resource/pass/sampler/depth-bias fields; targeted unit coverage locks active descriptor viewport state to `0/0/2048/2048/0/1000`, while fresh env-on release smoke kept disabled runtime viewport markers at `0/0/0/0/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.112`, `gpu_compositor_submit_max_ms=0.134`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces all six viewport maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `b1721e8` | Native-shadow depth bias descriptor proof | `logs/gpu_native_shadow_depth_bias_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-depth-bias-descriptor.txt` | Native-shadow resource descriptors now expose future depth bias markers (`native_shadow_depth_bias_constant_milli`, `native_shadow_depth_bias_slope_milli`, `native_shadow_depth_bias_clamp_milli`) alongside existing resource/pass/sampler fields; targeted unit coverage locks active descriptor depth bias state to `2/1500/0`, while fresh env-on release smoke kept disabled runtime bias markers at `0/0/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.233`, `gpu_compositor_submit_max_ms=0.141`, and `process_wall_p95_ms=0.051`; aggregate report now surfaces all three bias maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `74cc460` | Native-shadow sampler descriptor proof | `logs/gpu_native_shadow_sampler_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-sampler-descriptor.txt` | Native-shadow resource descriptors now expose future depth sampler markers (`native_shadow_sampler_filter`, `native_shadow_sampler_address`, `native_shadow_sampler_compare_op`, `native_shadow_sampler_compare_enabled`) alongside existing resource/pass fields; targeted unit coverage locks active descriptor sampler state to `linear/clamp_to_edge/less_equal/1`, while fresh env-on release smoke kept disabled runtime sampler markers at `none/none/none/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.149`, `gpu_compositor_submit_max_ms=0.138`, and `process_wall_p95_ms=0.049`; aggregate report now surfaces `native_shadow_sampler_compare_enabled` and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `8ee1e0b` | Native-shadow pass descriptor proof | `logs/gpu_native_shadow_pass_descriptor_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-pass-descriptor.txt` | Native-shadow resource descriptors now expose future depth pass markers (`native_shadow_pass_load_op`, `native_shadow_pass_store_op`, `native_shadow_pass_clear_depth_milli`) alongside existing resource fields; targeted unit coverage locks active descriptor pass state to `clear/store/1000`, while fresh env-on release smoke kept disabled runtime pass markers at `none/none/0`, `resource_bytes=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.999`, and `gpu_compositor_submit_max_ms=0.142`; aggregate report now surfaces `native_shadow_pass_clear_depth_milli` and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `d84f485` | Native-shadow resource extent proof | `logs/gpu_native_shadow_resource_extent_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-resource-extent.txt` | Native-shadow resource descriptors now expose future texture extent markers (`native_shadow_resource_width`, `native_shadow_resource_height`, `native_shadow_resource_layers`) alongside the existing map/format/byte fields; targeted unit coverage locks the active descriptor to `2048 x 2048 x 1`, while fresh env-on release smoke kept the disabled runtime markers at `0/0/0`, `resource_bytes=0`, `resource_format=none`, `resource_usage=none`, `gpu_upload_fail=0`, `terrain_queue_max_ms=3.008`, and `gpu_compositor_submit_max_ms=0.126`; aggregate report now surfaces the extent maxima and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `101265c` | Native-shadow resource byte estimate proof | `logs/gpu_native_shadow_resource_bytes_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-resource-bytes.txt` | Native-shadow resource descriptors now compute the future shadow-map memory estimate from `map_size_px * map_size_px * bytes_per_texel`; targeted unit coverage locks the active descriptor to `16 MiB` for the current `2048 x 2048 x 4` plan, while fresh env-on release smoke kept the disabled runtime marker at `resource_bytes_per_texel=0`, `resource_bytes=0`, `resource_format=none`, `resource_usage=none`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.313`, and `gpu_compositor_submit_max_ms=0.112`; aggregate report now surfaces `native_shadow_resource_bytes` and selected the fresh summary | descriptor/marker/report proof only; no RenderingDevice allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `9127d7b` | Native-shadow resource format proof | `logs/gpu_native_shadow_resource_format_20260614/movement-stress-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-resource-format.txt` | Native-shadow resource descriptors now carry explicit future shadow-map allocation labels (`native_shadow_resource_format=d32_sfloat`, `native_shadow_resource_usage=depth_stencil_attachment\|sampling`) and movement summaries surface them in `movement_native_shadow`; targeted unit coverage locks disabled resources to `none` labels and active descriptors to the explicit format/usage; fresh env-on release smoke passed with `resource_status=disabled`, `resource_format=none`, `resource_usage=none`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.481`, and `gpu_compositor_submit_max_ms=0.226` | descriptor/marker proof only; no RenderingDevice RID allocation, native shadow render pass, shader, Godot scene, CPU proxy fallback, default behavior, shadow quality, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `841b7ea` | Native-shadow movement summary fields | `logs/gpu_native_shadow_summary_fields_20260614/movement-stress-summary.txt` | Movement stress summaries now emit a compact `movement_native_shadow` row from existing marker metrics; fresh env-on release smoke passed with `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `native_shadow_resource_status=disabled`, zero native-shadow resource lifecycle counters, zero coverage counters, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.179`, and `gpu_compositor_submit_max_ms=0.121` | summary/report surfacing only; no native shadow render path, RenderingDevice shadow-map allocation, shader, Godot scene, CPU proxy fallback, shadow quality, default behavior, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `post-06b1ebc` | Native-shadow coverage marker scaffold | `logs/gpu_native_shadow_coverage_scaffold_20260614/movement-stress-summary.txt` | Added native-shadow coverage markers for future active-path coverage (`native_shadow_covered_chunks`, `native_shadow_covered_subchunks`) and kept the env-on fallback guard at zero while `native_shadow_implemented=0`; fresh release movement smoke passed with `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_resource_status=disabled`, zero lifecycle counters, zero coverage counters, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.536`, and `gpu_compositor_submit_max_ms=0.121` | telemetry/guard scaffold only; no native shadow render path, RenderingDevice shadow-map allocation, shader, Godot scene, CPU proxy fallback, shadow quality, default behavior, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `post-40f0995` | Native-shadow resource lifecycle scaffold | `logs/gpu_native_shadow_resource_lifecycle_20260614/movement-stress-summary.txt`, `logs/gpu_native_shadow_resource_lifecycle_parity_20260614/parity-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-resource-lifecycle.txt` | Added a state-only `GpuNativeShadowResources` lifecycle scaffold plus perf/report/parity guards for `native_shadow_resource_status` and resource create/replace/release counters; fresh env-on release movement smoke passed with `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `native_shadow_resource_status=disabled`, zero resource lifecycle counters, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.455`, and `gpu_compositor_submit_max_ms=0.127`; fresh full parity passed with `case_count=17` and fallback pair zero visual delta | lifecycle/telemetry scaffold only; no RenderingDevice shadow-map RID allocation, shader path, Godot scene, shadow atlas integration, shadow quality, CPU proxy fallback, native shadow-map rendering, default behavior, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |
| `post-3961af6` | Native-shadow implementation gate marker | `logs/gpu_native_shadow_implemented_marker_20260614/movement-stress-summary.txt`, `logs/gpu_native_shadow_implemented_parity_20260614/parity-summary.txt`, `/tmp/rumpel-gpu-report-native-shadow-implemented.txt` | Runtime perf markers, movement-stress fallback validation, parity fallback validation, and aggregate reports now expose `native_shadow_implemented`; fresh env-on release movement smoke passed with `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_implemented=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.586`, and `gpu_compositor_submit_max_ms=0.749`; fresh full parity passed with `case_count=17` and fallback case `native_shadow_requested=1 active=0 fallback=1 implemented=0` with zero visual delta against the ordinary GPU marker | marker/guard slice only; no renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, native shadow-map path, default behavior, draw distance, texture quality, protocol, storage, world generation, or chunk serialization changed |

## 2026-06-13

| Commit | Slice | Artifact | Key Result | Caveat |
| --- | --- | --- | --- | --- |
| `post-ff28abf` | Low-angle compact shadow parity gate | `logs/gpu_lighting_low_angle_compact_parity_20260613/parity-summary.txt` | Extended parity smoke to capture and validate `gpu-terrain-compact-lighting-low-angle-parity` against the full-shadow GPU low-angle marker; fresh full parity passed with `case_count=17`, `lighting_low_angle_compact` pair `avg_luma=0.2671/0.2671 delta=0.0000`, `terrain_luma_range=0.3357/0.3357 delta=0.0000`, `terrain_samples=498/498`, `terrain_color_buckets=9/9`, compact marker `shadow_mesh=compact`, `lighting_variant="low_angle"`, `gpu_light_energy=0.700`, and `gpu_upload_fail=0` | harness/validation slice only; no shader, protocol, storage, world generation, chunk serialization, native shadow-map path, draw distance, texture quality, default scene lighting, or visible-quality reduction changed |
| `post-1cdae5a` | Low-angle lighting parity gate | `logs/gpu_lighting_low_angle_parity_20260613/parity-summary.txt` | Extended parity smoke to capture and validate CPU/GPU `lighting_low_angle` cases with explicit `lighting_variant="low_angle"` checks; fresh full parity passed with `case_count=16`, low-angle pair `avg_luma=0.2865/0.2671 delta=0.0194`, `terrain_luma_range=0.3664/0.3357 delta=0.0307`, `terrain_samples=498/498`, `terrain_color_buckets=8/9`, GPU `gpu_light_dir=-0.775/0.407/0.484`, and `gpu_light_energy=0.700` | harness/validation slice only; no shader, protocol, storage, world generation, chunk serialization, native shadow-map path, draw distance, texture quality, default scene lighting, or visible-quality reduction changed |
| `post-8b3f870` | Controlled low-angle lighting smoke | `logs/gpu_lighting_low_angle_smoke_20260613_retry/movement-stress-summary.txt` | Added `RUMPELMC_VISUAL_SMOKE_POSE=lighting_low_angle` and movement-stress pose passthrough so lighting comparisons can capture a smoke-only low-angle sun variant; fresh release movement smoke passed with `lighting_variant="low_angle"`, `gpu_light_dir=-0.775/0.407/0.484`, `gpu_light_energy=0.700`, `gpu_light_ambient=0.550`, `terrain_queue_max_ms=1.200`, `gpu_compositor_submit_max_ms=0.174`, `gpu_upload_fail=0`, and `terrain_samples=509` | smoke harness/pose slice only; no protocol, storage, world generation, chunk serialization, native shadow-map path, draw distance, texture quality, default scene lighting, or visible-quality reduction changed; first attempt hit a stale local server listener before the clean retry passed |
| `post-43ba324` | GPU lighting marker surfacing | `logs/gpu_lighting_marker_summary_retry_20260613/movement-stress-summary.txt` | GPU terrain stats, perf markers, movement summaries, and aggregate reports now surface sanitized scene lighting values from `SunLight`; fresh release movement smoke passed with `gpu_light_dir=-0.355/0.743/0.567`, `gpu_light_color=1.000/1.000/1.000`, `gpu_light_energy=0.450`, `gpu_light_ambient=0.550`, `terrain_queue_max_ms=1.179`, `gpu_compositor_submit_max_ms=0.130`, `gpu_upload_fail=0`, and `terrain_samples=384` | telemetry/report slice over the existing directional lighting shader; no protocol, storage, world generation, chunk serialization, shadow-map/native shadow path, draw distance, texture quality, or visible-quality reduction changed; first retry hit a local stale server lifecycle race before chunk load, then the clean retry passed |
| `post-f768c2c` | Startup reader/main-thread lag telemetry | `logs/world_streaming_startup_reader_lag_rle_20260613_retry/world-streaming-rle-summary.txt` | Rust perf markers and world-streaming summaries now expose `startup_packet_reader_elapsed_ms` and `startup_packet_queue_lag_ms`; fresh RLE smoke passed with `394` chunks, wire percent `0.004168%`, startup packet/reader elapsed/queue lag/chunk decode/first mesh work `70.480ms / 2.891ms / 10.885ms / 0.233ms / 3.965ms`, while the broader retry had passing bootstrap, batch, and RAW-vs-RLE legs showing default radius `0` reader elapsed around `2-3ms` and queue lag around `12-16ms` | telemetry/harness slice only; no protocol, storage, world generation, chunk serialization, server scheduling defaults, renderer behavior, visible quality, or startup loading policy changed; the broad regression-pack retry did not write a top-level pass summary because the final standalone RLE leg exceeded the terrain queue budget by `0.099ms`, then the standalone RLE retry passed |
| `post-fce90d5` | Startup receive/decode telemetry | `logs/world_streaming_startup_receive_breakdown_20260613/world-streaming-regression-summary.txt` | Rust perf markers and world-streaming summaries now split startup packet arrival, packet read/decode work, chunk decode work, chunk insertion, mesh dispatch, first mesh work, and spawn timing; fresh release regression pack passed with bootstrap candidate first stream `1` chunk and startup packet/chunk decode/first mesh work `89.553ms / 0.403ms / 5.876ms`, batch `64` startup `84.343ms / 0.397ms / 5.201ms`, encoding RLE wire percent `0.004168%` with startup `71.415ms / 0.441ms / 6.327ms`, and standalone RLE startup `91.099ms / 0.499ms / 5.790ms` | telemetry/harness slice only; no protocol, storage, world generation, chunk serialization, server scheduling defaults, renderer behavior, visible quality, or startup loading policy changed; wall-clock packet timing includes waiting for the first startup packet, so read-work duration is the narrower local IO measure |
| `post-d5e0b15` | Startup first-mesh work telemetry | `logs/world_streaming_startup_work_breakdown_20260613_frames_retry/world-streaming-regression-summary.txt` | Rust perf markers and world-streaming summaries now separate first startup mesh wall-clock timing from actual first-mesh work duration and collision work; fresh release regression pack passed with bootstrap candidate first stream `1` chunk, startup first mesh/work `63.932ms / 4.308ms`, batch `64` startup first mesh/work `88.853ms / 4.143ms`, encoding RLE wire percent `0.004168%` with startup first mesh/work `78.148ms / 4.729ms`, and standalone RLE startup first mesh/work `78.337ms / 4.223ms` | telemetry/harness slice only; no protocol, storage, world generation, chunk serialization, server scheduling defaults, renderer behavior, visible quality, or startup loading policy changed; two earlier attempts hit screenshot capture before PNG save, and the passing run used `GODOT_QUIT_AFTER_FRAMES=30000` to avoid early Godot exit |
| `post-47ae8d7` | World startup phase telemetry | `logs/world_streaming_startup_phase_regression_20260613_retry/world-streaming-regression-summary.txt` | Rust perf markers and world-streaming summaries now split startup readiness into chunk loaded, mesh queued, first mesh, collision, and player spawn phases; fresh release regression pack passed with bootstrap candidate first stream `1` chunk and startup first mesh/player spawn `94.706ms`, batch `64` startup `87.334ms`, encoding RLE wire percent `0.004168%` with startup `93.806ms`, and standalone RLE startup `90.329ms` | telemetry/harness slice only; no protocol, storage, world generation, chunk serialization, server scheduling defaults, renderer behavior, or visible quality changed; first attempt hit a known screenshot-capture timeout and the `GODOT_TIMEOUT_SEC=240` retry passed |
| `post-ecc7b23` | World-loading regression pack | `logs/world_streaming_regression_pack_20260613/world-streaming-regression-summary.txt` | added a combined world-loading regression wrapper that builds/signs the server once, runs bootstrap, batch-size, RAW-vs-RLE encoding, and standalone RLE movement gates, then emits one top-level pass summary; fresh release run passed with bootstrap candidate first stream `1` chunk and startup player spawn `93.318ms`, batch `64` streaming `394` chunks over `9` batches with startup `83.367ms`, encoding RLE wire percent `0.004168%`, and standalone RLE startup `87.231ms` | harness/report slice only; it composes existing gates and does not change runtime protocol, storage, world generation, chunk serialization, renderer behavior, or visible quality |
| `post-1617ecb` | Current-HEAD aggregate GPU report refresh | `logs/gpu-terrain-report.txt` | refreshed the full-log aggregate report for the current docs checkpoint; the report records `gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=13344`, max draw command occupancy `30.298%`, max `terrain_queue_gpu_uploads=1`, max `terrain_queue_gpu_upload_kb=1.600`, max `transparent_fixture_overlay_roles=5`, and selects the current RLE world-streaming movement summary with startup chunk/collision/player spawn `84.197ms / 84.197ms / 84.197ms` | report refresh only; it aggregates all historical `logs` summaries, so old outliers such as `terrain_queue_max_ms=28.314` and `gpu_compositor_submit_max_ms=5.277` remain historical maxima, not new runtime regressions |
| `post-6e74488` | Transparent fixture overlay fixed metadata entries | `client/rust_ext/src/lib.rs`, `docs/GPU_TRANSPARENT_PATH.md` | Rust now carries a fixed client-only `transparent_test_glass` fixture overlay metadata list with five role/offset entries and a unit test locking the role order, unique coordinates, `5/5` counters, and inactive implementation gate | metadata/test/docs slice only; no overlay geometry, `ChunkData` mutation, block ID, packet, storage, worldgen, asset, shader, transparent pass, sorting, `.tscn`, or default behavior changed |
| `post-f3de47e` | Transparent fixture overlay metadata runtime markers | `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt`, `/tmp/rumpel-gpu-report-transparent-overlay-metadata-fresh.txt` | runtime perf markers, fixture scene smoke, movement-stress guard, and aggregate reports now expose `transparent_fixture_overlay_roles=5` and `transparent_fixture_overlay_blocks=5` when the overlay is requested; the fresh release smoke kept `transparent_fixture_overlay_requested=1`, `transparent_fixture_overlay_active=0`, `transparent_fixture_overlay_fallback=1`, zero transparent workload markers, `terrain_samples=488`, and `gpu_upload_fail=0` | metadata/report slice only; no overlay geometry, `ChunkData` mutation, block ID, packet, storage, worldgen, asset, shader, transparent pass, sorting, `.tscn`, or default behavior changed |
| `post-9cc323f` | Transparent fixture overlay fallback and metadata guards | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` | no-render fixture plan/smoke/checklist/harness/acceptance/default-off/final-report/scene-implementation artifacts now require overlay fallback markers `transparent_fixture_overlay_requested=1`, `transparent_fixture_overlay_active=0`, `transparent_fixture_overlay_fallback=1` and carry the client-only metadata scaffold `transparent_fixture_overlay_roles=5`, `transparent_fixture_overlay_blocks=5`, `geometry_active=0`, `chunk_data_mutation=no` | script/checklist/docs sync only; no overlay geometry, `ChunkData` mutation, block ID, packet, storage, worldgen, asset, shader, transparent pass, sorting, `.tscn`, or default behavior changed |
| `post-a903017` | GPU report summary scan optimization | `/tmp/rumpel-full-report-indexed.txt`, `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` | cached the report summary file list plus an indexed `path:line` scan so full historical `logs` report generation dropped from timeout/30s+ behavior to about 3s; the full transparent fixture pack now passes end-to-end in about 33s and still surfaces overlay marker maxima/origins | script/report performance slice only; no runtime renderer, overlay geometry, protocol, storage, worldgen, asset, shader, transparent pass, sorting, or default behavior changed |
| `post-675f720` | Transparent fixture overlay marker scaffold | `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt`, `/tmp/rumpel-gpu-report-transparent-overlay-markers.txt` | Rust perf markers now expose `transparent_fixture_overlay_requested/active/fallback`; the fixture scene smoke requests the overlay and still proves fallback with `requested=1 active=0 fallback=1`, while aggregate reports surface the new maxima and origins | marker/guard/report scaffold only; no overlay geometry, block ID, chunk mutation, packet, storage, worldgen, asset, shader, transparent pass, sorting, `.tscn`, or default behavior changed |
| `post-0f94128` | Client-only transparent fixture overlay design | `logs/gpu_transparent_fixture_plan/transparent-fixture-overlay-design.txt`, `/tmp/rumpel-gpu-report-transparent-overlay-design.txt` | designed the client-only `transparent_test_glass` fixture overlay path with smoke-owned lifetime, explicit env gate, no chunk/protocol/storage/worldgen mutation, future overlay markers, and aggregate report surfacing | docs/script/report design slice only; no runtime overlay, block ID, asset, protocol, storage, worldgen, renderer, shader, `.tscn`, or default behavior changed |
| `post-a896908` | Transparent fixture material identity blocker | `docs/GPU_TRANSPARENT_PATH.md` | recorded that a real `transparent_test_glass` fixture identity is blocked without either a production block ID/asset/protocol/storage/worldgen change or a new client-only fixture overlay path; current serialized `u16` chunk IDs and block definition lookup make a direct transparent material a compatibility decision | docs-only blocker record; no block ID, asset, protocol, storage, worldgen, renderer, shader, `.tscn`, or default behavior changed |
| `post-73f3305` | Transparent fixture pack zero-workload guard sync | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` | no-render fixture plan/smoke/checklist/harness/default-off/acceptance/implementation gate artifacts now require current env-on fallback guards to include `transparent_blocks=0`, `transparent_faces=0`, `transparent_draws=0`, and `transparent_subchunks=0`; `sh scripts/gpu_terrain_transparent_fixture_pack.sh logs/gpu_transparent_fixture_plan` passed with `env_on_expected=1/0/1` and all fixture checks pass | script/checklist sync only; future active workload markers remain blocked/pending, and no active transparent workload, fixture block/material identity, shader alpha, blending, transparent pass, sort policy, block ID, atlas asset, protocol, storage, worldgen, `.tscn` rewrite, or default render behavior changed |
| `post-5183810` | Transparent workload marker scaffold | `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt`, `/tmp/rumpel-gpu-report-transparent-workload-markers.txt` | runtime perf markers now include `transparent_blocks`, `transparent_faces`, `transparent_draws`, and `transparent_subchunks`; the fresh fixture scene fallback smoke passed with all four at `0`, `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, and `terrain_samples=496`, and aggregate reports surface their metric origins | telemetry/guard slice only; no active transparent workload, fixture block/material identity, shader alpha, blending, transparent pass, sort policy, block ID, atlas asset, protocol, storage, worldgen, `.tscn` rewrite, or default render behavior changed |
| `post-3533d79` | Transparent fixture scene smoke report surfacing | `/tmp/rumpel-gpu-report-transparent-fixture-scene-smoke.txt` | `scripts/gpu_terrain_report.sh` now surfaces the latest `transparent-fixture-scene-smoke-summary.txt`; the generated aggregate report selected `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt` with `status=pass`, `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, and `terrain_samples=488` | report surfacing only; no fixture block/material identity, shader alpha, blending, transparent pass, sort policy, block ID, atlas asset, protocol, storage, worldgen, `.tscn` rewrite, or default render behavior changed |
| `post-79ae6ae` | Transparent fixture scene fallback smoke | `logs/gpu_transparent_fixture_scene_smoke/transparent-fixture-scene-smoke-summary.txt`, `logs/gpu_transparent_fixture_scene_smoke/gpu-transparent-fixture-scene-smoke.png.txt` | added `RUMPELMC_VISUAL_SMOKE_POSE=transparent_fixture` in `client/main.gd` plus `scripts/gpu_terrain_transparent_fixture_scene_smoke.sh`; the release fallback capture passed with `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, `smoke_err=0`, `terrain_samples=488`, `terrain_color_buckets=8`, `gpu_subchunks=108`, `gpu_draws=108`, `terrain_queue_max_ms=1.053`, `process_wall_p95_ms=0.019`, and `gpu_compositor_submit_max_ms=0.145` | fixture fallback smoke only; no fixture block/material identity, shader alpha, blending, transparent pass, sort policy, block ID, atlas asset, protocol, storage, worldgen, `.tscn` rewrite, or default render behavior changed |
| `post-9012942` | Transparent fixture scene implementation plan | `docs/GPU_TRANSPARENT_PATH.md` | replaced the open-ended "next safe implementation slice" note with a concrete env-driven fixture scene plan: use the existing `main.gd` visual smoke path, add a future `transparent_fixture` pose/wrapper first, keep `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`, assert current fallback markers, and avoid new `.tscn` resources unless later evidence requires them | docs-only planning slice; no Godot launch, scene/resource edit, fixture block identity, shader alpha, blending, transparent pass, block ID, atlas asset, protocol, storage, worldgen, or render behavior changed |
| `d88596c` | Week 1 isolated GPU baseline and 100-week roadmap checkpoint | `logs/week1_gpu_baseline_20260613/gpu-terrain-report.txt`, `logs/week1_gpu_baseline_20260613/movement`, `logs/week1_gpu_baseline_20260613/workload_standard`, `logs/week1_gpu_baseline_20260613/fill`, `logs/week1_gpu_baseline_20260613/perf_baseline` | scoped report over the isolated baseline produced `gpu_upload_fail=0`, `gpu_cull=back`, `gpu_front_face=clockwise`, max `gpu_effective_draws=13344`, max draw command occupancy `10.181%`, max workload `terrain_queue_max_ms=2.155`, max fill `terrain_queue_max_ms=2.125`, movement `terrain_queue_max_ms=1.146`, and fill repeats `1/4/8/16` all passed with `smoke_err=0` and `terrain_samples=384`; `docs/GPU_ROADMAP.md` now records the 100-week rolling long-horizon plan | perf baseline exited nonzero because the CPU ArrayMesh control had `terrain_queue_max_ms=16.862 > 6.667`; the GPU terrain side of the same perf baseline passed with `terrain_queue_max_ms=1.003` and `gpu_upload_fail=0`; local FPS and GPU timestamp samples remain report-only on macOS/Metal |

## 2026-06-12

| Commit | Slice | Artifact | Key Result | Caveat |
| --- | --- | --- | --- | --- |
| `post-4111aed` | Transparent fixture scene implementation gate-check pack/report integration | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt`, `logs/gpu_transparent_fixture_plan/gpu-terrain-transparent-fixture-report.txt` | `scripts/gpu_terrain_transparent_fixture_pack.sh` now emits `transparent-fixture-scene-implementation-gate-check.txt`, refreshes the aggregate report with `Selected Transparent Fixture Scene Implementation Gate Check`, and includes `transparent_fixture_scene_implementation_gate_check_status=pass` plus `scene_implementation_gate_steps=scene_implementation_gate_check/report_refresh` in the final pack | pack/report integration only; no Godot launch, scene/asset creation, transparent render evidence, runtime, shader, block ID, protocol, storage, worldgen, or render behavior changed |
| `post-91848e1` | Transparent fixture scene implementation gate check | `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-implementation-gate-check.txt` | added `scripts/gpu_terrain_transparent_fixture_scene_implementation_gate_check.sh` to validate that the pending scene implementation checklist, linked pack, and aggregate report still preserve `transparent_implementation_gate=false`, current env-on fallback `1/0/1`, and future active-path gates `1/0/0` before any real fixture-scene work | gate validator only; no Godot launch, scene/asset creation, transparent render evidence, runtime, shader, block ID, protocol, storage, worldgen, or render behavior changed |
| `post-68d7d4a` | Transparent fixture scene implementation checklist pack/report integration | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt`, `logs/gpu_transparent_fixture_plan/gpu-terrain-transparent-fixture-report.txt` | `scripts/gpu_terrain_transparent_fixture_pack.sh` now emits `transparent-fixture-scene-implementation-checklist.txt`, refreshes the aggregate report with `Selected Transparent Fixture Scene Implementation Checklist`, and includes `transparent_fixture_scene_implementation_checklist_status=pending_scene_implementation` plus `scene_implementation_steps=scene_implementation_checklist/report_refresh` in the final pack | pack/report integration only; no Godot launch, scene/asset creation, transparent render evidence, runtime, shader, block ID, protocol, storage, worldgen, or render behavior changed |
| `post-eb2bd9b` | Transparent fixture scene implementation checklist | `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-implementation-checklist.txt` | added `scripts/gpu_terrain_transparent_fixture_scene_implementation_checklist.sh` to validate the final no-render pack and scene artifact chain, then emit the required future fixture-scene roles, current fallback guard, and future active-path gates as a pending implementation checklist | checklist validator only; no Godot launch, scene/asset creation, transparent render evidence, runtime, shader, block ID, protocol, storage, worldgen, or render behavior changed |
| `post-ca0f403` | Transparent fixture final report pack/report integration | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt`, `logs/gpu_transparent_fixture_plan/gpu-terrain-transparent-fixture-report.txt` | `scripts/gpu_terrain_transparent_fixture_pack.sh` now emits `transparent-fixture-final-report-check.txt`, refreshes the aggregate report with `Selected Transparent Fixture Final Report Check`, and includes `transparent_fixture_final_report_check_status=pass` plus `final_report_steps=final_report_check/report_refresh` in the final pack | pack/report integration only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-5bb8cd5` | Transparent fixture final report guard | `logs/gpu_transparent_fixture_plan/transparent-fixture-final-report-check.txt` | added `scripts/gpu_terrain_transparent_fixture_final_report_check.sh` to validate the final no-render pack and aggregate report together, requiring selected acceptance and default-off sections to point at the linked artifacts and include their pass summaries | final report validator only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-5dbec7d` | Transparent fixture default-off pack/report integration | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt`, `logs/gpu_transparent_fixture_plan/gpu-terrain-transparent-fixture-report.txt` | `scripts/gpu_terrain_transparent_fixture_pack.sh` now emits `transparent-fixture-default-off-check.txt`, refreshes the aggregate report with `Selected Transparent Fixture Default-Off Check`, and includes `transparent_fixture_default_off_status=pass` plus `default_off_steps=default_off_check/report_refresh` in the final pack | pack/report integration only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-10d9c1c` | Transparent fixture default-off guard | `logs/gpu_transparent_fixture_plan/transparent-fixture-default-off-check.txt` | added `scripts/gpu_terrain_transparent_fixture_default_off_check.sh` to validate the final no-render pack against `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`, the current Rust requested/active/fallback decisions, the movement-stress env-on fallback guard, and fixture non-goals | default-off validator only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-13de065` | Transparent fixture acceptance check | `logs/gpu_transparent_fixture_plan/transparent-fixture-acceptance-check.txt` | added `scripts/gpu_terrain_transparent_fixture_acceptance_check.sh` to validate the no-render pack, report-check, smoke-plan, scene harness-check, current fallback gates, future scene/workload/active gates, and non-goals together | acceptance validator only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-b3c36da` | Transparent fixture scene harness check | `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-harness-check.txt` | added `scripts/gpu_terrain_transparent_fixture_scene_harness_check.sh` to validate the generated scene checklist and scene harness together, including role count, env fallback gates, future active/workload gates, and non-goals | scene harness validator only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-8281507` | Transparent fixture scene harness | `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-harness.txt` | added `scripts/gpu_terrain_transparent_fixture_scene_harness.sh` to validate the scene checklist and emit placeholder role checks for front transparent, behind-wall transparent, opaque depth occluder, adjacent same-material pair, and collision probe acceptance | scene harness placeholder only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-2645dc8` | Transparent fixture scene checklist | `logs/gpu_transparent_fixture_plan/transparent-fixture-scene-checklist.txt` | added `scripts/gpu_terrain_transparent_fixture_scene_checklist.sh` to validate the smoke plan and emit fixed future scene roles for front transparent, behind-wall transparent, opaque depth occluder, adjacent same-material pair, and collision probe checks | scene checklist only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-87b06d1` | Transparent fixture smoke plan | `logs/gpu_transparent_fixture_plan/transparent-fixture-smoke-plan.txt` | added `scripts/gpu_terrain_transparent_fixture_smoke_plan.sh` to validate the no-render fixture pack and emit current env-off/env-on fallback smoke commands plus future fixture-scene, workload-marker, and active-path gates | smoke-plan/checklist only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-597535e` | Transparent fixture no-render pack | `logs/gpu_transparent_fixture_plan/transparent-fixture-pack.txt` | added `scripts/gpu_terrain_transparent_fixture_pack.sh` to regenerate the transparent fixture plan, harness, check, aggregate report, and report-check artifacts in one no-render command, then emit `transparent_fixture_pack_status=pass` with the report/check summaries and `env_on_expected=1/0/1` | pack/checklist only; no Godot launch, capture, transparent render evidence, runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-82d848e` | Transparent fixture report guard | `/tmp/rumpel-transparent-fixture-report-check.txt` | added `scripts/gpu_terrain_transparent_fixture_report_check.sh` to validate that an aggregate report sourced from `logs` includes the selected transparent fixture plan, harness, and check artifacts from `logs/gpu_transparent_fixture_plan`, plus the pass summary and requested/active/fallback triplet | report validator only; fixture artifacts are validators/checklists, not Godot captures or transparent render evidence, and no runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-ee3433b` | Transparent fixture real-log artifacts | `logs/gpu_transparent_fixture_plan/transparent-fixture-check.txt`, `/tmp/rumpel-gpu-report-transparent-fixture-real.txt` | generated ignored local transparent fixture plan, harness, and check artifacts under `logs/gpu_transparent_fixture_plan`; aggregate report over `logs` now surfaces all three sections and the check summary reports `transparent_fixture_check_status=pass` with `env_on_expected=1/0/1` | artifact/report checkpoint only; fixture artifacts are validators/checklists, not Godot captures or transparent render evidence, and no runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-ba65294` | Transparent fixture check report surfacing | `/tmp/rumpel-gpu-report-transparent-fixture-check.txt` | `scripts/gpu_terrain_report.sh` now surfaces the latest `transparent-fixture-check.txt` artifact under the selected log directory alongside the transparent fixture plan and harness sections | report surfacing only; check artifacts are validators/checklists, not Godot captures or transparent render evidence, and no runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-6a3f006` | Transparent fixture artifact validator | `/tmp/rumpel-transparent-fixture-check/transparent-fixture-check.txt` | added `scripts/gpu_terrain_transparent_fixture_check.sh` to validate matching `transparent-fixture-plan.txt` and `transparent-fixture-harness.txt` artifacts, enforce the requested/active/fallback and future-active triplets, and emit `transparent_fixture_check_status=pass` | validator/checklist only; no Godot capture, Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, or render behavior changed |
| `post-2733b35` | Transparent fixture report surfacing | `/tmp/rumpel-gpu-report-transparent-fixture.txt` | `scripts/gpu_terrain_report.sh` now surfaces the latest `transparent-fixture-plan.txt` and `transparent-fixture-harness.txt` artifacts under the selected log directory as selected optional sections | report surfacing only; fixture artifacts are checklists, not Godot captures or transparent render evidence, and no runtime, shader, block ID, asset, protocol, storage, worldgen, or render behavior changed |
| `post-a2ac1e8` | Transparent fixture harness placeholder | `/tmp/rumpel-transparent-fixture-harness.txt` | added `scripts/gpu_terrain_transparent_fixture_harness.sh` to consume `transparent-fixture-plan.txt`, validate the plan's contract/fallback/non-goal fields, and emit a placeholder harness artifact with env-off, env-on fallback, future marker, and future active gates | script/checklist only; no Godot capture, Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, or render behavior changed |
| `post-c8faf03` | Transparent fixture plan guard | `/tmp/rumpel-transparent-fixture-plan.txt` | added `scripts/gpu_terrain_transparent_fixture_plan.sh` to validate the transparent fixture contract tokens, verify the current movement-stress requested/active/fallback guard, and emit a line-oriented fixture plan for the next no-render harness slice | script/checklist only; no Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, or render behavior changed |
| `post-9e51973` | Transparent fixture contract | `docs/GPU_TRANSPARENT_PATH.md` | documented the first fixture-only transparent material contract: stable fixture identity, render opacity vs collision solidity rules, fixed depth/collision scene shape, required future workload markers, and fallback/active acceptance gates | docs-only contract; no Rust runtime, shader alpha, transparent pass, sorting, block ID, atlas asset, protocol, storage, worldgen, or render behavior changed |
| `post-38835f1` | Transparent fallback harness guard | `logs/gpu_transparent_fallback_guard_capture` | `scripts/gpu_terrain_movement_stress.sh` now requires `transparent_requested=1`, `transparent_active=0`, and `transparent_fallback=1` whenever `RUMPELMC_GPU_TERRAIN_TRANSPARENT` is enabled; the fresh release guard capture passed with those markers plus `gpu_upload_fail=0` and `smoke_err=0` | harness-only guard; no Rust runtime, shader alpha, transparent pass, block ID, atlas asset, protocol, or render behavior changed |
| `post-122078d` | Transparent terrain marker scaffold | `logs/gpu_transparent_fallback_capture`, `/tmp/rumpel-gpu-report-transparent-fallback.txt`, unit tests | reserved `RUMPELMC_GPU_TERRAIN_TRANSPARENT` and marker fields `transparent_requested`, `transparent_active`, and `transparent_fallback`; the release env-on smoke passed with `transparent_requested=1`, `transparent_active=0`, `transparent_fallback=1`, `gpu_upload_fail=0`, and `smoke_err=0`, and aggregate reports resolve the marker origins to the fresh capture | telemetry/test scaffolding only; no transparent face buffer, alpha blending, sort policy, shader alpha path, Godot transparent material, block ID, atlas asset, protocol, or render behavior changed |
| `post-58e0b84` | GPU transparent terrain path design | `docs/GPU_TRANSPARENT_PATH.md` | documented the Phase 12 transparent terrain rollout: keep current opaque pass as default rollback, split render opacity from collision solidity, require requested/active/fallback telemetry, and gate default enablement on visual/depth/collision parity plus external profiler evidence | design/docs-only checkpoint; no Rust runtime, shader, Godot scene, atlas, protocol, block ID, or render behavior changed |
| `post-892fcd9` | Real-log shadow profiler capture pack | `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-capture-pack.txt`, `/tmp/rumpel-gpu-report-shadow-profiler-real-pack.txt` | generated the ignored real-log capture pack from the wide matrix manifest; report output now surfaces it with `results_file_status=missing` and `capture_pack_status=pending_external_profiler`, while the generated template remains rejected by the results validator | artifact/checklist only; no Metal/Xcode capture, no `shadow-radius-profiler-results.txt`, no profiler numbers, and no renderer behavior, shadow defaults, or production decision changed |
| `post-0930068` | Shadow profiler capture pack report surfacing | `/tmp/rumpel-gpu-report-shadow-profiler-capture-pack.txt` | `scripts/gpu_terrain_report.sh` now includes the latest `shadow-radius-profiler-capture-pack.txt` as a pending external-profiler handoff section, separate from the validated `shadow-radius-profiler-results-summary.txt` evidence section | report surfacing only; capture packs are checklists, not Metal/Xcode evidence, and no renderer behavior, shadow defaults, or production decision changed |
| `post-e73c711` | Shadow profiler capture pack | `/tmp/rumpel-shadow-profiler-capture-pack.txt` | new `scripts/gpu_terrain_shadow_profiler_capture_pack.sh` regenerates the pending plan, TODO results template, and a checklist with exact validate/report commands; test pack produced `rows=4 capture_pack_status=pending_external_profiler` and the generated template remained rejected by the results validator | handoff helper only; no Metal/Xcode capture, profiler numbers, renderer behavior, shadow defaults, or production decision changed |
| `post-fa7af1b` | Shadow profiler results template | `/tmp/rumpel-shadow-profiler-results-template.txt` | new `scripts/gpu_terrain_shadow_profiler_results_template.sh` writes fillable external-profiler result rows from the pending plan; generated template rows are commented and contain `TODO`, so `scripts/gpu_terrain_shadow_profiler_results_check.sh` rejects the template until real captured rows are recorded | template/handoff slice only; generated TODO rows are not Metal/Xcode evidence and no renderer behavior, shadow defaults, or production decision changed |
| `post-3dd4e60` | Shadow profiler report surfacing | `/tmp/rumpel-gpu-report-shadow-profiler-results.txt` | `scripts/gpu_terrain_report.sh` now includes the latest `shadow-radius-profiler-results-summary.txt` when present under the selected log dir; a temp validated synthetic summary was surfaced in the report section during testing | report surfacing only; synthetic `/tmp` profiler data is not Metal/Xcode evidence and no runtime, renderer, shadow default, or production decision changed |
| `post-32b67e5` | Shadow profiler results validator | `/tmp/rumpel-shadow-profiler-results-summary.txt` | new `scripts/gpu_terrain_shadow_profiler_results_check.sh` validates captured external profiler rows against the pending plan; synthetic captured rows produced `planned_rows=4 captured_rows=4 missing_rows=0`, `external_profile_status=pending` rows are rejected, and partial rows fail by default unless `RUMPELMC_SHADOW_PROFILER_RESULTS_ALLOW_PARTIAL=1` is explicit | validator/template slice only; synthetic profiler rows are not Metal/Xcode evidence and no renderer behavior, shadow defaults, or production decision changed |
| `post-9a7431f` | Shadow profiler plan guard | `/tmp/rumpel-shadow-profiler-plan.txt`, `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` | new `scripts/gpu_terrain_shadow_profiler_plan.sh` validates profiler manifest rows and referenced artifacts, sorts rows by priority, emits `external_profile_status=pending`, and maps the wide matrix to `rows=4 usable=3 rejected=1`; radius `1` becomes `normal_total_decision=do_not_cite` | external profiler handoff guard only; no Metal/Xcode capture, renderer behavior, shadow defaults, or production decision changed |
| `post-b86c965` | Shadow normal-total evidence guard | `logs/gpu_shadow_radius_matrix_wide/shadow-radius-matrix-summary.txt`, `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` | matrix wrapper now emits `shadow_normal_total_savings_evidence`; report-mode over the wide artifact marked `scene`, radius `2`, and radius `4` as `usable`, while radius `1` is explicitly `rejected` because its `shadow_normal_total_status=increased` | parser/report guard only; no new capture or runtime behavior changed, and `rejected` rows can still be useful profiler controls via per-marker compact counters |
| `post-bf6c974` | Shadow radius profiler manifest | `logs/gpu_shadow_radius_matrix_wide/shadow-radius-profiler-manifest.txt` | `scripts/gpu_terrain_shadow_radius_matrix.sh` now writes a line-oriented profiler manifest next to the matrix summary; report-mode regenerated the wide artifact with prioritized external Metal capture rows: `scene` priority `1` `default_high_proxy`, radius `2` priority `2` `mid_proxy_control`, radius `1` priority `3` `low_proxy_control`, and radius `4` priority `4` `extra_proxy_control`, each with full/compact screenshot paths and compact proxy counters | manifest/report helper only; no new capture and no runtime behavior changed, and profiler rows still require external Metal evidence before production shadow decisions |
| `post-f8631e0` | Wide shadow radius matrix capture | `logs/gpu_shadow_radius_matrix_wide/shadow-radius-matrix-summary.txt`, `/tmp/rumpel-gpu-report-shadow-radius-matrix-wide.txt` | fresh release `scene 1 2 4` matrix reused the existing server and passed all rows with full/compact `avg_luma=0.2805` and `terrain_samples=472`; compact rows reported `scene` `compact_shadow_proxy=201`/`compact_shadow_normals_saved=3932`, radius `1` `14`/`720`, radius `2` `58`/`1492`, and radius `4` `201`/`3932` | measurement artifact only; radius `1` again showed `shadow_normal_total_status=increased`, so cross-row `normals_total` deltas remain unreliable compared with per-marker compact savings and external profiler evidence |
| `post-b20cda6` | Shadow radius matrix wrapper | `logs/gpu_shadow_radius_matrix/shadow-radius-matrix-summary.txt`, `/tmp/rumpel-gpu-report-shadow-radius-matrix.txt` | new `scripts/gpu_terrain_shadow_radius_matrix.sh` runs/reports multiple compact-proxy shadow radius controls into one summary; fresh release `scene 1` matrix reused the existing server and passed with matching full/compact `avg_luma=0.2805`; `scene` reported compact `compact_shadow_proxy=201`, `compact_shadow_normals_saved=3956`, and `shadow_normal_total_delta=5538 reduced`, while radius `1` reported compact `compact_shadow_proxy=14`, `compact_shadow_normals_saved=720`, and `shadow_normal_total_delta=732 reduced` | measurement harness only; no renderer/default/shadow-quality behavior changed, and this still needs external Metal profiler evidence before deciding whether to build or prefer a native shadow path |
| `post-14832d1` | Shadow proxy explicit-radius benchmark | `logs/gpu_shadow_proxy_radius1_capture/compact-proxy-benchmark-summary.txt`, `/tmp/rumpel-gpu-report-shadow-radius1.txt` | compact proxy benchmark now supports `RUMPELMC_COMPACT_PROXY_BENCH_SHADOW_RADIUS` for full/compact shadow-casting cases and `RUMPELMC_COMPACT_PROXY_BENCH_REUSE_SERVER=1` for intentional captures against an already-running local server; fresh release radius-1 capture passed with `shadow_radius_override=1`, `gpu_upload_fail=0`, full/compact `avg_luma=0.2805`, `terrain_samples=472`, compact `compact_shadow_proxy=14`, and `compact_shadow_normals_saved=720` | measurement harness only; radius 1 is a profiler/control workload, not a production shadow-distance change, and this reuse-server capture reported `shadow_normal_total_status=increased`, so cross-run `normals_total` deltas are not a reliable savings signal without tighter workload/profiler control |
| `post-221c0cc` | GPU-native shadow fallback parity gate | `logs/gpu_native_shadow_fallback_parity/parity-summary.txt` | parity smoke now prepares the requested Godot Rust extension profile, validates that profile in fresh markers, normalizes relative output paths to the repo root, and adds a 14th `gpu-terrain-native-shadow-fallback-parity` case; the fresh release run passed with `shadow_path=godot_proxy`, `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, and `pair=native_shadow_fallback` visual deltas all `0` against the ordinary GPU terrain marker | harness/parity proof only; no renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed |
| `post-75e23d1` | GPU-native shadow proxy-radius contract | unit tests | `terrain_godot_shadow_proxy_chunk_distance` now separates the future `gpu_native_shadow` renderer marker path from the current Godot shadow-proxy refresh radius: native-active decisions report `gpu_native_shadow` while Godot proxy radius becomes `0`; current runtime remains unchanged because `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false` | contract/test slice only; no renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed |
| `post-2dbb3e9` | GPU-native shadow fallback marker | `logs/gpu_native_shadow_fallback_capture`, `/tmp/rumpel-gpu-report-native-shadow-fallback.txt` | with `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW=1`, release movement stress still reported `shadow_path=godot_proxy`, while `native_shadow_requested=1`, `native_shadow_active=0`, and `native_shadow_fallback=1`; aggregate report surfaces max `native_shadow_requested=1.000`, max `native_shadow_active=0.000`, and max `native_shadow_fallback=1.000`, with origins pointing at the fresh marker | telemetry and rollback proof only; no renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed |
| `post-dc56a48` | GPU-native shadow requested/active fresh capture | `logs/gpu_native_shadow_requested_active_capture`, `/tmp/rumpel-gpu-report-native-shadow-requested-active-fresh.txt` | release movement stress reused the already-running local server and passed with `shadow_path=godot_proxy`, `native_shadow_requested=0`, `native_shadow_active=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.824`, `gpu_compositor_submit_max_ms=0.368`, and `process_wall_p95_ms=0.036`; aggregate report now resolves both native-shadow fields to `0.000` with origins pointing at the fresh marker instead of `n/a` | local movement smoke only, not a focused shadow-proxy benchmark or external profiler capture; visual FPS/GPU timestamps remain report-only, and current behavior still relies on Godot proxy shadows |
| `post-e701d00` | GPU-native shadow requested/active telemetry | unit tests, `scripts/gpu_terrain_report.sh` | perf markers now split `native_shadow_requested` from `native_shadow_active`, and aggregate reports surface both fields with metric origins so future captures can prove whether the env flag was requested separately from the implementation gate | telemetry-only Rust/script slice; no renderer implementation, shader path, Godot scene, shadow quality, CPU proxy fallback, or default behavior changed |
| `post-c43171e` | GPU-native shadow marker scaffold | unit tests, `scripts/gpu_terrain_compact_proxy_benchmark.sh` | reserved `shadow_path=gpu_native_shadow` in Rust, added `RUMPELMC_GPU_TERRAIN_NATIVE_SHADOW` behind an additional `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false` gate, kept current runtime markers on `godot_proxy`, and split focused benchmark validation so shadow-casting paths cannot be confused with diagnostic no-shadow paths | scaffolding-only Rust/script slice; no renderer implementation, shader path, Godot scene, shadow quality, or default behavior changed |
| `post-705f6b9` | GPU-native shadow path design | `docs/GPU_SHADOW_PATH.md` | documented the Phase 12 rollout for a future GPU-native terrain shadow path: keep `godot_proxy` default, keep diagnostic no-shadow modes separate, require an off-by-default rollback flag, reuse current shadow radius/chunk-role refresh rules, keep collision proxies independent, and gate default enablement on visual parity plus external profiler evidence | design/docs-only checkpoint; no Rust runtime, shader, Godot scene, shadow quality, or artifact-generation behavior changed |
| `post-447e2d5` | Shadow proxy focused capture summary | `logs/gpu_shadow_proxy_focused_capture/compact-proxy-benchmark-summary.txt` | compact proxy benchmark captured full, compact, shadow-disabled, and collision-only proxy rows; compact shadow preserved `avg_luma=0.2812`, reported `compact_shadow_proxy=189` and `shadow_normals_saved=3158`, and reduced `normals_total` from `49834` to `45472` (`shadow_normal_total_delta=4362`, `shadow_normal_total_reduction=8.8%`); shadow-disabled/collision-only rows each had `compact_collision_proxy=13` with about `1.5%` collision normal payload reduction; harness now writes a summary artifact, exact-parses metric tokens, normalizes output paths, and cleans the local server port after capture | local visual smoke and CPU-side proxy counters only; visual FPS/GPU timestamps remain report-only and this does not justify changing shadow behavior without an external profiler or a rollback-gated GPU-native shadow design |
| `post-3ee1b22` | Shadow proxy report signals | `/tmp/rumpel-gpu-report-shadow-proxy-cost.txt` | aggregate report now surfaces existing compact shadow/collision proxy savings fields and their origins: max `mesh_shadow_only=262`, `proxy_shadow=262`, `proxy_shadow_only=252`, `compact_shadow_proxy=949`, `compact_shadow_normals_saved=1579008`, `compact_collision_proxy=20`, `compact_collision_normals_saved=186192`, `fast_proxy=949`, `proxy_refresh_reuse=267`; report command found no summary error patterns | report-only Phase 12 measurement slice over historical logs; counters are cumulative marker totals and need a fresh focused capture or external profiler before changing shadow behavior |
| `post-541cf55` | Shader lighting contract guard | unit tests | added `render_shader_lighting_contract_keeps_vertex_lighting_and_fragment_handoff` to lock current Phase 11 assumptions: vertex shader computes lighting from face normals plus lighting push constants, passes it through `lighting_out`/`lighting_in`, and fragment shader only applies it to sampled atlas color with opaque alpha; `./scripts/check.sh fast` passed Rust `107/107` | test-only guard; no GLSL runtime behavior, renderer settings, captures, or perf artifacts changed |
| `post-770d613` | Parity smoke summary | `logs/gpu_shader_branchless_corners_parity/parity-summary.txt` | parity wrapper now writes `parity-summary.txt` after successful full or validate-only validation; validate-only against branchless-corner artifacts passed and recorded all 13 cases plus visual deltas: default `0.0076`, atlas_depth `0.0141`, lighting_shadow `0.0137`, lighting_shadow_compact `0.0000`, texture_stand `0.0075`; `./scripts/check.sh fast` passed Rust `106/106` | report-only/test-infrastructure slice; summary reflects existing artifacts, no new Godot capture for this slice, and local visual FPS/timestamps remain report-only |
| `post-dc1324b` | Branchless shader face corners | `logs/gpu_shader_branchless_corners`, `logs/gpu_shader_branchless_corners_parity`, `/tmp/rumpel-gpu-report-shader-branchless-corners.txt` | render shader now uses `FACE_CORNER_BASES`, `FACE_CORNER_EXTENT_X_FACTORS`, and `FACE_CORNER_EXTENT_Y_FACTORS` instead of per-face `face_corner` branches/local arrays; release movement stress compiled/rendered the GLSL and passed with `smoke_err=0`, `terrain_samples=384`, `terrain_color_buckets=6`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.007`, `gpu_compositor_submit_max_ms=0.212`, and `process_wall_p95_ms=0.035`; full CPU/GPU terrain parity passed 13 cases after fixing exact metric parsing in the parity wrapper; Rust tests passed 106/106 | geometry-affecting Phase 11 cleanup, so parity evidence matters more than FPS; parity wrapper still runs debug-profile Godot cases and local visual FPS/GPU timestamps remain report-only |
| `post-f332190` | Branchless shader face UVs | `logs/gpu_shader_branchless_uv`, `/tmp/rumpel-gpu-report-shader-branchless-uv.txt` | render shader now uses a 32-entry `FACE_UV_FACTORS` table instead of per-face `face_uv` branches; release movement stress compiled/rendered the GLSL and passed with `smoke_err=0`, `terrain_samples=384`, `terrain_color_buckets=6`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.265`, `gpu_compositor_submit_max_ms=0.225`, and `process_wall_p95_ms=0.050`; Rust tests passed 105/105 | small Phase 11 UV hot-path cleanup; no external GPU profiler capture, and vertex-position `face_corner` was intentionally left for the later geometry parity pass recorded above |
| `post-a3761de` | Branchless shader face normals | `logs/gpu_shader_branchless_normals`, `/tmp/rumpel-gpu-report-shader-branchless-normals.txt` | render shader now uses an 8-entry `FACE_NORMALS` table instead of an `if` chain for `face_normal`; release movement stress compiled/rendered the GLSL and passed with `smoke_err=0`, `ground_misses=0`, `gpu_upload_fail=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.417`, `gpu_compositor_submit_max_ms=0.195`, and `process_wall_p95_ms=0.050`; Rust tests passed 104/104 | small Phase 11 shader hot-path cleanup; no external GPU profiler capture, and visual FPS/GPU timestamps remain local report-only signals |
| `post-ab54ae6` | Binding invariant guard | unit tests | Rust unit coverage now locks the render shader binding contract: set 0 binding 0 is `FaceBuffer`, set 0 binding 1 is atlas `sampler2D` in vertex and fragment stages, vertex reads `face_buffer.faces[face_instance]`, and fragment samples `atlas_texture`; `cargo test --manifest-path client/rust_ext/Cargo.toml` passed 103/103 | test-only Phase 10 guard; no fresh runtime capture because renderer behavior and telemetry did not change |
| `post-c56a746` | Push constant frame-data telemetry | `logs/gpu_frame_data_push_constants`, `/tmp/rumpel-gpu-report-frame-data-push-constants.txt` | release movement stress passed with `gpu_frames=851`, `gpu_push_constant_updates=852`, `gpu_push_constant_total_bytes=95424`, `gpu_push_constant_avg_bytes=112.0`, byte split `64/32/16`, `gpu_scene_target_replace=0`, `gpu_upload_fail=0`, `terrain_queue_max_ms=2.463`, `gpu_compositor_submit_max_ms=0.151`, and `process_wall_p95_ms=0.045`; aggregate report now surfaces push-constant update and byte-split metrics | telemetry-only Phase 10 slice; extra update is the one-time offscreen debug draw, and constants phase stayed tiny (`0.004ms` max in the fresh run), so moving atlas layout out of push constants needs a focused justification |
| `post-161c950` | Binding/frame-data churn telemetry | `logs/gpu_binding_churn_movement`, `/tmp/rumpel-gpu-report-binding-churn.txt` | release movement stress passed with `gpu_scene_target_create=1`, `gpu_scene_target_reuse=852`, `gpu_scene_target_replace=0`, `gpu_uniform_set_create=1`, `gpu_atlas_texture_create=1`, `gpu_atlas_sampler_create=1`, `gpu_push_constant_bytes=112`, `gpu_upload_fail=0`, `terrain_queue_max_ms=1.859`, `gpu_compositor_submit_max_ms=0.087`, and `process_wall_p95_ms=0.036`; aggregate report now surfaces these binding metrics and their origins | telemetry-only Phase 10 slice; current evidence says scene target and immutable bindings are stable in this movement run, but per-frame camera/light constant update work still needs audit |
| `post-8bd11dc` | Draw command report headroom | `/tmp/rumpel-gpu-report-draw-command-occupancy.txt` | aggregate report now derives max `gpu_draw_cmd_occupancy_pct=30.298` and min `gpu_draw_cmd_headroom_bytes=91360.000` from existing marker fields; source is `logs/gpu_draw_command_buffer_workload_heavy_radius16/extended/gpu-terrain-movement-stress.png.txt` | report-only slice; no new runtime capture, and the selected report still aggregates all historical matching summaries |
| `post-99fae04` | Draw command ABI guard plus heavy radius-16 telemetry | `logs/gpu_draw_command_buffer_workload_heavy_radius16` | indirect draw command fields are now justified by a shader contract test; heavy radius-16 matrix passed all six cases with `gpu_upload_fail=0` and `terrain_samples=384`, with maxima `gpu_draws=2482`, `gpu_draw_cmd_bytes=39712`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `gpu_faces=3256`, `terrain_queue_max_ms=3.781`, `gpu_compositor_submit_max_ms=0.161`, and `process_wall_p95_ms=0.055` | telemetry indicates about 30.3% command-buffer occupancy at this radius; grouped draw submission still needs profiler or stronger submit-pressure evidence |
| `post-dad073e` | Draw command buffer telemetry | `logs/gpu_draw_command_buffer_workload_standard` | standard workload matrix now records indirect draw command buffer bytes in movement summaries, workload summaries, and aggregate reports; all cases passed with `gpu_upload_fail=0` and `terrain_samples=384`, with maxima `gpu_draw_cmd_bytes=15936`, `gpu_draw_cmd_capacity_bytes=131072`, `gpu_draw_cmd_stride=16`, `gpu_draws=996`, `gpu_faces=1522`, `terrain_queue_max_ms=2.324`, `gpu_compositor_submit_max_ms=0.177`, and `process_wall_p95_ms=0.048` | telemetry-only slice; standard workload does not prove grouped draw submission is needed without heavier resident/profiler evidence |
| `post-55b9a07` | Partial dirty default-on standard workload | `logs/gpu_default_partial_workload_standard` | with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset, standard workload matrix passed `short`, `long`, `long-filled`, and `max-resident`; all cases had `gpu_upload_fail=0` and `terrain_samples=384`, with matrix maxima `terrain_queue_max_ms=2.196`, `gpu_compositor_submit_max_ms=0.143`, `process_wall_p95_ms=0.055`, `gpu_draws=996`, and `gpu_faces=1522` | standard workload only; heavier radius runs remain optional before checkpointing the dirty-update/default-on slice |
| `post-55b9a07` | Partial dirty default-on movement sanity | `logs/gpu_default_partial_movement_stress` | with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset, normal movement passed through the wrapper-default path with no dirty edits: `block_edit_dirty_observed=0`, `dirty_chunks=0`, `dirty_blocks=0`, `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `current_chunk_collision=2`, `ground_misses=0`, `terrain_samples=384`, `terrain_queue_max_ms=2.202`, `gpu_compositor_submit_max_ms=0.147`, `process_wall_p95_ms=0.052`, `gpu_draws=336`, `gpu_faces=637`, `gpu_fragmentation_pct=0.0`, and `gpu_upload_fail=0` | normal movement only; next broaden with the standard workload matrix before checkpointing the dirty-update slice |
| `post-55b9a07` | Partial dirty default-on rollback control | `logs/gpu_single_edge_dirty_compare_default_on` | after flipping partial dirty upload default-on, explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` still produced full rebuild control counters (`dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `terrain_queue_max_ms=2.217`, `gpu_upload_fail=0`), while explicit `=1` produced partial counters (`dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `terrain_queue_max_ms=1.980`, `gpu_upload_fail=0`) on the same `pos_x` dirty surface | validates rollback flag for one single-edge shape; broaden with a normal movement/workload sanity run under the new default |
| `post-55b9a07` | Partial dirty default-on sanity | `logs/gpu_default_partial_dirty_block_edit` | with `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` unset, single-edge edit at global `127,64,80` now used partial dirty by default: `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `terrain_queue_max_ms=1.635`, `gpu_compositor_submit_max_ms=0.284`, `process_wall_p95_ms=0.040`, `ground_misses=0`, and `gpu_upload_fail=0` | default-on proof for one dirty block-edit shape; visual FPS remains display-paced locally |
| `post-55b9a07` | Single-edge partial dirty repeat | `logs/gpu_single_edge_dirty_repeat` | opt-in partial dirty toggle at global `127,64,80` passed 3 fresh server sessions with stable single-edge evidence: `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `proxy_refresh_reuse=23`, and `gpu_upload_fail=0` on each run; aggregate maxima were `terrain_queue_max_ms_max=3.059`, `gpu_compositor_submit_max_ms_max=0.140`, and `process_wall_p95_ms_max=0.035` | still opt-in; next step is a default-on decision with the rollback flag kept explicit |
| `post-55b9a07` | Single-edge dirty full-vs-partial compare | `logs/gpu_single_edge_dirty_compare` | same non-corner `pos_x` toggle passed in full rebuild and opt-in partial dirty modes with matching dirty surface (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`); full control had `dirty_edge_neighbor_subchunks=0`, `dirty_partial_subchunks=0`, `dirty_partial_saved_subchunks=0`, `terrain_queue_max_ms=2.222`; partial had `dirty_edge_neighbor_subchunks=4`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `terrain_queue_max_ms=3.541`; both had `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, and `gpu_upload_fail=0` | single compare sample only; use this with the corner repeat/compare evidence before deciding whether default-on partial dirty is ready |
| `post-55b9a07` | Single-edge partial dirty smoke | `logs/gpu_single_edge_block_edit_stress` | opt-in partial dirty toggle at global `127,64,80` passed exact single-edge validation with `dirty_last_edges=pos_x`, `dirty_last_bounds=31,64,16:31,64,16`, `dirty_edge_neighbor_subchunks=4`, `dirty_last_edge_neighbor_subchunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=21`, `terrain_queue_max_ms=3.494`, `gpu_compositor_submit_max_ms=0.148`, `process_wall_p95_ms=0.052`, and `gpu_upload_fail=0` | single smoke sample only; run a single-edge compare/repeat before making partial dirty upload default |
| `post-55b9a07` | Edge dirty partial repeat | `logs/gpu_edge_dirty_repeat` | opt-in partial edge toggle at global `127,64,95` passed 3 fresh server sessions with stable dirty evidence: `dirty_edge_neighbor_subchunks=8`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=21`, and `gpu_upload_fail=0` on each run; aggregate maxima were `terrain_queue_max_ms_max=4.905`, `gpu_compositor_submit_max_ms_max=0.140`, and `process_wall_p95_ms_max=0.052` | repeat covers the corner edge case only; add one single-edge/non-corner dirty shape before making partial dirty upload default |
| `post-55b9a07` | Edge dirty full-vs-partial compare | `logs/gpu_edge_dirty_compare` | same edge toggle passed in full rebuild and opt-in partial dirty modes with matching dirty surface (`dirty_blocks=2`, `dirty_last_rebuild_subchunks=2`, `dirty_last_edges=pos_x,pos_z`, `dirty_last_bounds=31,64,31:31,64,31`); full control had `dirty_partial_subchunks=0`, `dirty_edge_neighbor_subchunks=0`, `terrain_queue_max_ms=2.304`; partial had `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `dirty_edge_neighbor_subchunks=8`, `terrain_queue_max_ms=5.039`; both had `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=162`, `proxy_refresh_reuse=21`, and `gpu_upload_fail=0` | single compare sample only; partial stayed under the 6.667ms CPU-side terrain budget but should get repeat coverage before making the env flag default |
| `post-55b9a07` | Edge dirty refresh smoke | `logs/gpu_edge_collision_shadow_refresh_stress` | opt-in partial dirty toggle at global `127,64,95` passed with `dirty_last_edges=pos_x,pos_z`, `dirty_last_bounds=31,64,31:31,64,31`, `dirty_edge_neighbor_chunks=4`, `dirty_edge_neighbor_subchunks=8`, `dirty_last_edge_neighbor_chunks=2`, `dirty_last_edge_neighbor_subchunks=4`, `dirty_partial_chunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `current_chunk_collision=2`, `collision_refresh_last_rebuilt=1`, `proxy_shadow=200`, `compact_shadow_proxy=602`, `proxy_refresh_reuse=23`, `terrain_queue_max_ms=2.195`, `gpu_compositor_submit_max_ms=0.405`, `ground_misses=0`, and `gpu_upload_fail=0` | validates loaded-neighbor refresh plus collision/shadow/proxy path evidence, not full visual parity; repeat coverage is still needed before making partial dirty upload default |
| `post-53bd520` | Snap-back chunk-hole stress | `logs/gpu_flyback_snap_back_default` | default flyback smoke now uses `chunk_fly_snap_back` with `motion_steps=9`, `motion_chunks=8`, diagonal negative-Z travel faster than position-send cadence, `ground_samples=9`, `ground_hits=9`, `ground_misses=0`, `terrain_samples=378`, `terrain_queue_max_ms=2.257`, `gpu_compositor_submit_max_ms=0.124`, and `gpu_upload_fail=0` | redundant single-ray ground assertions were removed; `logs/gpu_flyback_snap_back_grace0` also passed but showed heavy unload/reload churn and lower residency |
| `post-73d076e` | Partial dirty upload prototype | `logs/gpu_partial_dirty_block_edit_retry` | opt-in `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=1` release toggle edit passed with `dirty_partial_chunks=2`, `dirty_partial_subchunks=4`, `dirty_partial_saved_subchunks=2`, `dirty_blocks=2`, `terrain_queue_max_ms=2.685`, `gpu_compositor_submit_max_ms=0.194`, and `gpu_upload_fail=0` | behind env flag; a separate clean retry in `logs/gpu_partial_dirty_block_edit_clean` hit transient `terrain_queue_max_ms=7.514` and failed the budget gate |
| `post-9b096bf` | Block-edit dirty stress | `logs/gpu_block_edit_stress` | release toggle edit passed with `block_edit_dirty_observed=1`, `chunk_replace=2`, `dirty_chunks=2`, `dirty_blocks=2`, `dirty_last_blocks=1`, `dirty_last_rebuild_subchunks=2`, `dirty_last_bounds=16,64,16:16,64,16`, `terrain_queue_max_ms=2.231`, `gpu_compositor_submit_max_ms=0.229`, and `gpu_upload_fail=0` | still uses full chunk replacement/rebuild; this is the baseline before partial GPU dirty uploads |
| `post-3d97ea2` | Dirty-update telemetry | `logs/gpu_dirty_update_telemetry_frame` | release movement marker now reports `chunk_initial=168`, `chunk_replace=0`, `dirty_chunks=0`, `dirty_blocks=0`, `dirty_last_rebuild_subchunks=0`, `terrain_queue_max_ms=2.032`, `gpu_compositor_submit_max_ms=0.170`, and `gpu_upload_fail=0` | movement-only run has no block edits; next dirty sample needs a block-edit smoke |
| `post-da6ebb3` | GPU upload breakdown telemetry | `logs/gpu_upload_breakdown_frame` | release movement passed with `gpu_upload_ms=0.046/0.006/0.052`, `gpu_upload_encode_ms=0.002/0.000/0.004`, `gpu_upload_stage_ms=0.009/0.001/0.014`, `gpu_upload_update_ms=0.034/0.004/0.043`, `terrain_queue_max_ms=2.042`, `gpu_compositor_submit_max_ms=0.154`, and `gpu_upload_fail=0` | default radius/load only; visual FPS stayed display-paced at 60 Hz |
| `post-da6ebb3` | Heavy radius-16 upload telemetry | `logs/gpu_terrain_upload_heavy_radius16` | 5/6 heavy cases passed with stable upload pressure: `terrain_queue_gpu_uploads_max=1`, `terrain_queue_gpu_upload_kb_max=1.5`, `gpu_upload_fail=0`, `gpu_upload_ms_max=0.062`, `gpu_effective_draws=1584`, `gpu_faces=2246`, `terrain_queue_max_ms=1.928`, `gpu_compositor_submit_max_ms=0.165`, and `process_wall_p95_ms=0.051` | `extended-filled` reached the expected chunk but missed/hung during screenshot capture; visual FPS stayed display-paced at 60 Hz |
| `post-d270c2a` | GPU upload frame telemetry | `logs/gpu_terrain_queue_upload_frame` | release movement passed with `terrain_queue_gpu_uploads=1/0.84/1`, `terrain_queue_gpu_upload_kb=0.0/0.0/1.5`, `gpu_upload_ms=0.014/0.005/0.136`, `gpu_upload_fail=0`, `gpu_fragmentation_pct=0.0`, `smoke_err=0`, `terrain_queue_max_ms=3.660`, and `gpu_compositor_submit_max_ms=0.169` | visual FPS was display-paced at 60 Hz; default server batch/radius produced `gpu_draws=246`, so repeat under heavy workload before drawing upload-budget conclusions |

## 2026-06-11

| Commit | Slice | Artifact | Key Result | Caveat |
| --- | --- | --- | --- | --- |
| `2635006` | GPU terrain back-face culling | `logs/gpu_terrain_cull_back_smoke` | radius-16 movement passed with `gpu_draws=1582`, `gpu_faces=2244`, `gpu_upload_fail=0`, `smoke_err=0` | fill-stress comparison was display-limited at 60 FPS in that session |
| `c5dad7f` | GPU roadmap/profiling docs | `docs/GPU_ROADMAP.md`, `docs/GPU_PROFILING.md` | 120 sequential GPU iterations and trusted/untrusted metric definitions | docs only |
| `a79fe71` | Unified GPU report | `logs/gpu-terrain-report.txt` | aggregate report reads existing movement/fill/workload/baseline summaries | aggregates historical logs, not only newest runs |
| `c24cb98` | Rasterization state markers | `logs/gpu_terrain_cull_summary` | marker and movement summary report `gpu_cull=back gpu_front_face=clockwise`, `gpu_upload_fail=0`, `fps_p05=144.0` | same run reported very large `post_draw_wait_ms`, so capture timing needs watching |
| `50be49c` | Fill-stress report-only repeats | `/tmp/rumpel_fill_report_only_fail` | report-only failure path writes `status=failed` and exits 0 | normal repeat=1 validation can still miss screenshot capture |
| `8ad45ad` | Fill-stress visual correctness fields | `scripts/gpu_terrain_fill_stress.sh` | pass lines include `status`, `smoke_err`, `terrain_samples`, and `terrain_color_buckets` | normal pass-line format was not revalidated by a successful fill-stress capture due capture-window instability |
| `74518b8` | Fragmentation telemetry | unit tests | derives `gpu_fragmented_free_faces` and `gpu_fragmentation_pct` without allocator behavior changes | existing logs show `n/a` until a fresh marker is captured |
| `82532a5` | Metric origins in GPU report | `logs/gpu-terrain-report.txt` | report points aggregate spikes to exact summary/marker files | selected summaries now use file mtime after `493c8e6` |

## Next Trend Entry

Record the next entry after a fresh successful dirty-update, external-profiler, or fill-stress run that includes:

- `gpu_cull` and `gpu_front_face`.
- `gpu_fragmented_free_faces` and `gpu_fragmentation_pct`.
- `terrain_queue_gpu_uploads`, `terrain_queue_gpu_upload_kb`, and the new-slot/replacement-slot upload lane metrics.
- `dirty_blocks`, `dirty_last_blocks`, and `dirty_last_rebuild_subchunks` if block edits are part of the run.
- fill-stress visual correctness fields on a pass line, if the run is fill stress.
- the artifact path and exact env vars.
