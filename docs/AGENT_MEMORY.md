# Agent Memory

This file stores stable project decisions and invariants for agents. Keep it short and factual.

## Project Decisions

- The approved project databases are PostgreSQL and RocksDB.
- Do not introduce, expand, or migrate to another database engine without explicit approval.

## Sensitive Areas

- Client/server protocol compatibility matters.
- World generation and chunk serialization must remain deterministic unless explicitly changed.
- Current worldgen determinism is guarded in `server/pkg/world` tests: flat strata, repeat generation, little-endian serialized block order, and `World.ChunkSnapshot()` stability must stay compatible unless explicitly changed.
- Current chunk payload compatibility is guarded in `server/pkg/api`, `server/pkg/world`, and `server/pkg/network` tests: packet field numbers, chunk field numbers, enum wire values, raw default fields, RLE wire vectors, and Go protobuf unknown-field preservation must not regress without an explicit protocol task.
- Current RocksDB chunk persistence is guarded in `server/pkg/storage` tests: key format, signed coordinate key ordering, save/load round-trip, missing chunk behavior, overwrite isolation, and corrupt payload rejection must stay compatible unless explicitly changed.
- `scripts/world_streaming_exploration_soak.sh` is the current bounded long-run exploration wrapper. Its one-run smoke is harness validation only; raise `RUMPELMC_EXPLORATION_SOAK_REPEATS` before treating it as long-run stability evidence.
- `scripts/world_streaming_resident_set_matrix.sh` is the current resident-set trend wrapper. By default it is summary-only over existing resident-set growth artifacts; `RUMPELMC_RESIDENT_SET_MATRIX_RUN=1` is the explicit heavy-capture path and must not change default draw distance, visible quality, protocol, storage, worldgen, or chunk serialization. Fresh clean radii `16 18` evidence in `logs/world_streaming_resident_set_matrix_radius16_18_clean` failed only the pressure threshold (`max_gpu_draws=1859`, occupancy `22.693%`, upload failures `0`), so it does not justify active GPU repack or draw-capacity changes.
- `scripts/gpu_terrain_load_scaling.sh` is the current high resident-set GPU terrain gate. Standard workload results around `996` draws are not enough for this gate; current radius-16 evidence reaches `2482` draws/subchunks and `3165` faces.
- `scripts/gpu_terrain_upload_pressure.sh` is the current upload-pressure gate. It composes fill stress and allocator stress; do not change upload capacity, allocator policy, or visible quality based on it without a separate explicit implementation task.
- `scripts/gpu_terrain_in_place_upload_gate.sh` is the current opt-in same-face-count GPU subchunk upload gate. It runs block-edit stress with `RUMPELMC_GPU_TERRAIN_IN_PLACE_SUBCHUNK_UPLOAD=1` against an isolated RocksDB path and should report at least one `gpu_in_place_uploads`, nonzero terrain queue new-slot and replacement-slot upload marker maxima, and zero upload failures.
- `scripts/gpu_terrain_upload_budget.sh` is the current per-frame GPU terrain upload budget gate. It consumes movement plus in-place upload lane artifacts and checks total/new-slot/replacement-slot upload count and KiB maxima with zero upload failures and current retry/backoff policy `none/0`; it is governance only, not runtime throttling.
- `scripts/gpu_terrain_upload_stage_pool_gate.sh` is the current default-off GPU upload staging pool comparison gate. It runs baseline plus `RUMPELMC_GPU_TERRAIN_UPLOAD_STAGE_POOL=1` movement and in-place dirty upload captures, requires each baseline to report pool enabled/creates/reuses `0/0/0`, requires each pooled run to reuse exact-size `PackedByteArray` stage arrays with creates lower than total uploads, requires in-place dirty upload to still execute, and keeps all upload failure/retry/backoff counters clean. Do not enable the pool by default until broader upload-pressure, resident-set/load-scaling, and external profiler evidence supports it.
- `scripts/gpu_terrain_upload_stage_pool_load_scaling_gate.sh` is the high resident-set companion gate for the default-off GPU upload staging pool. It compares baseline and pooled `gpu_terrain_load_scaling` summaries, defaults to the stress-only `pressure` workload with `RUMPELMC_VISUAL_SMOKE_TERRAIN_PRESSURE_FIXTURE=chunk_disc`, requires the existing subchunk/draw/face/occupancy and queue/process/submit budgets, rejects old summaries that lack workload/fixture/upload/stage-pool counters, requires baseline pool telemetry to stay disabled, and requires pooled creates lower than total uploads. Fresh 2026-06-16 evidence passed with baseline and pooled `2289` subchunks/draws, `6292` faces, zero upload failures, and pooled stage-pool creates/reuses `9/3128`. This remains default-off local macOS/Metal evidence; do not enable the pool by default without external profiler and Windows/Vulkan/Direct3D validation.
- Current GPU terrain upload retry/backoff telemetry is marker-only: `gpu_upload_retry_policy=none`, retry attempts/success/giveups `0`, and backoff active/frames/max frames `0`. Do not add hidden retry loops or backoff windows without explicit rebaseline across movement, in-place dirty update, upload budget, failure recovery, and external profiler evidence.
- GPU upload failure recovery is fallback-first: a subchunk only uses GPU proxy behavior after a confirmed GPU slot; failed or missing uploads must keep `FullArrayMesh` / `BuildMesh` CPU fallback paths until fresh failure-injection evidence proves a different policy safe.
- `scripts/gpu_terrain_upload_failure_fallback_gate.sh` is the current diagnostic upload-failure fallback gate. It enables `RUMPELMC_GPU_TERRAIN_UPLOAD_FAILURE_INJECTION=1`, expects nonzero `gpu_upload_fail` and `gpu_upload_fail_injected`, keeps capacity/fragmentation failure counters at `0`, and requires CPU ArrayMesh visual readiness, `shadow_path=arraymesh`, current collision readiness, zero ground misses, and non-sky terrain samples. Its terrain queue budget is report-only because it intentionally forces CPU fallback. Do not use this injected-failure artifact as a normal performance baseline or real allocator-pressure signal.
- `scripts/gpu_terrain_mass_chunk_load_gate.sh` is the current mass chunk-load summary gate. It composes high resident-set load-scaling evidence with the upload budget gate and requires thousands of GPU subchunks/draws/faces, draw-command occupancy, real queue/process/submit budgets, and zero upload failures without changing runtime quality or defaults.
- GPU repack activation is currently deferred by `scripts/gpu_repack_activation_preflight.sh`; current evidence reports `active_repack_allowed=0` because fragmentation pressure is `0.0%` and upload failures are zero.
- GPU repack soak/rollback is currently deferred by `scripts/gpu_repack_soak_rollback_gate.sh`; `active_soak_run=0` must not be cited as active-repack runtime validation.
- `scripts/gpu_resource_lifecycle_audit.sh` is the current RenderingDevice resource lifecycle audit gate. It is audit-only and checks clean GPU report scans, scene-target reuse without replacement, required default terrain resources, zero upload failures, and zero native-shadow resource error counters.
- `scripts/gpu_terrain_memory_budget.sh` is the current summary-only GPU terrain memory budget gate. It sets explicit budgets for configured terrain buffers, active face bytes, subchunks, draws, faces, draw-command occupancy/headroom, fragmentation, free ranges, and upload failures without changing runtime allocator policy.
- `scripts/gpu_terrain_report_v2.sh` is the current classified GPU report wrapper. It separates one scoped summary, fail gates, historical aggregate metrics from `gpu_terrain_report.sh`, and warning-only local FPS/GPU timestamp fields. Transparent workload counters are now surfaced as scoped values when present and as historical maxima for `transparent_blocks`, `transparent_faces`, `transparent_draws`, and `transparent_subchunks`.
- `scripts/performance_baseline_governance.sh` compares classified V2 report summaries against accepted files in `docs/performance_baselines/`. Local FPS/frame/GPU timestamp fields remain warning-only unless external profiler evidence changes that trust policy.
- `scripts/test_strategy_gate.sh` records the current fast/full/nightly strategy. `check.sh fast` and `check.sh full` stay normal development checks; heavy runtime/nightly performance wrappers remain separate.
- `scripts/gpu_native_shadow_prototype_preflight.sh` is the current native-shadow prototype readiness gate. While `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, it should report `status=deferred`, `active_prototype_allowed=0`, and `reason=implementation_gate_false`; do not treat this as active native-shadow rendering validation.
- `scripts/shadow_quality_parity_program.sh` is the current shadow quality parity gate. It validates Godot proxy and native-shadow fallback parity while marking active native-shadow comparison as deferred until a real `shadow_path=gpu_native_shadow` capture exists.
- `scripts/shadow_proxy_cost_decision_gate.sh` is the current shadow proxy cost decision gate. It consumes shadow quality parity, the shadow-radius matrix, the pending capture pack, and optional validated external profiler results; without captured external rows it should pass only with `decision=defer_runtime_change` and `default_runtime_change_allowed=0`.
- `scripts/shadow_proxy_retirement_plan.sh` is the current CPU shadow proxy retirement guard. It should keep `retirement_allowed=0` until active native-shadow captures, external profiler results, and Godot proxy rollback evidence exist.
- `scripts/lighting_stability_matrix.sh` is the current lighting stability gate. It validates default and low-angle lighting movement, shadow parity, compact shadow parity, and dense resident load; ambient-variant coverage remains deferred until a dedicated smoke pose exists.
- `scripts/transparent_active_path_preflight.sh` is the current transparent active-path readiness gate. While `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`, it should report `status=deferred`, `active_path_allowed=0`, and zero transparent workload markers.
- `scripts/transparent_sorting_depth_program.sh` is the current transparent sorting/depth policy gate. It should remain deferred until active transparent workload markers are nonzero and fixture occlusion/collision evidence exists.
- `scripts/transparent_fixture_acceptance_suite.sh` is the current transparent fixture acceptance summary. Current fallback acceptance should pass while active fixture acceptance remains deferred.
- `scripts/transparent_prototype_shape_decision_gate.sh` is the transparent prototype-shape decision gate for broadening beyond cutout-only. Current expected shape is `decision=cutout_only_first`; default runtime changes and split/full blended alpha remain deferred until active workload, sorting/depth, and profiler evidence exist.
- `RUMPELMC_GPU_TERRAIN_CUTOUT_PROTOTYPE=1` is the current default-off transparent-family runtime prototype. It only enables leaf-style cutout alpha-test in the existing GPU opaque pass, keeps `RUMPELMC_GPU_TERRAIN_TRANSPARENT=1` as the legacy full-transparent fallback request while `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`, does not add blending/sorting/split buffers, and must report `transparent_active=1`, `transparent_fallback=0`, nonzero transparent workload, and `gpu_upload_fail=0` in a cutout block-edit smoke. `scripts/gpu_terrain_cutout_prototype_acceptance_gate.sh` promotes that smoke into `transparent-cutout-prototype-acceptance-summary.txt` and requires aggregate report surfacing while keeping default runtime changes blocked. Fresh local evidence is `logs/gpu_transparent_cutout_prototype_current/block-edit-stress-summary.txt`.
- `scripts/block_material_metadata_design_gate.sh` is the current production block material metadata design gate. Current expected status is `pass` with `active_schema_change=0`, `current_runtime_contract=opaque_only`, and `block_id` remaining the only chunk wire/storage identity.
- `scripts/texture_atlas_evolution_gate.sh` is the current texture atlas metadata evolution gate. Current expected status is `pass` with the atlas still `640x64`, tile size `64`, grid `10x1`, `MAX_TEXTURE_TILE=9`, packed tile capacity `2048`, no atlas asset diff, and no shader layout change.
- `scripts/biome_visual_variety_foundation_gate.sh` is the current biome/visual-variety foundation gate. Current expected status is `pass` with no `server/pkg/world/chunk.go` generation/serialization diff and runtime biome visuals deferred until an explicit deterministic seed/model exists.
- `scripts/world_generation_quality_gate.sh` is the current worldgen quality planning gate. Current expected status is `pass` with no active generator/chunk-byte change and runtime terrain height, cave, resource, and structure layers deferred until explicit seed/version tests exist.
- `scripts/server_scalability_pass_gate.sh` is the current server scalability checkpoint. Current expected status is `pass` with per-client sent-chunk state, interested-client block-edit fanout, failed-broadcast cleanup, and session write deadlines unit-guarded, no protocol diff, optional `scripts/server_multi_client_smoke.sh` live two-client fanout evidence, and broader live load/CPU/memory profiling deferred.
- `scripts/networking_robustness_gate.sh` is the current networking robustness checkpoint. Current expected status is `pass` with Go/Rust packet boundary tests covering short, oversized, and malformed frames, session write deadlines unit-guarded, optional two-client live fanout status consumed from server scalability, optional `scripts/server_slow_reader_smoke.sh` bounded live slow-reader evidence, no protocol diff, and reconnect/overload/backpressure policy deferred.
- `scripts/client_state_machine_hardening_gate.sh` is the current client lifecycle checkpoint. Current expected status is `pass` with connecting/waiting_chunks/spawning/active/reconnecting/shutdown transitions unit-guarded, no protocol diff, and runtime reconnect/state telemetry deferred.
- `scripts/gameplay_loop_foundation_gate.sh` is the current gameplay foundation checkpoint. Current expected status is `pass` with local creative hotbar inventory unit-guarded, mining/building still using `BlockAction` and `World.SetBlockGlobal`, no protocol diff, and full edit reload persistence deferred.
- `scripts/block_edit_persistence_gate.sh` is the current block edit persistence checkpoint. Current expected status is `pass` with `SetBlockGlobal` place/destroy edits reloaded through a fresh `World(store)`, no protocol diff, and runtime persisted-reload visual/collision/GPU smoke deferred.
- `scripts/dirty_update_scalability_gate.sh` is the current dirty update scalability checkpoint. Current expected status is `pass` with mass dirty update math unit-guarded, edge dirty runtime wrappers syntax-checked, no protocol diff, and heavy runtime mass-edit evidence deferred.
- `scripts/tooling_debug_overlay_gate.sh` is the current tooling/debug overlay checkpoint. Current expected status is `pass` with compact HUD overlay wired through `GameClient.get_debug_overlay_text()`, full `get_perf_text` logging preserved, no protocol diff, and no Godot scene/resource/import diff.
- `scripts/observability_logs_cleanup_gate.sh` is the current observability/log cleanup checkpoint. Current expected status is `pass` with `RUMPELMC_RUN_ID` HUD perf-log stamping wired, `logs/*current/*summary.txt` indexed, summary naming/status clean, and current error scan clean.
- `scripts/automated_handoff_discipline_gate.sh` is the current handoff automation checkpoint. Current expected status is `pass` with generated handoff snapshots including quality inputs and the current observability artifact index; `docs/AGENT_HANDOFF.md` remains manually updated before actual delegation or pause.
- `scripts/architecture_documentation_refresh_gate.sh` is the current architecture documentation checkpoint. Current expected status is `pass` with `docs/ARCHITECTURE.md` reflecting the actual Godot/Rust/Go/RocksDB/protobuf architecture and no runtime behavior change.
- `scripts/security_data_integrity_review_gate.sh` is the current security/data-integrity checkpoint. Current expected status is `pass` with packet length guards, protobuf decode tests, storage save/load/corrupt-payload tests, block-edit validation, and no active protocol diff.
- `scripts/release_candidate_gate.sh` is the current release-candidate evidence checkpoint. Current expected status is `pass` in summary mode; live `check.sh fast`, `check.sh full`, `git diff --check`, and `diff_guard.sh` execution is opt-in through `RUMPELMC_RC_RUN_FAST_CHECKS`, `RUMPELMC_RC_RUN_FULL_CHECKS`, and `RUMPELMC_RC_RUN_DIFF_GUARD`.
- `scripts/external_profiling_campaign_gate.sh` is the current external-profiler campaign checkpoint. Current expected status is `pass` with `external_profile_status=pending_external_profiler`; generated capture packs and TODO templates are not profiler evidence until real captured rows validate through `scripts/gpu_terrain_shadow_profiler_results_check.sh`.
- `scripts/gpu_shader_profiler_capture_pack.sh` is the current shader hot-path external-profiler handoff guard. It validates the current movement artifact and render shader source contracts, then emits pending macOS Metal and Windows GPU capture rows; the generated pack/manifest are checklists, not profiler evidence.
- `scripts/gpu_shader_profiler_results_check.sh` is the current shader hot-path external-profiler results validator. It accepts only captured rows matching the shader profiler manifest with non-placeholder profiler artifacts, counter evidence, positive pass timings, and stage timings; synthetic rows are test fixtures only and not profiler evidence.
- `scripts/production_readiness_milestone_gate.sh` is the current 50-block milestone checkpoint. Current expected status is `pass` with `production_readiness=rc_evidence_ready`; it is an evidence milestone, not a shipped-release declaration, and live release checks plus external profiler evidence remain separate.
- Storage and persistence changes require extra review before finalizing.
- Rust GDExtension changes can affect client behavior and performance; review them carefully.
- Client startup readiness telemetry is split into one-shot wall-clock timestamps `startup_chunk_packet_ms`, `startup_chunk_inserted_ms`, `startup_chunk_loaded_ms`, `startup_mesh_queued_ms`, `startup_mesh_dispatched_ms`, `startup_first_mesh_ms`, `startup_collision_ms`, and `startup_player_spawn_ms`; movement gates should preserve that nondecreasing order and keep `startup_packet_read_work_ms`, `startup_packet_decode_work_ms`, `startup_packet_reader_elapsed_ms`, `startup_packet_queue_lag_ms`, `startup_chunk_decode_work_ms`, `startup_first_mesh_work_ms`, `startup_first_mesh_phase_ms`, and `startup_first_mesh_collision_work_ms` as work-duration/scheduling evidence separate from wall-clock startup timestamps.
- After Godot observes the local server listening, the main scene attaches `GameClient` after one frame by default; `RUMPELMC_CLIENT_ATTACH_DELAY_SEC` is a diagnostic rollback/control for adding an explicit post-listen delay.
- Server chunk delivery order is nearest-first by default. `RUMPELMC_SERVER_CHUNK_ORDER=directional` is opt-in and only applies movement-direction tie-breaks among chunks at the same distance; it must not change protocol, chunk payload format, storage, world generation, view distance, batch size, or bootstrap radius.
- Rust client packet queue metrics are observational drain-depth proxies over the existing unbounded reader-thread channel. They must not be treated as a backpressure/drop/reorder policy without a separate explicit design change.
- Current chunk compression decision is RLE default with `RUMPELMC_SERVER_CHUNK_ENCODING=raw` rollback. Do not add another compression format until representative non-flat terrain entropy or latency evidence justifies it.
- Current streaming protocol plan is no schema change. Future packet variants must use new oneof tags greater than `3`, and future `ChunkData` fields must use new field numbers greater than `5` with compatibility tests and rollback documented first.
- Resident-set growth gates are stress-only measurement runs. They may raise server view distance and client keep distance inside scripts, but must not change default gameplay draw distance, visible quality, protocol, storage, world generation, or chunk serialization without a separate explicit task.
- Client chunk unload remains keep-distance plus grace based by default. `RUMPELMC_CLIENT_CHUNK_UNLOAD_GRACE_SEC=0` is an immediate-unload rollback/control path; unload churn metrics are telemetry only and must not be treated as a new unload policy without pop-in/collision evidence.
- World pop-in metrics are report-only player-neighborhood probes around the current chunk. They may count missing loaded chunks and loaded-but-collision-missing chunks, but must not become fail gates or policy changes until explicit budgets are defined.
- Player spawn remains collision-gated. Movement readiness summaries may separate render-ready and collision-ready evidence, but they do not authorize lazy collision or reduced collision coverage without an explicit opt-in experiment and gates.
- Client collision radius remains `1` by default. `RUMPELMC_CLIENT_COLLISION_CHUNK_DISTANCE=0` is an opt-in lazy-collision experiment only and must not be promoted while pop-in collision-missing evidence is high.
- GPU terrain render shader assumptions matter: Rust sanitizes terrain lighting before push-constant packing, vertex code computes terrain lighting from packed face normals and those sanitized lighting constants without repeating shader-side light guards, face normals/UVs/corners plus signed chunk/subchunk coordinate unpack are branchless hot-path helpers, atlas tile offsets are computed in the vertex stage and passed with `flat` interpolation while UV stays interpolated for merged-face repetition, terrain lighting and the cutout alpha-test bit are also `flat` per-face varyings, fragment code may discard low-alpha texels only for default-off cutout faces and otherwise applies the passed lighting to atlas color with opaque alpha, perf markers expose the sanitized `gpu_light_*` values, and scene depth uses reverse-Z `GREATER_OR_EQUAL`.
- GPU terrain push constants are currently a fixed `112`-byte camera/projection + lighting + atlas-layout block packed into a stack array before `draw_list_set_push_constant`; tests guard the byte layout and runtime markers expose the `64/32/16` split. Do not move atlas layout into immutable shader/uniform data unless profiler evidence shows the constants phase is material.
- GPU terrain partial dirty upload is default-on when `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` is unset; explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` is the full-rebuild rollback/control path.
- `scripts/gpu_terrain_pressure_dirty_compare.sh` is the current full-vs-partial dirty pressure gate. It must run full rebuild (`RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0`) and partial dirty (`=1`) lanes against isolated RocksDB paths, reject local server reuse on port `25565`, use the same `chunk_disc` pressure fixture by default, and require the edge/corner local block `31,31` partial lane to produce saved subchunks plus edge-neighbor refresh while upload failures, queue/process/submit budgets, collision, ground misses, and terrain samples stay valid.
- `scripts/gpu_terrain_grouped_draws_gate.sh` is the current default-off grouped indirect draw comparison gate. `RUMPELMC_GPU_TERRAIN_GROUPED_DRAWS=1` may rebuild the indirect draw buffer as contiguous face-range records; unset or `0` must keep the existing one-record-per-subchunk path and incremental draw-command patching. In grouped mode, `gpu_draws` reports actual grouped indirect records, so use `gpu_draw_records_logical` for workload comparisons and require `gpu_draw_grouped_saved_records`, reduced `gpu_draw_cmd_bytes`, zero upload failures, current collision, zero ground misses, terrain samples, and queue/process/submit budgets before treating the path as evidence. Do not enable grouped draws by default without broader runtime stability plus external macOS and Windows/Vulkan/Direct3D profiler validation.
- GPU terrain in-place subchunk upload remains default-off behind explicit `RUMPELMC_GPU_TERRAIN_IN_PLACE_SUBCHUNK_UPLOAD=1`. It may only update an existing nonzero GPU subchunk in place when the new packed face count matches the current slot; misses must fall back to the existing remove/allocate/insert path.
- GPU terrain queue upload telemetry is split into total, new-slot, and replacement-slot lanes. Use `terrain_queue_gpu_upload_new_slots` / `terrain_queue_gpu_upload_new_slot_kb` for initial GPU-resident world-load pressure and `terrain_queue_gpu_upload_replace_slots` / `terrain_queue_gpu_upload_replace_slot_kb` for dirty/update replacement pressure; do not treat the total upload count alone as enough evidence for upload-pipeline decisions.
- GPU terrain buffer repack must remain default-off behind explicit `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`; unset or `0` keeps the current allocator path until planner tests, allocator gates, profiling artifacts, and rollback behavior justify any broader rollout.
- Visual smoke lighting variants are smoke-only controls. They must not change default scene lighting, draw distance, terrain quality, protocol, storage, world generation, or chunk serialization; use marker fields such as `lighting_variant` plus `gpu_light_*` before comparing captures.
- GPU-native terrain shadows remain behind `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`. While runtime markers report `native_shadow_implemented=0`, env-on captures must stay on `shadow_path=godot_proxy` with `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_resource_status=disabled`, and zero native-shadow resource lifecycle plus coverage counters.
- Godot scene/resource/import files should not be reformatted casually.

## Agent Roles

- Research agents are read-only and should return concise findings with file paths.
- Review agents look for risks, regressions, missing tests, and code degradation.
- Protocol/world review is required for networking, storage, world generation, chunk serialization, persistence, or Rust extension changes.
- The main agent decides what to change and remains responsible for the final patch.
