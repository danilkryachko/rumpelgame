# Agent Memory

This file stores stable project decisions and invariants for agents. Keep it short and factual.

## Project Decisions

- The approved project databases are PostgreSQL and RocksDB.
- Do not introduce, expand, or migrate to another database engine without explicit approval.

## Sensitive Areas

- Client/server protocol compatibility matters.
- World generation and chunk serialization must remain deterministic unless explicitly changed.
- Current worldgen determinism is guarded in `server/pkg/world` and `server/cmd/server` tests plus `scripts/server_height_generator_smoke.sh`: explicit `GeneratorConfig` seed/dimension/version inputs, server env parsing for `RUMPELMC_WORLD_SEED`, `RUMPELMC_WORLD_DIMENSION_ID`, and `RUMPELMC_WORLD_GENERATOR_VERSION`, `flat_v1` byte preservation across configured seeds, opt-in `height_v1` deterministic height bytes and seed/dimension sensitivity, `height_v1` RLE round-trip, persisted reload, and live server streaming coverage, unknown generator-version rejection, flat strata, representative flat and height chunk SHA-256 bytes, repeat generation, little-endian serialized block order, and `World.ChunkSnapshot()` stability must stay compatible unless explicitly changed.
- Current chunk payload compatibility is guarded in `server/pkg/api`, `server/pkg/world`, and `server/pkg/network` tests: empty-packet zero-wire bytes, packet field numbers, chunk field numbers, enum wire values, raw default fields, RLE wire vectors, and Go protobuf unknown-field preservation must not regress without an explicit protocol task.
- `scripts/protocol_generated_drift_gate.sh` is the current protocol generated-code drift checkpoint. Current expected status is `pass` with `generated_header=guarded`, `schema_identity=guarded`, `partial_generated_drift=guarded`, `schema_diff=0`, and `generated_diff=0`; protocol/schema work must not leave only `api/schema/packets.proto` or only `server/pkg/api/packets.pb.go` changed.
- Current RocksDB persistence is guarded in `server/pkg/storage` tests: chunk key format, signed coordinate key ordering, save/load round-trip, missing chunk behavior, overwrite isolation, concurrent distinct-key save/load, corrupt chunk payload rejection, player inventory key separation under the `p i NUL` prefix, player inventory round-trip, corrupt player inventory rejection, actionable open/decode error context, missing-parent path creation, regular-file parent path rejection, regular-file database path rejection, closed-store operation errors, repeated close safety, and nil chunk save rejection must stay compatible unless explicitly changed. Server config tests guard `RUMPELMC_SERVER_ROCKSDB_PATH` as the only current runtime store path override; PostgreSQL environment variables must not select a backend without a separate storage design.
- `scripts/world_streaming_exploration_soak.sh` is the current bounded long-run exploration wrapper. Its one-run smoke is harness validation only; raise `RUMPELMC_EXPLORATION_SOAK_REPEATS` before treating it as long-run stability evidence.
- `scripts/world_streaming_resident_set_matrix.sh` is the current resident-set trend wrapper. By default it is summary-only over existing resident-set growth artifacts; `RUMPELMC_RESIDENT_SET_MATRIX_RUN=1` is the explicit heavy-capture path and must not change default draw distance, visible quality, protocol, storage, worldgen, or chunk serialization. Fresh clean radii `16 18` evidence in `logs/world_streaming_resident_set_matrix_radius16_18_clean` failed only the pressure threshold (`max_gpu_draws=1859`, occupancy `22.693%`, upload failures `0`), so it does not justify active GPU repack or draw-capacity changes.
- `scripts/gpu_terrain_load_scaling.sh` is the current high resident-set GPU terrain gate. Standard workload results around `996` draws are not enough for this gate; current radius-16 evidence reaches `2482` draws/subchunks and `3296` faces.
- `scripts/gpu_terrain_upload_pressure.sh` is the current upload-pressure gate. It composes fill stress and allocator stress; do not change upload capacity, allocator policy, or visible quality based on it without a separate explicit implementation task.
- GPU upload staging telemetry is exposed as `gpu_upload_staging_buffers`, `gpu_upload_staging_mb`, `gpu_last_upload_staging_kb`, and `gpu_upload_staging_kb=last/avg/max`. These are CPU-side materialization counters for the existing `PackedByteArray` staging path; use them as evidence before staging reuse or upload-pool work, not as permission to change allocator policy by themselves.
- GPU repack activation is currently deferred by `scripts/gpu_repack_activation_preflight.sh`; current evidence reports `active_repack_allowed=0` because fragmentation pressure is `0.0%` and upload failures are zero.
- GPU repack soak/rollback is currently deferred by `scripts/gpu_repack_soak_rollback_gate.sh`; `active_soak_run=0` must not be cited as active-repack runtime validation.
- `scripts/gpu_resource_lifecycle_audit.sh` is the current RenderingDevice resource lifecycle audit gate. It is audit-only and checks clean GPU report scans, scene-target reuse without replacement, required default terrain resources, zero upload failures, and zero native-shadow resource error counters.
- `scripts/gpu_terrain_memory_budget.sh` is the current summary-only GPU terrain memory budget gate. It sets explicit budgets for configured terrain buffers, active face bytes, subchunks, draws, faces, draw-command occupancy/headroom, fragmentation, free ranges, and upload failures without changing runtime allocator policy.
- `scripts/gpu_terrain_report_v2.sh` is the current classified GPU report wrapper. It separates one scoped summary, fail gates, historical aggregate metrics from `gpu_terrain_report.sh`, and warning-only local FPS/GPU timestamp fields.
- `scripts/performance_baseline_governance.sh` compares classified V2 report summaries against accepted files in `docs/performance_baselines/`. Local FPS/frame/GPU timestamp fields remain warning-only unless external profiler evidence changes that trust policy.
- `scripts/test_strategy_gate.sh` records the current fast/full/nightly strategy and requires `gpu_report_freshness_status=guarded` from `scripts/gpu_terrain_report_freshness_gate.sh`. `check.sh fast` and `check.sh full` stay normal development checks; heavy runtime/nightly performance wrappers remain separate.
- `scripts/gpu_native_shadow_prototype_preflight.sh` is the current native-shadow prototype readiness gate. While `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`, it should report `status=deferred`, `active_prototype_allowed=0`, `reason=implementation_gate_false`, `fallback_readiness_fields=present`, and `fallback_readiness_status=disabled_clean`; compact fallback summaries without framebuffer/pass/command-buffer readiness fields must fail as stale evidence. Do not treat this as active native-shadow rendering validation.
- `scripts/shadow_quality_parity_program.sh` is the current shadow quality parity gate. It validates Godot proxy and native-shadow fallback parity while marking active native-shadow comparison as deferred until a real `shadow_path=gpu_native_shadow` capture exists, and it must carry forward `native_preflight_readiness_status=disabled_clean` from Block 26.
- `scripts/shadow_proxy_retirement_plan.sh` is the current CPU shadow proxy retirement guard. It should keep `retirement_allowed=0` until active native-shadow captures, external profiler results, Godot proxy rollback evidence, and clean native preflight readiness evidence exist.
- `scripts/lighting_stability_matrix.sh` is the current lighting stability gate. It validates default and low-angle lighting movement, shadow parity, compact shadow parity, and dense resident load; ambient-variant coverage remains deferred until a dedicated smoke pose exists.
- `scripts/transparent_active_path_preflight.sh` is the current transparent active-path readiness gate. While `GPU_TERRAIN_TRANSPARENT_IMPLEMENTED=false`, it should report `status=deferred`, `active_path_allowed=0`, and zero transparent workload markers.
- `scripts/transparent_sorting_depth_program.sh` is the current transparent sorting/depth policy gate. It should remain deferred until active transparent workload markers are nonzero and fixture occlusion/collision evidence exists.
- `scripts/transparent_fixture_acceptance_suite.sh` is the current transparent fixture acceptance summary. Current fallback acceptance should pass while active fixture acceptance remains deferred; no-render fixture artifacts should report contract-ready statuses such as `fixture_plan_status=contract_ready`, `transparent_fixture_harness_status=contract_ready`, and `transparent_fixture_scene_implementation_checklist_status=implementation_contract_ready`.
- `scripts/block_material_metadata_design_gate.sh` is the current production block material metadata design gate. Current expected status is `pass` with `production_metadata_status=server_registry_guarded`, `server_material_metadata=guarded`, `client_material_metadata=guarded`, `server_flags=solid_opaque_placeable_material_policies_textures_mining_duration`, `active_schema_change=0`, `current_runtime_contract=opaque_only`, and `block_id` remaining the only chunk wire/storage identity.
- `scripts/block_material_registry_foundation_gate.sh` is the current existing-ID server block material registry foundation gate. Current expected status is `pass` with `material_registry_status=guarded`, `server_registry_identity=guarded`, registry hash `5f6e32de12b5baa99a11d61d985f52fa7c4d5061090d91f82c1744e1c47ba21e`, `block_count=6`, `networked_blocks=6`, `opaque_solid_blocks=5`, `mining_duration_metadata=guarded`, no block-ID/protocol/storage/renderer diff, and `world_tests=pass`.
- `scripts/client_block_material_registry_foundation_gate.sh` is the current existing-ID client block material registry foundation gate. Current expected status is `pass` with `client_material_registry_status=guarded`, `client_registry_identity=guarded`, `server_material_registry_status=guarded`, `server_registry_identity=guarded`, `client_block_count=6`, `client_opaque_solid_blocks=5`, no protocol/storage/renderer-code diff, and `rust_tests=pass`.
- `scripts/block_material_registry_parity_gate.sh` is the current existing-ID client/server material parity gate. Current expected status is `pass` with `parity_status=guarded`, `server_registry_identity=guarded`, `client_registry_identity=guarded`, matching block, opaque-solid, placeable, air, emissive, and liquid counts across server/client summaries, no protocol/storage/renderer-code diff, and `current_runtime_contract=opaque_only`.
- `scripts/texture_atlas_evolution_gate.sh` is the current texture atlas metadata evolution gate. Current expected status is `pass` with `atlas_tile_identity=guarded`, `block_texture_usage=guarded`, the atlas still `640x64`, tile size `64`, grid `10x1`, `MAX_TEXTURE_TILE=9`, packed tile capacity `2048`, no atlas asset diff, and no shader layout change.
- `scripts/biome_sampler_matrix_gate.sh` is the current metadata-only biome matrix artifact gate. Current expected status is `pass` with `matrix_status=guarded`, `sample_hash=6fa59c33bbf1c4aa1a86028e89b04ce1dc26d9bb6fe5fa32e6f5085f5983fb4e`, `samples=9`, no chunk/protocol diff, and `world_tests=pass`.
- `scripts/cave_sampler_matrix_gate.sh` is the current metadata-only cave matrix artifact gate. Current expected status is `pass` with `matrix_status=guarded`, `sample_hash=f265ead2a700736c1e9b13056004af9c5c242d083c74c56d48e2cc76d1dfeeee`, `samples=12`, no chunk/protocol diff, and `world_tests=pass`.
- `scripts/cave_height_generator_matrix_gate.sh` is the current opt-in cave-height generator matrix artifact gate. Current expected status is `pass` with `generator_version=cave_height_v1`, chunk hash `b68d1ca3e6471015c317c4b1d750dcddc59a8481f4cc2d26e394730d55fd7541`, `carved_air=34728`, `surface_preserved=1024`, no protocol diff, and `world_tests=pass`.
- `scripts/biome_cave_height_generator_matrix_gate.sh` is the current opt-in biome-cave-height generator matrix artifact gate. Current expected status is `pass` with `generator_version=biome_cave_height_v1`, chunk hash `c781f5530436094665f9596b9338065717400f4d497b9dd32f3f0d8839ff76a0`, `carved_air=28152`, `surface_preserved=1024`, `biome_surface_changed=1024`, no protocol diff, and `world_tests=pass`.
- `scripts/resource_distribution_matrix_gate.sh` is the current metadata-only resource matrix artifact gate. Current expected status is `pass` with `matrix_status=guarded`, `sample_hash=f2beb912cffae722b9ea0322c68caa58a853c3c953fa4a0a80e618b42924e430`, `samples=12`, no chunk/protocol diff, and `world_tests=pass`.
- `scripts/biome_visual_variety_foundation_gate.sh` is the current biome/visual-variety foundation gate. Current expected status is `pass` with `biome_sampler=guarded`, `biome_matrix=guarded`, `metadata_layer=guarded`, `atlas_tile_identity=guarded`, `atlas_block_texture_usage=guarded`, no `server/pkg/world/chunk.go` generation/serialization diff, and default runtime biome visuals deferred while `biome_v1` metadata remains guarded.
- `scripts/world_generation_quality_gate.sh` is the current worldgen quality planning gate. Current expected status is `pass` with `worldgen_seed_version=guarded`, `worldgen_height_v1=guarded`, `worldgen_biome_height_v1=guarded`, `worldgen_cave_height_v1=guarded`, `worldgen_biome_cave_height_v1=guarded`, `height_v1_serialization=guarded`, `biome_height_v1_serialization=guarded`, `cave_height_v1_serialization=guarded`, `biome_cave_height_v1_serialization=guarded`, `cave_distribution=guarded`, `resource_distribution=guarded`, `height_v1_live_smoke=guarded`, `biome_sampler=guarded`, `biome_matrix=guarded`, `biome_atlas_tile_identity=guarded`, `biome_atlas_block_texture_usage=guarded`, `cave_matrix=guarded`, `resource_matrix=guarded`, no default generator/chunk-byte change, `origin_chunk=guarded` for the stable flat origin snapshot, `flat_byte_hash=guarded` for representative flat chunk bytes, `coordinate_mapping=guarded` for positive/negative/high-positive global block-to-chunk boundaries, and `runtime_quality_pass=opt_in_biome_cave_height_v1_guarded`; resource-to-block generation, structures, default-world terrain changes, and broader biome runtime behavior remain blocked until separate deterministic implementations and downstream evidence exist.
- `scripts/server_scalability_pass_gate.sh` is the current server scalability checkpoint. Current expected status is `pass` with per-client sent-chunk state, interested-client block-edit fanout, sequential last-write-wins block-edit conflict semantics reported as `conflict_semantics=last_write_wins_guarded`, world-owned nearest-first request ordering reported as `chunk_request_ordering=guarded`, nil sent-state inputs reported as `nil_sent_state_policy=empty_guarded`, view-distance configuration reported as `view_distance_config=guarded`, failed-broadcast cleanup, session write deadlines, opt-in max-client admission unit-guarded plus bounded live one-holder/one-rejected evidence and bounded max-client matrix evidence reported as `admission_policy=matrix_live_guarded`, bounded classified connection lifecycle log summaries with zero close/accept failures and complete active-client log fields, no protocol diff, propagated worldgen atlas guards reported as `worldgen_biome_atlas_tile_identity=guarded` and `worldgen_biome_atlas_block_texture_usage=guarded`, `scripts/server_multi_client_smoke.sh` live two-client fanout/resource/per-client detail evidence, bounded six-client broader fanout/load/resource/per-client detail evidence, and bounded three-run six-client repeat evidence reported as `scalability_status=repeat_live_guarded` and `resource_profile_status=repeat_live_guarded`; longer CPU/memory profiling, sustained admission sizing, adaptive overload policy, broad slow-reader/load harnesses, and external session metric upload/retention remain outside this checkpoint.
- `scripts/networking_robustness_gate.sh` is the current networking robustness checkpoint. Current expected status is `pass` with Go/Rust packet boundary tests covering short, oversized, malformed, and zero-length empty-payload frames, empty packet payload handling reported as `unknown_packet_policy=ignored_guarded`, nil packet handler inputs reported as `nil_packet_policy=ignored_guarded`, nil client-position payload handling reported as `nil_position_policy=ignored_guarded`, nil block-action payload handling reported as `nil_block_action_policy=ignored_guarded`, repeated live scalability and resource-profile evidence consumed from server scalability and reported as `scalability_status=repeat_live_guarded` and `resource_profile_status=repeat_live_guarded`, multi-client sent-state, interested-client fanout, world-owned request ordering, propagated worldgen atlas guards, and nil world sent-state handling consumed from server scalability and reported as `multi_client_sent_state=guarded`, `block_edit_fanout=interested_clients_guarded`, `chunk_request_ordering=guarded`, `worldgen_biome_atlas_tile_identity=guarded`, `worldgen_biome_atlas_block_texture_usage=guarded`, and `nil_sent_state_policy=empty_guarded`, view-distance configuration consumed from server scalability and reported as `view_distance_config=guarded`, lifecycle cleanup consumed from server scalability and reported as `disconnect_cleanup_status=lifecycle_summary_guarded` with zero close/accept failures and complete active-client lifecycle fields, zero-length frame decode reported as `empty_payload_frame=decode_guarded`, server packet-error classification reported as `packet_error_classification=unit_guarded`, packet-error log aggregation reported as `packet_error_aggregation=parser_guarded`, live packet-error alert thresholds reported as `packet_error_alerts=threshold_guarded` over current live server logs including the slow-reader matrix, Rust reader-session stale-packet handling unit-guarded, session write deadlines unit-guarded, opt-in server admission reported as `overload_status=admission_matrix_guarded` when the current admission-limit matrix exists, `conflict_semantics=last_write_wins_guarded` consumed from server scalability, required `scripts/server_slow_reader_smoke.sh` bounded live slow-reader evidence plus required `scripts/server_slow_reader_matrix_smoke.sh` bounded multi-fast-client evidence reported as `slow_client_status=load_matrix_guarded`, required `scripts/client_reconnect_smoke.sh` live disconnect/server-restart/rebootstrap-to-active evidence, required `scripts/client_reconnect_soak.sh` bounded repeated reconnect/rebootstrap evidence, no protocol diff, and adaptive overload/backpressure policy deferred.
- `scripts/packet_error_monitoring_contract_gate.sh` is the current local packet-error monitoring export checkpoint. Current expected status is `pass` with `monitoring_contract=export_ready`, `metrics_export=present`, `alert_guard=threshold_guarded`, `classified_events=39`, `unknown_classes=0`, `protocol_errors=0`, `write_errors=0`, `networking_packet_error_alerts=threshold_guarded`, `observability_error_scan=clean`, `index_alert_status=present`, and `index_class_status=present`; external monitoring service/upload/retention integration remains outside this local contract.
- `scripts/server_session_monitoring_contract_gate.sh` is the current local server session monitoring export checkpoint. Current expected status is `pass` with `monitoring_contract=export_ready`, `metrics_export=present`, `scalability_guard=repeat_live_guarded`, `resource_profile_status=repeat_live_guarded`, `admission_policy=matrix_live_guarded`, `disconnect_cleanup_status=lifecycle_summary_guarded`, `lifecycle_status=pass`, connected/rejected/disconnected session counts present, zero close failures, zero accept failures, complete active-client fields, and observability index coverage for server scalability/lifecycle summaries; external monitoring service/upload/retention integration remains outside this local contract.
- `scripts/client_state_machine_hardening_gate.sh` is the current client lifecycle checkpoint. Current expected status is `pass` with connecting/waiting_chunks/spawning/active/reconnecting/shutdown transitions unit-guarded, reader-thread errors delivered to the main thread, marker telemetry for `client_state`/reconnect counters and `network_session`/stale-reader counters, required reconnect smoke evidence, required bounded repeated live disconnect/server-restart/rebootstrap-to-active soak evidence, `state_machine_status=repeated_runtime_guarded`, `runtime_reconnect=repeated_live_rebootstrap_guarded`, `state_telemetry=live_marker_guarded`, `stale_packet_policy=session_guarded`, no protocol diff, and broad state-reset/reconnect-failure policy deferred.
- `scripts/server_inventory_foundation_gate.sh` is the current server inventory foundation checkpoint. Current expected status is `pass` with `server_inventory_status=session_guarded`, `creative_inventory=unit_guarded`, `counted_inventory=unit_guarded`, `counted_inventory_runtime=live_server_guarded`, `counted_inventory_runtime_status=pass`, `break_drop_inventory=live_server_guarded`, `break_drop_inventory_status=pass`, `item_entity_pickup=live_server_guarded`, `item_entity_pickup_status=pass`, `block_action_inventory=session_guarded`, `player_inventory_persistence=rocksdb_guarded`, no protocol diff, no storage diff, and `go_tests=pass`; current default creative placement retains counts while `RUMPELMC_SERVER_INVENTORY_MODE=counted` is a guarded live mode with finite stack decrement, destroy-drop item entity pickup, and persisted reload evidence.
- `scripts/mining_rules_foundation_gate.sh` is the current mining rules checkpoint. Current expected status is `pass` with `mining_rules_status=cooldown_guarded`, `mining_block_durations=target_block_guarded`, `creative_default=unchanged`, `counted_mining_cooldown=server_guarded`, `active_protocol_change=0`, and `go_tests=pass`; `RUMPELMC_SERVER_MINING_COOLDOWN_MS` is the server-owned global cooldown override, default creative sessions keep zero cooldown, counted/survival sessions read the target block before destroy mutation so block IDs choose the server interval, and the item/tool foundation owns the first selected-tool adjustment while explicit global overrides stay exact.
- `scripts/item_tool_foundation_gate.sh` is the current item/tool foundation checkpoint. Current expected status is `pass` with `item_tool_foundation=server_guarded`, `item_identity=block_items_guarded`, `tool_catalog=wood_tools_guarded`, `first_tool_slot=hand_guarded`, `tool_mining=selected_tool_guarded`, no protocol/storage/worldgen diff, and `go_tests=pass`; `server/pkg/item` maps current placeable blocks to server gameplay item IDs, sessions start with selected `tool:hand`, wooden tools adjust target-block mining duration only when effective, and item/tool IDs are not current packet fields or RocksDB/chunk identities.
- `scripts/gameplay_loop_foundation_gate.sh` is the current gameplay foundation checkpoint. Current expected status is `pass` with local creative hotbar inventory unit-guarded, selected hotbar slot state reported as `hotbar_selection=unit_guarded`, selected tool slot state reported as `tool_selection=inventory_action_guarded`, authoritative inventory HUD labels/counts reported as `inventory_hud=authoritative_guarded`, authoritative tool HUD text reported as `tool_hud=authoritative_guarded`, item entity pickup reported as `item_entity_pickup=live_server_guarded`, server session inventory reported as `server_inventory_status=session_guarded`, `server_inventory_block_action=session_guarded`, and `server_inventory_persistence=rocksdb_guarded`, mining rules reported as `mining_rules_status=cooldown_guarded`, `mining_cooldown=server_guarded`, and `mining_block_durations=target_block_guarded`, item/tool foundation reported as `item_tool_foundation=server_guarded`, `item_identity=block_items_guarded`, `tool_catalog=wood_tools_guarded`, `first_tool_slot=hand_guarded`, and `tool_mining=selected_tool_guarded`, live local-player inventory reconnect/restart evidence reported as `player_inventory_reconnect=live_server_guarded`, mining/building still using `BlockAction` and server world edit boundaries `World.SetBlockGlobal`/`World.ReplaceBlockGlobal`, connected-session placement rejecting player-body AABB intersections before world mutation or inventory consumption, no protocol diff, required Block 41 persisted visual evidence, `full_reload_persistence=block_41_visual_guarded`, `block_edit_persistence_status=pass`, `block_edit_visual_path=godot_persisted_reload_guarded`, `block_edit_persisted_visual_smoke=godot_guarded`, `block_edit_persisted_visual_smoke_status=pass`, three persisted visual scenarios, place/destroy-after-reload/edge-place statuses pass, and `block_edit_active_protocol_change=0`.
- `scripts/third_person_character_visual_gate.sh` is the current third-person player visual checkpoint. Current expected status is `pass` with `third_person_visual=guarded`, `camera_toggle=rust_guarded`, `character_visual=veloren_composer_guarded`, `character_assets=veloren_voxygen_guarded`, `character_asset_license=gpl3_attributed`, `character_animation=procedural_idle_run_jump_guarded`, `veloren_composer=ron_manifest_guarded`, `vox_loader=magica_vox_guarded`, `rust_tests=pass`, `godot_smoke=pass`, no protocol/storage/worldgen diff, and client path changes limited to `client/rust_ext/src/player.rs`, `client/rust_ext/src/veloren_composer.rs`, `client/rust_ext/src/vox.rs`, `client/assets/veloren/`, plus docs/gate updates. First-person remains default at `1.80m` eye height, local player height is guarded around `1.90m`, `V` toggles third-person presentation, Veloren-derived voxygen RON and `.vox` assets must keep attribution/license evidence, and server reach validation must not be changed for this visual slice.
- `scripts/block_edit_persistence_gate.sh` is the current block edit persistence checkpoint. Current expected status is `pass` with `SetBlockGlobal` place/destroy edits reloaded through a fresh `World(store)`, negative-boundary edit persistence reported as `negative_boundary_edits=guarded`, opt-in height generator persisted reload reported as `height_v1_reload=guarded`, save-failure rollback reported as `save_failure_rollback=guarded`, persisted load errors reported as `store_load_errors=propagated_guarded`, `scripts/server_persisted_reload_smoke.sh` live server restart/reopen persistence evidence, `scripts/block_edit_persisted_visual_smoke.sh` Godot persisted-reload visual/collision/GPU screenshot evidence for place, destroy-after-reload, and chunk-edge scenarios, no protocol diff, and broader cross-chunk/non-current visual coverage deferred.
- `scripts/dirty_update_scalability_gate.sh` is the current dirty update scalability checkpoint. Current expected status is `pass` with mass dirty update math unit-guarded, opt-in edge dirty Godot runtime evidence consumed from `scripts/dirty_update_runtime_smoke.sh`, mixed place/destroy current-chunk mass runtime budget evidence consumed from `scripts/dirty_update_mass_edit_runtime_smoke.sh`, bounded cross-chunk mass runtime budget evidence consumed from `scripts/dirty_update_cross_chunk_mass_runtime_smoke.sh`, bounded three-cycle persisted-reload dirty runtime evidence consumed from `scripts/dirty_update_persisted_reload_runtime_smoke.sh`, five-cycle high-count persisted-reload matrix evidence consumed from `scripts/dirty_update_high_count_runtime_gate.sh`, and no protocol diff.
- `scripts/tooling_debug_overlay_gate.sh` is the current tooling/debug overlay checkpoint. Current expected status is `pass` with compact HUD overlay wired through `GameClient.get_debug_overlay_text()`, full `get_perf_text` logging preserved, no protocol diff, and no Godot scene/resource/import diff.
- `scripts/observability_logs_cleanup_gate.sh` is the current observability/log cleanup checkpoint. Current expected status is `pass` with `RUMPELMC_RUN_ID` HUD perf-log stamping wired, `logs/*current/*summary.txt` indexed, summary naming/status clean, current error scan clean, and `gpu_report_freshness=guarded` from `scripts/gpu_terrain_report_freshness_gate.sh`. The freshness gate refreshes ignored `logs/gpu-terrain-report.txt` by default and requires the report commit to match `HEAD`, a clean aggregate error scan, zero aggregate upload failures, and present aggregate workload metrics.
- `scripts/automated_handoff_discipline_gate.sh` is the current handoff automation checkpoint. Current expected status is `pass` with generated handoff snapshots including quality inputs, the current observability artifact index, and `observability_gpu_report_freshness=guarded`; `docs/AGENT_HANDOFF.md` remains manually updated before actual delegation or pause.
- `scripts/architecture_documentation_refresh_gate.sh` is the current architecture documentation checkpoint. Current expected status is `pass` with `docs/ARCHITECTURE.md` reflecting the actual Godot/Rust/Go/RocksDB/protobuf architecture and no runtime behavior change.
- `scripts/security_data_integrity_review_gate.sh` is the current security/data-integrity checkpoint. Current expected status is `pass` with packet length guards, exact packet frame-boundary coverage, classified packet-error labels, parser-guarded aggregation, live packet-error alert thresholds, local packet-error monitoring export reported as `packet_error_monitoring=export_ready`, local server session monitoring export reported as `server_session_monitoring=export_ready`, unknown packet payload handling reported as `unknown_packet_policy=ignored_guarded`, nil packet handler inputs reported as `nil_packet_policy=ignored_guarded`, nil client-position payload handling reported as `nil_position_policy=ignored_guarded`, nil block-action payload handling reported as `nil_block_action_policy=ignored_guarded`, protobuf decode tests, machine-readable deterministic RLE property coverage on Go/Rust paths including `height_v1`, `biome_height_v1`, `cave_height_v1`, and `biome_cave_height_v1` chunks, block-edit conflict semantics reported as `conflict_semantics=last_write_wins_guarded`, propagated worldgen atlas guards reported as `worldgen_biome_atlas_tile_identity=guarded` and `worldgen_biome_atlas_block_texture_usage=guarded`, opt-in max-client admission evidence reported as `overload_status=admission_matrix_guarded`, required reconnect evidence surfaced as `runtime_reconnect=repeated_live_rebootstrap_guarded`, `reconnect_smoke_status=pass`, and `reconnect_soak_status=pass`, required slow-reader evidence surfaced as `slow_client_status=load_matrix_guarded`, `slow_reader_smoke_status=pass`, and `slow_reader_matrix_status=pass`, enforced loopback-only server exposure reported as `local_server_exposure=loopback_enforced`, smoke script binds reported as `smoke_bind_exposure=loopback_guarded`, observability GPU report freshness reported as `observability_gpu_report_freshness=guarded`, storage save/load/corrupt-payload/open-path tests, storage package smoke reported as `storage_package_smoke=guarded`, storage config reported as `storage_config=path_guarded`, storage backend policy reported as `storage_backend_policy=approved_only_guarded`, storage backend ownership reported as `storage_backend_ownership=guarded`, storage concurrency reported as `storage_concurrency=guarded`, storage errors reported as `storage_errors=actionable_guarded`, storage lifecycle reported as `storage_lifecycle=guarded`, block-edit Y-bound validation reported as `block_edit_validation=y_bounds_guarded`, block-edit reach validation reported as `block_edit_reach=position_guarded`, player-intersecting placement rejection reported as `block_edit_player_collision=place_player_aabb_guarded`, block-edit failed-save rollback reported as `block_edit_save_failure_rollback=guarded`, bounded runtime persistence evidence consumed through prerequisite gates, and no active protocol diff.
- `scripts/release_candidate_gate.sh` is the current release-candidate evidence checkpoint. Current expected status is `pass` with `test_gpu_report_freshness=guarded`, `security_deterministic_property_tests=guarded`, `security_observability_gpu_report_freshness=guarded`, `security_packet_error_monitoring=export_ready`, `security_server_session_monitoring=export_ready`, `security_storage_package_smoke=guarded`, `security_storage_config=path_guarded`, `security_storage_backend_policy=approved_only_guarded`, `security_block_edit_validation=y_bounds_guarded`, `security_block_edit_save_failure_rollback=guarded`, `security_unknown_packet_policy=ignored_guarded`, `security_nil_packet_policy=ignored_guarded`, `security_nil_position_policy=ignored_guarded`, `security_nil_block_action_policy=ignored_guarded`, `security_conflict_semantics=last_write_wins_guarded`, `security_worldgen_biome_atlas_tile_identity=guarded`, `security_worldgen_biome_atlas_block_texture_usage=guarded`, `security_overload_status=admission_matrix_guarded`, `security_runtime_reconnect=repeated_live_rebootstrap_guarded`, `security_reconnect_smoke_status=pass`, `security_reconnect_soak_status=pass`, `security_slow_client_status=load_matrix_guarded`, `security_slow_reader_smoke_status=pass`, `security_slow_reader_matrix_status=pass`, `security_local_server_exposure=loopback_enforced`, `security_smoke_bind_exposure=loopback_guarded`, and `live_checks=full` after running with `RUMPELMC_RC_REQUIRE_LIVE_CHECKS=1`, `RUMPELMC_RC_RUN_FAST_CHECKS=1`, `RUMPELMC_RC_RUN_FULL_CHECKS=1`, and `RUMPELMC_RC_RUN_DIFF_GUARD=1`; a real release branch must rerun those live flags on the exact branch.
- `scripts/external_profiling_campaign_gate.sh` is the current external-profiler campaign checkpoint. Current expected status is `pass` with `capture_readiness=live_rc_ready_for_external_capture`, `external_profile_status=pending_external_profiler`, `results_template_status=operator_intake_template`, `rc_live_checks=full`, `rc_test_gpu_report_freshness=guarded`, `rc_security_deterministic_property_tests=guarded`, `rc_security_observability_gpu_report_freshness=guarded`, `rc_security_packet_error_monitoring=export_ready`, `rc_security_server_session_monitoring=export_ready`, `rc_security_storage_package_smoke=guarded`, `rc_security_storage_config=path_guarded`, `rc_security_storage_backend_policy=approved_only_guarded`, `rc_security_block_edit_validation=y_bounds_guarded`, `rc_security_block_edit_save_failure_rollback=guarded`, `rc_security_unknown_packet_policy=ignored_guarded`, `rc_security_nil_packet_policy=ignored_guarded`, `rc_security_nil_position_policy=ignored_guarded`, `rc_security_nil_block_action_policy=ignored_guarded`, `rc_security_conflict_semantics=last_write_wins_guarded`, `rc_security_worldgen_biome_atlas_tile_identity=guarded`, `rc_security_worldgen_biome_atlas_block_texture_usage=guarded`, `rc_security_overload_status=admission_matrix_guarded`, `rc_security_runtime_reconnect=repeated_live_rebootstrap_guarded`, `rc_security_reconnect_smoke_status=pass`, `rc_security_reconnect_soak_status=pass`, `rc_security_slow_client_status=load_matrix_guarded`, `rc_security_slow_reader_smoke_status=pass`, `rc_security_slow_reader_matrix_status=pass`, `rc_security_local_server_exposure=loopback_enforced`, `rc_security_smoke_bind_exposure=loopback_guarded`, and, when the local capture directory exists, `xctrace_review_packet_status=pass`; generated capture packs, results-intake files, operator-intake templates, `scripts/gpu_terrain_shadow_xctrace_attach_capture.sh` traces/exports, `scripts/gpu_terrain_shadow_xctrace_review_packet.sh` packets, and `scripts/gpu_terrain_shadow_xctrace_overhead_summary.sh` control-delta summaries are not profiler evidence until a manually reviewed positive `gpu_shadow_pass_ms` row validates through `scripts/gpu_terrain_shadow_profiler_results_check.sh`. The xctrace attach helper validates runtime terrain compositor markers (`gpu_profiler_breadcrumb=1381256515`, `gpu_profiler_shader=rumpel_gpu_terrain_render_shader`, `gpu_profiler_pipeline=rumpel_gpu_terrain_compositor_pipeline`), requires core command-buffer/encoder XML exports, best-effort writes extended Metal XML review tables, and reports `profiler_marker_xml_status`; the review packet revalidates those artifacts and marker scans. The overhead helper may report `shadow_overhead_estimate_*_ms` from shadow-on versus `scene_shadows_disabled` main command-buffer totals and `candidate_shadow_encoder_*` from `shadow-xctrace-encoder-candidates.tsv`, but it must keep `estimate_is_not_gpu_shadow_pass_ms=1`, `candidate_shadow_encoder_is_not_gpu_shadow_pass_ms=1`, and `control_delta_is_not_shadow_pass_row=1`. Current CLI XML scans still report missing terrain profiler markers; the current single missing encoder candidate is `Blit_Command_7` with p50 around `0.007ms`, so exported tables, packets, candidate tables, and control deltas are navigation aids rather than accepted shadow-pass timing evidence.
- `scripts/production_readiness_milestone_gate.sh` is the current 50-block milestone checkpoint. Current expected status is `pass` with `production_readiness=rc_evidence_ready`, `handoff_gpu_report_freshness=guarded`, `rc_test_gpu_report_freshness=guarded`, `rc_security_deterministic_property_tests=guarded`, `security_observability_gpu_report_freshness=guarded`, `security_storage_package_smoke=guarded`, `security_packet_error_monitoring=export_ready`, `security_server_session_monitoring=export_ready`, `security_storage_config=path_guarded`, `security_storage_backend_policy=approved_only_guarded`, `security_block_edit_validation=y_bounds_guarded`, `security_block_edit_save_failure_rollback=guarded`, `security_unknown_packet_policy=ignored_guarded`, `security_nil_packet_policy=ignored_guarded`, `security_nil_position_policy=ignored_guarded`, `security_nil_block_action_policy=ignored_guarded`, `security_conflict_semantics=last_write_wins_guarded`, `security_worldgen_biome_atlas_tile_identity=guarded`, `security_worldgen_biome_atlas_block_texture_usage=guarded`, `security_overload_status=admission_matrix_guarded`, `security_local_server_exposure=loopback_enforced`, `security_smoke_bind_exposure=loopback_guarded`, `rc_security_observability_gpu_report_freshness=guarded`, `rc_security_storage_package_smoke=guarded`, `rc_security_packet_error_monitoring=export_ready`, `rc_security_server_session_monitoring=export_ready`, `rc_security_storage_config=path_guarded`, `rc_security_storage_backend_policy=approved_only_guarded`, `rc_security_block_edit_validation=y_bounds_guarded`, `rc_security_block_edit_save_failure_rollback=guarded`, `rc_security_unknown_packet_policy=ignored_guarded`, `rc_security_nil_packet_policy=ignored_guarded`, `rc_security_nil_position_policy=ignored_guarded`, `rc_security_nil_block_action_policy=ignored_guarded`, `rc_security_conflict_semantics=last_write_wins_guarded`, `rc_security_worldgen_biome_atlas_tile_identity=guarded`, `rc_security_worldgen_biome_atlas_block_texture_usage=guarded`, `rc_security_overload_status=admission_matrix_guarded`, `rc_security_local_server_exposure=loopback_enforced`, `rc_security_smoke_bind_exposure=loopback_guarded`, `external_rc_test_gpu_report_freshness=guarded`, `external_rc_security_observability_gpu_report_freshness=guarded`, `external_rc_security_storage_package_smoke=guarded`, `external_rc_security_packet_error_monitoring=export_ready`, `external_rc_security_server_session_monitoring=export_ready`, `external_rc_security_storage_config=path_guarded`, `external_rc_security_storage_backend_policy=approved_only_guarded`, `external_rc_security_block_edit_validation=y_bounds_guarded`, `external_rc_security_block_edit_save_failure_rollback=guarded`, `external_rc_security_unknown_packet_policy=ignored_guarded`, `external_rc_security_nil_packet_policy=ignored_guarded`, `external_rc_security_nil_position_policy=ignored_guarded`, `external_rc_security_nil_block_action_policy=ignored_guarded`, `external_rc_security_conflict_semantics=last_write_wins_guarded`, `external_rc_security_worldgen_biome_atlas_tile_identity=guarded`, `external_rc_security_worldgen_biome_atlas_block_texture_usage=guarded`, `external_rc_security_overload_status=admission_matrix_guarded`, `external_rc_security_local_server_exposure=loopback_enforced`, `external_rc_security_smoke_bind_exposure=loopback_guarded`, `external_capture_readiness=live_rc_ready_for_external_capture`, and current `live_release_checks=full`; it is an evidence milestone, not a shipped-release declaration, and external profiler evidence remains separate.
- Production readiness runtime evidence must stay visible and enforced at direct security, release-candidate, and external-campaign layers: each layer reports `runtime_reconnect=repeated_live_rebootstrap_guarded`, reconnect smoke/soak pass states, `slow_client_status=load_matrix_guarded`, and slow-reader smoke/matrix pass states with the matching layer prefix.
- Storage and persistence changes require extra review before finalizing.
- Rust GDExtension changes can affect client behavior and performance; review them carefully.
- Client startup readiness telemetry is split into one-shot wall-clock timestamps `startup_chunk_packet_ms`, `startup_chunk_inserted_ms`, `startup_chunk_loaded_ms`, `startup_mesh_queued_ms`, `startup_mesh_dispatched_ms`, `startup_first_mesh_ms`, `startup_collision_ms`, and `startup_player_spawn_ms`; movement gates should preserve that nondecreasing order and keep `startup_packet_read_work_ms`, `startup_packet_decode_work_ms`, `startup_packet_reader_elapsed_ms`, `startup_packet_queue_lag_ms`, `startup_chunk_decode_work_ms`, `startup_first_mesh_work_ms`, `startup_first_mesh_phase_ms`, and `startup_first_mesh_collision_work_ms` as work-duration/scheduling evidence separate from wall-clock startup timestamps.
- After Godot observes the local server listening, the main scene attaches `GameClient` after one frame by default; `RUMPELMC_CLIENT_ATTACH_DELAY_SEC` is a diagnostic rollback/control for adding an explicit post-listen delay.
- Server chunk delivery order is nearest-first by default and unit-guarded through `chunk_request_ordering=guarded`. `RUMPELMC_SERVER_CHUNK_ORDER=directional` is opt-in and only applies movement-direction tie-breaks among chunks at the same distance; it must not change protocol, chunk payload format, storage, world generation, view distance, batch size, or bootstrap radius.
- Rust client packet queue metrics are observational drain-depth proxies over the existing unbounded reader-thread channel. They must not be treated as a backpressure/drop/reorder policy without a separate explicit design change.
- Current chunk compression decision is RLE default with `RUMPELMC_SERVER_CHUNK_ENCODING=raw` rollback. Do not add another compression format until representative non-flat terrain entropy or latency evidence justifies it.
- Current streaming protocol plan is no schema change for chunk streaming. Future packet variants must use new oneof tags greater than `7`, and future `ChunkData` fields must use new field numbers greater than `5` with compatibility tests and rollback documented first.
- `scripts/inventory_protocol_compatibility_gate.sh` is the current inventory protocol compatibility checkpoint. Current expected status is `pass` with `inventory_protocol_status=item_entity_pickup_guarded`, `active_schema_change=0`, `current_wire_contract=block_action_inventory_snapshot_action_tool_action_item_entity_pickup_and_player_id`, `position_identity_schema=tag_4_guarded`, `inventory_snapshot_schema=tag_4_guarded`, `inventory_action_schema=tag_5_guarded`, `tool_selection_schema=field_3_guarded`, `item_entity_snapshot_schema=tag_6_guarded`, `item_pickup_schema=tag_7_guarded`, `future_packet_tags=packet_gt_7`, `future_block_action_fields=block_action_gt_5`, `protocol_generated_drift=guarded`, `server_inventory_status=session_guarded`, and `gameplay_inventory_status=session_guarded`; current inventory placement stays on `Packet.block_action = 3` and `BlockAction.block_id = 5`, server-to-client inventory slots plus selected block/tool slots use `Packet.inventory_snapshot = 4`, selected-slot and selected-tool requests use `Packet.inventory_action = 5`, item entity snapshots use `Packet.item_entities = 6`, item pickup requests use `Packet.item_pickup = 7`, and `ClientPosition.player_id = 4` carries the optional local-player identity for inventory persistence.
- Resident-set growth gates are stress-only measurement runs. They may raise server view distance and client keep distance inside scripts, but must not change default gameplay draw distance, visible quality, protocol, storage, world generation, or chunk serialization without a separate explicit task.
- Client chunk unload remains keep-distance plus grace based by default. `RUMPELMC_CLIENT_CHUNK_UNLOAD_GRACE_SEC=0` is an immediate-unload rollback/control path; unload churn metrics are telemetry only and must not be treated as a new unload policy without pop-in/collision evidence.
- World pop-in metrics are report-only player-neighborhood probes around the current chunk. They may count missing loaded chunks and loaded-but-collision-missing chunks, but must not become fail gates or policy changes until explicit budgets are defined.
- Player spawn remains collision-gated. Movement readiness summaries may separate render-ready and collision-ready evidence, but they do not authorize lazy collision or reduced collision coverage without an explicit opt-in experiment and gates.
- Client collision radius remains `1` by default. `RUMPELMC_CLIENT_COLLISION_CHUNK_DISTANCE=0` is an opt-in lazy-collision experiment only and must not be promoted while pop-in collision-missing evidence is high.
- GPU terrain render shader assumptions matter: vertex code computes terrain lighting from packed face normals and lighting push constants, fragment code applies the passed lighting to atlas color with opaque alpha, perf markers expose the sanitized `gpu_light_*` values, and scene depth uses reverse-Z `GREATER_OR_EQUAL`.
- GPU terrain partial dirty upload is default-on when `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD` is unset; explicit `RUMPELMC_GPU_TERRAIN_PARTIAL_DIRTY_UPLOAD=0` is the full-rebuild rollback/control path.
- GPU terrain buffer repack must remain default-off behind explicit `RUMPELMC_GPU_TERRAIN_BUFFER_REPACK=1`; unset or `0` keeps the current allocator path until planner tests, allocator gates, profiling artifacts, and rollback behavior justify any broader rollout.
- Visual smoke lighting variants are smoke-only controls. They must not change default scene lighting, draw distance, terrain quality, protocol, storage, world generation, or chunk serialization; use marker fields such as `lighting_variant` plus `gpu_light_*` before comparing captures.
- GPU-native terrain shadows remain behind `GPU_TERRAIN_NATIVE_SHADOW_IMPLEMENTED=false`. While runtime markers report `native_shadow_implemented=0`, env-on captures must stay on `shadow_path=godot_proxy` with `native_shadow_requested=1`, `native_shadow_active=0`, `native_shadow_fallback=1`, `native_shadow_resource_status=disabled`, and zero native-shadow resource lifecycle plus coverage counters.
- Godot scene/resource/import files should not be reformatted casually.

## Agent Roles

- Research agents are read-only and should return concise findings with file paths.
- Review agents look for risks, regressions, missing tests, and code degradation.
- Protocol/world review is required for networking, storage, world generation, chunk serialization, persistence, or Rust extension changes.
- The main agent decides what to change and remains responsible for the final patch.
